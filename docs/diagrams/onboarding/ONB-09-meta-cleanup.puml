@startuml
!define RELATIVE_INCLUDE 1
!include ../system/_plantuml/C4_Container.puml

LAYOUT_WITH_LEGEND()
title Container diagram for Onboarding - ONB-09 Meta processed cleanup

AddRelTag("sync", $lineColor="#2F6BFF", $textColor="#2F6BFF", $legendText="sync call")

System_Boundary(mecontrola, "MeControla") {
  Container(workerProc, "Background Worker", "Go", "Executa onboarding-meta-processed-cleanup")
  ContainerDb(onboardingDb, "Onboarding Store", "PostgreSQL", "tabelas auxiliares da integracao Meta")
}

Rel(workerProc, onboardingDb, "[sync] limpa linhas antigas de processamento Meta", "SQL delete", $tags="sync")

SHOW_LEGEND()
@enduml
