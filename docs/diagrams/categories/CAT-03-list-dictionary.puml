@startuml
!define RELATIVE_INCLUDE 1
!include ../system/_plantuml/C4_Container.puml

LAYOUT_WITH_LEGEND()
title Container diagram for Categories - CAT-03 List dictionary

AddRelTag("sync", $lineColor="#2F6BFF", $textColor="#2F6BFF", $legendText="sync call")

Person(apiClient, "API Client", "Cliente autenticado")

System_Boundary(mecontrola, "MeControla") {
  Container(apiServer, "HTTP API Server", "Go, Chi", "Executa GET /api/v1/category-dictionary/")
  ContainerDb(categoriesDb, "Categories Store", "PostgreSQL", "dictionary e version")
}

Rel(apiClient, apiServer, "[sync] GET /api/v1/category-dictionary/", "HTTPS/JSON", $tags="sync")
Rel(apiServer, categoriesDb, "[sync] pagina entradas do dicionario e version para ETag", "SQL", $tags="sync")

SHOW_LEGEND()
@enduml
