apiVersion: 1

providers:
  - name: mecontrola
    orgId: 1
    folder: MeControla
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
