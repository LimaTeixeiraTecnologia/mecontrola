# Contrato de variaveis exigidas pelas tarefas. Copie para .env (local) e
# .env.test (testes de integracao). Nunca commite os arquivos reais.

# Aplicacao
APP_NAME=app
BIN_DIR=bin

# Testes de integracao
DATABASE_URL=postgres://user:pass@localhost:5432/app?sslmode=disable
