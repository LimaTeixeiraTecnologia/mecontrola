# Generated: 2026-06-27T23:51:45Z

# Relatório de Execução — Tarefa 2.0

## Tarefa

Atualizar `deployment/caddy/Caddyfile` com load balancing, health checks ativos e rate limiting por IP via plugin `caddy-ratelimit`.

## Slug

infra-producao-robusta-10k-dez-2026

## Arquivos Alterados

- `deployment/caddy/Caddyfile`
- `internal/platform/http/server/health/readiness.go`
- `internal/platform/http/server/health/readiness_test.go`

## Implementação

- Substituição do site block single-upstream (`server:8080`) por configuração que escuta nas portas `:80` e `{$APP_DOMAIN}:443`.
- Criação do snippet `(mecontrola-headers)` com headers de segurança (`-Server`, `Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, `Content-Security-Policy`).
- Criação do snippet `(rate-limit)` com `caddy-ratelimit` configurado para 100 req/s por IP, burst 200, retornando HTTP 429.
- Configuração de `reverse_proxy` para `server-1:8080 server-2:8080` com:
  - `lb_policy round_robin`
  - health check ativo em `/healthz`, intervalo 10s, timeout 5s, status 200
  - failover passivo `fail_duration 30s` e `max_fails 3`
- Manutenção do bloqueio de endpoints `/admin/*`, `/debug/pprof/*` e `/metrics/*` na borda.
- Adição da rota `/readyz` no `ReadinessRouter` para que o servidor exponha readiness/liveness nos caminhos esperados pelo PRD.

## Critérios de Aceite

| Critério | Status | Evidência |
|---|---|---|
| `caddy adapt --config deployment/caddy/Caddyfile` retorna JSON válido (exit 0) | atendido | `docker run ... caddy adapt` saiu com 0; `/tmp/caddy-adapt.json` gerado e inspecionado |
| Requisições são distribuídas entre `server-1` e `server-2` | atendido | Teste com mocks: respostas alternaram `server-1`/`server-2` em round-robin |
| Quando `server-1` é parado, Caddy passa a enviar tráfego apenas para `server-2` em até ~30s | atendido | Após `docker stop server-1` + 15s, todas as requisições responderam `server-2` |
| Requisições acima de 100 req/s por IP retornam HTTP 429 | atendido | Burst de 250 requisições: 100× `200` e 150× `429` |
| Headers de segurança estão presentes nas respostas | atendido | `curl -I http://localhost:8088/` mostrou HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy, CSP e remoção do `Server` |

## Testes Executados

```bash
# Validação de sintaxe do Caddyfile com imagem que contém caddy-ratelimit
docker build -f deployment/docker/Dockerfile.caddy -t mecontrola-caddy:latest deployment/docker
docker run --rm -e APP_DOMAIN=localhost.test -e CADDY_EMAIL=test@example.com \
  -v "$(pwd)/deployment/caddy/Caddyfile:/etc/caddy/Caddyfile:ro" \
  mecontrola-caddy:latest caddy adapt --config /etc/caddy/Caddyfile --adapter caddyfile

# Testes de rotas de readiness
go test -race -count=1 ./internal/platform/http/server/health/...

# Testes práticos com mocks de upstream (load balancing, failover, rate limit, headers)
# - subiram containers server-1/server-2 na rede caddy-test respondendo /healthz e /
# - subiu container caddy-test com Caddyfile do repositório
# - executados curls sequenciais, burst de 250 requisições e parada de server-1
```

## Comandos de Validação

```bash
gofmt -w internal/platform/http/server/health/readiness.go internal/platform/http/server/health/readiness_test.go
go build ./internal/platform/http/server/health/...
go vet ./internal/platform/http/server/health/...
go test -race -count=1 ./internal/platform/http/server/health/...
golangci-lint run ./internal/platform/http/server/health/...
```

## Revisão

- **Veredito:** `APPROVED`
- **Achados:** nenhum crítico ou de alta severidade.
- **Riscos residuais:**
  - Os testes de load balancing/failover/rate limit foram executados contra mocks simples, não contra a aplicação real, porque a stack completa (PostgreSQL, pgBouncer, etc.) não está disponível no ambiente local.
  - O endpoint `/readyz` recém-adicionado reflete apenas o contexto de vida do servidor; não executa checks de dependências. O health check ativo do Caddy continua usando `/healthz`.
  - A configuração de TLS na porta 443 depende de `{$APP_DOMAIN}` apontado para a VPS e de `{$CADDY_EMAIL}` válido para emissão pelo Let's Encrypt.

## Status

`done`
