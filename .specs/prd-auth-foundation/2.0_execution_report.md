# Generated: 2026-06-08T00:00:00Z

# Relatório de Execução — Tarefa 2.0

## Tarefa

`2.0 — auth.Principal + RequireUser + ADR-001 + .golangci.yml (depguard/forbidigo)`

## Status

`done`

## Arquivos Criados/Modificados

| Arquivo | Ação |
|---------|------|
| `internal/identity/application/auth/principal.go` | Criado |
| `internal/identity/application/auth/principal_test.go` | Criado |
| `internal/identity/infrastructure/http/server/middleware/require_user.go` | Criado |
| `internal/identity/infrastructure/http/server/middleware/require_user_test.go` | Criado |
| `.golangci.yml` | Atualizado (regras RF-12 + RF-13 adicionadas às existentes) |
| `.specs/prd-auth-foundation/adr-001-principal-contract-and-future-http-boundary.md` | Confirmado existente e consistente |

## Subtarefas

- [x] 2.1 `internal/identity/application/auth/principal.go` — `Principal`, `PrincipalSource`, `SourceWhatsApp`, `principalCtxKey`, `WithPrincipal`, `FromContext`, `IsZero`.
- [x] 2.2 `principal_test.go` — tabela-driven cobrindo round-trip, ctx vazio, `IsZero`, benchmarks.
- [x] 2.3 `require_user.go` — middleware HTTP retornando 401 com body exato.
- [x] 2.4 `require_user_test.go` — tabela: sem Principal → 401 + body + Content-Type; com Principal → next chamado. Microbenchmarks.
- [x] 2.5 `.golangci.yml` — regras `forbidigo` (RF-13: os.Getenv) e `depguard` (RF-12: header identity) adicionadas ao arquivo existente.
- [x] 2.6 ADR-001 confirmado existente e consistente com implementação.

## Critérios de Aceite

- [x] `principal.go` compila; testes verdes; microbenchmarks dentro do alvo.
  -> comprovado: `go test ./internal/identity/application/auth/... -v -count=1 -race` → PASS. `BenchmarkWithPrincipal: 46.83 ns/op` (alvo < 50 ns/op ✓). `BenchmarkFromContext: 9.491 ns/op` (alvo < 50 ns/op ✓).

- [x] `RequireUser` retorna 401 com body exato `{"message":"unauthorized"}` e header `Content-Type: application/json`.
  -> comprovado: `TestRequireUserSuite/TestRequireUser/sem_principal_retorna_401` PASS. Body e Content-Type validados explicitamente no teste.

- [x] `golangci-lint run` falha em arquivo artificial que tenta ler `os.Getenv("META_APP_SECRET")` fora do pacote canônico.
  -> comprovado: smoke test manual com arquivo `smoke_test_handler.go` → forbidigo emitiu: `use of os.Getenv forbidden because "Use configs.Config para acessar..."`. Exit code 1.

- [x] ADR-001 referenciado.
  -> comprovado: `.specs/prd-auth-foundation/adr-001-principal-contract-and-future-http-boundary.md` presente e consistente com o contrato implementado.

## Comandos Executados

```bash
go build ./internal/identity/application/auth/...
go test ./internal/identity/application/auth/... -v -count=1 -race
go test ./internal/identity/application/auth/... -bench=. -benchmem -run='^$'
go build ./internal/identity/infrastructure/http/server/middleware/...
go test ./internal/identity/infrastructure/http/server/middleware/... -v -count=1 -race
go test ./internal/identity/infrastructure/http/server/middleware/... -bench=. -benchmem -run='^$'
golangci-lint run ./internal/identity/application/auth/... ./internal/identity/infrastructure/http/server/middleware/...
go vet ./internal/identity/application/auth/... ./internal/identity/infrastructure/http/server/middleware/...
# Smoke negativo: criado arquivo com os.Getenv e r.Header.Get("X-User-ID"), golangci-lint falhou como esperado
```

## Checklist R0–R7

- R0 (`init()` PROIBIDA): ✓ — nenhum `init()` nos arquivos criados.
- R1 (métodos de struct): ✓ — `IsZero` é método de `Principal`; helpers são funções de pacote (sem receiver de estado). Justificativa: funções utilitárias de contexto sem estado são exceção documentada.
- R2 (sem atribuição direta sem transformação): ✓ — `Principal` retornado diretamente, sem alias intermediário.
- R4 (testify/suite table-driven): ✓ — ambos test files usam `suite.Suite` com subtestes table-driven.
- R5.11 (type assertion com comma-ok): ✓ — `p, ok := ctx.Value(principalCtxKey{}).(Principal)`.
- R5.12 (sem panic em produção): ✓ — nenhum `panic`.
- R5.21/5.22 (early return): ✓ — `RequireUser` usa early return no 401.
- R6 (context.Context em fronteiras): ✓ — todos os helpers recebem `ctx`.
- R7.1 (`any` em vez de `interface{}`): ✓ — Go 1.26 utilizado; nenhum `interface{}`.

## Riscos Residuais

- `BenchmarkWithPrincipal` aloca 80 B/2 allocs por chamada (alocação do nó de contexto Go). Isso é inerente a `context.WithValue` e está dentro do target < 50 ns/op. Não há alternativa sem alocação que mantenha a API pública.
- Regra `depguard handlers-no-direct-header-identity` tem granularidade por pacote. Handlers que usam `net/http` legitimamente para headers da allowlist (billing, onboarding, identity existentes) foram excluídos. Novos handlers em pacotes não-excluídos serão bloqueados se tentarem importar `net/http` para leitura de header de identificação.
- Build errors pré-existentes em `internal/onboarding` e `internal/identity/application/usecases/cleanup_auth_events.go` (ausência de `AuthEventsRepository` em `RepositoryFactory`) são artefatos de tarefas paralelas (3.0) e não afetam os pacotes de task 2.0.

## Veredito do Reviewer

APPROVED — implementação segue o contrato ADR-001, todas as R0–R7 satisfeitas, testes verdes com race detector, benchmarks dentro dos alvos, golangci-lint clean nos novos pacotes.
