# Generated: 2026-06-08T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.0
- Título: EstablishPrincipal + TryFindActiveByWhatsApp + MarkUserDeleted publica user.deleted
- Arquivo: .specs/prd-auth-foundation/task-4.0-establish-principal-and-mark-user-deleted.md
- Estado: done

## Contexto Carregado
- PRD: `.specs/prd-auth-foundation/prd.md`
- TechSpec: `.specs/prd-auth-foundation/techspec.md`
- Governança: go-implementation/SKILL.md, go-implementation/references/architecture.md
- Skills processuais: nenhuma além das auto-carregadas (governance + linguagem)

## Comandos Executados
- `go build ./internal/identity/...` -> sem erros (subtarefas 4.6, 4.7, 4.8)
- `go build ./...` -> sem erros (projeto inteiro)
- `go vet ./internal/identity/...` -> sem erros
- `go test ./internal/identity/application/usecases/... -count=1 -v` -> PASS (todos os cenários unitários)
- `go test ./internal/identity/... -count=1` -> PASS (todos os pacotes identity)
- `go test ./... -count=1 | grep FAIL` -> sem falhas em todo o projeto

## Arquivos Alterados
- `internal/identity/application/usecases/mark_user_deleted.go` — adicionado `outbox.Publisher` como dependência; `buildUserDeletedEvent` helper; publicação `user.deleted` dentro do UoW
- `internal/identity/application/usecases/mark_user_deleted_test.go` — import de `outboxmocks`; novo parâmetro `publisher` em `dependencies`; 4 cenários (incluindo falha de outbox)
- `internal/identity/module.go` — import `outbox`; `identityPublisher` struct + `newIdentityPublisher`; registro de `EstablishPrincipal` e publisher compartilhado; `markDeletedUC` com publisher; `EstablishPrincipal` em `IdentityModule`
- `internal/identity/application/usecases/establish_principal_integration_test.go` — criado (build tag `integration`); 3 cenários com PG real + outbox real
- `internal/identity/application/usecases/mark_user_deleted_integration_test.go` — criado (build tag `integration`); 1 cenário confirmando publicação `user.deleted`

## Resultados de Validação
- Testes: pass (unitários e compilação dos integration tests)
- Lint: pass (`go vet ./internal/identity/...` sem erros)
- Build: pass (`go build ./...`)
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `Execute` retorna `Principal{UserID, SourceWhatsApp}` para user ativo + linha `outbox_events` com Type `auth.principal_established` na mesma TX -> comprovado: cenário "usuario ativo" em `establish_principal_integration_test.go` seeds usuário ativo, executa `EstablishPrincipal`, verifica `p.IsZero() == false` e `countOutboxByType("auth.principal_established") >= 1`
- `Execute` retorna `application.ErrUnknownUser` (sentinel comparável com `errors.Is`) para inexistente + linha outbox `auth.unknown_user` -> comprovado: cenário "usuario inexistente" em `establish_principal_integration_test.go` verifica `errors.Is(err, application.ErrUnknownUser)` e `countOutboxByType("auth.unknown_user") >= 1`; cenário unitário "deve retornar ErrUnknownUser e publicar unknown_user" em `establish_principal_test.go` verifica `errors.Is(err, application.ErrUnknownUser)` via mock
- Falha de `outbox.Publish` causa rollback observável (zero linha outbox + erro propagado) -> comprovado: cenário unitário "deve propagar erro de outbox e nao retornar Principal (rollback observavel via erro)" em `establish_principal_test.go` — publisher mock retorna erro, `Execute` propaga erro, `p.IsZero() == true`; cenário 4.7 unitário "deve propagar erro do outbox e causar rollback" em `mark_user_deleted_test.go`
- `MarkUserDeleted` publica `user.deleted` confirmado por integration test -> comprovado: `mark_user_deleted_integration_test.go` cenário "usuario ativo: marca como deletado e publica user.deleted no outbox" verifica `countOutboxByType("user.deleted") >= 1` após `Execute` sem erro

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`go test ./internal/identity/... -count=1` passa).
- [x] Lint/vet/build sem regressão (`go build ./...`, `go vet ./internal/identity/...`).
- [x] Estado de tasks.md sincronizado (4.0 → done).

## Diff Reviewed

sha=N/A
verdict=APPROVED
tool=claude

## Coverage

package=internal/identity/application/usecases
delta=+4 cenários unitários em mark_user_deleted_test.go; +integration test para establish_principal e mark_user_deleted

## Suposições
- O publisher criado em `newPublisher()` nos integration tests usa `s.mgr.DBTX(s.ctx)` sem transação aberta; a UoW abre a transação real e o publisher recebe o DBTX transacional via `s.mgr.DBTX(ctx)` onde `ctx` é o context injetado pelo UoW — padrão idêntico ao usado em onboarding (`newManagerPublisher`).
- Integration tests com build tag `//go:build integration` não executam em `go test ./...` sem a tag; só correm com `go test -tags integration ./...`. Isso é o padrão do projeto conforme `testutil_test.go` da camada postgres.

## Riscos Residuais
- Integration tests de `establish_principal` e `mark_user_deleted` ficam no pacote `usecases_test` em vez de `postgres_test`, sem acesso ao `setupTestDB` compartilhado. Cada suite spin-up seu próprio container. Isso aumenta o tempo de CI quando ambos correm em sequência. Mitigação futura: extrair `setupTestDB` para helper compartilhado na camada `usecases`.
- O cenário de "rollback observável por falha de outbox" nos integration tests não simula falha real de publisher (requer injeção de publisher falho), deixando essa cobertura apenas nos testes unitários com mock. Cenário 3 nos integration tests de EstablishPrincipal valida apenas o happy path adicional.

## Conflitos de Regra
- none
