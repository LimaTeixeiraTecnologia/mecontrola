# Generated: 2026-07-04T11:22:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 3.0
- Título: Migration 000003 + repositórios unificados
- Arquivo: .specs/prd-transactions-crud-unificado/task-3.0-migration-repositories.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-transactions-crud-unificado/prd.md
- TechSpec: .specs/prd-transactions-crud-unificado/techspec.md (seções Migration, Interfaces Chave do TransactionRepository, Modelos de Dados / fonte única do resumo mensal)
- ADRs: adr-003-unified-transactions-schema.md (decisões 1–5), adr-002-card-purchase-cutover-drop.md
- DDL de referência: migrations/000001_initial_schema.up.sql (linhas 793–933), migrations/000002 (padrão idempotente com DO $$ blocks)
- Governança: agent-governance (auto), go-implementation (linguagem, inferida do diff .go), R-ADAPTER-001.1/.2, R-TXN-003/004, R-TESTING-001

## Comandos Executados
- `go build ./internal/transactions/infrastructure/repositories/postgres/ ./internal/transactions/application/interfaces/` -> pass (BUILD_EXIT=0)
- `go vet ./internal/transactions/infrastructure/repositories/postgres/ ./internal/transactions/application/interfaces/` -> pass (VET_EXIT=0)
- `mockery --config .mockery.yml` -> regenerou mock de TransactionRepository (novos métodos + SumByMonthExcludingCredit); único mock alterado é transaction_repository.go (0 mocks fora de transactions alterados). Erro pré-existente de `CardThresholdReader` (drift em .mockery.yml de budgets, ausente no source) não impacta e não é do escopo.
- `go build ./internal/transactions/application/interfaces/mocks/` -> pass (MOCKS_BUILD_EXIT=0)
- `go vet -tags integration ./internal/transactions/infrastructure/repositories/postgres/ ./migrations/` -> pass (após ajustar `repo.SumByMonth`->`SumByMonthExcludingCredit` no teste de integração existente)
- `go test -tags integration -count=1 -run 'TestUnify000003Suite' ./migrations/` -> ok 2.554s (up/down/up idempotente, colunas/constraints/FK/UNIQUE, drop de card_purchases, revert)
- `go test -tags integration -count=1 -run 'TestTransactionCardItemsSuite|TestCardInvoiceRepositoryIntegrationSuite|TestTransactionRepositorySuite' ./internal/transactions/infrastructure/repositories/postgres/` -> ok 6.448s
- `go test -tags integration -count=1 ./migrations/` -> ok 3.293s (suite completa de migrations, após bump das 2 asserções de head-version 2->3)
- gate R-ADAPTER-001.1 (zero comentários) -> OK vazio
- gate R-ADAPTER-001.2 (SQL direto em handlers/consumers/producers/jobs) -> OK vazio (mudança vive em repositories)
- gate R-TXN-004 (labels user_id/card_id/category_id) -> OK vazio
- gate idempotência (IF EXISTS/IF NOT EXISTS em up/down) -> 16 up / 17 down

## Arquivos Alterados
- migrations/000003_unify_transactions.up.sql — novo (ADR-003 decisões 1–4): ADD COLUMN card_id/installments_total/card_closing_day/card_due_day + CHECK completude(pm=7)/ranges + FK card_id→cards; RENAME purchase_id→transaction_id guardado + UNIQUE(transaction_id,installment_index) + FK→transactions(id); DROP COLUMN materialized_purchase_id; DROP TABLE transactions_card_purchases
- migrations/000003_unify_transactions.down.sql — novo (reconstrução vazia reversível)
- internal/transactions/infrastructure/repositories/postgres/transaction_repository.go — scan/insert/update nil-safe das 4 colunas de cartão via helper `cardColumns`; Reconstitute com os 3 novos option (fix do call-site quebrado pela Tarefa 2.0); novos `GetItemsByTransactionID`, `ReplaceItems` (ON CONFLICT (transaction_id, installment_index)), `ExistsActiveCreditByCard` (payment_method=7); `SumByMonth`->`SumByMonthExcludingCredit` (WHERE payment_method<>7)
- internal/transactions/infrastructure/repositories/postgres/card_invoice_repository.go — leitura de item por coluna `transaction_id` (rename); OCC ApplyDelta por (user,card,ref_month) intacto
- internal/transactions/infrastructure/repositories/postgres/monthly_summary_repository.go — `ListEntries` ramo transactions filtra `payment_method <> 7` (2 variantes: base + cursor)
- internal/transactions/infrastructure/repositories/postgres/recurring_materialization_repository.go — remove leitura/escrita de `materialized_purchase_id` (InsertIfAbsent/IsCompleted/MarkCompleted)
- internal/transactions/application/interfaces/transaction_repository.go — contrato: +GetItemsByTransactionID, +ReplaceItems, +ExistsActiveCreditByCard, SumByMonth->SumByMonthExcludingCredit
- internal/transactions/application/interfaces/mocks/transaction_repository.go — regenerado (mockery)
- internal/transactions/infrastructure/repositories/postgres/transaction_repository_integration_test.go — chamada atualizada para SumByMonthExcludingCredit
- migrations/migrations_integration_test.go — 2 asserções de head-version atualizadas 2->3 (necessário por adicionar 000003)
- migrations/000003_unify_transactions_integration_test.go — novo (up/down/up + colunas/constraints/FK + rejeição de cartão incompleto)
- internal/transactions/infrastructure/repositories/postgres/transaction_card_items_integration_test.go — novo (ReplaceItems/GetItemsByTransactionID, upsert on-conflict, ExistsActiveCreditByCard, SumByMonthExcludingCredit exclui crédito)

## Resultados de Validação
- Testes: pass (`go test -tags integration ./migrations/` ok; suites TransactionCardItems/CardInvoice/TransactionRepository ok)
- Lint: pass (`go vet` scoped e `-tags integration` sem saída)
- Veredito do Revisor: APPROVED (self-review; subagente estreito, escopo persistência/migration)

## Critérios de Aceite
<!-- RF cobertos (tasks.md): RF-12 (distribuição por fatura via GetItemsByTransactionID/ReplaceItems), RF-16 (update recompõe itens/deltas — persistência ReplaceItems ON CONFLICT), RF-16a (delete reverte via ReplaceItems nil), RF-24a (corte sem backfill: DROP TABLE transactions_card_purchases em ledger vazio) -->
- `migrate up` e `migrate down` idempotentes em banco limpo (reexecução não falha) -> comprovado: TestUnify000003Suite executa up→Steps(-1)→up e assertVersion 3/2/3; suite completa de migrations (TestBaselineUpDownUp com up→down-to-0→up) passa
- CHECKs de completude/ranges e FK `card_id → cards(id)` criados; UNIQUE e FK de `card_invoice_items(transaction_id, ...)` criados -> comprovado: assertConstraintPresent para transactions_card_completeness_chk, transactions_installments_range_chk, transactions_card_closing_day_chk, transactions_card_due_day_chk, transactions_card_fk, transactions_card_invoice_items_transaction_uk, transactions_card_invoice_items_transaction_fk (todos PASS)
- `transactions_card_purchases` inexistente após `up`; recriada vazia após `down` -> comprovado: assertTableMissing pós-up e assertTablePresent pós-down em TestUnify000003Suite
- `card_invoice_items.transaction_id` existente pós-`up`; `materialized_purchase_id` ausente -> comprovado: assertColumnPresent(transaction_id)+assertColumnMissing(purchase_id) e assertColumnMissing(materialized_purchase_id) pós-up
- Nenhum SQL de domínio fora do adapter Postgres; producers apenas mapeiam evento (R-TXN-003) -> comprovado: gate R-ADAPTER-001.2 vazio; alterações vivem em repositories/postgres; producers não tocados
- Zero comentários em Go; `defer func() { _ = rows.Close() }()` em toda iteração de `rows` -> comprovado: gate R-ADAPTER-001.1 vazio; GetItemsByTransactionID usa `defer func() { _ = rows.Close() }()`
- Resumo mensal não double-conta crédito (crédito somado só via fatura) -> comprovado: SumByMonthExcludingCredit tem `WHERE payment_method <> 7` e ListEntries filtra `payment_method <> 7`; TestSumByMonthExcludesCredit assert outcome=5000 (pix) ignorando a transação credit_card criada no mesmo mês

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos correspondentes em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree-3.0
verdict=APPROVED
tool=self-review

## Coverage

package=internal/transactions/infrastructure/repositories/postgres
delta=+integration (migration up/down, GetItemsByTransactionID, ReplaceItems on-conflict, ExistsActiveCreditByCard, SumByMonthExcludingCredit)

## Suposições
- "mover ReplaceItems/ExistsActiveCreditByCard" foi implementado como ADIÇÃO ao TransactionRepository (interface+impl), operando sobre `transaction_id`/`payment_method=7`. A versão em cardPurchaseRepository permanece intacta porque a interface `CardPurchaseRepository` e sua superfície são removidas na Tarefa 4.0/6.0 (ADR-002); removê-las agora quebraria a compilação fora do escopo desta tarefa.
- `CardInvoiceRepository` (interface em application/interfaces) NÃO teve assinatura alterada: o rename purchase_id→transaction_id é detalhe interno da coluna; a techspec não altera esse contrato. Só o adapter (impl) mudou.
- Interface `RecurringMaterializationRepository` mantida (fora da lista de edição da subtarefa 3.7); os parâmetros `materializedPurchaseID`/`purchaseID` tornam-se não usados (permitido em Go) até a Tarefa 4.0 limpar os call-sites.
- Entity `CardInvoiceItem` não foi renomeada (fora de escopo — Tarefa 2.0): o valor da coluna `transaction_id` é mapeado no slot `purchaseID` do construtor; semanticamente ambos carregam o id do agregado transação.

## Riscos Residuais
- Testes de integração pré-existentes de card_purchase (`card_purchase_repository_integration_test.go`) e de recurring materialization com `materialized_purchase_id` falham em runtime sob o schema pós-000003 (tabela/coluna removidas). Isso é esperado: card-purchase é removido na Tarefa 4.0; a execução consolidada da suíte de integração completa é responsabilidade da Tarefa 8.0. Por isso os testes de integração foram executados de forma direcionada (migração + itens por transação + card_invoice + transaction repo).
- `go build ./...` do repo inteiro ainda não fecha (application/usecases só é reconectado na Tarefa 4.0) — conforme instrução explícita da tarefa, não é critério aqui.
- Drift pré-existente em `.mockery.yml` (CardThresholdReader em budgets) faz `task mocks` global reportar erro; não afeta os mocks das transactions nem esta tarefa.

## Conflitos de Regra
- none
