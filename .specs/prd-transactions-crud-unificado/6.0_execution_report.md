# Generated: 2026-07-04T00:00:00Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 6.0
- Título: HTTP unificado + remoção de card-purchase + OpenAPI
- Arquivo: task-6.0-http-remocao-card-purchase.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-transactions-crud-unificado/prd.md
- TechSpec: .specs/prd-transactions-crud-unificado/techspec.md
- Governança: AGENTS.md, agent-governance, go-implementation (SKILL R0–R7), R-ADAPTER-001, R-DTO-VALIDATE-001, R-TXN-WORKFLOWS-001, R-TESTING-001; ADR-002 (cutover-drop)

Requisitos cobertos: RF-03, RF-05, RF-07, RF-11a, RF-11b, RF-13, RF-14, RF-19, RF-24.

## Comandos Executados
- `go build ./internal/transactions/...` -> exit 0 (BUILD_OK)
- `go vet ./internal/transactions/...` -> exit 0 (VET_OK)
- `go test ./internal/transactions/...` -> 13 pacotes ok, 0 falhas
- `gofmt -l internal/transactions/` -> vazio (GOFMT_CLEAN após reformatar transaction.go)
- gate R-ADAPTER-001.1 (comentários Go prod) -> vazio (OK)
- gate R-ADAPTER-001.2 (SQL em adapters handlers/producers) -> vazio (OK)
- gate R-DTO-VALIDATE (todo input DTO com Validate()) -> vazio (OK)
- `python3 -c "yaml.safe_load(openapi.yaml)"` -> YAML OK; RawCreateTransaction.if presente; sem refs pendentes
- subagent[reviewer diff internal/transactions] -> APPROVED_WITH_REMARKS (0 critical/high; 1 medium endereçado, 2 low registrados)

## Arquivos Alterados
- internal/transactions/application/dtos/input/errors.go (sentinel installments nomeando campo; remoção de sentinels órfãos total_amount/purchased_at)
- internal/transactions/application/dtos/input/raw_create_transaction.go (card_id/installments + Validate superfície — pré-existente, mantido)
- internal/transactions/application/dtos/input/raw_update_transaction.go (idem edição)
- internal/transactions/application/dtos/input/raw_transaction_validate_test.go (novo — testes de Validate)
- internal/transactions/application/dtos/input/raw_create_card_purchase.go (removido)
- internal/transactions/application/dtos/input/raw_update_card_purchase.go (removido)
- internal/transactions/application/dtos/output/transaction.go (card_id, installments_total, ref_months_affected, items omitempty + mapeamento em TransactionFrom)
- internal/transactions/application/dtos/output/card_invoice.go (CardInvoiceItemOutput movido para cá)
- internal/transactions/application/dtos/output/card_purchase.go (removido)
- internal/transactions/domain/valueobjects/payment_method.go (método IsCreditCard())
- internal/transactions/domain/entities/transaction.go (gofmt de alinhamento — pré-existente da tarefa 2.0)
- internal/transactions/application/usecases/errors.go (ErrPaymentMethodMigrationNotAllowed; remoção de ErrCardPurchaseNotFound/ErrCardPurchaseConflict)
- internal/transactions/application/usecases/helpers.go (guardPaymentMethodMigration puro)
- internal/transactions/application/usecases/update_transaction.go (guard de migração no persist())
- internal/transactions/application/usecases/helpers_guard_test.go (teste do guard — 4 casos)
- internal/transactions/application/usecases/materialize_recurring_for_day.go (recorrência credit_card materializa como Transaction unificada; removida dep cardPurchaseCreator)
- internal/transactions/application/usecases/materialize_recurring_for_day_test.go (ajuste dos testes de crédito)
- internal/transactions/application/usecases/get_list_invoice_test.go (removidas suites GetCardPurchase/ListCardPurchases; mantida invoice)
- internal/transactions/application/usecases/{create,update,delete,get}_card_purchase.go, list_card_purchases.go + *_test.go (removidos)
- internal/transactions/application/usecases/mocks/{card_purchase_creator,unit_of_work_card_purchase,unit_of_work_list_card_purchases,unit_of_work_output}.go (removidos)
- internal/transactions/infrastructure/http/server/handlers/{create,update,delete,get}_card_purchase_handler.go, list_card_purchases_handler.go (removidos)
- internal/transactions/infrastructure/http/server/handlers/error_mapper.go (código único version_conflict; ramo 422 payment_migration_forbidden; remoção dos ramos card-purchase)
- internal/transactions/infrastructure/http/server/handlers/handlers_extra_test.go (removidos mocks/testes card-purchase)
- internal/transactions/infrastructure/http/server/transactions_router.go (removida rota-grupo /card-purchases + 5 campos/params)
- internal/transactions/infrastructure/http/server/router_test.go (removidos dummies CP; novo teste 404 nas rotas removidas)
- internal/transactions/infrastructure/messaging/database/producers/card_purchase_event_publisher.go + *_integration_test.go (removidos)
- internal/transactions/module.go (wiring limpo: CP use cases/handlers/producer/workflow/idGen; assinatura de NewTransactionsRouter; struct fields; subscrições de evento card_purchase órfãs)
- internal/transactions/openapi.yaml (RawCreateTransaction card_id/installments + if/then; Transaction+CardInvoiceItem; remoção de RawCreateCardPurchase/CardPurchase/CardPurchasePage e 5 rotas)

## Resultados de Validação
- Testes: pass (`go test ./internal/transactions/...` — 13 pacotes ok, 0 falhas)
- Lint: pass (`go vet` exit 0; `gofmt -l` vazio; gates R-ADAPTER-001.1/.2, R-DTO-VALIDATE vazios)
- Veredito do Revisor: APPROVED_WITH_REMARKS

## Critérios de Aceite
- As 5 rotas `/api/v1/card-purchases` retornam 404 após o corte -> comprovado: `transactions_router.go` sem rota-grupo; `TestRouterCardPurchaseRoutesRemoved` (router_test.go) assere 404 nas 5 rotas; grep `card-purchases` em prod vazio.
- Contrato unificado documentado no openapi (RawCreateTransaction com card_id/installments + if/then; RawCreateCardPurchase e rotas ausentes) -> comprovado: `openapi.yaml` RawCreateTransaction com `if: payment_method const credit_card then required [card_id]` e installments 1..24 default 1; `python3 yaml.safe_load` OK; grep `CardPurchase`/`card-purchases` em openapi vazio.
- `Validate()` cobre card_id obrigatório e installments 1..24 para credit_card nomeando o campo, sem duplicar enum (RF-03/RF-13/RF-11b) -> comprovado: `raw_transaction_validate_test.go` (credit sem card_id → ErrInputCardIDRequired; installments 0/25 → ErrInputInstallmentsOutOfRange "installments:"; válido 1..24; origin_wamid sem operation → erro) — `go test .../dtos/input` ok; enum/doc permanecem em ParsePaymentMethodForCreate.
- Output expõe card_id, installments_total, ref_months_affected, items (omitempty) só para crédito (RF-14) -> comprovado: `output/transaction.go` struct + TransactionFrom mapeia CardID/InstallmentsTotal via Option (nil quando não-crédito); campos `omitempty`.
- `error_mapper` expõe único `version_conflict`, sem resíduo card_purchase_version_conflict -> comprovado: `error_mapper.go:34-41` ramo único code `version_conflict` (transaction + recurring); grep `card_purchase_version_conflict`/`transaction_version_conflict` em prod vazio.
- Nenhum handler com SQL/regra/branch de domínio; zero comentários Go prod (RF-05/RF-07/RF-19) -> comprovado: gates R-ADAPTER-001.1 e R-ADAPTER-001.2 retornam vazio; guard usa VO `IsCreditCard()` (sem literal `payment_method` no use case/handler).
- Build compila sem referências pendentes aos artefatos removidos (RF-24) -> comprovado: `go build`/`go vet`/`go test ./internal/transactions/...` verdes; grep aos artefatos removidos (RawCreateCardPurchase input, output.CardPurchase, 5 use cases, producer, handlers) sem match em prod.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com `go test ./internal/transactions/...`).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree-uncommitted
verdict=APPROVED_WITH_REMARKS
tool=reviewer

## Coverage

package=internal/transactions/...
delta=n/a (suíte unitária verde; cobertura não medida nesta tarefa)

## Suposições
- Escopo da 6.0 mantém entidade/commands/services/repository `CardPurchase*` (usados por materialização de recorrência e `has_open_installments`); a remoção definitiva do domínio card-purchase pertence a tarefa posterior (ADR-002).
- Recorrência credit_card passa a materializar como Transaction unificada (createTransaction) porque `CreateCardPurchase` foi removido; `MarkCompleted(&txID, nil)`.
- `internal/agents` (Tarefa 7.0) e wiring `cmd` final (Tarefa 8.0) não foram editados; `go build ./...` global não foi condição de done (apenas `./internal/transactions/...`).

## Riscos Residuais
- [low] `output.TransactionFrom` nunca popula `RefMonthsAffected`/`Items` (a entidade Transaction não os carrega); respostas HTTP de transação de crédito não trazem itens de fatura — gap contrato-vs-realidade, intencional por escopo; confirmar consumidores na Tarefa 7.0/8.0.
- [low] Invariante "credit_card exige installments" está na superfície (`Validate()`); o smart constructor exige apenas card_id+outcome (installments default 1 via `installmentCountOrSingle`). Consistente hoje pois todo entrypoint passa por `Validate()`; leve tensão com R-TXN-002.
- `materializeAsCreditTransaction` assume `InstallmentsTotal().Value() ≥ 1`; template de crédito com installments inválido reprovaria no `Validate()` e abortaria o batch do dia — data-dependente, baixo dado o cutover-drop (ledger prod vazio).
- Entidade/repo/commands/workflow `CardPurchase*` permanecem no módulo (fora do escopo da 6.0); sem referências pendentes (build verde), remoção definitiva pendente.
- Gate heurístico R-TXN-001 acusa `has_open_installments.go` (span/erro) e `errors.go` (palavra "direction") — matches pré-existentes em arquivos não editados por esta tarefa; não são branching de domínio introduzido aqui.

## Conflitos de Regra
- none
