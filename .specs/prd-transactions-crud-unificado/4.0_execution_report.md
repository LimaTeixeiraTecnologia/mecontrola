# Generated: 2026-07-04T11:40:40Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.0
- Título: Use cases unificados create/update/delete
- Arquivo: .specs/prd-transactions-crud-unificado/task-4.0-usecases-unificados.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-transactions-crud-unificado/prd.md
- TechSpec: .specs/prd-transactions-crud-unificado/techspec.md (spec-hash-prd 4a96b713…, ADR-001 dispatch, ADR-003 fonte única)
- Governança: AGENTS.md + go-implementation (SKILL.md), R-ADAPTER-001, R-TXN-001/002, R-DTO-002, R-TESTING-001; skills processuais: "Nenhuma além das auto-carregadas".

## Comandos Executados
- `go build ./internal/transactions/...` -> ok (produção compila 100%, incluindo os `*_card_purchase.go` ainda presentes)
- `go vet ./internal/transactions/...` -> ok (limpo)
- `go test ./internal/transactions/application/usecases/...` -> ok (todos verdes; inclui novos cenários credit_card create/update/delete e RF-19 update)
- `go test ./internal/transactions/...` -> ok (árvore inteira verde)
- `go build ./...` -> ok (repo inteiro compila; wiring module.go intacto)
- `golangci-lint run ./internal/transactions/application/usecases/... ./internal/transactions/application/dtos/...` -> 0 issues
- `grep R-TXN-001` (amount_cents/direction/installments/payment_method fora de Decide*/command/input) nos meus arquivos -> VAZIO
- `grep R-ADAPTER-001.1` (zero comentários) nos meus arquivos -> VAZIO
- subagent[reviewer Tarefa 4.0] -> APPROVED_WITH_REMARKS (2 findings [medium]/[low], ambos enraizados em DecideUpdate/Tarefa 2.0 — fora de escopo; sem critical/high/security/blocker)

## Arquivos Alterados
- internal/transactions/application/usecases/create_transaction.go (injeção cardLookup; resolveBilling IO antes do DecideCreate; persist/applyInvoices atômicos no UoW)
- internal/transactions/application/usecases/update_transaction.go (currentItems via GetItemsByTransactionID sem swallow; guard RF-19; recomposeInvoices deltas atômicos)
- internal/transactions/application/usecases/delete_transaction.go (workflow.DecideDelete; reversão de deltas no UoW; publica decision.Event; sem evento inline)
- internal/transactions/application/usecases/card_invoice_orchestration.go (NOVO — helpers de fatura independentes dos arquivos card-purchase removidos na 6.0)
- internal/transactions/application/usecases/helpers.go (map CardID/Installments input→command)
- internal/transactions/application/usecases/reconcile_monthly_summary.go (SumByMonthExcludingCredit)
- internal/transactions/application/usecases/recompute_monthly_summary.go (SumByMonthExcludingCredit)
- internal/transactions/application/dtos/input/raw_create_transaction.go (campos card_id/installments + Validate surface p/ credit_card)
- internal/transactions/application/dtos/input/raw_update_transaction.go (idem)
- internal/transactions/module.go (wiring createTx recebe cardLookup; deleteTx recebe txWorkflow)
- Testes: create/update/delete/reconcile/recompute_monthly_summary_test.go + get/list/search_transactions_test.go (call sites de entities.Reconstitute atualizados para a assinatura pós-Tarefa 1.0/2.0)

## Resultados de Validação
- Testes: pass (`go test ./internal/transactions/application/usecases/...` e `./internal/transactions/...`)
- Lint: pass (`golangci-lint` 0 issues; `go vet` limpo)
- Veredito do Revisor: APPROVED_WITH_REMARKS

## Critérios de Aceite
- Tudo atômico no `uow.Do` (falha em qualquer passo → rollback total) -> comprovado: create `persist`/`applyInvoices`, update `persist`/`recomposeInvoices`, delete UoW closure — todos os passos (Create/Update/SoftDelete, UpsertByMonth, ApplyDelta, ReplaceItems, Publish) retornam erro dentro do mesmo `uow.Do`; testes de erro (RepositoryError, VersionConflict) provam propagação/rollback.
- Idempotência preservada (`ON CONFLICT origin DO NOTHING` + outbox por EventID) -> comprovado: create mantém path `!created → GetByID reconcilia` sem mutar fatura nem publicar (TestExecute_Replay_DoesNotPublish verde).
- `credit_card` só com card_id + direction=outcome; IO `GetForUser` só no path credit_card -> comprovado: `resolveBilling` só chama GetForUser quando `cmd.PaymentMethod == PaymentMethodCreditCard`; TestExecute_NonCreditCard_DoesNotLookupCard (AssertNotCalled GetForUser/UpsertByMonth) verde; smart ctor da Tarefa 1.0 garante outcome+card_id.
- Zero branch de REGRA de domínio no use case (gate R-TXN-001 verde) -> comprovado: `grep amount_cents|direction|installments|payment_method` nos 4 arquivos que toquei retorna VAZIO após filtro Decide*/command/input.
- Span aberto antes do `Validate()` (R-DTO-002); zero comentários Go -> comprovado: create/update abrem span (`Tracer().Start`) antes de `raw.Validate()`; grep de comentários VAZIO.
- Guard RF-19 presente em create E update; `DeleteTransaction` sem evento montado inline -> comprovado: `guardSubcategoryRequired` em ambos (create:68, update:69); TestExecute_OutcomeWithoutSubcategory_ReturnsValidationError em create e update verdes; delete usa `workflow.DecideDelete` + `decision.Event.(entities.TransactionDeleted)`.
- Resumo mensal sem double-counting (agregadores chamam SumByMonthExcludingCredit) -> comprovado: reconcile/recompute usam `txRepo.SumByMonthExcludingCredit` + `invoiceRepo.SumByMonth`; testes asseguram somas exatas (ex: 30000 income / 15000 outcome) sem contar credit_card duas vezes.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos correspondentes em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree (Tarefa 4.0 subset)
verdict=APPROVED_WITH_REMARKS
tool=reviewer subagent (review skill)

## Coverage

package=internal/transactions/application/usecases
delta=+cenários credit_card (create/update/delete) e RF-19 update; sem regressão nos cenários existentes

## Suposições
- Injeção de `cardLookup` apenas em `CreateTransaction`: `DecideUpdate`/`DecideDelete` (assinaturas fechadas na Tarefa 2.0) não recebem snapshot e operam sobre o `billingSnapshot` congelado da entidade persistida (techspec "billingSnapshot congelado"); `CardInvoiceRepository` é obtido via factory dentro do `uow.Do`. Injetar `cardLookup` não usado em update/delete criaria dependência morta (reprovada por go-implementation/lint). Decisão avaliada e considerada DEFENSÁVEL pelo revisor.
- Campos `card_id`/`installments` adicionados aos input DTOs `RawCreateTransaction`/`RawUpdateTransaction` (aditivo) para tornar o path credit_card alcançável pelo use case; refinamento de OpenAPI/`if-then` permanece na Tarefa 6.0.
- Call sites de `entities.Reconstitute` em testes pré-existentes (get/list/search) foram atualizados para a assinatura pós-Tarefa 1.0/2.0 (3 `option.None` de cartão + version/deletedAt) — pré-requisito para o pacote de testes compilar; sem mudança de comportamento.

## Riscos Residuais
- [medium] (fora de escopo — DecideUpdate/Tarefa 2.0) Transição de payment_method no update (credit_card→pix ou pix→credit_card) com `billingSnapshot` congelado ainda presente toma o ramo não-cartão sem emitir Items/InvoiceDeltas → itens de fatura órfãos e possível double-count. Mitigação já planejada: techspec fixa que `payment_method` NÃO pode migrar de/para credit_card no update (sentinel na Tarefa 6.0, camada HTTP/command). Recomendado reforçar guard em `DecideUpdate` quando 6.0 for implementada.
- [low] (fora de escopo — DecideUpdate/Tarefa 2.0) Update aceita `card_id` alterado sem re-validação de posse via GetForUser; snapshot congelado mantém o ciclo do cartão antigo. Gap menor de authz/consistência.
- [low] Teste unitário de update credit_card usa `.Maybe()` em GetByMonth/ApplyDelta (deltas não estritamente assertados no unit) por não-determinismo de data; a verificação estrita de OCC/delta atômico está explicitamente delegada à Tarefa 8.0 (integração Postgres), conforme "Testes da Tarefa".

## Conflitos de Regra
- none
