# Generated: 2026-07-04T10:59:27Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Validação de categorias (raiz, filha direta, kind↔direction)
- Arquivo: .specs/prd-transactions-crud-unificado/task-5.0-validacao-categorias.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-transactions-crud-unificado/prd.md (RF-17..RF-21)
- TechSpec: .specs/prd-transactions-crud-unificado/techspec.md (Validação por camada; Pontos de Integração; Sequenciamento item 5)
- Governança: go-implementation (Go 1.26.4), R-ADAPTER-001.1 (zero comentários), R-TXN-001/002 (pureza Decide*), R-DTO/R-TESTING-001 (testify/suite whitebox, fake.NewProvider)

## Comandos Executados
- `go test ./internal/categories/... ./internal/transactions/application/interfaces/...` -> pass
- `go test ./internal/transactions/infrastructure/config/...` -> pass (config: cache RF-17/RF-18/Kind + regressão cache-hit)
- `go vet ./internal/categories/... ./internal/transactions/application/interfaces/... ./internal/transactions/infrastructure/config/...` -> exit 0
- `go run scratchpad/guard_check.go` (guards isolados RF-19/20/21) -> ALL GUARD CHECKS PASS
- Gate R-ADAPTER-001.1 (zero comentários) nos 9 arquivos de produção -> limpo
- Gate R-TXN (domínio fora de Decide* em helpers/create/update) -> CLEAN
- `gofmt -l` nos arquivos tocados -> vazio
- subagent[reviewer Task 5.0] -> APPROVED_WITH_REMARKS (1 achado MEDIUM cache-hit RF-18) -> corrigido via bugfix + teste de regressão

## Arquivos Alterados
- internal/categories/application/usecases/validate_subcategory.go (+ validate_subcategory_test.go)
- internal/transactions/application/interfaces/types.go
- internal/transactions/infrastructure/config/categories_cache.go (+ categories_cache_test.go)
- internal/transactions/infrastructure/repositories/postgres/categories_reader_adapter.go (+ categories_reader_adapter_test.go)
- internal/budgets/infrastructure/repositories/postgres/categories_reader_adapter.go (call Execute(ctx, id, uuid.Nil) — mantém build)
- internal/transactions/application/usecases/helpers.go, errors.go, create_transaction.go, update_transaction.go
- internal/transactions/application/usecases/helpers_guard_test.go (novo)

## Resultados de Validação
- Testes: pass (categorias + interfaces + config; guards validados isoladamente pois application/usecases não compila por drift da Task 2.0)
- Lint: pass (go vet exit 0 nos pacotes compiláveis; gofmt limpo; gate zero-comentários limpo)
- Veredito do Revisor: APPROVED_WITH_REMARKS (achado MEDIUM único corrigido antes do fechamento; sem remarks críticos)

## Critérios de Aceite
- category_id não-raiz → erro (RF-17) -> comprovado: `CategoriesCache.Validate` usa `rootKind/isRoot` retornando `ErrCategoryNotFound`; teste `TestValidate_SubcategoryID_NonRootCategory_Error` (config pass).
- Subcategoria de outra raiz → `ErrSubcategoryNotDirectChild` (RF-18) -> comprovado: `ValidateSubcategory` rejeita `*category.ParentID != expectedParentID`; teste `TestExecute_SubcategoriaDeOutraRaiz` (categorias pass) e `TestValidateSubcategory_NotDirectChild` (adapter); cache-hit não faz bypass: `TestValidate_SubcategoryID_CacheHit_DoesNotBypassParentCheck` (config pass).
- kind da categoria ≠ direction → erro (RF-21) -> comprovado: `guardCategoryKindDirection` + `kindForDirection`; teste isolado `guard_check.go` casos "outcome income"/"income expense" retornam `ErrCategoryKindDirectionMismatch`.
- income sem subcategoria → ok (RF-20); outcome sem subcategoria → erro (RF-19) -> comprovado: `guardSubcategoryRequired`; guard_check.go casos "income sem sub -> ok" e "outcome sem sub -> erro"; wiring em create_transaction.go (pre-validador) e update_transaction.go (adicionado).
- Snapshots carregam Name/ParentName reais (não slug composto) e Kind/ParentID -> comprovado: adapter mapeia `result.CategoryName`/`result.ParentName`/`result.Kind`/`&expectedParentID`; teste `TestValidateSubcategory_MapsRealNames` assere Name="Aluguel", ParentName="Custo Fixo", Kind="expense", ParentID=rootID.
- Decide* permanece puro; zero comentários; errors.Join -> comprovado: nenhuma edição em domain/services; guards vivem no use case com `errors.Join`; gate R-ADAPTER-001.1 limpo; gate R-TXN em helpers/create/update CLEAN.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos em Comandos Executados).
- [x] Lint/vet/gofmt sem regressão nos pacotes compiláveis; guards validados isoladamente (application/usecases bloqueado por drift Task 2.0, fora do escopo).
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=57d9693
verdict=APPROVED_WITH_REMARKS
tool=reviewer

## Coverage

package=internal/transactions/infrastructure/config; internal/categories/application/usecases
delta=novos testes RF-17/RF-18/Kind + regressão cache-hit; enriquecimento de ValidateSubcategory + guards isolados

## Suposições
- `toKind(direction)`: income→"income", outcome→"expense" (coerente com `valueobjects.Kind.String()`).
- `expectedParentID == uuid.Nil` desliga o check de filha-direta (usado por budgets, que não tem raiz esperada) — preserva o comportamento existente do módulo budgets.
- Snapshot da categoria raiz (sem subcategoria) mantém `Name` vazio (não há nome de raiz no cache); apenas `Kind` foi adicionado — não regride comportamento anterior.

## Riscos Residuais
- Pacotes `internal/transactions/application/usecases` e `.../infrastructure/repositories/postgres` não compilam por drift paralelo da Task 2.0 (`entities.Reconstitute`/`DecideCreate`/`DecideUpdate` — fora do escopo). Logo, o wiring dos guards em create/update_transaction.go e o `categories_reader_adapter_test.go` de transactions foram validados estaticamente (imports corretos, `valueobjects` órfão removido) e a lógica de guard por teste isolado; a execução end-to-end desses testes é deliverable da Tarefa 4.0/8.0.
- Achado do revisor (RF-18 bypass em cache-hit cross-root de mesmo kind) foi corrigido nesta tarefa (`entry.parentID == categoryID` no cache-hit) com teste de regressão.

## Conflitos de Regra
- none
