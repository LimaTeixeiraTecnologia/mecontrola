# Relatório de Execução — Tarefa 3.0

## Status: done

## Resumo
Baseline SQL reforçado com defesa canônica: colunas de evidência, FKs, checks e triggers semânticos adicionados a `transactions` e `transactions_recurring_templates`. 21 casos de integração passando, incluindo deprecated root/leaf e FK/trigger para UUID inexistente.

## Subtarefas Executadas
- [x] 3.1 `subcategory_id` e `subcategory_name_snapshot` tornados NOT NULL
- [x] 3.2 Colunas normalizadas de evidência categorial adicionadas
- [x] 3.3 FKs de `category_id` e `subcategory_id` para `mecontrola.categories(id)`
- [x] 3.4 Checks de outcome, score, kind, confidence, match quality, signal type, decision source e editorial version
- [x] 3.5 Função compartilhada `validate_category_write_gate` criada
- [x] 3.6 Triggers `transactions_category_write_gate_trg` e `transactions_recurring_templates_category_write_gate_trg` criados
- [x] 3.7 Down migration atualizada
- [x] 3.8 Testes de integração completos incluindo deprecated root/leaf e FK/trigger para UUID inexistente

## Validações
- `go build ./migrations/...` → pass
- `go test -tags integration -run TestMigrationSuite/TestCategoryWriteGateBaseline ./migrations/...` → 2 passed (21 cenários)
- `go test -tags integration -run TestMigrationSuite/TestCategoryWriteGateRecurringTemplates ./migrations/...` → pass

## Arquivos Modificados
- `migrations/000001_initial_schema.up.sql`
- `migrations/000001_initial_schema.down.sql`
- `migrations/migrations_integration_test.go`
