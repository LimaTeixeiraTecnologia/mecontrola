# Execution Report — Task 5.0

**Status:** done
**Date:** 2026-06-12

## Summary
Use case RecordGatewayAuthFailure implementado: valida 4 reasons gateway_*, publica auth.failed via outbox com campos forensics, event_id único por chamada.

## Arquivos Criados/Modificados
- `internal/identity/application/dtos/input/record_gateway_auth_failure.go` — DTO flat com UserIDRaw, Reason, RequestID, ClientIPRaw
- `internal/identity/application/usecases/record_gateway_auth_failure.go` — use case; whitelist de 4 reasons; time.Now().UTC() inline; uuid.NewV7() por chamada
- `internal/identity/application/usecases/record_gateway_auth_failure_test.go` — 12 sub-tests; interface{} → any corrigido
- `internal/identity/domain/entities/auth_event.go` — constante AuthEventSourceGateway = "gateway" adicionada

## Gates Verificados
- `go test ./internal/identity/application/usecases/... -run "RecordGatewayAuthFailure"` → PASS (12 sub-tests)
- R-ADAPTER-001.1 zero comentários → PASS
- `go build ./internal/identity/...` → PASS
- `git diff go.mod` → zero adições

## Observações
- `ErrInvalidGatewayReason` exportado para `errors.Is` nos callers.
- Consumer `ProjectAuthEvent` do prd-auth-foundation lê `source` como string livre — valor "gateway" projetado corretamente sem alteração.
- `task mocks` falha preexistente em onboarding (não relacionado a esta tarefa, confirmado em `main` antes das mudanças).
