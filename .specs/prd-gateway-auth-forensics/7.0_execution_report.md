# Execution Report — Task 7.0

**Status:** done
**Date:** 2026-06-12

## Summary
Cabeamento completo: NewRequireGatewayAuth em module.go, gatewayAuth injetado no CardRouter (ADR-004), integration test com testcontainers, build e testes do card passando.

## Arquivos Criados/Modificados
- `internal/identity/module.go` — factory `NewRequireGatewayAuth` exportada
- `internal/card/infrastructure/http/server/router.go` — campo `gatewayAuth`, `sub.Use(rt.gatewayAuth)` antes de `InjectPrincipalFromHeaderWithO11y` (ADR-004)
- `internal/card/module.go` — wiring atualizado
- `cmd/server/server.go` — injeção do middleware ao montar CardRouter
- `internal/identity/infrastructure/http/server/middleware/require_gateway_auth_integration_test.go` — `//go:build integration`; 2 cenários: gateway válido → next, inválido → 401 + outbox_events row

## Gates Verificados
- `go build ./...` → PASS
- `go test ./internal/card/... -count=1` → PASS (todos os pacotes)
- `task lint:user-isolation` → PASS
- R-ADAPTER-001.1 zero comentários → PASS
- ADR-004: `sub.Use(rt.gatewayAuth)` linha 56, `InjectPrincipalFromHeaderWithO11y` linha 57 — ordem correta confirmada
- Webhooks `/whatsapp/`, `/kiwify/` — zero diff nesses routers

## Observações (APPROVED_WITH_REMARKS)
- Integration test verifica `outbox_events` count; o consumer que projeta em `auth_events` não roda na suite (correto arquiteturalmente).
- GatewayAuthWindow=0 em dev rejeitará todos os requests — operador deve configurar env.
- Subtarefa 7.6 (curl manual) não verificável por diff — validação E2E fica para smoke pós-deploy.
