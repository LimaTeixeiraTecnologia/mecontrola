# Relatório de Execução de Tarefa

<!-- Generated: 2026-06-29T07:45:00Z -->

## Tarefa
- ID: 2.0
- Título: Migration `000003` — storage genérico inspirado no Mastra + pgvector
- Arquivo: .specs/prd-platform-mastra/task-2.0-migration-000003-storage.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-platform-mastra/prd.md
- TechSpec: .specs/prd-platform-mastra/techspec.md (seção "Modelos de Dados" e referência ADR-005)
- Governança: go-implementation SKILL.md, R-WF-KERNEL-001, R-ADAPTER-001.1, AGENTS.md

## Comandos Executados
- `grep -niE "intent_kind|category_id|direction|payment_method" migrations/000003_platform_mastra.up.sql` → vazio (gate OK)
- `go build ./...` → OK (zero erros)
- `go vet ./migrations/...` → OK
- `go test -v -tags=integration -run TestMigrationSuite/TestMigration000003PlatformMastra ./migrations/...` → PASS (0.18s)
- `go test -v -tags=integration -run "TestMigrationSuite/TestMigration000003PlatformMastra|TestMigrationSuite/TestFinalSchemaColumnsAndConstraints|TestMigrationSuite/TestCardsNoPCIColumns" ./migrations/...` → todos PASS
- `go test -v -tags=integration -run TestMigrationSuite ./migrations/...` → 10/11 PASS; TestBaselineUpDownUp FAIL pré-existente (confirmado independente de 000003)

## Arquivos Alterados
- `migrations/000003_platform_mastra.up.sql` — novo; DROP 7 agent_*, CREATE EXTENSION vector, CREATE 6 platform_*
- `migrations/000003_platform_mastra.down.sql` — novo; DROP platform_*, DROP EXTENSION vector, recriar 7 agent_*
- `deployment/docker/Dockerfile.postgres` — adiciona postgresql-16-pgvector ao apt-get install
- `internal/platform/database/postgres/test_helper.go` — muda testImage de postgres:16-alpine para pgvector/pgvector:pg16
- `migrations/migrations_integration_test.go` — adiciona TestMigration000003PlatformMastra e helpers assertVectorExtensionAvailable, assertHnswIndexPresent, assertNoDomainColumnsInPlatformTables

## Resultados de Validação
- Testes: pass (TestMigration000003PlatformMastra + 9 outros; TestBaselineUpDownUp pré-existente descartado)
- Lint/Build: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite

- `migrate up` e `migrate down` aplicam sem erro; `up` após `down` é idempotente → comprovado: TestMigration000003PlatformMastra valida Steps(-1) + Steps(+1) em sequência, PASS
- Após `up`: tabelas `platform_*` presentes, extensão `vector` ativa, `agent_*` ausentes, `workflow_*` intactas → comprovado: TestMigration000003PlatformMastra asserta platform_threads/messages/resources/runs/embeddings/scorer_results presentes + agent_* ausentes + workflow_runs/steps presentes + assertVectorExtensionAvailable, PASS
- Nenhuma coluna de domínio nas `platform_*` → comprovado: assertNoDomainColumnsInPlatformTables verifica intent_kind/category_id/direction/payment_method + gate grep retorna vazio
- Gate `grep -niE "intent_kind|category_id|direction|payment_method" migrations/000003_platform_mastra.up.sql` retorna vazio → comprovado: executado, saída "OK: gate passed"
- índice HNSW em `platform_embeddings.embedding` → comprovado: assertHnswIndexPresent verifica pg_indexes para platform_embeddings_hnsw_idx, PASS

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [ ] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=n/a
verdict=APPROVED
tool=internal

## Coverage

package=migrations
delta=+5 métodos de teste

## Suposições
- TestBaselineUpDownUp falhava antes desta tarefa (confirmado por teste isolado: revertendo image + removendo 000003, o mesmo erro TRUNCATE mecontrola.schema_migrations ocorre). Falha causada por 000001.down.sql que dropa o schema inteiro, impossibilitando que golang-migrate atualize sua tabela de tracking. Fora do escopo desta tarefa.
- pgvector/pgvector:pg16 é a imagem canônica para testes com extensão vector, alinhada com produção (Dockerfile.postgres agora instala postgresql-16-pgvector).
- DRO EXTENSION IF EXISTS vector no down é seguro porque nenhuma outra migration criada neste repo depende da extensão vector além de 000003.

## Riscos Residuais
- TestBaselineUpDownUp permanece quebrado (pré-existente). Requer correção de 000001.down.sql para não dropar o schema de tracking, ou ajuste no downToVersion para ignorar erros após DROP SCHEMA CASCADE.
- A imagem pgvector/pgvector:pg16 é ~700MB vs postgres:16-alpine ~250MB; startup de CI pode ser ligeiramente mais lento. Mitigação: container é compartilhado via singleton (sync.Once) na suite.

## Conflitos de Regra
- none
