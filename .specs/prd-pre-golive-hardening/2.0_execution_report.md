# Generated: 2026-06-12T00:00:00Z

# Execution Report — Task 2.0: B2 Timestamp WhatsApp anti-replay

## Status

done

## Arquivos Alterados

- `internal/platform/whatsapp/dispatcher/dispatcher.go` — adicionado `checkTimestamp`, `OutcomeStaleTS`, `timestampWindow`, contador `staleWebhook`, validação pós-dedup pré-`EstablishPrincipal`
- `internal/platform/whatsapp/dispatcher/dispatcher_test.go` — atualizado `validPayload` para timestamp dinâmico; adicionado `payloadWithTimestamp`; adicionado `TestTimestampValidation` com 5 cenários table-driven
- `migrations/000016_auth_events_stale_webhook_reasons.up.sql` — amplia CHECK constraint de `auth_events.reason` com `stale_webhook` e `invalid_webhook_timestamp`
- `migrations/000016_auth_events_stale_webhook_reasons.down.sql` — reverte para constraint de 000015

## Critérios de Aceite

- RF-01: extrair `entry[].changes[].value.messages[].timestamp` do payload Meta
  -> comprovado: `checkTimestamp(msg.Timestamp)` chamado logo após dedup em `dispatcher.go`; `msg.Timestamp` vem de `payload.ExtractFirstMessage` que extrai de `messages[0].Timestamp`

- RF-02: `|now - ts| > 5min` → 200 OK + `auth_events.reason="stale_webhook"`
  -> comprovado: cenários `+6min` e `-6min` em `TestTimestampValidation` retornam `OutcomeStaleTS`; handler retorna 200 OK para qualquer outcome não-erro

- RF-03: timestamp ausente/inválido → 200 OK + `auth_events.reason="invalid_webhook_timestamp"`
  -> comprovado: cenários `absent_timestamp` e `invalid_timestamp_string` em `TestTimestampValidation` retornam `OutcomeStaleTS`; `publishAuthFailed` chamado com `reason="invalid_webhook_timestamp"`

- RF-04: `time.Now().UTC()` inline; sem `Clock` interface
  -> comprovado: `checkTimestamp` usa `time.Now().UTC()` e `time.Unix(ts, 0).UTC()` inline; nenhuma interface de clock criada

- RF-05: testes unitários cobrindo 5 cenários (dentro janela, +6min, -6min, ausente, inválido)
  -> comprovado: `TestDispatcherSuite/TestTimestampValidation` — 5 sub-cenários PASS

- RF-32: skill `go-implementation` aplicada
  -> comprovado: SKILL.md Etapas 1-5 seguidas; task_type=`http-handler`; refs `architecture.md` + `http-handler.md` carregadas

- RF-33: lint+test verde (no escopo alterado)
  -> comprovado: `go test ./internal/platform/whatsapp/... -count=1` — PASS em todos os 5 sub-pacotes; `go vet ./internal/platform/whatsapp/...` sem erros; lint failures pré-existentes em `internal/card` e `internal/identity` não introduzidas por esta tarefa

- RF-34: sem nova dependência
  -> comprovado: apenas `strconv` da stdlib adicionado (já transitiva); sem `go.mod` alterado

- Zero comentário em `.go` produção
  -> comprovado: gate `grep` retornou vazio em `internal/platform/whatsapp/dispatcher/`

## Comandos Executados

```
go build ./internal/platform/whatsapp/...                          # OK
go test ./internal/platform/whatsapp/dispatcher/... -run "TestDispatcherSuite/TestTimestampValidation" -v -count=1
  # 5/5 PASS
go test ./internal/platform/whatsapp/... -count=1
  # ok dispatcher, handlers, payload, ratelimit, signature
go vet ./internal/platform/whatsapp/...                            # OK
grep zero-comments gate                                             # vazio (PASS)
grep SQL-in-adapter gate                                           # vazio (PASS)
```

## DoD

- [x] Implementação completa das subtarefas 2.1–2.6
- [x] 5 cenários de teste PASS
- [x] Métrica `whatsapp_stale_webhook_total{reason}` registrada
- [x] auth_events publicado com reason correto via `publishAuthFailed`
- [x] Migration 000016 criada (up + down)
- [x] Zero comentários em produção Go
- [x] tasks.md atualizado para `done`

## Riscos Residuais

- A migration 000016 deve ser aplicada antes do deploy em produção. O rollback (down.sql) remove apenas os dois novos reasons; o constraint anterior de 000015 é restaurado.
- Lint global falha em pacotes não relacionados (`internal/card`, `internal/identity/domain/entities`): são regressões pré-existentes, fora do escopo desta tarefa.
