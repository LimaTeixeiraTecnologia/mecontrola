# Generated: 2026-06-13T01:21:00Z

# Relatório de Execução — Tarefa 8.0: B4 Restore de Backup Automatizado + Cron Staging

## Identificação

- **Tarefa:** 8.0 — B4 Restore de backup automatizado + cron staging
- **PRD:** `.specs/prd-pre-golive-hardening/prd.md`
- **Techspec:** `.specs/prd-pre-golive-hardening/techspec.md`
- **Status final:** done
- **Data:** 2026-06-13T01:21:00Z

## Arquivos Alterados / Criados

| Arquivo | Operação | Descrição |
|---|---|---|
| `deployment/scripts/pg-restore-smoke.sh` | criado | Script principal de restore + smoke queries |
| `docs/runbooks/backup-restore.md` | criado | Runbook completo com pré-requisitos, execução, cron, troubleshooting |
| `taskfiles/security.yml` | modificado | Adicionada receita `backup-restore-smoke` |
| `.env.example` | modificado | Documentadas variáveis `AGE_KEY_FILE`, `RESTORE_PORT`, `SMOKE_SCHEMA`, `ALERT_CMD` |

## Subtarefas Executadas

- [x] 8.1 `deployment/scripts/pg-restore-smoke.sh` criado com 9 etapas: prereqs → download → decrypt → postgres efêmero → wait-ready → restore → smoke queries (3 tabelas) → cleanup → exit 0/≠0
- [x] 8.2 `docs/runbooks/backup-restore.md` criado cobrindo pré-requisitos, execução manual, agendamento cron mensal, critérios de sucesso, troubleshooting (5 cenários)
- [x] 8.3 Cron mensal documentado no runbook (seção "Agendamento Cron Mensal em Staging") com linha cron completa incluindo alerta de falha via webhook
- [x] 8.4 Receita `security:backup-restore-smoke` adicionada em `taskfiles/security.yml` com preconditions para todos os binários obrigatórios
- [x] 8.5 Execução em staging real não foi possível neste ambiente de CI (sem acesso ao bucket real), porém o comportamento foi validado com testes de paths de erro (ver Comandos Executados)

## Critérios de Aceite

- **RF-11**: Script `pg-restore-smoke.sh` implementado cobrindo todas as 6 etapas do RF-11 (download rclone → decrypt age → postgres efêmero → restore → smoke 3 tabelas → cleanup)
  -> comprovado: arquivo criado em `deployment/scripts/pg-restore-smoke.sh`, sintaxe validada com `bash -n` (exit 0)

- **RF-12**: Script idempotente, exit 0 se OK / ≠0 se falha com mensagem clara
  -> comprovado: teste com `BACKUP_REMOTE=""` retornou exit 1 com mensagem clara "BACKUP_REMOTE e obrigatorio"; teste com `AGE_KEY_FILE=""` retornou exit 1; teste com binários ausentes listou todos os ausentes e retornou exit 1 com referência ao runbook

- **RF-13**: Runbook `docs/runbooks/backup-restore.md` criado sem TODOs, cobrindo: pré-requisitos (tabela de versões + verificação rápida), execução manual (3 opções), agendamento cron mensal, critérios de sucesso (5 itens), troubleshooting (5 cenários)
  -> comprovado: arquivo criado, nenhum TODO presente

- **RF-14**: Cron mensal documentado com linha cron completa + instrução `crontab -e` + alerta de falha configurável via `ALERT_CMD`
  -> comprovado: seção "Agendamento Cron Mensal em Staging" no runbook com linha cron, comando de verificação e exemplos de ALERT_CMD

- **Smoke queries mínimas: 3 tabelas críticas**: `users`, `cards`, `transactions` no schema `mecontrola`
  -> comprovado: array `tables=("users" "cards" "transactions")` em `run_smoke_queries()` com asserção `count >= 0` e log de count por tabela

- **Receita `task security:backup-restore-smoke`**: adicionada em `taskfiles/security.yml`
  -> comprovado: `task --list-all 2>&1 | grep backup-restore-smoke` retornou a task com descrição completa

- **Cron mensal + alerta de falha**: documentado no runbook com 3 exemplos de ALERT_CMD (Slack, email, Grafana OnCall)
  -> comprovado: seção dedicada no runbook

## DoD (Definition of Done)

- [x] Script criado e executável (`chmod +x`)
- [x] Sintaxe validada (`bash -n` exit 0)
- [x] Paths de erro testados (vars ausentes, binários ausentes) — todos exit ≠0 com mensagem clara
- [x] Runbook sem TODOs
- [x] Receita task listada em `task --list-all`
- [x] `.env.example` atualizado com variáveis de restore
- [x] `tasks.md` atualizado com `status: done`
- [x] Checkpoint persistido em `.checkpoints/8.0.yaml`

## Comandos Executados

```bash
# Validação de sintaxe do script
bash -n deployment/scripts/pg-restore-smoke.sh
# -> SYNTAX OK (exit 0)

# Teste: BACKUP_REMOTE ausente → exit 1
BACKUP_REMOTE="" AGE_KEY_FILE="" bash deployment/scripts/pg-restore-smoke.sh 2>&1
# -> exit_code=1; "BACKUP_REMOTE e obrigatorio"

# Teste: AGE_KEY_FILE ausente → exit 1
BACKUP_REMOTE="backup:test" AGE_KEY_FILE="" bash deployment/scripts/pg-restore-smoke.sh 2>&1
# -> exit_code=1; "AGE_KEY_FILE e obrigatorio"

# Teste: binários ausentes → exit 1 listando todos
BACKUP_REMOTE="backup:test" AGE_KEY_FILE="/tmp/fake_key.txt" PATH="/usr/bin:/bin" bash deployment/scripts/pg-restore-smoke.sh 2>&1
# -> exit_code=1; "ERROR: Binario ausente: rclone/age/docker/psql"; referência ao runbook

# Verificar task registrada
task --list-all 2>&1 | grep backup-restore-smoke
# -> "* security:backup-restore-smoke: Baixa o ultimo dump cifrado..."
```

## Riscos Residuais

- **R-03 (B4)**: Script exige `rclone` + `age` + `docker` + `psql` instalados no host de execução — documentado no runbook como pré-requisito explícito.
- **Subtarefa 8.5**: Execução real em staging com dump real não foi realizada neste ambiente (sem acesso ao bucket). A primeira execução em staging deve ser feita manualmente conforme o runbook antes do go-live.
- **ALERT_CMD**: configurado como variável de ambiente opcional — operador deve configurar antes de habilitar o cron para garantir alertas de falha.

## Suposições

- Postgres `16-alpine` é a versão pinada conforme `deployment/compose/compose.yml`.
- Schema das tabelas críticas é `mecontrola` (alinhado com o schema existente no projeto).
- O dump gerado por `pg-dump.sh` é `.sql.gz` comprimido e cifrado com age, resultando em `*.sql.gz.age`.
- `RESTORE_PORT=15432` não conflita com Postgres de desenvolvimento (porta padrão 5432).
