# Generated: 2026-06-12T00:00:00Z

# Relatório de Execução — Tarefa 4.0: A10 Rate limit por user_id

## Status Final

`done`

## Arquivos Alterados

| Arquivo | Ação |
|---|---|
| `internal/platform/ratelimit/extractors.go` | novo |
| `internal/platform/ratelimit/middleware.go` | novo |
| `internal/platform/ratelimit/extractors_test.go` | novo |
| `internal/platform/ratelimit/middleware_test.go` | novo |
| `internal/card/module.go` | modificado (adiciona userRateLimit, usa cfg para PerUserPerMin/Burst) |
| `internal/card/infrastructure/http/server/router.go` | modificado (adiciona userRateLimit ao construtor e chain) |
| `internal/card/infrastructure/http/server/contract_openapi_test.go` | modificado (passa passthrough como userRateLimit) |
| `internal/card/infrastructure/http/server/contract_test.go` | modificado (passa passthrough como userRateLimit) |
| `internal/card/infrastructure/http/server/router_test.go` | modificado (passa passthrough como userRateLimit) |
| `configs/config.go` | modificado (adiciona AuthRateLimitConfig, defaults 120/60, envKeys) |

## Critérios de Aceite

- RF-27: generalização com `KeyExtractor` mantendo `ByIP` default (zero regressão em onboarding)
  -> comprovado: `go test ./internal/onboarding/... -short` PASS; onboarding usa `RateLimiter` original inalterado

- RF-28: plug em `/api/v1/cards*` após `RequireUser`
  -> comprovado: `router.go` cadeia `gatewayAuth → InjectPrincipal → RequireUser → userRateLimit → handlers`

- RF-29: envs `AUTH_RATE_LIMIT_PER_USER_PER_MIN` (default 120), `AUTH_RATE_LIMIT_PER_USER_BURST` (default 60)
  -> comprovado: `AuthRateLimitConfig` em `configs/config.go`, defaults via `setAuthRateLimitDefaults()`

- RF-30: testes unit cobrindo ByIP, ByUserID, ByUserIDFallbackIP
  -> comprovado: `go test ./internal/platform/ratelimit/... -v` PASS — 10 testes (3 extractor, 7 middleware)

- RF-31: métrica `auth_rate_limit_exceeded_total{scope}` com `scope ∈ {ip, user}`
  -> comprovado: `middleware.go` registra `auth_rate_limit_exceeded_total` com label `scope` vindo de `RateLimitConfig.Scope`; card module passa `Scope: "user"`

- Zero comentário em `.go`
  -> comprovado: gate `grep` retorna vazio em todos os novos/modificados arquivos de produção

## Comandos Executados

```
go build ./...                                          # OK
go build ./internal/platform/ratelimit/...             # OK
go build ./internal/card/...                           # OK
go test ./internal/platform/ratelimit/... -v -count=1  # PASS (10 testes)
go test ./internal/onboarding/... -run TestRateLimit   # PASS (5 testes legacy)
go test ./internal/card/... -count=1 -short            # PASS
go test ./... -count=1 -short | grep FAIL              # vazio
go vet ./internal/platform/ratelimit/... ./internal/card/... ./configs/...  # OK
grep de comentários proibidos                          # OK: zero comentarios
grep de SQL direto em adapters                         # OK: sem SQL direto
```

## DoD

- [x] Subtarefa 4.1: `internal/platform/ratelimit/` criado com `KeyExtractor`, `RateLimitConfig`, `NewRateLimitMiddleware`
- [x] Subtarefa 4.2: `ByIP`, `ByUserID`, `ByUserIDFallbackIP` implementados em `extractors.go`
- [x] Subtarefa 4.3: métrica `auth_rate_limit_exceeded_total{scope}` no construtor
- [x] Subtarefa 4.4: envs `AUTH_RATE_LIMIT_PER_USER_PER_MIN` / `AUTH_RATE_LIMIT_PER_USER_BURST` em `configs/config.go`
- [x] Subtarefa 4.5: onboarding continua usando `RateLimiter` original — zero mudança de comportamento
- [x] Subtarefa 4.6: plug em `internal/card/infrastructure/http/server/router.go` após `RequireUser`
- [x] Subtarefa 4.7: testes unit cobrindo 3 extractors + middleware (incluindo passthrough quando sem principal)

## Riscos Residuais

- A goroutine de GC em `rateLimitMiddleware` não tem shutdown cooperativo (não expõe `Stop()`). Em testes de curta duração não há leak; em produção o processo encerra normalmente com o servidor. Pode ser adicionado futuramente se necessário.
- `ByUserID` retorna `""` quando não há Principal, causando passthrough (comportamento seguro — sem rate limit aplicado a request sem autenticação). Isso é intencional e coberto por teste.
