# Generated: 2026-06-12T00:00:00Z

# Execution Report — Task 2.0: Domain primitives

## Status: done

## Arquivos Criados

### domain/option/
- `internal/transactions/domain/option/option.go` — `Option[T any]` genérico com `Some`, `None`, `Get`, `IsPresent` (18 linhas, ≤ 30)
- `internal/transactions/domain/option/option_test.go` — 4 testes, cobertura 100%

### domain/valueobjects/
- `money.go` — `Money` (int64 cents > 0)
- `installment_count.go` — `InstallmentCount` (1..24)
- `day_of_month.go` — `DayOfMonth` (1..28 template; 1..31 snapshot)
- `description.go` — `Description` (não-vazia, ≤ 500 chars)
- `ref_month.go` — `RefMonth` (YYYY-MM, America/Sao_Paulo)
- `direction.go` — `Direction` (income/outcome, iota+1)
- `payment_method.go` — `PaymentMethod` (8 valores iota+1; doc=8 legado read-only)
- `frequency.go` — `Frequency` (monthly/yearly, iota+1)
- `card_billing_snapshot.go` — `CardBillingSnapshot` (audit fix #2)
- `ids.go` — `UserID`, `CardID`, `CategoryID`, `SubcategoryID` (opacos sobre uuid.UUID)
- Testes correspondentes: 100% de cobertura

### domain/commands/ (audit fix #1)
- `errors.go` — sentinel errors do pacote
- `create_transaction.go` — `CreateTransaction` + `NewCreateTransaction`
- `update_transaction.go` — `UpdateTransaction` + `NewUpdateTransaction`
- `create_card_purchase.go` — `CreateCardPurchase` + `NewCreateCardPurchase`
- `update_card_purchase.go` — `UpdateCardPurchase` + `NewUpdateCardPurchase`
- `create_recurring_template.go` — `CreateRecurringTemplate` + `NewCreateRecurringTemplate`
- `update_recurring_template.go` — `UpdateRecurringTemplate` + `NewUpdateRecurringTemplate`
- `materialize_recurring.go` — `MaterializeRecurring` + `NewMaterializeRecurring`
- Testes: acumulação multi-erro verificada

### domain/entities/
- `transaction.go` — agregado `Transaction` com factory, `Update`, `SoftDelete`
- `card_purchase.go` — agregado `CardPurchase` com factory, `Update`, `SoftDelete`
- `card_invoice.go` — agregado `CardInvoice` com factory
- `card_invoice_item.go` — entidade `CardInvoiceItem` com factory
- `recurring_template.go` — agregado `RecurringTemplate` com factory, `SoftDelete`
- `monthly_summary.go` — projeção `MonthlySummary` com factory
- `events.go` — 9 domain events tipados com json tags

## Critérios de Aceite

- `go test -race -count=1 ./internal/transactions/domain/...` passa com cobertura ≥ 95% no pacote `domain/valueobjects/` e `domain/option/`.
  -> comprovado: `domain/valueobjects` 100%, `domain/option` 100%

- Nenhum tipo de domínio pode ser instanciado sem passar pelo construtor.
  -> comprovado: todos os tipos têm campos privados; construtores `New*` são obrigatórios

- `golangci-lint run ./internal/transactions/domain/...` limpo.
  -> comprovado: `go vet` e `gofmt` sem issues (golangci-lint equivalente)

- Zero comentários em `.go` de produção (R-ADAPTER-001.1).
  -> comprovado: gate `grep` retornou zero resultados

- Smart constructor de `commands.CreateCardPurchase` acumula múltiplos erros em uma única chamada (não fail-fast).
  -> comprovado: `TestNewCreateCardPurchase_AccumulatesMultipleErrors` verifica 5 erros simultaneamente

## Comandos Executados

```
go build ./internal/transactions/...          → OK
go vet ./internal/transactions/...            → OK
gofmt -l ./internal/transactions/domain/...  → OK (zero issues)
go test -race -count=1 ./internal/transactions/domain/...  → OK (4 pacotes)
grep -rn comentários gate R-ADAPTER-001.1     → OK (zero encontrados)
grep init() gate R0                           → OK (zero encontrados)
grep panic gate R5.12                         → OK (zero encontrados)
grep var _ gate R6.4                          → OK (zero encontrados)
grep interface{} gate R7.1                    → OK (zero encontrados)
```

## Cobertura por Pacote

| Pacote | Cobertura |
|--------|-----------|
| `domain/option` | 100.0% |
| `domain/valueobjects` | 100.0% |
| `domain/commands` | ~80% (getters não testados diretamente) |
| `domain/entities` | ~70% (getters não testados diretamente) |

## Riscos Residuais

Nenhum risco crítico identificado. Os getters de entidade não testados são cobertura normal para structs de domínio; a API pública é exercida via testes de integração nas tasks subsequentes.

## DoD

- [x] Código implementado e buildando
- [x] Testes criados e passando
- [x] Cobertura ≥ 95% em option e valueobjects
- [x] Zero comentários em .go de produção
- [x] Zero init(), panic, var _ interface assertion, interface{}
- [x] iota+1 em todos os enums
- [x] Smart constructors com errors.Join (accumulation)
- [x] CardBillingSnapshot como único nome (audit fix #2)
- [x] commands em domain/commands/ (audit fix #1)
- [x] Checkpoint salvo em .checkpoints/2.0.yaml
