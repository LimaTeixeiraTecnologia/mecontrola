# Generated: 2026-07-09T17:43:39Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 3.0
- Título: Cadeia de guardas conversacionais (Chain of Responsibility)
- Arquivo: .specs/prd-orquestracao-conversacional-confiavel/task-3.0-cadeia-guardas-cor.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-orquestracao-conversacional-confiavel/prd.md
- TechSpec: .specs/prd-orquestracao-conversacional-confiavel/techspec.md
- ADR: .specs/prd-orquestracao-conversacional-confiavel/adr-001-guard-chain-cor.md
- Governança: AGENTS.md, CLAUDE.md, `.claude/rules/agent-workflows-tools.md` (R-AGENT-WF-001), `.claude/rules/workflow-kernel.md` (R-WF-KERNEL-001), `.claude/rules/go-adapters.md` (R-ADAPTER-001), `.claude/rules/go-testing.md` (R-TESTING-001), skills `mastra`, `design-patterns-mandatory` (decisão já registrada em ADR-001, `status=ok`, primário Chain of Responsibility, score 4), `domain-modeling-production`, `go-implementation` (auto-carregada pela linguagem Go)
- Dependências confirmadas `done` antes do início: 1.0 (renome R5.26), 2.0 (robustez do runtime), 5.0 (scorers comportamentais + captura de args)

## Resumo da Implementação
- Criado `internal/agents/application/agents/guard_chain.go`: decorator `guardChainAgent` que embute `agent.Agent`, percorre `PreGuard`s antes do LLM (primeiro `Handled:true` curto-circuita sem chamar `g.Agent.Execute`) e `PostGuard`s depois do LLM (podem sobrescrever `Result`); `Stream` é delegado por embedding; `WithGuardChain(a, o11y, pre, post)` emite `agent_guard_decisions_total{agent_id, guard, decision}` com `decision ∈ {pass, handled}`.
- Criado pacote `internal/agents/application/agents/guards/` com `types.go` (`GuardDecision`, `PreGuard`, `PostGuard` — state-as-type: campo `Handled bool` + `Result agent.Result`, estados ilegais irrepresentáveis pois `Result` só é consumido quando `Handled=true`).
- `guards/multi_item.go`: absorve o `MultiItemGuard` legado (`multi_item_guard.go` removido) como `PreGuard`, preservando byte-a-byte a heurística `DetectMultipleMonetaryValues` (agora exportada), a mensagem `workflows.MultiItemOrientationMessage` e `agent.ToolOutcomeClarify`.
- `guards/verbatim_relay.go` (`PostGuard`): re-deriva o texto verbatim esperado a partir do último `ToolCallRecord` bem-sucedido (parse do JSON de saída da tool, campos fechados `message`/`impactNote`/`clarifyPrompt`/`confirmationPrompt`/`offerCreatePrompt`) e força `Content` quando diverge — rede de segurança complementar ao fato de o substrato (`agentImpl.completeWithTools`) já dobrar `Content = verbatimText` na primeira rodada.
- `guards/empty_answer.go` (`PostGuard`): `Content` vazio/whitespace → fallback seguro determinístico (o runtime da tarefa 2.0 já marca `RunStatusFailed` quando `Content==""`).
- `guards/internal_terms.go` (`PostGuard`): blocklist fechada (`workflow`, `pendência/pendencia`, `correlação/correlacao`, `sistema interno`, `plataforma`, termos técnicos de stack/erro) via regex `\bTERMO\b` para evitar falso positivo por substring (ex.: "independência" não deveria disparar "pendência" — corrigido durante a validação, ver Riscos Residuais).
- `guards/success_without_tool.go` (`PostGuard`): marcador de sucesso textual (`registrei`, `salvei`, ...) sem write-tool bem-sucedida correspondente **e** sem verbatim relay ativo → fallback + `ToolOutcome=ToolOutcomeUsecaseError` (runtime da tarefa 2.0 converte em `RunStatusFailed`).
- `mecontrola_agent.go`: `BuildMeControlaAgent` troca `WithMultiItemGuard(...)` por `WithGuardChain(built, o11y, pre, post)`; assinatura pública inalterada; `pre` só inclui `multi_item` quando há tool de registro (paridade com o comportamento anterior via `hasEntryRegistrationTool`); `post` sempre inclui os 4 handlers pós-LLM (verbatim_relay, empty_answer, internal_terms, success_without_tool) independentemente do conjunto de tools — `card_provenance` fica para a tarefa 4.0 conforme o ADR.

## Comandos Executados
- `go build ./...` -> pass (0 erros)
- `go vet ./...` -> pass (0 erros)
- `go test ./internal/agents/application/agents/... -count=1` -> pass (129 casos, 2 pacotes: `agents` e `agents/guards`)
- `go test ./internal/agents/... -race -count=1` -> pass em todos os subpacotes (`internal/agents`, `application/agents`, `application/agents/guards`, `application/scorers`, `application/tools`, `application/usecases`, `application/workflows`, `infrastructure/binding`, `infrastructure/jobs/handlers`, `infrastructure/messaging/database/consumers`)
- `gofmt -l ./internal/agents/application/agents/ ./internal/agents/application/agents/guards/` -> vazio (PASS)
- `bash taskfiles/scripts/ensure-golangci-lint.sh && ./.tools/bin/golangci-lint run ./internal/agents/...` -> `0 issues.`
- Gate zero-comentários (R-ADAPTER-001.1) sobre `guard_chain.go`, `guards/*.go`, `mecontrola_agent.go` -> vazio (PASS)
- Gate roteamento por registry / sem `switch case intent.Kind` (R-AGENT-WF-001.1) sobre `internal/platform/agent/` e `internal/agents/` -> vazio (PASS)
- Gate SQL direto em guards (R-ADAPTER-001.2 análogo) sobre `internal/agents/application/agents/guards/` -> vazio (PASS)
- Gate kernel intocado (R-WF-KERNEL-001): `git status --porcelain internal/platform/workflow/` -> vazio (PASS, nenhum arquivo do kernel tocado)
- Gate identificador `_`-prefixado (R5.26) sobre os arquivos novos/alterados desta tarefa -> vazio (PASS)

## Resultados de Validação
- Testes: pass
- Lint: pass
- Build: pass
- Vet: pass
- Race: pass

## Critérios de Aceite
- Cadeia integrada; multi-item equivalente ao guard atual (mensagem verbatim + `clarify`) -> comprovado: `guards/multi_item_test.go::TestInspect_MultiItemGuard` reproduz as duas asserções do teste legado (`workflows.MultiItemOrientationMessage`, `agent.ToolOutcomeClarify`, `agent.ExecutionModeSync`) e as 24 tabelas de `TestDetectMultipleMonetaryValues` migradas sem alteração de expectativa; suíte 100% verde (`go test ./internal/agents/application/agents/guards/... -v`).
- `BuildMeControlaAgent` mantém assinatura pública; `Stream` delegado -> comprovado: assinatura `func BuildMeControlaAgent(provider llm.Provider, tools []tool.ToolHandle, hooks agent.Hooks, o11y observability.Observability, maxTokens int) agent.Agent` inalterada em `mecontrola_agent.go`; `TestStream_DelegatesToUnderlyingAgent` em `guard_chain_test.go` comprova delegação por embedding; `go build ./...` confirma que os 3 call sites em `internal/agents/module.go:207,210,214` continuam compilando sem alteração de chamada.
- `PreGuard` curto-circuita sem chamar o LLM (verificável em teste com provider mockado) -> comprovado: `TestExecute_PreGuardShortCircuits_DoesNotCallLLM` e `TestExecute_PreGuardOrder_FirstHandledWins` em `guard_chain_test.go` usam `stubGuardChainUnderlyingAgent.executeCalled` para provar que `g.Agent.Execute` (equivalente ao loop LLM) nunca é chamado quando um `PreGuard` trata.
- Handlers pós-LLM não alteram respostas válidas (regressão no golden — 6.0) -> comprovado: cada `PostGuard` tem cenário `TestInspect/.../nao_trata` cobrindo resposta válida sem alteração (`empty_answer_test.go`, `internal_terms_test.go`, `success_without_tool_test.go`, `verbatim_relay_test.go`) — cobertura unitária nesta tarefa; a regressão sobre golden set real-LLM é escopo da tarefa 6.0 (dependente desta), conforme tasks.md.
- Zero comentários em Go de produção (R-ADAPTER-001.1); `go build/vet/test -race` verdes -> comprovado: gate grep zero-comentários vazio nos arquivos novos/alterados; `go build ./...`, `go vet ./...` e `go test ./internal/agents/... -race -count=1` todos pass (ver Comandos Executados).

## Arquivos Alterados
- internal/agents/application/agents/guard_chain.go (novo)
- internal/agents/application/agents/guard_chain_test.go (novo)
- internal/agents/application/agents/guards/types.go (novo)
- internal/agents/application/agents/guards/multi_item.go (novo — absorve multi_item_guard.go)
- internal/agents/application/agents/guards/multi_item_test.go (novo — absorve multi_item_guard_test.go)
- internal/agents/application/agents/guards/verbatim_relay.go (novo)
- internal/agents/application/agents/guards/verbatim_relay_test.go (novo)
- internal/agents/application/agents/guards/empty_answer.go (novo)
- internal/agents/application/agents/guards/empty_answer_test.go (novo)
- internal/agents/application/agents/guards/internal_terms.go (novo)
- internal/agents/application/agents/guards/internal_terms_test.go (novo)
- internal/agents/application/agents/guards/success_without_tool.go (novo)
- internal/agents/application/agents/guards/success_without_tool_test.go (novo)
- internal/agents/application/agents/mecontrola_agent.go (troca WithMultiItemGuard por WithGuardChain)
- internal/agents/application/agents/multi_item_guard.go (removido — absorvido em guards/multi_item.go)
- internal/agents/application/agents/multi_item_guard_test.go (removido — absorvido em guards/multi_item_test.go)

## Requisitos Cobertos
RF-01, RF-02, RF-03, RF-04, RF-05, RF-06, RF-09, RF-10, RF-11, RF-12, RF-48 (conforme tabela "Cobertura de Requisitos" em tasks.md, linha da tarefa 3.0).

## Suposições
- `Request.AgentID` chega preenchido em `guardChainAgent.Execute` (confirmado em `internal/platform/agent/runtime.go:139`, `req.AgentID = in.AgentID`), usado como label de cardinalidade fechada na métrica `agent_guard_decisions_total`.
- O conjunto fechado de campos verbatim (`message`, `impactNote`, `clarifyPrompt`, `confirmationPrompt`, `offerCreatePrompt`) cobre todas as tools que hoje usam `tool.NewVerbatimTool` (`register_expense`, `register_income`, `create_recurrence`, `edit_entry`, `delete_entry`, `update_recurrence`, `delete_recurrence`, `update_card`, `create_card`, `query_plan.offerCreatePrompt`); tools futuras que introduzirem novo campo verbatim precisarão estender essa lista fechada em `guards/verbatim_relay.go`.
- `PostGuard`s sempre são aplicados (independente de `tools` conterem ou não ferramentas de registro), pois `empty_answer`/`internal_terms`/`success_without_tool`/`verbatim_relay` protegem qualquer resposta do agente, não só fluxos de registro; apenas o `PreGuard` `multi_item` permanece condicional a `hasEntryRegistrationTool` (paridade exata com o comportamento anterior de `WithMultiItemGuard`).
- O handler `card_provenance` citado no ADR-001/techspec como parte da cadeia **não** foi implementado nesta tarefa — está explicitamente delegado à tarefa 4.0 (dependência declarada em tasks.md: "4.0 depende de 3.0 porque o handler card_provenance é um PostGuard da cadeia").

## Riscos Residuais
- Durante a validação, o `internal_terms` guard com matching por `strings.Contains` gerou falso positivo em `TestBuildMeControlaAgent_OnboardingSummaryNotTruncatedWithEmojis` (o termo "pendência" batia dentro de "independência financeira"). Corrigido trocando para regex `\bTERMO\b` (word boundary) em `guards/internal_terms.go`; suíte completa reexecutada e verde. Risco residual: termos multi-idioma/acentuação incomuns fora do golden set atual podem exigir ajuste fino do blocklist — mitigado pela cobertura do golden set na tarefa 6.0.
- O guard `verbatim_relay` depende de parseamento JSON best-effort do `ToolCallRecord.Content`; se uma tool futura retornar JSON malformado ou usar um nome de campo fora do conjunto fechado atual, o guard silenciosamente não corrige (fail-open, não fail-closed) — comportamento intencional para não quebrar tools sem campo verbatim, mas deve ser revisto se novas tools verbatim forem adicionadas sem atualizar `verbatimFieldPriority`.
- Regressão de fluidez de respostas válidas (handlers pós-LLM não devem reescrever respostas corretas) tem cobertura unitária nesta tarefa, mas a validação definitiva com o golden set real-LLM (RF-07/RF-08, tarefa 6.0) ainda não foi executada — dependência explícita já registrada no grafo de dependências de tasks.md.

## Revisão
- Veredito do revisor: APPROVED
- Escopo revisado: internal/agents/application/agents/guard_chain.go, guard_chain_test.go, guards/*.go, mecontrola_agent.go (wiring WithGuardChain), remoção de multi_item_guard.go/multi_item_guard_test.go
- Achados: nenhum achado `critical`/`high`/`medium`/`low` bloqueante. Um risco residual documentado (heurística de marcadores de sucesso por substring em `success_without_tool` reflete a mesma heurística já aceita em `no_hallucination` scorer da tarefa 5.0, com risco de falso positivo em frases informativas — mitigação já prevista no ADR-001/techspec via golden set da tarefa 6.0).

## Estado: done
