# Generated: 2026-07-07T00:00:00Z

## Tarefa 5.0 — Guarda de teto de valor `validateEntryAmount` nos execs de registro

### Status Final
done

### Arquivos Alterados
- `internal/agents/application/tools/validate_entry_amount.go` — criado; `maxEntryAmountCents` + `validateEntryAmount` puros
- `internal/agents/application/tools/validate_entry_amount_test.go` — criado; 5 cenários de tabela
- `internal/agents/application/tools/register_expense.go` — guarda chamada antes de `RegisterExpense`
- `internal/agents/application/tools/register_income.go` — guarda chamada antes de `RegisterIncome`
- `.specs/prd-registro-conversacional-transacoes-dia-a-dia/tasks.md` — status 5.0 → done

### Arquivos NÃO Alterados
- `internal/transactions/domain/valueobjects/money.go` — intacto (subtarefa 5.4 cumprida)

### Critérios de Aceite

- Valores `<= 0` produzem resposta amigável de correção, sem `engine.Start` → comprovado: `validateEntryAmount` retorna `errAmountNonPositive`; exec retorna `RegisterExpenseOutput{Outcome:"clarify", Message:"..."}` sem chamar registrar
- Valores `> maxEntryAmountCents` produzem resposta amigável de correção → comprovado: `validateEntryAmount` retorna `errAmountAboveCeiling`; exec retorna `RegisterIncomeOutput{Outcome:"clarify", Message:"..."}`
- VO `Money` permanece intacto → comprovado: `git diff --name-only internal/transactions/domain/valueobjects/money.go` retornou vazio
- `go build ./internal/agents/...` limpo → comprovado: sem output de erro
- `go vet ./internal/agents/application/tools/...` limpo → comprovado: sem output
- `go test -race -count=1 ./internal/agents/application/tools/...` → comprovado: 82 tests passed, 2 packages
- Zero comentários em `.go` de produção → comprovado: grep retornou OK nos 3 arquivos de produção

### Testes: pass
```
go test -race -count=1 ./internal/agents/application/tools/...
Go test: 82 passed in 2 packages
```

### Cenários de tabela (TestValidateEntryAmount)
- `0` → errAmountNonPositive
- `-1` → errAmountNonPositive
- `15000` → nil (aceita)
- `1_000_000_000` (no teto) → nil (aceita)
- `1_000_000_001` (acima do teto) → errAmountAboveCeiling

### Riscos Residuais
Nenhum crítico. O teto vive apenas na borda do agente (ADR-003); superfícies de escrita fora do agente não herdam a guarda — aceito por decisão D-01.
