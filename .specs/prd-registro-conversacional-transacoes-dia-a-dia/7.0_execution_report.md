# Generated: 2026-07-07T00:00:00Z

# Relatório de Execução — Tarefa 7.0: Endurecimento das instruções do agente `mecontrola`

## Status: done

## Resumo

Adicionadas três novas `REGRA ABSOLUTA` à `const mecontrolaAgentInstructions` em `internal/agents/application/agents/mecontrola_agent.go`:
- **CAMPOS OBRIGATÓRIOS**: cinco campos obrigatórios + regra de não-invenção (RF-01, RF-21)
- **DATA (occurredAt)**: repasse do texto de data CRU sem conversão pelo agente (RF-07, RF-09)
- **LANÇAMENTO ÚNICO**: fronteira multi-item com frase verbatim do PRD (RF-16)

Atualizada também a linha de chamada de ferramenta para incluir `occurredAt` com texto CRU.

Adicionados 6 novos cenários de teste em `mecontrola_agent_test.go` (suite whitebox, package `agents`).

## Arquivos Alterados

- `internal/agents/application/agents/mecontrola_agent.go` — const `mecontrolaAgentInstructions` (+3 blocos REGRA ABSOLUTA + atualização da instrução de chamada de ferramenta)
- `internal/agents/application/agents/mecontrola_agent_test.go` — +6 cenários de asserção sobre a const

## Subtarefas

- [x] 7.1 Bloco CAMPOS OBRIGATÓRIOS com cinco campos + regra de não-invenção
- [x] 7.2 Instrução de repasse de data CRU em `occurredAt` sem conversão pelo LLM
- [x] 7.3 Fronteira LANÇAMENTO ÚNICO com frase verbatim do PRD (RF-16)
- [x] 7.4 Instrução de chamada de ferramenta atualizada para incluir `occurredAt` com texto CRU
- [x] 7.5 Verificação de coerência — nenhum conteúdo existente removido; novos blocos não contradizem regras de asterisco nem confirmação

## Critérios de Aceite

- Instruções cobrem os cinco campos → comprovado: teste `deve conter os cinco campos obrigatorios (RF-01)` passa
- Nenhuma regra instrui o LLM a converter data → comprovado: REGRA ABSOLUTA DE DATA instrui repasse CRU; "o sistema converte; o agente NÃO converte"
- Fronteira multi-item com frase verbatim → comprovado: "Percebi mais de um lançamento na mesma mensagem..." presente na const
- `go build` limpo → comprovado: `go build ./internal/agents/...` sem erros
- `go vet` limpo → comprovado: `go vet ./internal/agents/...` sem erros
- Zero comentários → comprovado: gate grep retornou "OK: zero comentarios"
- Testes passam: 25 passed, 0 failed

## Comandos Executados

```
go build ./internal/agents/...                                                → OK (sem erros)
go vet ./internal/agents/...                                                  → OK (sem erros)
go test -race -count=1 ./internal/agents/application/agents/ -run TestMecontrolaAgentBuilderSuite → 25 passed
grep ^[[:space:]]*//) mecontrola_agent.go | grep -Ev ...                      → OK: zero comentarios
```

## Riscos Residuais

- Testes de integração real-LLM (M-04 ≥ 0,90) ficam para a tarefa 8.0, conforme dependência declarada no tasks.md.
- A const contém `**` em exemplos de formatação proibida (existia antes desta tarefa); isso é intencional e não viola RF-23 pois a instrução proíbe usar `**` nas respostas ao usuário.
