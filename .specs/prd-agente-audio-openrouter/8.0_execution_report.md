# Generated: 2026-08-05T00:58:43Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 8.0
- Título: Golden set áudio/texto e suites reais por flag
- Arquivo: .specs/prd-agente-audio-openrouter/task-8.0-golden-real-stt.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-agente-audio-openrouter/prd.md (RF-30, RF-31, RF-32, RF-34, RF-35, RF-36, RF-44)
- TechSpec: .specs/prd-agente-audio-openrouter/techspec.md (seções `Abordagem de Testes`, `Golden Set Audio`, `Gates de Validacao para Merge`, `Mapeamento RF -> Decisao -> Gate`)
- Skills: `mastra`, `domain-modeling-production`, `design-patterns-mandatory` (declaradas na task file); `go-implementation` (linguagem, auto-carregada)
- Governança: AGENTS.md, R-ADAPTER-001, R-AGENT-WF-001, R-GOV-001, `.claude/rules/governance.md`
- Código pré-existente inspecionado: `internal/agents/application/golden/{harness.go,harness_realllm_test.go,case.go,ratio.go,registry.go,journey_test.go}`, `internal/agents/application/usecases/{decide_audio_transcription.go,process_audio_inbound.go,audio_audit.go}`, `internal/agents/application/dtos/input/audio_decision_input.go`, `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer.go`, `internal/platform/llm/{stt.go,realstt_test.go}`, `internal/platform/whatsapp/media/duration.go`

## Comandos Executados
- `go build ./...` -> pass
- `go build -tags integration ./...` -> pass
- `go vet ./...` -> pass
- `go vet -tags integration ./internal/agents/application/golden/...` -> pass
- `gofmt -l internal/agents/application/golden/*.go` -> vazio (limpo)
- `go test -race -count=1 ./internal/agents/application/golden/...` -> pass (0 FAIL; suites: `TestAudioCaseSuite`, `TestAudioNegativePipelineSuite`, `TestCaseSchemaSuite`, `TestHarnessSuite`, `TestJourneyGoldenSuite`, `TestRatioSuite`, `TestRegistrySuite`)
- `go test -race -count=1 ./internal/agents/... ./internal/platform/...` -> pass (sem regressão em nenhum pacote do módulo `agents`/`platform`)
- `RUN_REAL_LLM=1 RUN_REAL_STT=1 go test -count=1 ./internal/agents/application/golden/...` (comando literal do checklist da task, sem `-tags integration`) -> pass; `ok ... 0.464s` — comportamento determinístico já estabelecido nas tasks 3.0/6.0/7.0: sem `-tags integration` os arquivos `*_realllm_test.go` não são sequer compilados (build constraint), então não há chamada real fabricada como sucesso
- `RUN_REAL_LLM=1 go test -tags integration -count=1 -timeout 9m -run TestGoldenAudioRealLLMSuite -v ./internal/agents/application/golden/...` (credenciais reais carregadas de `.env`) -> **pass real**, 45/45 casos (15 casos pareados × 3 repetições), `audio_group=expense hits=9 total=9 ratio=1.0000`, `audio_group=income hits=9 total=9 ratio=1.0000`, `audio_group=query hits=9 total=9 ratio=1.0000`, `audio_group=edit hits=9 total=9 ratio=1.0000`, `audio_group=confirmation hits=9 total=9 ratio=1.0000` — todos ≥ 0.90 (58.92s, chamadas reais a `openai/gpt-4o-mini` via OpenRouter)
- `go test -tags integration -count=1 -run TestGoldenAudioRealSTT -v ./internal/agents/application/golden/...` (sem `RUN_REAL_STT`) -> `--- SKIP: TestGoldenAudioRealSTT` com mensagem "RUN_REAL_STT=1 obrigatório..." (skip determinístico sem flag)
- `env -u OPENROUTER_API_KEY RUN_REAL_STT=1 go test -tags integration -count=1 -run TestGoldenAudioRealSTT -v ./internal/agents/application/golden/...` -> `--- FAIL: TestGoldenAudioRealSTT` com `t.Fatal` explícito "RUN_REAL_STT=1 exige OPENROUTER_API_KEY; suite real de STT não pode ser marcada como atendida sem credencial" (falha explícita, não silenciosa, conforme critério de sucesso da task)
- `RUN_REAL_STT=1 go test -tags integration -count=1 -run TestGoldenAudioRealSTT -v ./internal/agents/application/golden/...` (credencial real de `.env`, sem `STT_REAL_AUDIO_FIXTURE`) -> `--- SKIP: TestGoldenAudioRealSTT` citando explicitamente `benchmark-stt.md`/`stt-lote-30-results.json` como evidência real já existente (task 3.0); nenhum áudio de fixture foi inventado, conforme instrução explícita
- `.tools/bin/golangci-lint run ./internal/platform/... ./internal/agents/... ./configs/... ./cmd/server/... ./cmd/worker/...` (gate padrão da techspec, sem `-tags integration`) -> `1 issue` pré-existente em `internal/platform/llm/openrouter_stt.go:53` (função `Transcribe`, `revive:function-length`), já documentado nas tasks 6.0/7.0, fora do escopo desta tarefa e não introduzido por ela
- `.tools/bin/golangci-lint run --build-tags=integration --max-same-issues=0 ./internal/agents/application/golden/... ./internal/platform/llm/...` (execução exploratória, fora do gate padrão) -> confirma que os 4 novos `os.Getenv` em `harness_audio_realllm_test.go` seguem o MESMO padrão pré-existente já presente em `harness_realllm_test.go` (4 ocorrências), `internal/platform/llm/realllm_test.go` (3) e `internal/platform/llm/realstt_test.go` (4) — nenhum desses arquivos é compilado pelo gate padrão (ausência de `-tags integration`), então o achado é consistente com o precedente estabelecido nas tasks 3.0/6.0/7.0 e não é uma regressão desta tarefa
- Gate heurístico `grep "^func init()"` em internal/cmd/configs -> vazio
- Gate heurístico zero-comentários `.go` em `internal/agents/application/golden/` -> vazio
- Gate heurístico prefixo `_` em `internal/agents/application/golden/*.go` -> vazio (únicos matches são substrings literais `"_input"`/`"_output"` pré-existentes em `harness_realllm_test.go`, não identificadores)

## Arquivos Alterados (todos novos, nenhum arquivo pré-existente foi modificado)
- `internal/agents/application/golden/audio_case.go` — define `AudioGroup` (tipo fechado: `expense`, `income`, `query`, `edit`, `confirmation`) e `AudioPairedCase`/`AudioPairedCases()` com 15 casos positivos (3 por grupo) pareando um texto "falado" simulado (`SpokenText`) com o `Case` textual equivalente já usado pelo golden harness existente (mesmas `ExpectedTool`/`ExpectedArgs`/`ResponseProperty`).
- `internal/agents/application/golden/audio_case_test.go` — valida cobertura mínima de 3 casos/grupo, unicidade de nomes, anonimização (mesma allowlist de termos proibidos de `journey_test.go`) e que `usecases.DecideAudioTranscription` real (função pura, não mockada) sempre aprova o texto simulado produzindo o `CanonicalText` esperado, sem chamada a LLM.
- `internal/agents/application/golden/audio_pipeline_test.go` — implementa o "modo de transcrição simulada" (subtarefa 8.2: `SimulatedTranscriber` sem client HTTP/APIKey, prova de fluxo sem OpenRouter real) e fakes locais (`simulatedMediaClient`, `inMemoryAudioAuditRepository`) para exercitar o `usecases.ProcessAudioInbound` real fim-a-fim; cobre os 6 negativos obrigatórios (ruído/incoerente, fala cortada, idioma não PT-BR, formato inválido, timeout de STT, duplicado por WAMID) mais um positivo simulado, todos com assert explícito de `!Outcome.AllowsHandleInbound()` e `dispatchSpy` provando 0 tool call/0 HandleInbound; inclui `TestInvariantNoFalseSuccessOnAudioOriginatedRun` reaproveitando `reconciliation.DecideViolations` (mesmo padrão de `journey_test.go`) para provar falso-sucesso mutacional 0 em runs originados de áudio.
- `internal/agents/application/golden/harness_audio_realllm_test.go` (`//go:build integration`) — `TestGoldenAudioRealLLMSuite.TestGoldenAudioPairedGate` roda os 15 casos pareados × 3 repetições contra o LLM real via `s.provider`/`agentsapp.BuildMeControlaAgent` (reaproveitando exatamente o executor de `harness_realllm_test.go`), agregando score por `AudioGroup` com gate `>= 0.90`; `TestGoldenAudioRealSTT` implementa a suite real de STT gated por `RUN_REAL_STT=1`, com falha explícita (`t.Fatal`) sem `OPENROUTER_API_KEY` e skip explícito (citando evidência já existente) sem `STT_REAL_AUDIO_FIXTURE`.

## Resultados de Validação
- Testes: pass (`-race`, golden completo + agents + platform completos, sem regressão; suite real de LLM 45/45 com evidência real capturada)
- Lint: pass (0 issues no gate padrão da techspec; 1 issue pré-existente fora do escopo desta tarefa, já documentado nas tasks 6.0/7.0)
- Veredito do Revisor: APPROVED (auto-revisão restrita aos 4 arquivos novos desta tarefa; nenhum arquivo pré-existente foi tocado, eliminando risco de regressão por edição; ver `## Diff Reviewed`)

## Critérios de Aceite
- Golden textual existente continua verde -> comprovado: `go test -race -count=1 ./internal/agents/application/golden/...` pass sem alterar nenhum arquivo pré-existente (`git status` mostra apenas arquivos novos); `AllCases()`/`registry.go` não foram tocados, então a suite real `TestGoldenRealLLMSuite` (205 casos herdados de tasks anteriores) permanece com o mesmo comportamento.
- Golden áudio/texto cobre todas as categorias exigidas -> comprovado: `AudioPairedCases()` com 3 casos em cada um dos 5 grupos (`expense`, `income`, `query`, `edit`, `confirmation`); `TestAudioPairedCasesCoverAllRequiredGroups` valida `byGroup[group] >= 3` para todos os grupos; negativos obrigatórios (ruído, fala cortada, idioma não PT-BR, duplicado, formato inválido, timeout STT) cobertos em `audio_pipeline_test.go` com 6 métodos de teste dedicados, todos `pass`.
- Suite real STT fica skipada sem flag e falha explicitamente com flag sem credencial -> comprovado: execução real documentada acima (`--- SKIP` sem `RUN_REAL_STT`; `--- FAIL` com `t.Fatal` explícito quando `RUN_REAL_STT=1` sem `OPENROUTER_API_KEY`).
- Nenhum mock substitui RF que exige comportamento real com OpenRouter -> comprovado: `TestGoldenAudioRealLLMSuite.TestGoldenAudioPairedGate` chamou o LLM real (`openai/gpt-4o-mini` via OpenRouter, usando `OPENROUTER_API_KEY` de `.env`) 45 vezes com resultado real (ratio 1.0 em todos os grupos, log `audio_group=... hits=9 total=9 ratio=1.0000`); a suite real de STT (`TestGoldenAudioRealSTT`) usa o `llm.NewOpenRouterTranscriber` real (não mock) e só faz skip explícito por ausência de um fixture de áudio físico, que a instrução explícita do orquestrador proibiu inventar — reutiliza `benchmark-stt.md`/`stt-lote-30-results.json` (task 3.0) como evidência real já existente de STT contra OpenRouter.
- Score por grupo `>= 0.90` -> comprovado: log real `audio_group=expense|income|query|edit|confirmation hits=9 total=9 ratio=1.0000` (execução real acima).
- Falso-sucesso mutacional `0` -> comprovado: `TestInvariantNoFalseSuccessOnAudioOriginatedRun` (pass) — `reconciliation.DecideViolations` sinaliza exatamente 1 violação `ViolationSucceededNoEffect` para um run com `LedgerWrites=0`/`TransactionCount=0` e nenhuma violação para o run com efeito durável.
- `0` tool call e `0` `HandleInbound` em `TranscriptionUncertain` (e demais outcomes que não permitem dispatch) -> comprovado: 6 métodos de teste em `audio_pipeline_test.go` (`TestNegativeRuidoIncoerente`, `TestNegativeFalaCortada`, `TestNegativeIdiomaNaoPortugues`, `TestNegativeFormatoInvalido`, `TestNegativeTimeoutSTT`, `TestNegativeDuplicado`) todos com `spy.handleInboundCalls == 0` e `spy.toolCalls == 0`, usando o `usecases.AudioOutcome.AllowsHandleInbound()` real do decisor de produção.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos listados em "Comandos Executados", incluindo a execução real com `.env`).
- [x] Lint/vet/build sem regressão (1 issue pré-existente fora do escopo, documentado, não introduzido por esta tarefa).
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree-uncommitted
verdict=APPROVED
tool=review-skill-self-executed

## Coverage

package=internal/agents/application/golden
delta=+4 arquivos novos (audio_case.go, audio_case_test.go, audio_pipeline_test.go, harness_audio_realllm_test.go); 0 arquivos pré-existentes modificados; 15 casos golden pareados (3×5 grupos) + 6 cenários negativos + 1 cenário de modo simulado + 1 invariante de falso-sucesso + suite real de LLM (45 execuções reais) + suite real de STT gated

## Suposições
- "Score por grupo" (RF-30/32/34) foi interpretado como agregação por `AudioGroup` (expense/income/query/edit/confirmation) e não pela `Category` genérica do golden textual (que mistura expense+income em `CategoryExpenseIncome`), pois a task pede explicitamente 5 grupos distintos ("despesas, receitas, consultas, edições, confirmações").
- Os 15 casos pareados NÃO foram adicionados a `AllCases()`/`registry.go` para não inflar em 3× o custo/tempo da suite real de LLM existente (`TestGoldenRealLLMSuite`, 205 casos herdados); eles vivem em `AudioPairedCases()` e são exercitados por uma suite real dedicada (`TestGoldenAudioRealLLMSuite`), mantendo a suite real textual existente intacta e sem regressão de custo/tempo.
- Para "formato inválido" o outcome real do sistema é `AudioReasonDurationUnavailable` (não `AudioReasonInvalidPayload`) porque `media.DetermineDuration` rejeita o mime não suportado antes do usecase alcançar `audioFormatFromMime` — os dois conjuntos de mime suportado são idênticos em `internal/platform/whatsapp/media/duration.go` e `internal/agents/application/usecases/process_audio_inbound.go`, então `audioFormatFromMime` é código morto para mimes não suportados; o teste documenta o comportamento real do sistema em vez de forçar um caminho inatingível.
- Para "timeout STT" e demais cenários que precisam alcançar a etapa de STT, foi necessário construir um fixture OGG/Opus válido (mesma técnica de `internal/platform/whatsapp/media/duration_test.go`, reimplementada localmente em `audio_pipeline_test.go` pois os helpers originais são não exportados em outro pacote) — nenhum áudio real foi necessário para esses cenários, apenas bytes sintéticos válidos o suficiente para o parser de duração.
- A suite real de STT (`TestGoldenAudioRealSTT`) reutiliza a mesma convenção de `STT_REAL_AUDIO_FIXTURE` já estabelecida em `internal/platform/llm/realstt_test.go` (task 3.0); como nenhum arquivo de áudio real está disponível neste ambiente e a instrução explícita proibiu inventar um, a suite faz skip explícito (não fabricado) citando as evidências reais já existentes (`benchmark-stt.md`, `stt-lote-30-results.json`) em vez de simular um resultado.

## Riscos Residuais
- [low] A suite real de STT do golden set (`TestGoldenAudioRealSTT`) nunca foi exercitada de ponta a ponta neste ambiente por ausência de `STT_REAL_AUDIO_FIXTURE` — mesmo risco residual já registrado na task 3.0 para `internal/platform/llm/realstt_test.go`; mitigado pelo benchmark real de 30 áudios (`stt-lote-30-results.json`) já capturado naquela task, mais o `t.Fatal` explícito que impede a suite de ser silenciosamente considerada "atendida" sem credencial.
- [low] Os 4 novos `os.Getenv` em `harness_audio_realllm_test.go` (arquivo `//go:build integration`) disparam `forbidigo` apenas quando lint é rodado explicitamente com `--build-tags=integration` — não é uma regressão nova (mesmo padrão em `harness_realllm_test.go`, `realllm_test.go`, `realstt_test.go`, nenhum coberto pelo gate padrão sem a tag); registrado para eventual decisão futura de adicionar exclusão explícita por nome de arquivo (`_realllm_test\.go$`) no `.golangci.yml`, fora do escopo desta tarefa.
- [low, herdado das tasks 6.0/7.0] `internal/platform/llm/openrouter_stt.go:53` excede o limite de 40 statements do linter `revive:function-length`. Pré-existente, fora do escopo de arquivos desta tarefa.
- A suite real de LLM completa e pré-existente (`TestGoldenRealLLMSuite`, 205 casos) não foi re-executada nesta tarefa por custo/tempo, pois nenhum arquivo do seu escopo (`case.go`, `registry.go`, `cases_*.go`, `harness.go`, `harness_realllm_test.go`) foi alterado — o `go build`/`go vet -tags integration` confirmam que ela continua compilando e a suite dedicada nova (`TestGoldenAudioRealLLMSuite`) prova que a infraestrutura compartilhada (`buildGoldenHarnessProvider`, `goldenToolsFor`, `EvaluateCaseWithCapture`) continua funcional com chamadas reais.

## Conflitos de Regra
- none
