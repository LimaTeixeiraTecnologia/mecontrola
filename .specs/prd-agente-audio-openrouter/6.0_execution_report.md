# Generated: 2026-08-04T22:03:05Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 6.0
- Título: Integração consumer, outbox e wiring Mastra
- Arquivo: .specs/prd-agente-audio-openrouter/task-6.0-integracao-consumer-wiring.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-agente-audio-openrouter/prd.md (RF-01..RF-46, seções Funcionalidades Core, Requisitos Funcionais, Fora de Escopo)
- TechSpec: .specs/prd-agente-audio-openrouter/techspec.md (seções Fluxo de Dados, Fronteiras, Design de Implementação, Persistencia de Auditoria, Idempotencia e Outcome Terminal, Pontos de Integracao, Conformidade com Skills e Regras, Gates de Validacao para Merge)
- Skills: `mastra`, `domain-modeling-production`, `design-patterns-mandatory` (declaradas na task file); `go-implementation` (linguagem, auto-carregada); `postgresql-production-standards` consultada informalmente ao redigir migration 000016 (padrão espelhado da 000015 já existente)
- Governança: AGENTS.md, R-ADAPTER-001, R-AGENT-WF-001, R-DTO-VALIDATE-001, R-TESTING-001, R-GOV-001

## Comandos Executados
- `go build ./...` -> pass
- `go vet ./...` -> pass
- `go test -race -count=1 ./internal/agents/infrastructure/messaging/database/consumers/...` -> pass (todos os cenários, incluindo os 9 novos de áudio)
- `go test -race -count=1 ./internal/agents/...` -> pass (todos os pacotes)
- `go test -race -count=1 ./configs/...` -> pass
- `go test -race -count=1 ./...` (repo inteiro) -> pass (0 falhas, todos os módulos)
- `go test -tags integration -count=1 ./migrations/...` -> pass (após correção de 2 constantes stale de versão de migration, ver Riscos Residuais/achados)
- `go test -tags integration -count=1 ./internal/agents/infrastructure/persistence/...` -> pass (inclui novo teste `TestInsertTerminalAcceptsPreSTTRejectionReasons` contra Postgres 16 real via testcontainers)
- `go test -tags integration -count=1 ./internal/agents/...` -> pass
- `.tools/bin/golangci-lint run ./internal/agents/... ./internal/platform/whatsapp/...` -> `0 issues`
- `.tools/bin/golangci-lint run ./internal/platform/... ./internal/agents/... ./configs/... ./cmd/server/... ./cmd/worker/...` -> `1 issue` (pré-existente em `internal/platform/llm/openrouter_stt.go:53`, função `Transcribe` da task 3.0 já concluída; não tocada nesta tarefa — ver Riscos Residuais)
- `gofmt -l internal/agents internal/platform migrations` -> vazio (sem divergência de formatação)
- Gate heurístico `grep "^func init()"` em internal/cmd/configs -> vazio
- Gate heurístico `grep -E "\b_[a-zA-Z]"` (identificador com prefixo `_`) -> vazio
- Gate heurístico zero-comentários em `.go` (`internal/platform internal/agents`) -> vazio
- Gate heurístico labels proibidos em métricas (`user_id|resource_id|thread_id|wamid|media_id`) -> vazio
- `subagent[review self-executado via skill review]` -> achados: 0 critical/high, 2 low (ver Riscos Residuais); veredito `APPROVED_WITH_REMARKS`

## Arquivos Alterados
- `internal/agents/module.go` — `AudioConfig`, `Deps.Audio`, DI manual do `wamedia.Client`, `llm.NewOpenRouterTranscriber`, `persistence.NewAudioAuditRepository`, `usecases.NewProcessAudioInbound`; `whatsAppInboundPayload` com campos de áudio; `buildWhatsAppAgentRoute` com parâmetro `audioEnabled` populando `MessageType`/`Audio*` apenas quando `AudioEnabled=true` e `msg.Type==MessageTypeAudio`.
- `internal/agents/module_test.go` — assinatura de `buildWhatsAppAgentRoute` atualizada nos 4 call-sites existentes (`audioEnabled=false`, preserva comportamento textual testado).
- `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer.go` — `inboundMessageKind` (tipo fechado texto/áudio), `validateInboundPayload`, `handleAudioInbound` (validate->dispatch usecase->outcome gate->dispatch textual ou reply seguro), `markAudioDispatched`, `WithAudioProcessor`.
- `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer_test.go` — 9 novos cenários: regressão textual, áudio aprovado (dispatch com texto canônico), incerto, falha STT, falha download/rejeição, feature flag off, erro de infra, payload de áudio incompleto.
- `internal/agents/application/usecases/process_audio_inbound.go` (novo) — usecase `ProcessAudioInbound` orquestrando `validate -> idempotência(FindByWAMID) -> resolve/download -> checksum -> duração -> preflight custo(dentro do Transcribe) -> STT -> decide -> persist auditoria -> resultado`; `MarkDispatched` para transição pós-dispatch.
- `internal/agents/application/usecases/process_audio_inbound_test.go` (novo) — suite testify com 10 cenários (aprovado, incerto/truncado, falha STT, custo excedido, tamanho excedido, media indisponível, duração excedida, duração indeterminável, idempotência por WAMID, input inválido) + testes de `MarkDispatched`.
- `internal/agents/application/usecases/decide_audio_transcription.go` — extensão aditiva de `AudioReason` com 6 novos valores fechados para rejeições pré-STT (`invalid_payload`, `media_unavailable`, `size_exceeded`, `duration_unavailable`, `duration_exceeded`, `cost_exceeded`); `IsValid()` atualizado.
- `internal/agents/application/usecases/decide_audio_transcription_test.go` — `TestAudioReasonIsValid` estendido com os 6 novos valores.
- `internal/agents/infrastructure/persistence/audio_audit_repository_integration_test.go` — novo teste `TestInsertTerminalAcceptsPreSTTRejectionReasons` provando contra Postgres real que a migration 000016 aceita as 6 novas razões.
- `migrations/000016_agents_whatsapp_audio_messages_widen_reason.up.sql` (novo) — amplia `CHECK` de `reason` na tabela de auditoria de áudio para cobrir rejeições pré-STT (necessário para RF-05/06/07/08 fecharem outcome/reason tipados; a migration 000015 da task 5.0 só cobria razões pós-STT).
- `migrations/000016_agents_whatsapp_audio_messages_widen_reason.down.sql` (novo) — reverte para o conjunto original de 7 razões.
- `migrations/migrations_integration_test.go` — 2 asserções de versão `uint(15)`→`uint(16)` (a versão real do baseline já era 15 pela migration 000015 da task 5.0 antes desta tarefa tocar o arquivo; ficou desatualizada para 14 no arquivo — corrigido para o valor correto após a 000016).
- `migrations/000003_unify_transactions_integration_test.go` — `stepsFromLatestToBeforeUnify` de `-12` para `-14` (drift pré-existente do total de migrations, ver Riscos Residuais).
- `migrations/000004_category_write_gate_schema_integration_test.go` — `latestMigrationVersion` de `14` para `16` e `stepsFromLatestToBeforeCategoryWriteGate` de `-11` para `-13` (mesmo drift).

## Resultados de Validação
- Testes: pass (unitários com `-race`, integração com Postgres 16 real via testcontainers, suíte completa do repositório)
- Lint: pass (0 issues nos pacotes desta tarefa; 1 issue pré-existente fora do escopo em arquivo de outra tarefa já concluída)
- Veredito do Revisor: APPROVED_WITH_REMARKS (2 achados `low`, sem `critical`/`high`; ver Riscos Residuais)

## Critérios de Aceite
- Fluxo textual atual continua verde -> comprovado: `go test -race -count=1 ./...` 100% pass; teste dedicado `TestHandleAudio_TextRegressionUnaffectedByAudioWiring` prova que o payload textual e o dispatch não mudam mesmo com `WithAudioProcessor` configurado; `buildWhatsAppAgentRoute` só popula campos de áudio quando `audioEnabled && msg.Type==MessageTypeAudio`.
- Áudio aprovado entra no mesmo agente textual -> comprovado: `handleAudioInbound` reusa `c.tryResume`/`c.handleAgentInbound` com `textPayload.Text = result.CanonicalText`; teste `TestHandleAudio_ApprovedDispatchesToSameHandleInboundWithCanonicalText` valida chamada idêntica a `input.InboundInput` que o fluxo textual usaria.
- Áudio incerto/rejeitado/falho cria outcome terminal e resposta segura, sem run/thread/message de agente -> comprovado: `!result.Outcome.AllowsHandleInbound()` retorna via `sendReply` antes de `tryResume`/`handleAgentInbound`; testes `TestHandleAudio_UncertainNeverCallsHandleInboundOrTools`, `TestHandleAudio_STTFailureNeverCallsHandleInbound`, `TestHandleAudio_RejectedDownloadFailureNeverCallsHandleInbound` usam `inboundMock.AssertNotCalled`.
- WAMID duplicado não duplica lançamento -> comprovado: `ProcessAudioInbound.checkExisting` consulta `FindByWAMID` antes de qualquer download/STT e retorna o outcome já persistido; `terminal()` trata `ErrAudioAuditAlreadyExists` como corrida segura; teste unitário "deve retornar resultado idempotente quando wamid ja foi processado sem chamar media ou stt" prova 0 chamadas a media/STT; teste de integração real `TestInsertTerminalDuplicateWAMIDReturnsAlreadyExists_RF22` (herdado da task 5.0) permanece verde.
- `AudioEnabled=false` mantém comportamento atual -> comprovado: `Deps.Audio.Enabled` tem zero-value `false`; `audioProcessor` fica `nil`; `buildWhatsAppAgentRoute` nunca popula `MessageType`; teste `TestHandleAudio_FeatureFlagOffSendsSafeReplyWithoutProcessor` cobre o caminho defensivo quando um payload de áudio chega mesmo assim.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos listados em "Comandos Executados").
- [x] Lint/vet/build sem regressão (1 issue pré-existente fora do escopo documentado, não introduzido por esta tarefa).
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree-uncommitted
verdict=APPROVED_WITH_REMARKS
tool=review-skill-self-executed

## Coverage

package=internal/agents/application/usecases,internal/agents/infrastructure/messaging/database/consumers
delta=+2 arquivos de produção, +2 arquivos de teste dedicados, cobertura de todos os outcomes fechados de AudioOutcome/AudioReason

## Suposições
- `llm.NewOpenRouterTranscriber` (task 3.0) e `wamedia.NewClient` (task 2.0) são a única fronteira de I/O para STT/mídia; nenhum fallback provider foi introduzido, preservando RF-39/ADR de provider único.
- A migration 000015 (task 5.0) fechou apenas razões pós-STT; a ampliação de razões pré-STT via migration 000016 é necessária para persistir corretamente outcomes de rejeição RF-05/06/07/08 sem reaproveitar `stt_error` de forma semanticamente incorreta — decisão registrada e testada contra Postgres real.
- `AudioInboundInput.Voice` é repassado da rota ao usecase mas não é persistido (a tabela de auditoria da task 5.0 não tem coluna `voice`); mantido apenas como metadado de payload para uso futuro (ver Riscos Residuais).
- Config de produção (env vars `AGENT_AUDIO_*`, `AGENT_STT_*`) e wiring em `cmd/server`/`cmd/worker` ficam para a task 7.0 conforme tasks.md; nesta tarefa `Deps.Audio` é populável apenas via DI manual, com zero-value seguro (`Enabled=false`) garantindo que nenhum código de `cmd/` precisou mudar para manter o comportamento atual.

## Riscos Residuais
- [low] `internal/agents/application/dtos/input/AudioInboundInput.Voice` (via `usecases.AudioInboundInput.Voice`) chega ao usecase mas não é persistido nem usado na decisão — campo funcionalmente morto até a task 7.0 (métricas) ou uma necessidade de produto o consumir. Não bloqueante.
- [low] `internal/platform/llm/openrouter_stt.go:53` (`Transcribe`, função da task 3.0 já concluída) excede o limite de 40 statements do linter `revive:function-length`. Pré-existente, não tocado por esta tarefa, fora do escopo declarado da task 6.0 (arquivos relevantes não incluem `openrouter_stt.go`). Recomendo registrar para a task 7.0/10.0 decidir se refatora.
- [medium->mitigado] Falha de infraestrutura genuína dentro de `ProcessAudioInbound.Execute` (ex.: banco de auditoria indisponível) propaga erro sem enviar resposta ao usuário e sem compensar o dedup de mensagem — é uma decisão deliberada espelhando a regra explícita do techspec ("não chamar dedup.Delete para falha de download, validação ou STT"), evitando reabrir o WAMID para replay automático de custo de STT/mutação financeira. Efeito colateral aceito: em uma falha de infraestrutura muito rara nesse trecho, o usuário pode não receber resposta até reenviar o áudio como nova mensagem. Coberto por teste `TestHandleAudio_ProcessorInfraErrorPropagatesWithoutSendingReply`.
- [descoberto e corrigido durante a tarefa] `migrations/000003_unify_transactions_integration_test.go` e `migrations/000004_category_write_gate_schema_integration_test.go` tinham constantes de contagem/passos de migration desatualizadas (drift da task 5.0 que nunca rodou a suíte completa de integração `-tags integration`). Corrigido nesta tarefa como pré-requisito para a suíte de migrations ficar verde com a migration 000016; validado com Postgres 16 real via testcontainers.
- Testes reais OpenRouter/WhatsApp Media API (`RUN_REAL_STT=1`, `RUN_REAL_WHATSAPP_MEDIA=1`) e golden set pareado ficam para a task 8.0 conforme sequenciamento do techspec; esta tarefa validou o wiring com mocks determinísticos e testes de integração reais de Postgres.

## Conflitos de Regra
- none
