# Generated: 2026-08-05T01:08:04Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 9.0
- Título: Runbook, dashboards e readiness operacional
- Arquivo: .specs/prd-agente-audio-openrouter/task-9.0-runbook-dashboard-readiness.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-agente-audio-openrouter/prd.md (RF-28, RF-29, RF-46)
- TechSpec: .specs/prd-agente-audio-openrouter/techspec.md (seções `Monitoramento e Observabilidade`, `Alertas iniciais`, `Riscos e Mitigacoes`, `Configuracao`, tabela de métricas RF-28)
- Skills: `mastra`, `domain-modeling-production`, `design-patterns-mandatory`, `otel-grafana-dashboards`, `golden-signals-otel-standards` (declaradas na task file); nenhuma skill `*-implementation` de linguagem carregada — tarefa é somente docs/YAML/JSON (dashboard, alertas, runbook), nenhum arquivo `.go` foi alterado
- Evidência de produção: `.specs/prd-agente-audio-openrouter/prod-evidence-2026-08-04.md` (datasources reais `prometheus`/`loki`/`tempo`, stack `otel-lgtm`)
- Código pré-existente inspecionado (só leitura, para grounding factual): `internal/agents/application/usecases/process_audio_inbound.go` (6 métricas reais e seus labels), `internal/agents/application/usecases/decide_audio_transcription.go` (enums `AudioOutcome`/`AudioReason` fechados), `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer.go` (wiring `audioProcessor`, resposta de flag desligada), `internal/agents/module.go` (gate `deps.Audio.Enabled`), `configs/config.go` (nomes exatos de env vars e validação de produção), `migrations/000015_agents_whatsapp_audio_messages.up.sql`, `deployment/telemetry/grafana/provisioning/{datasources,alerting/rules,alerting/contact-points}.yaml`, `deployment/dashboards/{README.md,agent-runtime-overview.json,mecontrola-ops.json}`, `docs/runbooks/mecontrola-agent-gate-posdeploy.md`, `docs/alerts/mecontrola-agent-gate-posdeploy.yaml`, `deployment/runbooks/rollback.md`

## Comandos Executados
- `python3 -c "import json; json.load(open('deployment/dashboards/agent-audio-whatsapp.json')); print('OK')"` -> `agent-audio-whatsapp.json OK` (JSON sintaticamente válido)
- `python3 -c "import yaml; yaml.safe_load(open('deployment/telemetry/grafana/provisioning/alerting/rules.yaml')); print('OK')"` -> `rules.yaml OK` (YAML sintaticamente válido, arquivo completo incluindo os 6 grupos pré-existentes + o novo grupo `audio`)
- Script Python de verificação estrutural do grupo `audio` (uid dos 5 alertas + balanceamento de parênteses de cada `expr` PromQL) -> `mc-audio-stt-error-rate`, `mc-audio-transcription-uncertain-rate`, `mc-audio-transcription-latency-p95`, `mc-audio-cost-microusd-high`, `mc-audio-false-success`; todas as 5 expressões com parênteses balanceados
- `grep -o "uid: mc-[a-z0-9-]*" deployment/telemetry/grafana/provisioning/alerting/rules.yaml | sort | uniq -d` -> vazio (nenhum `uid` duplicado no arquivo completo após a inserção)
- `grep -in 'wamid|media_id|"peer"|"user_id"|"resource_id"|"thread_id"' deployment/dashboards/agent-audio-whatsapp.json deployment/telemetry/grafana/provisioning/alerting/rules.yaml` -> only 2 matches, ambos dentro de **texto de comentário/anotação** documentando a AUSÊNCIA desses campos como label ("Nenhum label carrega... wamid, media_id..." e "Nao expõe wamid/media_id/transcricao"), não uso real como label de métrica — confirmado manualmente lendo as 2 linhas
- `git status --short` -> confirma que nenhum arquivo `.go` foi tocado por esta tarefa; apenas `deployment/dashboards/README.md` (modificado), `deployment/telemetry/grafana/provisioning/alerting/rules.yaml` (modificado), `deployment/dashboards/agent-audio-whatsapp.json` (novo), `deployment/runbooks/audio-whatsapp-stt.md` (novo)

Nenhum gate de Go foi executado porque nenhum arquivo `.go` foi alterado por esta tarefa (confirmado por `git status --short`); a condição "Se houver Go alterado, rodar gates proporcionais do pacote afetado" dos Testes da Tarefa não se aplica.

## Arquivos Alterados
- `deployment/runbooks/audio-whatsapp-stt.md` (novo) — runbook RF-46 cobrindo os 6 sintomas exigidos (falha de download, falha STT, incerteza técnica alta, latência alta, custo alto, regressão golden/falso sucesso), tabela de estados fechados `AudioOutcome`/`AudioReason`, readiness authoring/merge/production-ready, canário `AGENT_AUDIO_ENABLED=false` + critérios de habilitação, rollback (feature flag primeiro, deploy completo depois) e evidências mínimas pós-deploy.
- `deployment/dashboards/agent-audio-whatsapp.json` (novo) — dashboard Grafana com 11 painéis: volume por outcome, taxa de aprovação, `stt_error_rate`, `transcription_uncertain_rate`, série temporal de inbound por outcome/reason, latência de transcrição p50/p95/p99, latência de download p50/p95, tamanho por `mime_family`, duração por outcome, custo por modelo/hora, tabela top reasons de rejeição/falha. Usa `${datasource}` (variável de template, mesmo padrão dos dashboards existentes) resolvendo para o datasource Prometheus real `prometheus` confirmado em produção.
- `deployment/telemetry/grafana/provisioning/alerting/rules.yaml` (modificado) — novo grupo `audio` com 5 regras (`mc-audio-stt-error-rate`, `mc-audio-transcription-uncertain-rate`, `mc-audio-transcription-latency-p95`, `mc-audio-cost-microusd-high`, `mc-audio-false-success`) usando exatamente os thresholds da techspec (`> 5%`, `> 20%`, `> 8s`, `> 120000`, `> 0`) e os 6 nomes reais de métrica emitidos por `process_audio_inbound.go`.
- `deployment/dashboards/README.md` (modificado) — nova linha na tabela de dashboards, seção de métricas do agente de áudio (nomes/labels reais), 5 linhas na tabela de alertas proativos.

## Resultados de Validação
- Testes: pass (validação sintática JSON/YAML pass; grep de labels proibidos pass — nenhuma ocorrência real como label, só menções documentais da ausência; verificação de unicidade de `uid` pass; revisão manual do runbook linha a linha contra os 6 sintomas de RF-46 pass)
- Lint: não aplicável — nenhum arquivo `.go` alterado (gates de `gofmt`/`go vet`/`golangci-lint` não se aplicam a JSON/YAML/Markdown)
- Veredito do Revisor: APPROVED (auto-revisão restrita aos 4 arquivos desta tarefa: 1 novo runbook, 1 novo dashboard JSON, 1 alteração em rules.yaml, 1 alteração em README; nenhum arquivo pré-existente de código foi tocado, eliminando risco de regressão de runtime)

## Critérios de Aceite
- Runbook permite triagem sem acesso a áudio bruto -> comprovado: seção 1 do runbook documenta o fluxo completo apenas com outcome/reason/métricas/logs estruturados; seções 3.1–3.6 usam exclusivamente PromQL, LogQL e SQL sobre a tabela de auditoria (que nunca armazena o áudio, só metadados e transcrição aprovada); nenhum passo pede acesso a arquivo de mídia.
- Dashboards não usam labels de alta cardinalidade -> comprovado: `grep` sobre `agent-audio-whatsapp.json` não retorna `wamid`/`media_id`/`user_id`/`resource_id`/`thread_id`/`peer` como label real; todos os 11 painéis usam apenas os labels reais das 6 métricas (`channel`, `outcome`, `reason`, `provider`, `model`, `mime_family`), confirmados em `process_audio_inbound.go`.
- Threshold de custo horário é numérico e rastreável -> comprovado: `audio_cost_microusd_1h > 120000` (regra `mc-audio-cost-microusd-high`), com descrição explícita da derivação (60 áudios × teto de 2000 microusd/áudio configurado em `AGENT_AUDIO_MAX_COST_MICROUSD`) e link para a fonte na techspec.
- Readiness pós-deploy distingue authoring-ready, merge-ready e production-ready -> comprovado: seção 4 do runbook, tabela com os 3 níveis e critérios objetivos e distintos para cada um (build/test/lint local vs gate real por flag vs janela de observação em produção com `AGENT_AUDIO_ENABLED=true`).
- Cobrir RF-28, RF-29 e RF-46 -> comprovado: RF-28 (métricas de baixa cardinalidade) via seção 7 do runbook + README atualizado + dashboard sem labels proibidos; RF-29 (nomes/janelas/thresholds na techspec) via os 5 alertas com os thresholds literais da techspec; RF-46 (runbook cobrindo os 6 sintomas) via seções 3.1–3.6.
- Runbook cobre falha de download, falha STT, incerteza técnica alta, latência alta, custo alto e regressão golden -> comprovado: seções 3.1 (download), 3.2 (STT), 3.3 (incerteza), 3.4 (latência), 3.5 (custo), 3.6 (regressão golden e falso sucesso) — 6 seções dedicadas, uma por sintoma exigido, cada uma com PromQL/LogQL/SQL reais e `error_code`s reais extraídos do código (`classifyMediaDownloadError`/`classifySTTError`).
- Alertas usam thresholds da techspec -> comprovado: os 5 valores nas regras (`5`, `20`, `8`, `120000`, `0`) são cópia literal de `stt_error_rate_15m > 5%`, `transcription_uncertain_rate_15m > 20%`, `transcription_p95_15m > 8s`, `audio_cost_microusd_1h > 120000`, `audio_false_success > 0` da seção `Alertas iniciais` da techspec.
- Dashboards usam datasources reais Prometheus, Loki e Tempo quando aplicável -> comprovado: dashboard usa variável `${datasource}` resolvendo para `prometheus` (uid real confirmado em `prod-evidence-2026-08-04.md`); nenhum painel de Loki/Tempo foi necessário porque as 6 métricas de áudio são inteiramente baseadas em Prometheus (não há necessidade de log/trace correlacionado nos painéis agregados) — o runbook complementa com queries LogQL/TraceQL reais para investigação pontual (seções 3.1–3.4), usando os labels Loki reais (`service_name`) confirmados em produção.
- Não incluir PII, WAMID, telefone, media id ou transcrição em queries/labels -> comprovado: gate de grep (seção 7 do runbook + comando executado nesta tarefa) retorna vazio para uso real como label; as 2 ocorrências textuais são anotações documentando a ausência desses campos, não uso.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (validação sintática JSON/YAML, grep de labels proibidos, revisão do runbook contra RF-46 — todos com comando/resultado listado em "Comandos Executados").
- [x] Nenhum arquivo `.go` alterado; gate de Go não se aplica (confirmado por `git status --short`).
- [x] Estado de `tasks.md` sincronizado com este relatório.

## Diff Reviewed

sha=working-tree-uncommitted
verdict=APPROVED
tool=review-skill-self-executed

## Coverage

package=deployment (dashboards, telemetry/grafana/provisioning/alerting, runbooks)
delta=+2 arquivos novos (`agent-audio-whatsapp.json`, `audio-whatsapp-stt.md`); 2 arquivos pré-existentes modificados (`README.md` do folder de dashboards, `rules.yaml` de alerting — apenas adição de seções novas, nenhuma regra/painel pré-existente removido ou alterado)

## Suposições
- `audio_false_success` (RF-29) não tem métrica dedicada por origem áudio/texto porque a tabela de labels permitidos da techspec (seção `Monitoramento e Observabilidade`) não lista uma dimensão de origem para nenhuma das 6 métricas de áudio, e nenhuma métrica de escrita financeira (`agents_*_false_success_total`) tem label de origem também — inventar um label novo violaria tanto o contrato de labels da techspec quanto a regra de cardinalidade controlada. A regra `mc-audio-false-success` reusa deliberadamente a família genérica já existente (mesma fonte do alerta `FinancialWriteFalseSuccess` documentado em `docs/alerts/mecontrola-agent-gate-posdeploy.yaml`), documentando essa limitação explicitamente na descrição do alerta e no runbook (seção 3.6). A garantia de tolerância zero específica de áudio já existe de forma determinística e mais forte no golden set (`TestInvariantNoFalseSuccessOnAudioOriginatedRun`, tarefa 8.0).
- O job/serviço (`api` vs `worker`) que emite as métricas `agents_audio_*` não foi assumido nas queries do dashboard/alertas — tanto `cmd/server/server.go` quanto `cmd/worker/worker.go` chamam `agents.NewModule`, então o processo real que emite depende de onde o outbox consumer roda em produção; para não fabricar um filtro `job=` incorreto, todas as queries agregam sem filtro de `job` (`sum(rate(agents_audio_inbound_total[...]))`), o que é seguro e não perde dados independente de qual processo emite.
- Os dois arquivos de alerting existentes no repositório (`deployment/telemetry/grafana/provisioning/alerting/rules.yaml`, formato nativo Grafana unified alerting, provisionado automaticamente pelo `otel-lgtm` conforme confirmado na evidência de produção) e (`docs/alerts/*.yaml`, formato Prometheus/Alertmanager clássico, sem `rule_files` apontando para ele em `deployment/telemetry/prometheus/prometheus.yml` de forma confirmada como ativa em produção) não são a mesma pipeline. Esta tarefa adicionou os 5 alertas de áudio ao mecanismo confirmado como ativo em produção (`rules.yaml`/Grafana, evidência: datasources `prometheus`/`loki`/`tempo` do `otel-lgtm` documentados em `prod-evidence-2026-08-04.md`), e apenas referenciou o `docs/alerts/mecontrola-agent-gate-posdeploy.yaml` existente como fonte histórica do padrão de família `agents_*_false_success_total`, sem duplicar ou modificar aquele arquivo.

## Riscos Residuais
- [low] O dashboard `agent-audio-whatsapp.json` não pôde ser importado/validado contra uma instância Grafana real neste ambiente (sem acesso de escrita à VPS de produção nesta tarefa) — a validação foi sintática (JSON parse) e estrutural (mesmo schema/convenções dos dashboards existentes que já rodam em produção, `schemaVersion: 39`, mesmo padrão de `datasource`/`targets`/`fieldConfig`). Mitigado por seguir byte a byte o padrão de `agent-runtime-overview.json`, já confirmado funcional em produção.
- [low] As 5 regras de alerta do grupo `audio` também não foram validadas contra uma instância Grafana Alerting real (mesma limitação de acesso). Mitigado por validação YAML sintática + verificação estrutural (uid único, parênteses balanceados nas expressões PromQL) + reuso literal do padrão de `condition`/`data`/`threshold` dos 6 grupos pré-existentes que já rodam em produção.
- [low] O grupo de alertas `audio` fica em `noDataState: OK` até `AGENT_AUDIO_ENABLED=true` ser habilitado em produção (comportamento intencional, documentado na seção 5 do runbook e no comentário do YAML) — nenhum destes 5 alertas dispara nem gera ruído enquanto o canário estiver desligado, mas também não há evidência viva de disparo real ainda (esperado, dado que o rollout de áudio ainda não ocorreu).
- [informativo] `docs/alerts/mecontrola-agent-gate-posdeploy.yaml` permanece como um segundo mecanismo de alerta documentado no repositório sem confirmação de que está ativamente montado em `deployment/telemetry/prometheus/prometheus.yml` de produção (a evidência de produção só confirma o stack `otel-lgtm`/Grafana unified alerting como ativo). Isso é um risco pré-existente do repositório, não introduzido por esta tarefa — apenas referenciado, não modificado.

## Conflitos de Regra
- none
