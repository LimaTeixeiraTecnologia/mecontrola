# Tarefa 2.0: Seed editorial do catálogo completo - Relatório de Execução

**Status:** done
**Data de conclusão:** 2026-06-09
**Executor:** Claude Code

## Resumo

Criada migration de seed com todas as raízes e subcategorias de despesas (5 raízes) e receitas (8 raízes), usando UUIDv5 determinístico com namespace fixo conforme ADR-004.

## Arquivos Criados

1. `migrations/000005_seed_categories.up.sql` - Migration de seed com todas as categorias
2. `migrations/000005_seed_categories.down.sql` - Rollback da migration

## Arquivos Modificados

1. `migrations/migrations_integration_test.go` - Adicionados testes de integração para validação do seed

## Estrutura do Seed

### Despesas (88 categorias)
- 5 raízes: Custo Fixo, Conhecimento, Prazeres, Metas, Liberdade Financeira
- 83 subcategorias distribuídas entre as raízes

### Receitas (21 categorias)
- 8 raízes: Salário, Renda Variável, Investimentos, Aluguel Recebido, Restituições e Cashback, Presentes Recebidos, Vendas, Outras Receitas
- 13 subcategorias distribuídas entre as raízes

### allocation_type
- `asset_allocation`: Todas as subcategorias de "Metas" e "Liberdade Financeira", e subcategorias de receita sob "Investimentos"
- `consumption`: Demais receitas e despesas

## IDs Determinísticos (UUIDv5)

Namespace: `uuid.NewSHA1(uuid.Nil, []byte("mecontrola.io/categories"))`

Fórmula: `uuid.NewSHA1(categoryNamespace, []byte(kind+":"+slug))`

Exemplos de IDs gerados:
- Custo Fixo (expense/custo-fixo): `66cb85a0-3266-5900-b8e3-13cdcd00ab62`
- Metas (expense/metas): `f133508e-7dc3-58a3-96db-199d8fbd2987`
- Salário (income/salario): `86dd34b0-7342-525a-9a30-b1b5a76b109f`
- Investimentos (income/investimentos): `1c801292-d1a0-56a9-8d05-a28f39f5e6dd`

## Critérios de Aceite Verificados

| Critério | Status | Evidência |
|----------|--------|-----------|
| Migration de seed aplica sem erro após baseline | ✅ | Teste `TestCategoriesMigrationsUpAndDown` passou |
| Todos os IDs são recalculáveis via UUIDv5 | ✅ | Teste `assertDeterministicCategoryIDs` passou |
| SELECT count(*) FROM categories WHERE kind='expense' retorna número esperado | ✅ | Teste `assertExpenseCategoriesCount` passou (≥88) |
| Índice único (kind, slug) não violado | ✅ | Migration usa `ON CONFLICT (kind, slug) DO NOTHING` |
| Teste de integração com seed completo passa | ✅ | Todos os testes de `TestCategoriesSeedMigration` passaram |
| allocation_type correto por categoria | ✅ | Teste `assertAllocationTypesCorrect` passou |
| Profundidade máxima 2 | ✅ | Teste `assertMaxDepthTwo` passou |
| Versão editorial incrementada | ✅ | Teste `assertEditorialVersionIncremented` passou (≥2) |

## Validações Executadas

```bash
# Build
$ go build ./...
OK

# Vet
$ go vet ./...
OK

# Testes de integração
$ go test -tags=integration ./migrations/... -run TestCategoriesSeedMigration -v
OK (6/6 cenários passaram)

$ go test -tags=integration ./migrations/... -run TestCategoriesMigrationsUpAndDown -v
OK (1/1 cenário passou)
```

## Conformidade com Regras

- ✅ R-ADAPTER-001.1: Zero comentários em arquivos .go de produção
- ✅ R-ADAPTER-001.2: Adaptadores finos (não aplicável - migration SQL)
- ✅ R0: Sem `init()`
- ✅ R5.12: Sem `panic` em produção
- ✅ R6: `context.Context` em fronteiras de IO
- ✅ R7: Uso de recursos modernos do Go (1.26.4)

## Notas

- A migration foi criada como idempotente usando `ON CONFLICT (kind, slug) DO NOTHING`
- O rollback (down) limpa primeiro o dicionário (devido a FK) e depois as categorias
- A versão editorial é incrementada de 1 para 2 após aplicação do seed
- Os testes de integração validam tanto os IDs determinísticos quanto as regras de negócio
