# Generated: 2026-06-29T00:00:00Z

# Relatório de Execução — Tarefa 1.0

## Identificação

- **Tarefa:** 1.0 — Indexação assíncrona de embeddings em `internal/platform/memory`
- **PRD slug:** agents-weather-mastra
- **Status final:** done

## Arquivos Alterados

| Arquivo | Tipo |
|---------|------|
| `internal/platform/memory/types.go` | modificado — adicionado `IndexMessagePayload` |
| `internal/platform/memory/ports.go` | modificado — adicionado `MessageIndexPublisher` |
| `internal/platform/memory/publishing_message_store.go` | novo — decorator `publishingMessageStore` |
| `internal/platform/memory/publishing_message_store_test.go` | novo — testes unitários do decorator |
| `internal/platform/memory/infrastructure/indexer/handler.go` | modificado — handle `outbox.Envelope`, métricas |
| `internal/platform/memory/infrastructure/indexer/handler_test.go` | novo — testes unitários do handler |
| `internal/platform/memory/infrastructure/indexer/outbox_publisher.go` | novo — `OutboxMessageIndexPublisher` |
| `internal/platform/memory/infrastructure/indexer/types.go` | modificado — esvaziado (tipo movido para `memory`) |
| `internal/platform/outbox/system_event_allowlist.go` | modificado — adicionado `platform.memory.embedding.index.v1` |

## Subtarefas

- [x] 1.1 `publishingMessageStore` (decorator de `MessageStore`) + interface `MessageIndexPublisher` em `internal/platform/memory`; publica `platform.memory.embedding.index.v1` com `IndexMessagePayload` após `Append` bem-sucedido.
- [x] 1.2 `EmbeddingIndexHandler` atualizado para handle de `outbox.Envelope`; idempotência por `ON CONFLICT (source_message_pk, model)` no `Index`; `OutboxMessageIndexPublisher` como adapter de outbox.
- [x] 1.3 Métricas com labels enums (`model`, `reason`); logs de erro; falha de publish é degradação controlada (retorna nil, não quebra Append).

## Critérios de Aceite

- **Append de mensagem dispara evento de indexação** — comprovado: `TestPublishingMessageStoreSuite/TestAppend/deve_publicar_evento_apos_append_bem-sucedido` valida que `PublishIndex` é chamado após `Append` bem-sucedido com payload correto.
- **Replay do mesmo `event_id` não duplica embedding** — comprovado: `ON CONFLICT (source_message_pk, model) DO NOTHING` já presente em `embedding_repository.go:33`; `TestEmbeddingIndexHandlerSuite/TestHandle_IdempotentBySourceMessagePK` valida chamadas repetidas ao handler.
- **Zero comentários em produção** — comprovado: gate `grep` retornou vazio nos arquivos de produção novos/modificados.
- **gofmt limpo** — comprovado: `gofmt -l` retornou vazio.
- **sem leak** — comprovado: decorator não lança goroutines; handler não mantém goroutines; shutdown cooperativo via contexto.

## Comandos Executados

```
go build ./... → EXIT 0
go vet ./... → EXIT 0
go test ./internal/platform/memory/... → ok (todos os pacotes)
gofmt -l ./internal/platform/memory/ → vazio (limpo)
grep (zero comentários) → OK: zero comments
```

## Testes: pass

```
TestPublishingMessageStoreSuite (4 cenários) — PASS
TestEmbeddingIndexHandlerSuite (5 cenários) — PASS
TestEmbeddingRepositorySuite — PASS (regressão)
```

## Decisões de Implementação

- `IndexMessagePayload` movido para `internal/platform/memory/types.go` para evitar import cíclico (`memory` → `indexer` → `memory`).
- `ThreadID` no payload usa `threadPK.String()` (UUID string) pois `Append` não recebe o `threadID` string; recall é escopado por `resourceID`, então o campo é informativo.
- Falha de `PublishIndex` é silenciada (log + métrica) sem retornar erro: o `Append` já persistiu a mensagem; embedding é best-effort.
- `platform.memory.embedding.index.v1` adicionado ao `noUserEventAllowlist` para suprimir warning de `aggregate_user_id` ausente.
- `EmbeddingIndexHandler` construtor atualizado com `o11y` para métricas/log (breaking change de assinatura — apenas wiring afetado).

## Riscos Residuais

- Integração testcontainers pgvector (Recall ANN real) não foi executada nesta tarefa (requer infra pgvector). Será coberta na Tarefa 7.0 (conformidade + gates).
- O wiring de `NewPublishingMessageStore` em `module.go` e em `AgentRuntime` é responsabilidade da Tarefa 4.0.
