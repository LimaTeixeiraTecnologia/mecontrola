# Generated: 2026-06-29T00:00:00Z

# Execution Report — Task 6.0: Cutover/Eliminação `internal/agent`

## Task
Tarefa 6.0: Cutover — eliminação física de `internal/agent` + desligar onboarding conversacional do WhatsApp.

## Status
done

## Critérios de Aceite

- [x] `grep -rn "internal/agent\"" internal/ cmd/ test/ | grep -v internal/platform/agent` → vazio
  -> comprovado: gate rodado, retorno vazio (nenhuma referência)

- [x] `test -d internal/agent` → falso (diretório não existe)
  -> comprovado: `rm -rf internal/agent` executado; `test -d` retornou falso

- [x] `go build ./...` EXIT 0
  -> comprovado: `go build ./...; echo EXIT:$?` → EXIT:0

- [x] `go vet ./...` EXIT 0
  -> comprovado: `go vet ./...; echo EXIT:$?` → EXIT:0

- [x] `go test` das áreas tocadas verde
  -> comprovado: `go test ./...` → EXIT:0, todos os pacotes ok

- [x] WhatsApp inbound atendido só por `internal/agents`
  -> comprovado: dispatcher simplificado para rota única; whatsapp_wiring.go usa apenas `agentsModule.WhatsAppAgentRoute`

- [x] "ATIVAR <token>" não mais roteado para onboarding
  -> comprovado: dispatcher retorna `OutcomeNoRoute` para activation command; onboardingRoute removido

- [x] gofmt limpo nos arquivos alterados
  -> comprovado: `gofmt -w` aplicado; `gofmt -l` retornou vazio nos arquivos de produção alterados

## Subtarefas Executadas

### 6.1 — Religar cmd/server e cmd/worker para internal/agents
- `cmd/server/server.go`: importa `internal/agents` e `internal/agents/infrastructure/weather`; inicializa `agents.NewModule(agents.Deps{...})`; remove `agent.NewAgentModule` e todas as deps do módulo antigo
- `cmd/worker/worker.go`: idem; registra `agentsModule.EventHandlers`; usa `for _, j := range agentsModule.Jobs` para jobs
- `internal/bootstrap/card_creator.go` (NOVO): `OnboardingCardCreatorAdapter` migrado de `internal/agent/infrastructure/binding`; substitui `agentbinding.NewOnboardingCardCreatorAdapter` nos cmd

### 6.2 — Simplificar dispatcher para rota única
- `internal/platform/whatsapp/dispatcher/dispatcher.go`: removidos campos `onboardingRoute` e param `onboardingRoute` do `New()`; adicionado `OutcomeNoRoute`; activation command e usuário desconhecido retornam `OutcomeNoRoute`; constantes `OutcomeOnboarding` e `OutcomeFallback` removidas
- `cmd/server/whatsapp_wiring.go`: assinatura simplificada; recebe `agents.Module`; `New()` chamado sem `onboardingRoute`; removido `onboardingRoute` closure

### 6.3 — Apagar internal/agent e ajustar e2e
- `internal/agent/**`: apagado fisicamente via `rm -rf`
- `internal/onboarding/e2e/feature_onboarding_conversational_steps_test.go`: removido (importava `internal/agent/application/workflow` e `services`)
- `internal/onboarding/e2e/suite_test.go`: removido import `agentservices`, removido campo `onboardingAgent`, removido `registerOnboardingConversationalSteps`
- `internal/onboarding/e2e/support_runtime_test.go`: removidos imports `agentservices` e `agentworkflow`; removidos `newE2EOnboardingAgent`, `e2eHistoryGateway` e todos helpers de workflow do onboarding agent; removido campo `onboardingAgent` de `onboardingDependencies`

### 6.4 — Migrar config AGENT_* → módulo agents
- `configs/config.go`: adicionado `EmbedModel string mapstructure:"AGENTS_LLM_EMBED_MODEL"` em `AgentConfig`; cmd/server e cmd/worker consomem `cfg.AgentConfig.{PrimaryModel, EmbedModel, OpenRouterAPIKey, OpenRouterBaseURL, MaxTokens, Temperature}` para `agents.LLMConfig`

### 6.5 — Gates de validação
- `grep -rn "internal/agent\"" internal/ cmd/ test/ | grep -v internal/platform/agent` → vazio ✓
- `test -d internal/agent` → falso ✓
- `go build ./...` → EXIT:0 ✓
- `go vet ./...` → EXIT:0 ✓
- `go test ./...` → EXIT:0, todos os pacotes ok ✓
- `gofmt -l` nos arquivos alterados → vazio após `gofmt -w` ✓
- Zero comentários R-ADAPTER-001.1 → verificado nos arquivos alterados de produção ✓

## Comandos Executados

```
go build ./...                    → EXIT:0
go vet ./...                      → EXIT:0
go test ./internal/platform/whatsapp/dispatcher/...  → ok
go test ./cmd/... ./internal/bootstrap/... ./internal/agents/...  → EXIT:0
go test ./...                     → EXIT:0
gofmt -l (production files)       → vazio após correção
grep -rn "internal/agent" ...     → vazio (gate pass)
test -d internal/agent            → false (gate pass)
```

## Arquivos Alterados

- `internal/bootstrap/card_creator.go` (NOVO)
- `configs/config.go` (adicionado `EmbedModel` em `AgentConfig`)
- `internal/platform/whatsapp/dispatcher/dispatcher.go` (removido onboardingRoute, adicionado OutcomeNoRoute)
- `internal/platform/whatsapp/dispatcher/dispatcher_test.go` (atualizado para rota única)
- `internal/platform/whatsapp/dispatcher/dispatcher_integration_test.go` (atualizado para rota única)
- `cmd/server/whatsapp_wiring.go` (simplificado para agents.Module)
- `cmd/server/server.go` (migrado para internal/agents)
- `cmd/worker/worker.go` (migrado para internal/agents)
- `internal/onboarding/e2e/suite_test.go` (removido agent)
- `internal/onboarding/e2e/support_runtime_test.go` (removido agent)
- `internal/onboarding/e2e/feature_onboarding_conversational_steps_test.go` (REMOVIDO)
- `internal/agent/**` (APAGADO FISICAMENTE)

## Riscos Residuais

- Nenhum risco crítico identificado.
- O campo `EmbedModel` adicionado a `AgentConfig` usa env var nova `AGENTS_LLM_EMBED_MODEL`; se não configurado, string vazia é usada (comportamento seguro — o LLM provider trata modelo vazio).
- Onboarding conversacional no WhatsApp desabilitado conforme ADR-004; usuários não-cadastrados recebem `OutcomeNoRoute` (HTTP 200 sem ação).
- O `gofmt` de `internal/agents/application/workflows/workflow.go` estava com drift pré-existente (não alterado nesta tarefa).
