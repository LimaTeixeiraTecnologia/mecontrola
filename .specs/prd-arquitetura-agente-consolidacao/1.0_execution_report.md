# Generated: 2026-06-24T00:00:00Z

## Tarefa 1.0 — Kernel como caminho padrão e obrigatório

### Status: done

### Arquivos Alterados

- `configs/config.go` — default de `WORKFLOW_KERNEL_TRANSACTIONS_WRITE_ENABLED` alterado de `false` para `true`
- `internal/agent/module.go` — `attachKernel` retorna `error` ao invés de degradar silenciosamente; `buildIntentRouter` propaga o erro
- `internal/agent/application/services/daily_ledger_agent.go` — `dispatchWrite` remove o guard duplo `confirmEngine != nil` para o ramo destrutivo; agora retorna `OutcomeUsecaseError` explícito se `confirmEngine` for nil
- `internal/agent/application/services/intent_router_new_writes_test.go` — 4 testes legados de `KindDeleteCard` sem kernel consolidados em 1 teste de contrato (`OutcomeUsecaseError` explícito)
- `internal/agent/application/services/intent_router_new_kinds_test.go` — 6 testes legados de `KindDeleteLastTransaction`/`KindEditLastTransaction` sem kernel consolidados em 2 testes de contrato

### Subtarefas Executadas

- [x] 1.1 `WORKFLOW_KERNEL_TRANSACTIONS_WRITE_ENABLED` default = `true` em `configs/config.go`
- [x] 1.2 `attachKernel` em `module.go` retorna `error` quando `sessionDB == nil || wfStoreFactory == nil` — módulo falha na construção ao invés de degradar
- [x] 1.3 `dispatchWrite` em `daily_ledger_agent.go`: ramo destrutivo sem `confirmEngine` retorna `OutcomeUsecaseError` explícito; ramo kernel não tem mais fallback legacy quando `kernelEnabled = true`
- [x] 1.4 Testes atualizados para novo contrato explícito de erro

### Critérios de Aceite

- `TransactionsWriteEnabled` default é `true` (config.go:651)
  -> comprovado: `grep "WORKFLOW_KERNEL_TRANSACTIONS_WRITE_ENABLED.*true" configs/config.go` → match

- `confirmEngine` sem ramo silencioso de bypass em `dispatchWrite`
  -> comprovado: `grep "confirmEngine == nil.*nil\|confirmEngine != nil" daily_ledger_agent.go` → apenas o guard de segurança explícito que retorna error, sem ramo legacy

- Kernel sem import de domínio (R-WF-KERNEL-001.1)
  -> comprovado: gate grep retorna vazio — OK

- Zero comentários em Go de produção do agent
  -> comprovado: gate grep retorna vazio — OK

- Build e suites
  -> comprovado: `go build ./... && go test ./internal/agent/... ./internal/platform/workflow/...` — todos verdes

### DoD Checklist

1. [x] `TransactionsWriteEnabled` é `true` por padrão
2. [x] `confirmEngine` não-opcional: ausência de store/factory causa erro de wiring na construção
3. [x] Nenhum teste de produção depende de `TransactionsWriteEnabled=false` para passar
4. [x] Build, vet e suites verdes

### Comandos Executados

```
go build ./internal/agent/... → ok (sem erros)
go test ./internal/agent/application/services/... → ok 0.560s
go test ./internal/agent/... → todos ok
go test ./internal/platform/workflow/... → ok
go build ./... → ok (sem erros)
```

### Riscos Residuais

- Nenhum: o caminho legacy em `dispatchWrite` (registry.Resolve) ainda existe para writes não-destrutivos quando `kernelEnabled=false` (ex: testes sem kernel). Isso é intencional para task 1 — remoção ocorre em 2.0/3.0.
- Em produção, `attachKernel` garante que `confirmEngine` seja sempre não-nil ao construir o módulo.
