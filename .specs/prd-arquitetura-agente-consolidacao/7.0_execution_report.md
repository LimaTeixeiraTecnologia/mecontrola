# Generated: 2026-06-24T00:00:00Z

# Relatório de Execução — Tarefa 7.0: Teste exaustivo intent.Kind

## Status Final
`done`

## Resumo
Criado `internal/agent/domain/intent/intent_registry_test.go` com suite testify exaustiva que itera sobre todos os 21 kinds via range iota, verifica round-trip String()/ParseKind(), ausência do slug "unknown" para kinds não-KindUnknown, paridade bidirecional schema↔kind com ParseIntentJSONSchema, e cobertura total do mapa de builders (kindBuilders).

## Arquivos Criados/Modificados
- `internal/agent/domain/intent/intent_registry_test.go` — criado (novo)
- `.specs/prd-arquitetura-agente-consolidacao/tasks.md` — task 7.0 marcada `done`

## Critérios de Aceite

### arquivo presente
-> comprovado: `internal/agent/domain/intent/intent_registry_test.go` criado com sucesso.

### testes passam
-> comprovado:
```
=== RUN   TestKindRegistrySuite
=== RUN   TestKindRegistrySuite/TestAllKindsHaveBuilder
=== RUN   TestKindRegistrySuite/TestBuildersMapExhaustiveness
=== RUN   TestKindRegistrySuite/TestParityKindToSchema
=== RUN   TestKindRegistrySuite/TestParitySchemaToKind
=== RUN   TestKindRegistrySuite/TestRoundTrip
=== RUN   TestKindRegistrySuite/TestSlugNotDefaultForNonUnknown
--- PASS: TestKindRegistrySuite (0.00s)
PASS
ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/agent/domain/intent  0.398s
```

### prova de eficácia — teste FALHA ao remover slug do schema
-> comprovado: remoção de `"edit_category_percentage"` do enum de `ParseIntentJSONSchema` provocou falha clara:
```
intent_registry_test.go:160:
    Error: Should be true
    Messages: kind 21 ("edit_category_percentage") não está presente no enum de ParseIntentJSONSchema
--- FAIL: TestKindRegistrySuite (0.00s)
    --- FAIL: TestKindRegistrySuite/TestParityKindToSchema (0.00s)
```
Revertido e validado verde novamente.

### go build ./... verde
-> comprovado: `go build ./...` sem erros.

### zero comentários (R-ADAPTER-001.1)
-> comprovado: grep de comentários no arquivo gerado retornou 0 ocorrências fora das exceções.

## Definition of Done (DoD)

1. [x] Teste exaustivo único presente, cobrindo (a)–(d) + exaustividade do mapa de builders.
2. [x] `allKinds()` deriva de fonte única (range iota KindUnknown..KindEditCategoryPercentage), não de lista manual.
3. [x] Paridade bidirecional schema↔kind implementada (TestParityKindToSchema + TestParitySchemaToKind).
4. [x] `go test ./internal/agent/domain/intent/...` e `go build ./...` verdes.

## Comandos Executados

```bash
go test ./internal/agent/domain/intent/... -v -run 'KindRegistry'
# PASS — 6 sub-testes

go test ./internal/agent/domain/intent/... -v
# PASS — 56 sub-testes (3 suites: IntentSuite, IntentNewKindsSuite, KindRegistrySuite)

# Injeção de divergência (prova de eficácia):
sed -i '' '/"edit_category_percentage",/d' internal/agent/application/prompting/prompts.go
go test ./internal/agent/domain/intent/... -v -run 'TestKindRegistrySuite/TestParityKindToSchema'
# FAIL: kind 21 ("edit_category_percentage") não está presente no enum de ParseIntentJSONSchema

# Revert:
cp /tmp/prompts_backup.go internal/agent/application/prompting/prompts.go
go test ./internal/agent/domain/intent/... -v
# PASS

go build ./...
# OK
```

## Decisão: testes parciais existentes mantidos sem remoção

`TestKindString`, `TestParseKind`, `TestNewKinds_StringAndParseRoundTrip` foram mantidos — cobrem casos concretos de validação de formato/campos que complementam o teste exaustivo sem redundância crítica. A task permitia "consolidar ou manter" — manter foi a escolha menos invasiva para esta tarefa isolada.

## Riscos Residuais
Nenhum. O teste `KindRegistrySuite` é a fonte canônica de detecção de drift schema↔kind para todas as adições futuras de kinds.
