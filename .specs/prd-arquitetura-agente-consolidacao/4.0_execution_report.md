# Generated: 2026-06-24T00:00:00Z

## Tarefa 4.0 — HITL sempre-on: remover bypass sem confirmação e duplo caminho de tools

### Status: done

### Arquivos Alterados

- `internal/agent/application/services/agent_workflows.go` — removidos `KindDeleteLastTransaction`, `KindEditLastTransaction` do `transactionsWorkflow` e `KindDeleteCard` do `cardsWorkflow` em `buildRegistry()`; removidos os mesmos 3 kinds de `routableKinds()` (de 21 para 18 kinds)
- `internal/agent/application/services/agent_workflows_test.go` — atualizado `TestRoutableKindsHasNoDuplicates` (21→18); atualizado `TestRoutableKindsContainsExpectedKinds` (removidos 3 kinds destrutivos); adicionado `TestDestructiveKindsNotInRoutableKinds` para verificar exclusão explícita

### Subtarefas Executadas

- [x] 4.1 Ramo de bypass verificado — já eliminado na Task 1.0: `confirmEngine == nil` retorna `OutcomeUsecaseError` explícito (linhas 262-270 de `daily_ledger_agent.go`)
- [x] 4.2 Caminho único de mutação: `KindDeleteLastTransaction`, `KindEditLastTransaction`, `KindDeleteCard` removidos do registry — só executam via `Executor` do `destructive_confirm` (path `dispatchWriteDestructive → confirmEngine.Start → steps.ExecuteDestructive`)
- [x] 4.3 Ordem determinística confirmada: `continuePendingApproval` (linha 189) → `a.parser.Parse` (linha 206) em `tryResumeInbound`/`Handle`; limpeza determinística presente em `finalizeConfirmResult`/`finalizeConfirmStart` para confirma/cancela/TTL
- [x] 4.4 `clearBudgetSession` chamado em `finalizeConfirmStart` (linha 604) e `finalizeConfirmResult` (linha 727) para `OperationBudgetCommit`

### Critérios de Aceite

- `AwaitingApproval`/`OperationKind` como tipos fechados, nunca string solta
  -> comprovado: `grep -rn 'AwaitingApproval\s*=\s*"[^"]*"\|OperationKind\s*=\s*"[^"]*"' internal/agent/` → vazio (OK)

- `continuePendingApproval` antes de `ParseInbound`
  -> comprovado: linha 189 (`continuePendingApproval`) < linha 206 (`a.parser.Parse`) em `daily_ledger_agent.go`

- Switch não cresceu em `daily_ledger_agent.go`
  -> comprovado: `grep -cE "^[[:space:]]*case intent\.Kind" daily_ledger_agent.go` → 0 cases

- Tools destrutivos não registrados no registry (único caminho é `dispatchWriteDestructive`)
  -> comprovado: `grep -n "NewDeleteLastTransaction\|NewEditLastTransaction\|NewDeleteCard" agent_workflows.go` → vazio (OK)

- `clearBudgetSession` presente no caminho do gate para `OperationBudgetCommit`
  -> comprovado: linhas 604-605 em `finalizeConfirmStart` e 727-729 em `finalizeConfirmResult`

- Build e suites HITL verdes
  -> comprovado: `go build ./internal/agent/...` → OK; `go test ./internal/agent/...` → todos os pacotes passing

### Validações Executadas

```
go build ./internal/agent/...                                          → OK
go vet ./internal/agent/application/services/                         → OK
go test ./internal/agent/application/services/ -count=1               → ok (0.537s)
go test ./internal/agent/... -count=1                                  → todos ok
go test .../workflow/ -run Parity -count=1                             → PASS (12 cenários)
go test .../services/ -run 'HITL' -count=1                            → PASS
go test .../workflow/... -run 'Destructive|HITL' -count=1             → PASS
```

### DoD Checklist

1. [x] Ramo de bypass sem confirmação removido (Task 1.0; mantido em 4.1)
2. [x] Caminho único de execução para cada operação destrutiva/sensível (4.2)
3. [x] `dispatchWriteDestructive`/`wireBudgetCommitGate` preservados como caminho canônico
4. [x] `ConfirmState` persistido antes da pergunta; resume antes do parse; sem run suspenso órfão (limpeza determinística em `finalizeConfirmResult`)
5. [x] `AwaitingApproval`/`OperationKind` tipos fechados; sem string solta. Build + suites HITL verdes

### Riscos Residuais

- Os campos `lastDeleter`, `lastEditor`, `cardDeleter` na struct `DailyLedgerAgent` são agora código morto (não usados pelo registry); os executors HITL usam os deps diretamente via `buildConfirmDefinition` em `module.go`. Limpeza dos campos fica como candidata para task futura de hygiene.
