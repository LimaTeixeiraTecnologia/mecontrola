# Generated: 2026-06-24T12:00:00Z

## Tarefa 3.0 — Consolidar WriteGuard única no kernel

### Status: done

### Arquivos Alterados

- `internal/agent/application/workflow/write_guard.go` — REMOVIDO (tipos `WriteGuard`, `GuardSteps`, `GuardDecision`, `SettleFunc`, `Apply`)
- `internal/agent/application/workflow/composite.go` — campo `guard *WriteGuard` removido do struct `composite`; `NewIntentWorkflow` não recebe mais o parâmetro `guard`; `Execute` simplificado: apenas chama `tool.Execute` sem branches de guard
- `internal/agent/application/services/agent_workflows.go` — `newWriteGuard()` removido; `buildRegistry` não cria nem injeta guard; todas as chamadas a `NewIntentWorkflow` passam somente `id` e `KindTool` bindings
- `internal/agent/application/workflow/workflow_test.go` — `TestWriteKindShortCircuitsAndSkipsTool` e `TestWriteKindProceedsAndSettlesExecuted` substituídos por `TestWriteKindRunsTool` (sem guard); `WriteGuardSuite` removida; `buildWorkflows` atualizado para nova assinatura; `TestConstructorRejectsEmptyAndNil` atualizado
- `internal/agent/application/workflow/concurrency_test.go` — `passthroughGuard()` removido; `sharedRegistry` atualizado para nova assinatura de `NewIntentWorkflow`
- `internal/agent/application/workflow/parity_test.go` — `runLegacy` removido (dependia de `WriteGuard`); `mustBuildIntentForKind` removido; todos os testes de parity adaptados para testar apenas o kernel (`runKernel`/`runKernelResume`)
- `internal/agent/application/services/agent_workflows_test.go` — `TestNewWriteGuardIsNotNil` removido
- `internal/agent/application/services/daily_ledger_replay_test.go` — `buildAgent` atualizado para incluir `KernelDeps` com engine em memória, `CategoryResolver` e `PersistFn`; `replayTestStore` adicionado como store em memória para os testes
- `internal/agent/application/services/intent_router_test.go` — `newMinimalKernel()` helper adicionado; `TestRouteWhatsApp_PolicyBlocked_WriteWithLowConfidence` atualizado para incluir kernel; `routerTestStore` adicionado como store em memória

### Subtarefas Executadas

- [x] 3.1 Removido `newWriteGuard()` de `agent_workflows.go` e arquivo `write_guard.go` deletado
- [x] 3.2 `composite.Execute` simplificado: apenas `tool.Execute(ctx, in)` sem ramo de guard
- [x] 3.3 `buildRegistry` / `NewIntentWorkflow` não recebem nem injetam guard
- [x] 3.4 `settle` (decision audit) continua sendo chamado via `audit_begin` do kernel — `AuditBeginResult.Settle` registrado via `OnSettle`/`SettleRegistry`
- [x] 3.5 `TestNewWriteGuardIsNotNil` removido; `parity_test.go` adaptado sem `runLegacy`

### Critérios de Aceite

- Gate 1 (WriteGuard legacy removido): comprovado — `grep` retorna vazio em produção
- Gate 2 (composite sem referência a guard): comprovado — `composite.go` sem `c.guard`, `GuardShortCircuit` ou `settle`
- Gate 3 (steps do kernel presentes): comprovado — `steps.NewAuthorize`/`NewReplay`/`NewPolicy`/`NewAuditBegin` encontrados em `transactions_write.go`
- Gate 4 (switch domínio não cresceu): comprovado — `daily_ledger_agent.go` tem 1 case `intent.Kind`
- Gate 5 (zero comentários): comprovado — nenhum comentário proibido em `tools/` e `workflow/`
- Testes: `go test ./internal/agent/...` — PASS (todos os pacotes)

### Comandos Executados

```
go build ./internal/agent/application/workflow/ -- OK
go build ./internal/agent/application/services/ -- OK
go build ./internal/agent/... -- OK
go test ./internal/agent/application/workflow/... ./internal/agent/application/services/... -- OK
go vet ./internal/agent/... -- OK
go test ./internal/agent/... -- OK (21 pacotes)
```

### Riscos Residuais

- Nenhum: todas as gates do critério de aceite passaram; build + suites verdes.

### Definition of Done

1. [x] Guard legacy (`newWriteGuard`, `WriteGuard`, ramo de guard no `composite`) não existe mais
2. [x] Cadeia authorize/replay/policy/audit existe apenas como steps do kernel
3. [x] `composite` ainda serve leituras sem guard
4. [x] Nenhum decision audit fica sem `settle`
5. [x] Build + suites verdes; `parity_test.go` adaptado e verde
