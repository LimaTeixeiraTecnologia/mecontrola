# Generated: 2026-06-24T00:00:00Z

## Tarefa 5.0 — Remover RenderSystemPrompt morto e consolidar DefaultRegistry na registry canônica

### Status: done

### Arquivos Alterados

- `internal/agent/application/tools/registry.go` — removidos `RenderSystemPrompt()`, `DefaultRegistry()`, constantes `toolSystemHeader` e `toolSystemFooter`; import `strings` mantido (usado em `NewRegistry`)
- `internal/agent/application/tools/registry_test.go` — removidos `TestRenderSystemPrompt`, `TestDefaultRegistry`, `TestSpecByIntentNotFound` (que dependiam de `DefaultRegistry`); adicionado `TestSpecByIntent` sem dependência de `DefaultRegistry`
- `internal/agent/application/services/intent_router.go` — reescrito `warnMissingToolBindings` para usar `buildToolBindingEntries` (17 bindings cobrindo todos os kinds não-conversacionais); eliminada dependência de `tools.DefaultRegistry()`; adicionados `toolBinding`, `buildToolBindingEntries`, `warnMissingToolBindingsKinds`
- `internal/agent/application/services/agent_workflows_test.go` — adicionado `TestWarnBindingsCoversAllNonConversationalKinds` (parity test de anti-reincidência)

### Subtarefas Executadas

- [x] 5.1 Removidos `RenderSystemPrompt` (linha 89), `toolSystemHeader` e `toolSystemFooter` (linhas 20-28) de `registry.go`; removidos `TestRenderSystemPrompt` de `registry_test.go`
- [x] 5.2 Opção (a) implementada: `DefaultRegistry` eliminado de `registry.go`; `warnMissingToolBindings` reescrito com `buildToolBindingEntries` cobrindo todos 17 kinds não-conversacionais; zero dependência de lista paralela parcial
- [x] 5.3 `TestDefaultRegistry` e `TestSpecByIntentNotFound` removidos de `registry_test.go`; substituídos por `TestSpecByIntent` sem `DefaultRegistry`
- [x] 5.4 Teste de paridade `TestWarnBindingsCoversAllNonConversationalKinds` adicionado em `agent_workflows_test.go`; falha automaticamente se um kind roteável não-conversacional não for coberto por `warnMissingToolBindings`

### Decisão Registrada

Opção (a) adotada: `DefaultRegistry()` eliminado. A registry canônica é `buildRegistry()`/`routableKinds()` em `agent_workflows.go`. `warnMissingToolBindings` deixa de depender de lista hardcoded paralela e passa a usar `buildToolBindingEntries`, que pode ser inspecionada pelo teste de paridade via `warnMissingToolBindingsKinds()`.

### Critérios de Aceite

- Nenhuma referência a `RenderSystemPrompt`
  -> comprovado: `grep -rn "RenderSystemPrompt" internal/ --include="*.go"` → vazio (OK)

- `DefaultRegistry` eliminado (opção a)
  -> comprovado: `grep -rn "DefaultRegistry" internal/ --include="*.go"` → vazio (OK)

- Constantes órfãs removidas
  -> comprovado: `grep -rn "toolSystemHeader\|toolSystemFooter" internal/agent/application/tools/ --include="*.go"` → vazio (OK)

- Zero comentários em Go de produção (R-ADAPTER-001.1)
  -> comprovado: gate de comentários executado em `internal/agent/application/tools/` e `internal/agent/application/services/` → OK

- `warnMissingToolBindings` não usa lista paralela parcial; nenhum spec cai em `tracked==false`
  -> comprovado: `buildToolBindingEntries` cobre 17 kinds; sem lógica de `tracked` condicional

- Teste de paridade presente e verde
  -> comprovado: `TestWarnBindingsCoversAllNonConversationalKinds` passa; falha se divergência reincidir

- Build + suites do agent verdes
  -> comprovado: `go build ./internal/agent/...` e `go test ./internal/agent/...` → todos os pacotes PASS

### Testes Executados

```
go test ./internal/agent/application/tools/... ./internal/agent/application/services/... -run "TestRegistrySuite|TestAgentWorkflowsSuite" -v
```

- TestRegistrySuite/TestNewRegistry (7 cenários) — PASS
- TestRegistrySuite/TestSpecByIntent — PASS
- TestAgentWorkflowsSuite/TestBuildRegistryCreates4Workflows — PASS
- TestAgentWorkflowsSuite/TestBuildRegistryResolvesAllRoutableKinds — PASS
- TestAgentWorkflowsSuite/TestDestructiveKindsNotInRoutableKinds — PASS
- TestAgentWorkflowsSuite/TestRoutableKindsContainsExpectedKinds — PASS
- TestAgentWorkflowsSuite/TestRoutableKindsHasNoDuplicates — PASS
- TestAgentWorkflowsSuite/TestWarnBindingsCoversAllNonConversationalKinds — PASS

```
go test ./internal/agent/... → todos os pacotes PASS
```

### Riscos Residuais

Nenhum crítico. `warnMissingToolBindingsKinds()` é exposta apenas para o teste de parity (package whitebox); não é API pública.

### Definition of Done (DoD)

1. [x] `RenderSystemPrompt` e constantes órfãs removidos de produção e teste
2. [x] Fonte única de roteamento = registry canônica; `DefaultRegistry` eliminado (opção a)
3. [x] Teste de paridade specs↔kinds presente e verde
4. [x] Build + suites verdes
