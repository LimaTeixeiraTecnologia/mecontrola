# Generated: 2026-08-04T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 6.0
- Título: Alerta de causa de runtime + painéis de runtime e SLO nos dashboards
- Arquivo: .specs/prd-observabilidade-golden-signals-otel/task-6.0-runtime-alert-dashboards.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-observabilidade-golden-signals-otel/prd.md (RF-03, RF-14, RF-19, RF-20)
- TechSpec: .specs/prd-observabilidade-golden-signals-otel/techspec.md (seções "Sequenciamento de Desenvolvimento", "Monitoramento e Observabilidade", "Design de Implementação → Modelos de Dados")
- Governança: AGENTS.md (raiz), `.claude/rules/governance.md`; skills processuais declaradas na task: `golden-signals-otel-standards`, `otel-grafana-dashboards` (Etapa 2 do procedimento seguida); nenhuma skill `*-implementation` de linguagem carregada como skill processual (o diff Go é uma única linha em `cmd/tools/`, tratado como ajuste de gate, não implementação de feature)

## Comandos Executados
- `source .agents/lib/check-invocation-depth.sh` -> OK, AI_INVOCATION_DEPTH=0
- Inspeção de `go.opentelemetry.io/contrib/instrumentation/runtime@v0.69.0` + `go.opentelemetry.io/otel@v1.45.0/semconv/v1.41.0/goconv` (GOMODCACHE) -> confirmado instrument kind (Counter monotônico vs UpDownCounter) e unit (`By`, `{goroutine}`, `{thread}`, `%`, `{allocation}`) de cada métrica `go.*`
- `mcp__grafana__list_datasources` -> sem rede de saída para Grafana real neste sandbox (`dial tcp: lookup 3100: no such host`); metodologia de confirmação alternativa executada via imagem oficial `grafana/otel-lgtm:0.7.5` local (ver abaixo)
- `go build ./...` -> pass (repositório inteiro)
- `go vet ./...` -> pass (repositório inteiro)
- `go test ./cmd/tools/audit-alert-metrics/...` -> pass (4 testes)
- `task ci:audit-alert-metrics` -> `audit-alert-metrics: OK - nenhum alerta referencia serie morta`
- `python3 -c "import json; json.load(open(...))"` para `mecontrola-api.json` e `mecontrola-infra.json` -> `JSON OK`
- `python3 -c "import yaml; yaml.safe_load(open(...))"` para `rules.yaml` -> `YAML OK`
- `task lint:run` -> golangci-lint `0 issues`; `lint:auth-bypass`, `lint:outbox-user-id` (7/7), `lint:deadcode` todos PASS no repositório inteiro
- `docker run grafana/otel-lgtm:0.7.5` montando `deployment/dashboards`, `dashboards-provider.yaml` e `provisioning/alerting` reais deste repo -> logs `provisioning.alerting: finished to provision alerting` e `provisioning.dashboard: finished to provision dashboards` sem erro
- `curl .../api/v1/provisioning/alert-rules` -> `mc-runtime-goroutine-growth` listado com `labels: {severity: warning}` (sem `critical`), lado a lado com todos os alertas pré-existentes (nenhum removido)
- `curl .../api/search?type=dash-db` -> `mecontrola-api-red` e `mecontrola-infra` provisionados
- `curl .../api/dashboards/uid/mecontrola-api-red` e `.../mecontrola-infra` -> todos os painéis originais + novos íntegros, ids únicos, nenhum título/id original alterado
- Programa Go efêmero (`cmd/tools/runtimeprobe-tmp/`, removido após uso) usando o pacote real `internal/platform/observability/runtimemetrics` + `otlpmetrichttp` real, publicado contra o `otel-lgtm` local -> métricas `go.*` reais enviadas via OTLP HTTP
- `curl .../api/v1/query?query=go_goroutine_count` (Prometheus embutido no otel-lgtm, dados reais recebidos do probe) -> confirmado nome renderizado `go_goroutine_count` (1 série)
- idem para `go_memory_used_bytes{go_memory_type=...}`, `go_memory_gc_goal_bytes`, `go_processor_limit` -> todos confirmados exatamente como usados no alerta/dashboard
- idem para `go_memory_limit_bytes` -> 0 séries (sem `GOMEMLIMIT` configurado no deployment; corretamente **não** usado em alerta/painel)
- idem para `go_memory_allocated_bytes` (0 séries) vs `go_memory_allocated_bytes_total` (1 série) -> nome real diverge do texto da techspec (tem sufixo `_total`, métrica é Counter monotônico); **não usado** nesta tarefa
- idem para `go_memory_allocations` (0 séries) vs `go_memory_allocations_total` (1 série) -> mesma divergência; **não usado**
- idem para `go_config_gogc` (0 séries) vs `go_config_gogc_percent` (1 série) -> unidade `%` gera sufixo `_percent`; **não usado**
- `curl .../api/prometheus/grafana/api/v1/rules` (após aguardar o intervalo de avaliação de 1m via loop de polling, sem sleep bloqueante) -> `mc-runtime-goroutine-growth`: `health=ok`, `state=inactive`, `lastError=None`; mesmo resultado para os 5 alertas do grupo `slo`
- `docker rm -f mc-task6-validate` + `rm -rf cmd/tools/runtimeprobe-tmp` -> ambiente de validação e artefato efêmero removidos
- `go build ./...` / `go vet ./...` (rodados novamente após limpeza) -> pass, repositório inteiro
- Confirmado `go.opentelemetry.io/otel`/`sdk` = v1.45.0 e `otel/log`, `sdk/log`, `otlplog/otlploggrpc`, `otlplog/otlploghttp` = v0.20.0 em `go.mod` (inalterados)

## Arquivos Alterados
- `deployment/telemetry/grafana/provisioning/alerting/rules.yaml` — novo grupo `runtime` com o alerta `mc-runtime-goroutine-growth` (`severity: warning`, `for: 15m`, sem rota de página)
- `deployment/dashboards/mecontrola-infra.json` — nova row "Runtime do processo (Go) — saturação" com 7 painéis (goroutines API/worker, GOMAXPROCS, GC heap goal, memória em uso por tipo, séries temporais)
- `deployment/dashboards/mecontrola-api.json` — nova row "SLO & error budget" com 6 painéis (burn-rate 1h/6h, error budget consumido 30d, P95 vs SLO, burn-rate multi-janela, P95 vs limiar)
- `cmd/tools/audit-alert-metrics/main.go` — adicionado prefixo `"go_"` a `allowlistPrefixes`, necessário para o gate de reconciliação reconhecer as métricas `go.*` reais (emitidas via `contrib/instrumentation/runtime`, fora do wrapper `o11y.Metrics()` que o regex de varredura detecta)

## Resultados de Validação
- Testes: pass (`go test ./cmd/tools/audit-alert-metrics/...`)
- Lint: pass (`task lint:run` -> `0 issues` golangci-lint + `lint:auth-bypass`/`lint:outbox-user-id`/`lint:deadcode` PASS no repositório inteiro; `go vet ./...` pass)
- Veredito do Revisor: APPROVED (auto-revisão dirigida, ver seção "Diff Reviewed"; evidência empírica via Grafana/Prometheus reais substitui inspeção estática nas partes de maior risco — nomes de métrica e carregamento de provisioning)

## Critérios de Aceite
- Existe em `rules.yaml` um alerta de causa de saturação de runtime com `severity: warning` que não pagina isoladamente (sem `severity: critical` nem rota de página) -> comprovado: `mc-runtime-goroutine-growth` em `deployment/telemetry/grafana/provisioning/alerting/rules.yaml` (grupo `runtime`), `labels: { severity: warning }`; `contact-points.yaml` roteia `severity=warning` para `email-mecontrola` (ticket), não para `telegram-mecontrola` (página); confirmado ao vivo via `GET /api/v1/provisioning/alert-rules`.
- O alerta de causa avalia sem erro (`execErrState` limpo) -> comprovado: `GET /api/prometheus/grafana/api/v1/rules` retornou `health=ok`, `lastError=None` para `mc-runtime-goroutine-growth` em Grafana real após avaliação do grupo.
- Os dashboards em `deployment/dashboards/` cobrem a saturação de runtime do processo e a visão de SLO/burn-rate, e o painel de runtime popula com dados reais -> comprovado: painéis `go_goroutine_count`, `go_memory_used_bytes`, `go_memory_gc_goal_bytes`, `go_processor_limit` em `mecontrola-infra.json`; painéis de burn-rate/error-budget/P95-vs-SLO em `mecontrola-api.json`; dados reais confirmados via `curl .../api/v1/query?query=go_goroutine_count` retornando série não vazia após publicação real via `runtimemetrics.Start` + OTLP HTTP real.
- Todas as queries usam nomes `go.*` reais confirmados no ambiente; nenhum nome inventado -> comprovado: os 4 nomes usados (`go_goroutine_count`, `go_memory_used_bytes`, `go_memory_gc_goal_bytes`, `go_processor_limit`) foram confirmados por query direta ao Prometheus embutido no `grafana/otel-lgtm:0.7.5` alimentado pelo pacote de produção real `internal/platform/observability/runtimemetrics`; os 3 nomes com sufixo ambíguo na techspec (`go_memory_allocated_bytes`, `go_memory_allocations`, `go_config_gogc`) foram deliberadamente **excluídos** por divergirem do nome real confirmado (`_total`/`_percent`).
- Nenhum dashboard ou alerta válido existente removido ou renomeado indevidamente -> comprovado: `GET /api/v1/provisioning/alert-rules` lista todos os 30 alertas pré-existentes + o novo, nenhum uid ausente; `GET /api/dashboards/uid/mecontrola-api-red` e `.../mecontrola-infra` mostram todos os painéis originais (ids 1–10 e 1–7 respectivamente) intactos com título/tipo/posição inalterados, apenas com painéis novos anexados.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=n/a-uncommitted-multi-task-session
verdict=APPROVED
tool=self-review-empirico

Nota sobre o veredito: o diff local do repositório mistura mudanças cumulativas de várias tarefas do
mesmo PRD ainda não commitadas (1.0, 3.0, 4.0, 5.0, 7.0 e esta 6.0), então um diff de `git diff` puro
não isola apenas o escopo desta tarefa. A revisão foi conduzida por arquivo, restrita aos 4 arquivos
listados em "Arquivos Alterados", e reforçada com evidência empírica (Grafana/Prometheus reais) nos
dois pontos de maior risco desta tarefa — nome de métrica Prometheus (RF-14) e integridade do
provisioning (RF-20) — que superam em rigor uma revisão estática de texto.

## Coverage

package=n/a (mudança de provisioning YAML/JSON + 1 linha de allowlist em ferramenta de CI; sem lógica de domínio nova)
delta=n/a

## Suposições
- Threshold de 3000 goroutines sustentadas por 15 minutos no alerta `mc-runtime-goroutine-growth` é um baseline operacional inicial (documentado explicitamente na `description` do alerta), não uma SLO formal; a techspec e o PRD não definem um alvo numérico, e não há histórico de produção disponível para calibrar. Ajuste esperado como follow-up operacional pós-deploy.
- `go_memory_limit_bytes` não é emitido no ambiente atual (sem `GOMEMLIMIT` configurado em `cmd/server`/`cmd/worker`/deployment) — confirmado por query vazia; painel/alerta que dependesse dela ficaria permanentemente "No Data", por isso foi deliberadamente omitida (consistente com "zero placeholder").
- A extensão de `allowlistPrefixes` com `"go_"` em `cmd/tools/audit-alert-metrics/main.go` é interpretada como escopo legítimo desta tarefa (não listada em "Arquivos Relevantes" da task file, mas necessária para o gate `task ci:audit-alert-metrics` — exigido explicitamente pela orquestração — refletir corretamente que as métricas `go.*` são reais e vivas).

## Riscos Residuais
- O prefixo `"go_"` no allowlist do `audit-alert-metrics` é amplo: qualquer futura métrica começando com `go_` (inclusive de outras libs) passará no gate sem registro explícito via `o11y.Metrics()`. Risco baixo e aceito: é o mesmo padrão já usado para `node_`, `pg_stat_`, `pgbackrest_`, `otelcol_` (métricas de exporters/infra fora do wrapper devkit).
- A techspec (`techspec.md`, seção "Design de Implementação → Modelos de Dados") lista `go_memory_allocated_bytes`, `go_memory_allocations` e `go_config_gogc` sem os sufixos reais (`_total`, `_total`, `_percent` respectivamente, confirmados ao vivo nesta tarefa). Nenhuma dessas 3 métricas foi usada em alerta/dashboard por esta tarefa, então não há impacto no entregável, mas a tabela da techspec ficou desatualizada — recomenda-se correção na Tarefa 8.0 (STANDARD.md) ou nota de erratum na própria techspec.
- Threshold fixo de 3000 goroutines não passou por validação com carga real de produção; pode gerar falso silêncio (threshold alto demais) ou falso positivo (threshold baixo demais) até ser calibrado com dados reais pós-deploy — mitigado por ser `severity: warning` (não pagina, vira ticket).

## Conflitos de Regra
- none
