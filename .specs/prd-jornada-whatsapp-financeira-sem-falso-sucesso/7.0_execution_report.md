# Generated: 2026-07-10T13:31:30Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 7.0
- Título: Migrations aditivas — resolve_path e backfill/CHECK de correlation_key
- Arquivo: .specs/prd-jornada-whatsapp-financeira-sem-falso-sucesso/task-7.0-migrations-resolve-path-correlation-check.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-jornada-whatsapp-financeira-sem-falso-sucesso/prd.md (RF-15, RF-21)
- TechSpec: .specs/prd-jornada-whatsapp-financeira-sem-falso-sucesso/techspec.md
- ADRs: adr-005-correlacao-wamid-e-run-update-observavel.md (item 4 + R1: backfill antes do CHECK); adr-006-identidade-canonica-resolve-path.md (item 2: coluna resolve_path aditiva, auth_events_reason_check intacta)
- Governança: agent-governance (base contract AGENTS.md), skill declarada `postgresql-production-standards` (migration aditiva segura, CHECK, backfill idempotente, lock_timeout, reversibilidade); R-ADAPTER-001.1 (zero comentários Go de produção — N/A: só `_test.go` e `.sql` tocados)
- Skills sync: task file `## Skills Necessárias` = `postgresql-production-standards`; coluna `Skills` em tasks.md = `postgresql-production-standards` → sincronizado

## Comandos Executados
- `bash .agents/lib/check-invocation-depth.sh` (subprocess) -> `export AI_INVOCATION_DEPTH=1` (dentro do limite max=2)
- `go vet -tags=integration ./migrations/` -> exit 0 (limpo)
- `docker info` -> docker OK (testcontainers disponível)
- `go test -race -count=1 -tags=integration ./migrations/ -run 'TestMigrationSuite/TestAuthEventsResolvePathAndCorrelationBackfill' -v` -> PASS (Postgres pgvector:pg16 via testcontainers, 4.075s)
- `go test -race -count=1 -tags=integration ./migrations/` (suíte completa) -> ok, todos os métodos PASS (6.194s) após ajuste de version head 7→8 e step counts
- `gofmt -l migrations/*.go` -> vazio (limpo)
- `go build ./migrations/...` -> build OK
- `./.tools/bin/golangci-lint run ./migrations/...` (CI-canônico, sem tag integration) -> 0 issues
- `./.tools/bin/golangci-lint run --build-tags=integration ./migrations/...` -> 4 issues PRÉ-EXISTENTES fora do meu diff (errcheck rows.Close L887/L1024; revive function-length e unused regclass em 000004) — nenhum introduzido por esta tarefa; fora do escopo do lint de CI (`_test.go` sob tag integration)
- subagent[reviewer — revisão do diff 7.0 com prd+techspec] -> APPROVED_WITH_REMARKS; 6 pontos críticos e 5 critérios confrontados end-to-end (live testcontainers); 1 finding [low] de drift de nome de constraint (não-defeito da entrega)

## Arquivos Alterados
- migrations/000008_auth_events_resolve_path.up.sql (NOVO) — coluna `resolve_path` + `auth_events_resolve_path_chk`; backfill idempotente de `platform_runs.correlation_key` ANTES de `platform_runs_correlation_len_chk`
- migrations/000008_auth_events_resolve_path.down.sql (NOVO) — DROP das 2 constraints + DROP COLUMN resolve_path; backfill não revertido
- migrations/migrations_integration_test.go — novo método `TestAuthEventsResolvePathAndCorrelationBackfill` + helpers `assertConstraintPresent`/`assertConstraintMissing`; version head 7→8 em 2 asserts de reconcile
- migrations/000003_unify_transactions_integration_test.go — version head 7→8 (2 asserts full-up); `Steps(-5)`→`Steps(-6)` para preservar landing v2
- migrations/000004_category_write_gate_schema_integration_test.go — version head 7→8 (2 asserts full-up); `Steps(-4)`→`Steps(-5)` para preservar landing v3

## Resultados de Validação
- Testes: pass (`go test -race -count=1 -tags=integration ./migrations/` suíte completa verde; método novo 2 passed live)
- Lint: pass (CI-canônico `golangci-lint run ./migrations/...` = 0 issues; findings sob tag integration são pré-existentes e fora do meu diff)
- Veredito do Revisor: APPROVED_WITH_REMARKS (sem remarks críticos — 1 finding [low] não-defeito)

## Critérios de Aceite
- `migrator.Up()` aplica limpo com runs legados `correlation_key = ''` (backfill antes do CHECK, sem abort) -> comprovado: teste semeia `platform_runs` vazio em v7, `migrator.Steps(1)` sobe para v8 sem erro; assert `s.Require().NoError(stepErr)` verde ao vivo.
- Após up: `auth_events.resolve_path` existe, `auth_events_resolve_path_chk` rejeita valor fora do enum, `auth_events_reason_check` presente e inalterada -> comprovado: `assertColumnPresent` + `assertConstraintPresent(auth_events_resolve_path_chk)` + insert `resolve_path='bogus'` rejeitado com `auth_events_resolve_path_chk`; `auth_events_reason_check` assertada presente E insert `kind=principal_established`+`reason=invalid_signature` rejeitado por ela.
- Após up: `platform_runs_correlation_len_chk` ativo; 0 linhas com `correlation_key = ''`; legados migrados para `'legacy:' || id` -> comprovado: `SELECT COUNT(*) WHERE correlation_key=''` = 0; run legado vazio = `legacy:<id>`; run preenchido `wamid.EXISTING` intocado; inserts `''` e string>256 rejeitados com `platform_runs_correlation_len_chk`.
- `down` reverte coluna e ambas as constraints; re-aplicação (down→up) idempotente -> comprovado: após `Steps(-1)`, `assertColumnMissing(resolve_path)` + `assertConstraintMissing` das 2 constraints + `auth_events_reason_check` ainda presente; `Steps(1)` reaplica sem erro e reassere presença. Backfill não revertido (`COUNT WHERE correlation_key=''` = 0 pós-down).
- Migration reentrante: reexecutar `up` não falha -> comprovado: `ADD COLUMN IF NOT EXISTS`, `DROP CONSTRAINT IF EXISTS` antes de cada `ADD CONSTRAINT`, `DROP ... IF EXISTS` no down; ciclo up→down→up verde ao vivo; suíte completa (que reaplica baseline idempotente) verde.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando de teste correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão (CI-canônico 0 issues; vet 0; build OK; gofmt limpo).
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=a63343629cf2
verdict=APPROVED_WITH_REMARKS
tool=reviewer

## Coverage

package=github.com/LimaTeixeiraTecnologia/mecontrola/migrations
delta=n/a (migrations é pacote de SQL embarcado; cobertura medida por testes de integração comportamentais sob tag `integration`, não por cobertura de linha do pacote)

## Suposições
- Nomeação da constraint `auth_events_resolve_path_chk` (sufixo `_chk`): seguida a task file (autoritativa, 6 menções) e a convenção real do repositório (`platform_runs_resource_len_chk`, `platform_runs_thread_len_chk`), em vez do `_check` narrativo do ADR-006. Sem acoplamento por nome na task 6.0 (persiste via coluna).

## Riscos Residuais
- [low] Drift de nome de constraint entre ADR-006/task-6.0 (`auth_events_resolve_path_check`) e a implementação/task-7.0 (`auth_events_resolve_path_chk`). Não é defeito desta entrega — a task-7.0 pina `_chk` e a task-6.0 persiste via coluna (não consulta a constraint por nome). Mitigação futura: quem implementar a task-6.0 não deve hardcodar `_check` em assert/query; opcionalmente alinhar a narrativa do ADR-006/task-6.0 para `_chk`.
- `ADD CONSTRAINT ... CHECK` validado sobre `platform_runs` faz varredura completa, mas com `lock_timeout=5s`/`statement_timeout=120s` e volume legado trivial (4 runs em produção) é seguro. `ADD COLUMN` nullable é operação de metadados.
- Verificado que nenhum caminho de runtime em `internal/platform/agent`/`internal/agents` insere `correlation_key = ''`, então o novo CHECK não regride escrita corrente.

## Conflitos de Regra
- none
