# Generated: 2026-07-10T14:05:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.0
- Título: Scorer de persistência per-run e diferenciação operacional
- Arquivo: .specs/prd-jornada-whatsapp-financeira-sem-falso-sucesso/task-4.0-scorer-persistencia-per-run.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-jornada-whatsapp-financeira-sem-falso-sucesso/prd.md (RF-17, RF-18)
- TechSpec: .specs/prd-jornada-whatsapp-financeira-sem-falso-sucesso/techspec.md
- ADRs: adr-004-scorer-persistencia-per-run.md (Decisões 3, 4, 5, 6; R3/R4/R5/R6)
- Governança: agent-governance, go-implementation (inferida do diff .go); skills processuais declaradas: mastra, design-patterns-mandatory (sync file↔table OK)
- Dependência 3.0 (done): `scorer.ToolCallRecord.Outcome` já existe e é propagado por `ScoringHooks.AfterTool` (extractOutcome de resultBytes + marcador usecaseError em tools com erro Go). Esta tarefa CONSOME o campo.

## Comandos Executados
- `go build ./internal/agents/application/scorers/... ./internal/agents/application/postdeploy/... ./internal/agents/infrastructure/persistence/postdeploy/...` -> exit 0
- `go build ./internal/agents/...` (produção) -> exit 0
- `go vet ./internal/agents/application/scorers/... ./internal/agents/application/postdeploy/... ./internal/agents/infrastructure/persistence/postdeploy/...` -> exit 0
- `go vet -tags integration ./internal/agents/infrastructure/persistence/postdeploy/...` -> exit 0
- `go test -race -count=1 ./internal/agents/application/scorers/... ./internal/agents/application/postdeploy/...` -> TEST_EXIT=0 (todos PASS)
- `./.tools/bin/golangci-lint run ./internal/agents/application/scorers/... ./internal/agents/application/postdeploy/... ./internal/agents/infrastructure/persistence/postdeploy/...` (v2 pinado) -> 0 issues
- `gofmt -w` nos arquivos alterados; `gofmt -l` -> vazio (formatado)
- GATE layering: `grep internal/platform/agent internal/platform/scorer/` -> vazio (kernel scorer não importa agent, R3)
- GATE state-as-type: `grep Outcome internal/platform/scorer/scorer.go` -> `Outcome string` (tipo string no kernel; semântica fechada em scorers via agent.ToolOutcome.String())
- GATE zero comentários (R-ADAPTER-001.1) nos 6 arquivos de produção alterados -> vazio
- GATE 4.4: `anyFinancialToolScorer` (tool-call-accuracy) sem referência a Outcome -> baseline 0,304 intacta
- subagent[reviewer — review escopo task 4.0] -> APPROVED (0 achados)

## Arquivos Alterados
- internal/agents/application/scorers/write_persistence_accuracy.go (NOVO — writePersistenceAccuracyScorer, ScorerKindCodeBased, política falha-segura por outcome; sem LLM, sem IO)
- internal/agents/application/scorers/write_persistence_accuracy_test.go (NOVO — table-driven RunSample→ScoreResult por outcome)
- internal/agents/application/scorers/behavioral_scorers.go (no_hallucination endurecido: marcador exige write-tool com Outcome ∈ {routed, reconciled})
- internal/agents/application/scorers/mecontrola_scorers.go (registro de write_persistence_accuracy em BuildMeControlaScorers com AlwaysSample())
- internal/agents/application/scorers/behavioral_scorers_test.go (novos cenários de endurecimento: marcador+replay/clarify/usecaseError⇒0, marcador+routed/reconciled⇒1)
- internal/agents/application/scorers/mecontrola_scorers_test.go (contagem de scorers 11→12)
- internal/agents/application/postdeploy/gate.go (constante ScorerIDWritePersistenceAccuracy; campo WritePersistenceViolations em OperationalCounters; incorporado a NoRegressionOperational)
- internal/agents/application/postdeploy/reader.go (método WritePersistenceViolations no contrato AggregateReader; wiring em ComputeGate)
- internal/agents/infrastructure/persistence/postdeploy/aggregate_reader.go (helper scorerViolations parametrizado por scorer_id reusando a MESMA query; DuplicateWriteViolations e WritePersistenceViolations delegam — sem query estrutural nova)

## Resultados de Validação
- Testes: pass (`go test -race -count=1 ./internal/agents/application/scorers/... ./internal/agents/application/postdeploy/...` -> TEST_EXIT=0)
- Lint: pass (golangci-lint v2 pinado -> 0 issues)
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- Scorer reprova (0) o primeiro "Sim" que invoca write-tool mas retorna usecaseError ou vazio-com-marcador -> comprovado: cenários "usecaseError reprova com 0" e "vazio-com-marcador (write-tool sem outcome) reprova com 0" em write_persistence_accuracy_test.go, ambos PASS.
- replay e clarify são neutros (fora do denominador) -> comprovado: cenários "clarify é neutro ... pontua 1" e "replay é neutro ... pontua 1" PASS; código pula `mecontrolaNeutralWriteOutcomes` antes de `denominator++`.
- Run sem write-tool ⇒ 1.0 (leitura não penalizada) -> comprovado: cenários "sem write-tool (apenas leitura) ... pontua 1" e "sample sem tool-calls ... pontua 1" PASS.
- routed/reconciled ⇒ conta como efeito legítimo (score 1 quando único) -> comprovado: cenários "routed conta como efeito legítimo e pontua 1" e "reconciled ... pontua 1" PASS.
- Score misto (write sem efeito + write legítima) ⇒ 0 (falha-segura) -> comprovado: cenário "misto: write legítima e write sem efeito reprova com 0" PASS.
- no_hallucination endurecido: marcador+replay/clarify/usecaseError ⇒ 0; marcador+routed/reconciled ⇒ 1 -> comprovado: 5 novos cenários em TestNoHallucinationScorer (behavioral_scorers.go usa `mecontrolaEffectiveWriteOutcomes`), todos PASS.
- tool-call-accuracy mantém score do golden pré-mudança (baseline 0,304) -> comprovado: `anyFinancialToolScorer` intacto (grep sem Outcome); TestFinancialToolCallAccuracyScorer + TestFinancialToolCallAccuracyScorer_Covers22Tools inalterados PASS.
- RF-18: RunAggregate por outcome e violações disponíveis para scorer_id=write_persistence_accuracy -> comprovado: RunAggregate já filtra por outcome (aggregate_reader.go); WritePersistenceViolations reusa a MESMA query via scorerViolations(scorer_id); wired em ComputeGate/NoRegressionOperational; build+vet integration exit 0.
- Gates Go verdes; gate de import (scorer não importa agent) verde; zero comentários em Go -> comprovado: build/vet/test-race/lint exit 0; grep layering vazio; grep zero-comentários vazio.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree
verdict=APPROVED
tool=claude

## Coverage

package=internal/agents/application/scorers, internal/agents/application/postdeploy
delta=+scorer write_persistence_accuracy (11 cenários) +5 cenários no_hallucination +contagem registry

## Suposições
- RF-18 exige capacidade de agregação por scorer_id disponível; wiring em ComputeGate/NoRegressionOperational torna a capacidade viva (bloqueia promoção em violação) sem query estrutural nova — coerente com ADR-004 Decisão 6.
- Assertion de presença por ID em BuildMeControlaScorers foi mantida como contagem (11→12) porque ScorerEntry.scorer é campo não exportado do kernel; a presença de write_persistence_accuracy é provada pelo teste dedicado (TestKindAndID) e pela contagem.

## Riscos Residuais
- Caso golden "primeiro Sim com ledger vazio" reprova e população de platform_scorer_results com kind=code_based são validados na tarefa 8.0 (golden/real-LLM), fora do escopo unitário desta tarefa.
- RF-18 validado end-to-end via testes de integração Postgres (aggregate_reader/compute_gate) na tarefa 8.0; aqui coberto por build/vet integration + unit da política.

## Conflitos de Regra
- none
