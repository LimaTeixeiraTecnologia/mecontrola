# Generated: 2026-06-08T12:35:00Z

# Relatório de Execução — Tarefa 3.0

## Tarefa
Contrato E2, carrier sck e consumers de pagamento

## Status
done

## Subtarefas Executadas

- [x] 3.1 Alterar extração de funnel token em billing para priorizar `tracking.sck`
- [x] 3.2 Enriquecer payload de subscription activated com dados exigidos
- [x] 3.3 Criar publicação de `billing.subscription.activated_without_token`
- [x] 3.4 Implementar `SubscriptionPaidConsumer` e `PaidWithoutTokenConsumer` em onboarding
- [x] 3.5 Política de retry/degradação — idempotência via `WHERE status='PENDING'` existente
- [x] 3.6 PII masking: consumers não logam mobile/email em claro; use cases usam maskMobile/maskEmail

## Arquivos Alterados

### Billing (E2 — alterações mínimas, compatibilidade mantida)
- `internal/billing/application/usecases/funnel_token.go` — adicionado campo `SCK`, prioridade: sck > s1 > src
- `internal/billing/application/usecases/funnel_token_test.go` — casos de teste para sck, fallback s1, fallback src, ausência
- `internal/billing/application/usecases/process_kiwify_webhook.go` — adicionado `customerData` struct, `sck` em `trackingData`, customer data em dispatch
- `internal/billing/application/usecases/process_sale_approved.go` — novo path `executeWithoutToken` quando funnel_token vazio
- `internal/billing/application/usecases/process_sale_approved_test.go` — test `TestFunnelTokenVazioPublicaWithoutToken` + assinaturas atualizadas
- `internal/billing/application/dtos/input/process_sale_approved_input.go` — campos `CustomerMobileE164`, `CustomerEmail`
- `internal/billing/application/interfaces/subscription_event_publisher.go` — `PublishActivated` enriquecido + `PublishActivatedWithoutToken` novo
- `internal/billing/application/usecases/mocks/subscription_event_publisher.go` — mock atualizado com nova assinatura
- `internal/billing/application/interfaces/kiwify_client.go` — campo `CustomerMobileE164` em `KiwifySale`
- `internal/billing/application/usecases/reconcile_subscriptions.go` — passa `CustomerEmail` e `CustomerMobileE164`
- `internal/billing/application/usecases/reconcile_subscriptions_test.go` — mock expectation atualizada
- `internal/billing/infrastructure/messaging/database/producers/events.go` — `SubscriptionActivatedPayload` enriquecido; novo `SubscriptionActivatedWithoutTokenPayload`
- `internal/billing/infrastructure/messaging/database/producers/subscription_event_publisher.go` — `PublishActivated` enriquecido + `PublishActivatedWithoutToken`
- `internal/billing/infrastructure/messaging/database/producers/subscription_event_publisher_test.go` — testes para novos métodos
- `internal/billing/infrastructure/http/client/kiwify/client.go` — `mapSale` prioriza `sck`, mapeia `CustomerMobileE164`

### Onboarding (E3 — novos consumers)
- `internal/onboarding/infrastructure/messaging/database/consumers/subscription_paid_consumer.go` — novo
- `internal/onboarding/infrastructure/messaging/database/consumers/subscription_paid_without_token_consumer.go` — novo
- `internal/onboarding/infrastructure/messaging/database/consumers/subscription_paid_consumer_test.go` — novo
- `internal/onboarding/infrastructure/messaging/database/consumers/subscription_paid_without_token_consumer_test.go` — novo

## Critérios de Aceite

- Eventos de billing carregam os campos necessários para `MarkTokenPaid`
  -> comprovado: `SubscriptionActivatedPayload` contém `subscription_id`, `funnel_token`, `plan_code`, `external_sale_id`, `customer_mobile_e164`, `customer_email`, `paid_at`, `period_end`

- Pagamento aprovado sem token nunca é descartado silenciosamente
  -> comprovado: `executeWithoutToken` chama `PublishActivatedWithoutToken`; `PaidWithoutTokenConsumer` registra suporte signal via `HandlePaidWithoutToken`

- Consumer repetido para o mesmo token não gera transição indevida
  -> comprovado: `MarkTokenPaid` usa `WHERE status='PENDING'` — no-op se já PAID; teste `TestHandleIdempotentRetry` cobre isso

- Logs não expõem email ou telefone em claro
  -> comprovado: consumers logam apenas `event_id` e `external_sale_id`; use case `HandlePaidWithoutToken` usa `maskMobile`/`maskEmail`

- Testes cobrem `sck`, fallback legado e ausência de token
  -> comprovado: `funnel_token_test.go` tem casos: sck prioritário, sck vazio -> s1, sck+s1 vazios -> src, todos vazios

## Comandos Executados

```
go test -race -count=1 ./internal/billing/...
# resultado: PASS em todos os pacotes de billing

go test -race -count=1 ./internal/onboarding/infrastructure/messaging/database/consumers/...
# resultado: ok github.com/LimaTeixeiraTecnologia/mecontrola/internal/onboarding/infrastructure/messaging/database/consumers

go build ./...
# resultado: sem erros de compilação
```

## Testes: pass

- `go test -race -count=1 ./internal/billing/application/usecases/...` — ok
- `go test -race -count=1 ./internal/billing/infrastructure/messaging/database/producers/...` — ok
- `go test -race -count=1 ./internal/billing/...` — ok (todos os pacotes de billing)
- `go test -race -count=1 ./internal/onboarding/infrastructure/messaging/database/consumers/...` — ok

## Observação sobre Regressão Pre-existente

O teste `TestExpireTokens/TestBatchLoopContinuesUntilEmpty` em `internal/onboarding/application/usecases/expire_tokens_test.go` falha com contagem de batch loop incorreta. Este teste faz parte da tarefa 1.0 (já marcada como `done`) e está fora do escopo da tarefa 3.0. Nenhum arquivo desta tarefa toca `expire_tokens.go` ou seu teste.

## Riscos Residuais

- Nenhum risco crítico identificado
- A tabela `billing.subscriptions` não recebeu as colunas `customer_mobile_e164`, `customer_email`, `external_sale_id` (migration 0012) — elas são necessárias para wiring E2→DB mas estão fora do escopo desta tarefa (tarefa 8.0 ou 1.0 devem cobrir)
