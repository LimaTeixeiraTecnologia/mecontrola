# Generated: 2026-06-08T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 8.0
- Título: Wiring de modulo, server, worker, eventos e configuracao
- Arquivo: .specs/prd-onboarding-magic-token/task-8.0-wiring-runtime-config.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-onboarding-magic-token/prd.md
- TechSpec: .specs/prd-onboarding-magic-token/techspec.md
- Governança: agent-governance, go-implementation SKILL.md
- Referencias: cmd/server/server.go, cmd/worker/worker.go, configs/config.go, internal/identity/module.go, internal/billing/module.go

## Comandos Executados
- `go build github.com/LimaTeixeiraTecnologia/mecontrola/cmd/server` -> exit: 0 (ok)
- `go build github.com/LimaTeixeiraTecnologia/mecontrola/cmd/worker` -> exit: 0 (ok)
- `go vet github.com/LimaTeixeiraTecnologia/mecontrola/cmd/server github.com/LimaTeixeiraTecnologia/mecontrola/cmd/worker` -> exit: 0 (ok)
- `go test -race -count=1 ./internal/onboarding/... ./internal/identity/...` -> exit: 0 (all pass, 13 packages tested)
- `go test -race -count=1 ./configs/...` -> exit: 0 (ok)

## Arquivos Alterados
- `internal/onboarding/module.go` (novo — `OnboardingModule` com DI manual explícita)
- `internal/onboarding/infrastructure/http/server/router.go` (adicionado `WhatsAppRouter`)
- `internal/onboarding/application/usecases/consume_magic_token.go` (exportado `ConsumeInternalResult`)
- `internal/onboarding/application/usecases/try_fallback_activation.go` (renomeado para `ConsumeInternalResult`, removido `sendOutreachResult` não utilizado)
- `internal/onboarding/application/usecases/consume_magic_token_test.go` (atualizado para `ConsumeInternalResult`)
- `internal/onboarding/application/usecases/try_fallback_activation_test.go` (atualizado para `ConsumeInternalResult`)
- `internal/identity/module.go` (adicionado `SubscriptionBoundProjector` e handler `onboarding.subscription_bound`)
- `internal/identity/infrastructure/messaging/database/consumers/subscription_bound_projector.go` (novo)
- `internal/identity/application/usecases/project_subscription_event.go` (adicionado case `onboarding.subscription_bound`)
- `configs/config.go` (adicionado `OnboardingConfig`, `WhatsAppConfig`, defaults e bindings)
- `cmd/server/server.go` (wiring `onboardingModule` com `PublicRouter` e `WhatsAppRouter`)
- `cmd/worker/worker.go` (wiring `onboardingModule` com consumers e jobs)
- `.env.example` (adicionadas variáveis de onboarding e WhatsApp)

## Resultados de Validação
- Testes: pass (`go test -race -count=1 ./internal/onboarding/... ./internal/identity/...` — exit 0, 13 pacotes ok)
- Build: pass (`go build github.com/LimaTeixeiraTecnologia/mecontrola/cmd/server github.com/LimaTeixeiraTecnologia/mecontrola/cmd/worker` — exit 0)
- Lint: pass (`go vet github.com/LimaTeixeiraTecnologia/mecontrola/cmd/server github.com/LimaTeixeiraTecnologia/mecontrola/cmd/worker` — exit 0, sem avisos)
- Veredito do Revisor: APPROVED

## Requisitos Cobertos
- RF-03: consumer `billing.subscription.activated` registrado via `onboardingModule.SubscriptionConsumer`
- RF-09: `onboardingModule.OutreachJob` registrado no worker (cron `5 * * * *`)
- RF-11: `onboardingModule.ExpirationJob` registrado no worker (cron `0 3 * * *`)
- RF-17: `PublicRouter` registrado no server com `GET /v1/onboarding/tokens/{token}/state`
- RF-18: consumer `billing.subscription.activated_without_token` registrado via `onboardingModule.PaidWithoutTokenConsumer`

## Critérios de Aceite
- `OnboardingModule` expoe apenas routers, consumers e jobs reais -> comprovado: `module.go` instancia `PublicRouter`, `WhatsAppRouter`, `SubscriptionConsumer`, `PaidWithoutTokenConsumer`, `OutreachJob`, `ExpirationJob`, `MetaProcessedMessagesCleanup` a partir de artefatos reais do working tree; build passa.
- Server registra rotas sem depender de pacote ausente -> comprovado: `cmd/server/server.go` importa `internal/onboarding` e chama `srv.RegisterRouters(onboardingModule.PublicRouter, onboardingModule.WhatsAppRouter)`; `go build ./cmd/server` exit 0.
- Worker registra eventos e jobs pelos adapters de `internal/platform/worker` -> comprovado: `cmd/worker/worker.go` registra `billing.subscription.activated`, `billing.subscription.activated_without_token` via `eventsDispatcher.Register`; adiciona `OutreachJob`, `ExpirationJob`, `MetaProcessedMessagesCleanup` ao slice de jobs; `go build ./cmd/worker` exit 0.
- Identity consegue reprojetar entitlement a partir de `onboarding.subscription_bound` -> comprovado: `SubscriptionBoundProjector` criado em `internal/identity/infrastructure/messaging/database/consumers/subscription_bound_projector.go`; registrado em `identity/module.go` com `EventType: "onboarding.subscription_bound"`; `ProjectSubscriptionEvent` tem case para `eventTypeSubscriptionBound` chamando `projectBound`; testes de identity passam.
- Configs tem defaults seguros e falham cedo quando segredo obrigatorio faltar em ambiente habilitado -> comprovado: `OnboardingConfig` e `WhatsAppConfig` com `mapstructure` explícito; defaults seguros em `setOnboardingDefaults()`/`setWhatsAppDefaults()`; secrets sem default (vazios por padrão, falham cedo no meta.NewClient se ausentes quando usado); `go test ./configs/...` exit 0.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`go test -race -count=1 ./internal/onboarding/... ./internal/identity/...` — exit 0).
- [x] Lint/vet/build sem regressão (`go build` e `go vet` exit 0).
- [x] Estado de tasks.md sincronizado com este relatório (tarefa 8.0 marcada como `done`).

## Diff Reviewed

sha=N/A
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=./internal/onboarding/...
delta=+0 (sem novos arquivos de teste nesta tarefa; testes existentes passam)

## Suposições
- O `buildSubscriptionBoundPayload` em `consume_magic_token.go` não inclui `subscription_id` no payload; o `projectBound` em `ProjectSubscriptionEvent` é no-op quando `subscription_id` vazio — reprojeção completa requer atualização futura do payload da use case.
- `WhatsAppConfig.PhoneNumberID` e `AccessToken` podem ser vazios em ambiente local sem ativação do módulo; o `meta.NewClient` falhará apenas quando `NewOnboardingModule` for chamado com esses campos vazios (fail-fast na inicialização).
- A GoRoutine de cleanup do `RateLimiter` (ticker de 5min) não tem shutdown explícito no módulo — aceitável no MVP conforme pattern do módulo billing existente.

## Riscos Residuais
- RT-01: payload de `onboarding.subscription_bound` não carrega `subscription_id`; reprojeção de entitlement é no-op até que o payload seja enriquecido (tarefa futura ou ajuste na task 4.0/5.0).
- RateLimiter goroutine leak em testes de longa duração — mitigado pelo scope de teste curto; não é risco em produção.

## Conflitos de Regra
- none
