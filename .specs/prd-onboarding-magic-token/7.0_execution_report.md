# Generated: 2026-06-08T09:30:00Z

# Relatório de Execução — Tarefa 7.0

## Tarefa
Job diário de expiração e sinais de subscription órfã (RF-11, RF-12, RF-13, RF-14)

## Status
**done**

## Arquivos Alterados

### Criados
- `internal/onboarding/application/usecases/cleanup_onboarding_tables.go`
- `internal/onboarding/application/usecases/expire_tokens_test.go`
- `internal/onboarding/application/interfaces/onboarding_cleanup_repository.go`
- `internal/onboarding/infrastructure/jobs/handlers/token_expiration_job.go`
- `internal/onboarding/infrastructure/jobs/handlers/token_expiration_job_test.go`
- `internal/onboarding/infrastructure/jobs/handlers/meta_processed_messages_cleanup_job.go`
- `internal/onboarding/infrastructure/repositories/postgres/onboarding_cleanup_repository.go`

### Modificados
- `internal/onboarding/application/usecases/expire_tokens.go` — adicionado counter `onboarding_orphan_expired_total`
- `internal/onboarding/application/interfaces/repository_factory.go` — adicionado `OnboardingCleanupRepository`
- `internal/onboarding/infrastructure/repositories/factory.go` — implementado `OnboardingCleanupRepository`

## Subtarefas Executadas

### 7.1 — ExpireTokens com batches e transições idempotentes
Já existia em `expire_tokens.go`. Confirmado: loop com batches de 1000, `BulkExpire` com `RETURNING`, idempotente por `WHERE status IN ('PENDING','PAID')`.

### 7.2 — Support signal para token pago expirado
Já existia em `emitOrphanSignal`. Confirmado: payload contém `external_sale_id`, `token_hash_prefix`, `expired_at`, `has_paid_state=true`.

### 7.3 — TokenExpirationJob via worker job adapter
Criado `token_expiration_job.go` com `Name()="onboarding-token-expiration"`, `Schedule()` configurável e `Run()` delegando ao use case. Padrão idêntico ao billing.

### 7.4 — Limpeza de meta_processed_messages e consumer_lookup_attempts
Criados:
- `onboarding_cleanup_repository.go` (postgres) com `DeleteMetaProcessedOlderThan` e `DeleteConsumerLookupAttemptsOlderThan`
- `cleanup_onboarding_tables.go` (use case) com loops canceláveis por `context.Context`
- `meta_processed_messages_cleanup_job.go` (job adapter)
- `OnboardingCleanupRepository` interface em `application/interfaces/`
- `RepositoryFactory` estendida com `OnboardingCleanupRepository`

### 7.5 — Métricas de expiração e signals
Adicionado counter `onboarding_orphan_expired_total` em `NewExpireTokens` via `o11y.Metrics().Counter(...)`. Incrementado com `uc.orphanExpired.Add(ctx, 1)` para cada token PAID expirado.

### 7.6 — Testes
- `expire_tokens_test.go`: 9 casos cobrindo PENDING expirado (sem signal), PAID expirado (com signal), CONSUMED ignorado pelo BulkExpire, no-op quando vazio, loop para quando < batchSize, sinal com erro não aborta job, BulkExpire error propaga, execuções repetidas idempotentes
- `token_expiration_job_test.go`: nome, schedule, delegação ao use case, propagação de erro

## Critérios de Aceite

- **Job diário não cancela assinatura nem altera token CONSUMED** → comprovado: `BulkExpire` query usa `WHERE status IN ('PENDING','PAID')` — CONSUMED não é incluído; subscription nunca é cancelada pelo use case.

- **Support signal contém external_sale_id, hash/prefixo do token, instante de expiração e indicação de assinatura ativa sem usuário vinculado** → comprovado: `emitOrphanSignal` serializa `{"token_hash_prefix": token.ID(), "external_sale_id": token.ExternalSaleID(), "expired_at": expiredAt, "has_paid_state": true}`.

- **Execuções repetidas não duplicam sinais indevidamente** → comprovado: `BulkExpire` é idempotente por SQL `WHERE status IN ('PENDING','PAID') AND expires_at < $1`; uma vez EXPIRED, não é processado novamente. Teste `TestRepeatExecutionIdempotent` valida isso.

- **Logs mascaram PII** → comprovado: `slog.WarnContext` usa apenas `token_id` (UUID) e `external_sale_id` — sem mobile ou email em texto claro.

## Comandos Executados

```
go build ./... → success (0 erros)
go vet ./internal/onboarding/... → success (0 warnings)
go test -race -count=1 ./internal/onboarding/... → all pass
```

## Checklist R0-R7

| Regra | Status | Evidência |
|-------|--------|-----------|
| R0 — sem init() | PASS | grep: sem ocorrências |
| R5.12 — sem panic | PASS | grep: sem ocorrências |
| R6 — context.Context em fronteiras IO | PASS | todos os métodos recebem ctx |
| R6.4 — sem var _ Interface = (*Type)(nil) | PASS | grep: sem ocorrências |
| R6.7 — sem Clock interface | PASS | time.Now().UTC() inline |
| R7.1 — any em vez de interface{} | PASS | map[string]any nos payloads JSON |
| R7.2 — log/slog | PASS | slog.WarnContext/InfoContext |
| R7.6 — errors.Join/fmt.Errorf("%w") | PASS | fmt.Errorf("onboarding: ...: %w", err) |

## Testes: pass

```
ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/onboarding/application/usecases
ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/onboarding/infrastructure/jobs/handlers
ok  (todos os demais pacotes onboarding também passam)
```

## Riscos Residuais
Nenhum risco crítico. A tabela `consumer_lookup_attempts` é limpa pelo `MetaProcessedMessagesCleanupJob` com a mesma janela de retenção que `meta_processed_messages`. O `CleanupOnboardingTables` recebe `retentionPeriod` via construtor, permitindo configuração independente por ambiente.
