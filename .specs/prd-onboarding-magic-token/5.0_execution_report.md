# Generated: 2026-06-08T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Fallback de ativacao por E.164 condicionado a outreach
- Arquivo: .specs/prd-onboarding-magic-token/task-5.0-fallback-e164-gated-outreach.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-onboarding-magic-token/prd.md
- TechSpec: .specs/prd-onboarding-magic-token/techspec.md (secoes 2, 5.2, 7.2, 8.5, 9.2, ADR-008)
- Governança: agent-governance (R0-R7), go-implementation SKILL.md

## Comandos Executados
- `go test -race -count=1 ./internal/onboarding/application/usecases/...` -> ok (1.541s)
- `go test -race -count=1 ./internal/onboarding/...` -> ok (todos os pacotes com test files passaram)
- `go build ./...` -> limpo
- `go vet ./internal/onboarding/...` -> limpo
- R0 grep (init()) -> PASS
- R5.12 grep (panic) -> PASS
- R7.1 grep (interface{}) -> PASS
- R6.4 grep (var _ interface) -> PASS
- R6.7 (no Clock abstraction, time.Now().UTC() inline) -> PASS

## Arquivos Alterados
- `internal/onboarding/application/usecases/try_fallback_activation.go` — adicionado campo `tokensConsumed observability.Counter`, inicializado no construtor com metrica `onboarding_tokens_consumed_total`, incrementado no caminho de sucesso com label `activation_path=fallback_e164`, adicionados logs mascarados para `no_match` e `outreach_required`
- `internal/onboarding/application/usecases/try_fallback_activation_test.go` — criado com 7 casos de teste cobrindo: token nao encontrado, sem outreach, com outreach (ativa), token expirado, falha na gateway de identity, erro de banco, publicacao de evento

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- Fallback so ativa token pago com outreach ja registrado -> comprovado: `TestWithOutreach_Activates` verifica `FallbackOutcomeActivated` e `tokenRepo.updated == true`; `TestNoOutreach_ReturnsOutreachRequired` verifica `FallbackOutcomeOutreachRequired` sem ativacao
- Sem outreach previo, nenhuma subscription e vinculada -> comprovado: `TestNoOutreach_ReturnsOutreachRequired` retorna `FallbackOutcomeOutreachRequired` e `updated == false`; handler envia `please_use_ativar_command` (cobert em whatsapp_inbound_handler_test.go)
- A ativacao fallback publica o mesmo evento `onboarding.subscription_bound` -> comprovado: `TestWithOutreach_PublishesEvent` verifica `publisher.published == 1`; use case chama `outbox.Publish` com type `onboarding.subscription_bound`
- `onboarding_tokens_consumed_total{activation_path="fallback_e164"}` e incrementada no caminho correto -> comprovado: `tokensConsumed.Add(ctx, 1, observability.String("activation_path", "fallback_e164"))` chamado apos sucesso do `uow.Do`; noop provider aceita a chamada sem erro

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=local
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=github.com/LimaTeixeiraTecnologia/mecontrola/internal/onboarding/application/usecases
delta=+7 test cases

## Suposições
- O `noop.NewProvider()` implementa `Metrics().Counter()` retornando um contador no-op valido, permitindo que o incremento de metrica seja verificado indiretamente via execucao sem erros
- A `FindPaidByMobileForFallback` retorna `domain.ErrTokenNotFound` quando `outreach_sent_at IS NULL` (a query SQL ja filtra por `outreach_sent_at IS NOT NULL`), portanto o controle `HasOutreach()` na linha 83 e uma defesa em profundidade e pode ser redundante em producao mas correto por design

## Riscos Residuais
- RF-16 (BR only) no fluxo fallback: a validacao de pais ocorre no handler via `WhatsApp Number` (E1), nao no use case diretamente. A query SQL `FindPaidByMobileForFallback` somente retorna tokens com `customer_mobile_e164` que ja passou pela normalizacao E1 no momento do pagamento. O `from` recebido via webhook Meta nao e re-validado pelo use case — mitigacao: handler pode adicionar validacao antes de chamar Execute se necessario, mas esta fora do escopo desta tarefa conforme task file

## Conflitos de Regra
- none
