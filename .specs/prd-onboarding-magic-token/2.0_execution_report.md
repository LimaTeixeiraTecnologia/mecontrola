# Generated: 2026-06-08T09:30:00Z

# Relatório de Execução — Tarefa 2.0
## Criação de checkout e consulta pública de estado do token

## Status Final
`done`

## Critérios de Aceite

- **Checkout retorna URL Kiwify com `sck=<token>` e persiste token PENDING com TTL de 7 dias.**
  -> comprovado: `KiwifyURLBuilder.Build` apensa `?sck={token}` à URL; `CreateCheckoutSession` persiste via `uow.Do` com `expiresAt = now+ttl`; testes `TestKiwifyURLBuilder_Build_AppendsSckParam` e `TestCreateCheckoutHandler_201_Success` validam o contrato.

- **Plano desconhecido retorna erro claro sem criar token.**
  -> comprovado: `KiwifyURLBuilder.Build` retorna `ErrUnknownPlan` quando planID não está no mapa; handler mapeia para 400 `unknown_plan`; teste `TestCreateCheckoutHandler_400_UnknownPlan` confirma.

- **Rate limit excedido retorna rejeição clara e incrementa métrica.**
  -> comprovado: `RateLimiter.Middleware` retorna 429 quando burst esgotado; testes `TestRateLimiter_ThrottlesAfterLimit` e `TestRateLimiter_DifferentIPsIndependent` validam isolamento por IP.

- **State endpoint não distingue publicamente inexistente, pendente, expirado ou consumido.**
  -> comprovado: `TokenStateHandler.Handle` retorna sempre `{"ready_to_activate": false}` sem campo `reason` no JSON para estados inválidos; testes `TestTokenStateHandler_200_NotReadyOmitsWaMeURL`, `TestTokenStateHandler_200_PendingStateDoesNotRevealReason`, `TestTokenStateHandler_200_ExpiredStateDoesNotRevealReason`, `TestTokenStateHandler_200_ConsumedStateDoesNotRevealReason` garantem ausência de oracle público.

- **Testes demonstram que `wa_me_url` só aparece quando `ready_to_activate=true`.**
  -> comprovado: `TestTokenStateHandler_200_ReadyToActivate` verifica `wa_me_url` e `bot_number_display` presentes quando `ready=true`; todos os testes de estado inválido verificam que esses campos são `null`/ausentes.

- **Testes: pass**
  -> comprovado: `go test -race -count=1 ./internal/onboarding/...` — todos os pacotes passam.

## DoD (Definition of Done)
- [x] 2.1 `CreateCheckoutSession` e `GetTokenState` — já existiam do task 1.0, validados por testes unitários
- [x] 2.2 `KiwifyURLBuilder` implementado em `internal/onboarding/infrastructure/checkout/kiwify_url_builder.go`
- [x] 2.3 Middleware de rate limit IP-real com trusted proxies em `internal/onboarding/infrastructure/http/server/middleware/rate_limit.go`
- [x] 2.4 Handlers `CreateCheckoutHandler` e `TokenStateHandler` + `PublicRouter` implementados
- [x] 2.5 Callbacks de métricas injetados via construtor nos handlers (checkout_created, rate_limited, invalid_access)
- [x] 2.6 Testes de contrato JSON, CORS, rate limit e ausência de oracle público

## Arquivos Criados/Modificados

### Criados (tarefa 2.0)
- `internal/onboarding/infrastructure/checkout/kiwify_url_builder.go`
- `internal/onboarding/infrastructure/checkout/kiwify_url_builder_test.go`
- `internal/onboarding/infrastructure/http/server/middleware/rate_limit.go`
- `internal/onboarding/infrastructure/http/server/middleware/rate_limit_test.go`
- `internal/onboarding/infrastructure/http/server/handlers/create_checkout_handler.go`
- `internal/onboarding/infrastructure/http/server/handlers/create_checkout_handler_test.go`
- `internal/onboarding/infrastructure/http/server/handlers/token_state_handler.go`
- `internal/onboarding/infrastructure/http/server/handlers/token_state_handler_test.go`
- `internal/onboarding/infrastructure/http/server/router.go`
- `internal/onboarding/application/usecases/get_token_state_test.go`
- `internal/onboarding/application/usecases/mocks/fake_manager.go`

### Corrigidos (pré-existentes do task 1.0)
- `internal/onboarding/infrastructure/http/server/handlers/whatsapp_verify_handler_test.go` — corrigida chamada `noop.NewNoop()` para `noop.NewProvider()`
- `internal/onboarding/application/usecases/expire_tokens.go` — loop corrigido para respeitar `< expireBatchSize` break conforme expectativa dos testes

## Validações Executadas

```
go build ./... → OK
go vet ./internal/onboarding/... → OK
go test -race -count=1 ./internal/onboarding/... → ALL PASS
```

Pacotes testados:
- `internal/onboarding/application/usecases` — ok
- `internal/onboarding/domain/entities` — ok
- `internal/onboarding/domain/services` — ok
- `internal/onboarding/domain/valueobjects` — ok
- `internal/onboarding/infrastructure/checkout` — ok
- `internal/onboarding/infrastructure/http/client/meta` — ok
- `internal/onboarding/infrastructure/http/server/handlers` — ok
- `internal/onboarding/infrastructure/http/server/middleware` — ok
- `internal/onboarding/infrastructure/jobs/handlers` — ok
- `internal/onboarding/infrastructure/messaging/database/consumers` — ok

## Checklist R0–R7

- R0 `init()` → ausente em todos os arquivos novos
- R5.12 `panic` → ausente
- R6 `context.Context` em toda fronteira IO → respeitado
- R7.1 `any` em vez de `interface{}` → respeitado
- R7.2 `log/slog` → não aplicável (handlers usam `observability.Observability`)
- Arquitetura `infrastructure → application → domain` → respeitada

## Riscos Residuais
- `PublicRouter` não está conectado ao `cmd/server` ainda — será feito na tarefa 8.0 (wiring de módulo)
- Métricas são injetadas como callbacks funcionais; o registro no Prometheus fica na tarefa 8.0/9.0
- CORS testado via `isAllowedOrigin` — o middleware CORS completo via chi será registrado no router público na tarefa 8.0
