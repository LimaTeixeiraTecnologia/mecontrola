# Generated: 2026-06-08T12:30:00Z

# Execution Report — Task 6.0: Cliente Meta Cloud API e job horario de outreach

## Estado

Estado: done

## Contexto Carregado

- `AGENTS.md` (governanca base)
- `.agents/skills/go-implementation/SKILL.md`
- `.specs/prd-onboarding-magic-token/task-6.0-meta-client-outreach-job.md`
- `.specs/prd-onboarding-magic-token/techspec.md` (secoes 2, 6.8, 7.1, 8.4, 9.2, ADR-005)
- `internal/platform/httpclient/client.go` (pattern de cliente HTTP)
- `internal/billing/infrastructure/http/client/kiwify/client.go` (pattern de referencia)

## Arquivos Criados/Modificados

### Novos
- `internal/onboarding/infrastructure/http/client/meta/errors.go`
- `internal/onboarding/infrastructure/http/client/meta/models.go`
- `internal/onboarding/infrastructure/http/client/meta/client.go`
- `internal/onboarding/infrastructure/http/client/meta/client_test.go`
- `internal/onboarding/infrastructure/gateway/whatsapp_gateway.go`
- `internal/onboarding/infrastructure/jobs/handlers/outreach_job.go`
- `internal/onboarding/infrastructure/jobs/handlers/outreach_job_test.go`
- `internal/onboarding/application/usecases/send_outreach_test.go`

### Modificados
- `internal/onboarding/application/errors.go` — adicionados `ErrWhatsAppClientError` e `ErrWhatsAppServerError`
- `internal/onboarding/application/usecases/send_outreach.go` — diferenciacao 4xx (sem reset) vs 5xx (reset)

## Comandos Executados

```
go build ./internal/onboarding/...   -> exit 0
go vet ./internal/onboarding/...     -> exit 0
go test -race -count=1 ./internal/onboarding/infrastructure/http/client/meta/...   -> ok (10 testes)
go test -race -count=1 ./internal/onboarding/infrastructure/jobs/handlers/...      -> ok (4 testes)
go test -race -count=1 ./internal/onboarding/application/usecases/                 -> ok (8 SendOutreach + expire + getstate)
```

## Criterios de Aceite

- [ ] Cliente Meta serializa payload de template conforme contrato oficial descrito na techspec
  -> comprovado: `TestClient_SendTemplate_Success` valida `messaging_product=whatsapp`, `type=template`, path `/{phone_number_id}/messages`, header Authorization Bearer, Content-Type application/json.

- [ ] Nenhum envio ocorre quando `OutreachEnabled=false`
  -> comprovado: `TestOutreachJob_Disabled_SkipsExecution` verifica que `gateway.called == false` quando `enabled=false`.

- [ ] Um token com erro 4xx nao e reenviado em ticks futuros
  -> comprovado: `TestSendOutreach/TestError4xx_NoReset` verifica `len(repo.resetCalls) == 0` quando gateway retorna `ErrWhatsAppClientError` (4xx). `outreach_sent_at` permanece marcado.

- [ ] Um token com erro 5xx pode ser retentado no proximo tick
  -> comprovado: `TestSendOutreach/TestError5xx_ResetsOutreach` verifica `len(repo.resetCalls) == 1` quando gateway retorna `ErrWhatsAppServerError` (5xx). `outreach_sent_at` e resetado para NULL.

- [ ] Telefone nunca aparece em claro em logs
  -> comprovado: `send_outreach.go` usa `maskMobile(token.CustomerMobileE164())` em todos os logs. Gateway recebe `toE164` sem mascarar (necessario para envio), mas logs usam versao mascarada.

## DoD

- [x] Subtarefa 6.1: `WhatsAppCloudClient` em `infrastructure/http/client/meta` — implementado com `SendTemplate` e `SendText`
- [x] Subtarefa 6.2: gateway de envio de template `activation_reminder` — `WhatsAppGateway` implementa `appinterfaces.WhatsAppGateway`
- [x] Subtarefa 6.3: `SendOutreach` com selecao, lock e politicas 4xx/5xx — use case atualizado com diferenciacao de erro
- [x] Subtarefa 6.4: handler de job via adapter de `internal/platform/worker/job` — `NewOutreachJob` retorna `*job.Adapter`
- [x] Subtarefa 6.5: metricas `onboarding_outreach_sent_total` e logs PII-safe — logs com `maskMobile`, `outreach.sent`, `outreach.failed_4xx`, `outreach.failed_5xx`
- [x] Subtarefa 6.6: testes sucesso, toggle off, erro 4xx, erro 5xx, idempotencia — todos passando
- [x] Testes: `go test -race -count=1` passando

## Validacao R0-R7

- R0 (sem init()): verificado — nenhum arquivo novo contem `func init()`
- R1 (metodos de struct): verificado — todas as funcoes sao metodos ou construtores
- R5.12 (sem panic): verificado — nenhum `panic(` nos arquivos de producao
- R6 (context.Context): verificado — todos os metodos de IO recebem `context.Context`
- R6.4 (sem var _ Interface): verificado — nao ha asercoes de interface em tempo de compilacao
- R7.1 (any em vez de interface{}): verificado — `components []any` usado em models.go
- Zero comentarios adicionados em arquivos Go: verificado

## Riscos Residuais

- A metrica `onboarding_outreach_sent_total` e emitida como log estruturado, nao como contador Prometheus; o registro de contadores Prometheus fica para tarefa 9.0 (Observabilidade).
- O `WhatsAppGateway` nao e registrado no modulo ainda; o wiring fica para tarefa 8.0.
- O erro generico (nao 4xx nem 5xx) no gateway causa reset de `outreach_sent_at` (conservador), o que pode causar retry para erros de rede transientes — comportamento correto pelo PRD.
