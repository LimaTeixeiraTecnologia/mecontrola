# Execution Report — Task 6.0

**Task:** Structured Output Strict=true + roteamento por classe + onboarding json_schema
**Date:** 2026-06-25
**Status:** done

## Subtasks Completed

| Subtask | Description | Status |
|---------|-------------|--------|
| 6.1 | `LLMClass` tipo fechado + `ClassRouter` + `AgentModelClassConfig` + 3 chains em `module.go` | done |
| 6.2 | `parse_inbound.go`: `Strict:true`; `ParseIntentJSONSchema` com `required` completo + `additionalProperties:false` | done |
| 6.3 | Onboarding migrado de tool-calling para json_schema por fase; `run_onboarding_turn.go` usa `decodeOnboardingStructured`; dispatcher recebe objeto estruturado | done |
| 6.4 | Envs por-classe (`AGENT_LLM_PARSE_*`, `AGENT_LLM_ONBOARDING_*`, `AGENT_LLM_CONV_*`) em `configs/config.go` + `.env.example` atualizado | done |
| 6.5 | Métrica `agent_llm_class_total{class,model,outcome}` via `classMetricInterpreter` wrapper no `ClassRouter` | done |
| 6.6 | Guard real-LLM `TestStructuredOutputGuard_ParseClass_StrictTrue` e `TestStructuredOutputGuard_OnboardingClass_StrictTrue` em `e2e/structured_output_guard_realllm_test.go` com `//go:build integration` + `RUN_REAL_LLM` guard | done |

## Files Created

- `internal/agent/application/services/class_router.go` — `LLMClass` (tipo fechado), `ClassRouter`, `ClassInterpreter`, `classMetricInterpreter`
- `internal/agent/application/usecases/onboarding_structured_schema.go` — schemas json_schema Strict=true por fase (objective, budget, cards, financial_plan, first_tx)
- `internal/agent/e2e/structured_output_guard_realllm_test.go` — guard tests per-class com `RUN_REAL_LLM`

## Files Modified

- `internal/agent/application/prompting/prompts.go` — `ParseIntentJSONSchema()`: `required` completo (24 campos) + `additionalProperties:false`
- `internal/agent/application/usecases/parse_inbound.go` — `Strict:true`; `rawIntentDTO` pointer fields → plain types; `resolveConfidence` corrigido (0=missing→1.0, negativo→clampa para 0); helpers `nilIfEmptyStr`/`nilIfZeroDay`
- `internal/agent/application/usecases/run_onboarding_turn.go` — migrado de `Tools`/`ToolChoice` para `JSONSchema`; `decodeOnboardingStructured`, `dispatchCardActions`, suporte a `action=clarify`
- `internal/agent/application/usecases/run_onboarding_turn_test.go` — testes atualizados para usar `RawJSON` em vez de `ToolCalls`
- `internal/agent/application/usecases/onboarding_tools_scripts_test.go` — `TestObjectiveProfileStoredInDraftAndPassedToSuggester` atualizado para `RawJSON`
- `internal/agent/module.go` — `llmRuntime.Router`; `newLLMRuntime` constrói 3 chains via `buildClassChain`; `ClassRouter` com `classMetricInterpreter` por classe
- `configs/config.go` — campos `ParsePrimaryModel`, `ParseFallbackModels`, `ParseMaxTokens`, `OnboardingPrimaryModel`, `OnboardingFallbackModels`, `ConvPrimaryModel`, `ConvFallbackModels`, `ConvMaxTokens`
- `.env.example` — vars por-classe documentadas; nota sobre strict=true quebrando haiku/gpt-5-nano

## Validation

```
go test ./internal/agent/...  → PASS (all 20 packages)
go build ./...                → OK
R-ADAPTER-001.1 gate (zero comments)  → OK
R-AGENT-WF-001 gate (no SQL in tools) → OK
R-AGENT-WF-001.7-A gate (closed types) → OK
Strict:true verificado em todos os schemas estruturados (parse + 5 fases onboarding + budget_conversation)
```

## Decisions

- `ClassInterpreter` definida localmente em `services` (não importa `usecases`) para evitar ciclo de importação
- `resolveConfidence`: `raw==0` → default 1.0; `raw<0` → clamp para 0 (nunca tratar negativo como missing)
- Cards fase onboarding: array `"cards":[...]` no schema; `dispatchCardActions` itera e despacha cada card
- `classMetricInterpreter` wraps each interpreter in `ClassRouter` — métrica emitida com `class`, `model` (Provider.String()), `outcome` (ok/error)
- `configure_budget_conversation.go` já usava `Strict:true` — sem modificação necessária (RF-10 satisfeito)

## Risks Residuais

- Guard real-LLM (6.6) requer `RUN_REAL_LLM=1` + `OPENROUTER_API_KEY` — skippado em CI unitário por design
- Onboarding com haiku (`anthropic/claude-haiku-4.5`) via `AGENT_ONBOARDING_LLM_MODEL` legado: campo mantido para compat mas comentário em `.env.example` indica que deve passar guard Strict=true antes de ser promovido
