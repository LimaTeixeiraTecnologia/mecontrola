# Execution Report — Task 9.0
## Operação diária via portas (recorrência, % categoria, consultas, casos especiais)

**Data:** 2026-06-25
**Status:** done
**PRD:** `.specs/prd-refatoracao-agent-canonico/prd.md`
**RFs cobertos:** RF-23, RF-26, RF-27, RF-28, RF-29, RF-30, RF-33, RF-34, RF-35

---

## Escopo

A task 9.0 é a última de 9 tarefas da refatoração canônica do `internal/agent`. Sua função era garantir que as operações diárias via portas (recorrência de orçamento, edição de % de categoria, consultas de income/category/goal, casos especiais) estivessem cobertas pelo padrão `WorkflowRegistry → Workflow → Tool → binding → usecase`.

---

## Análise pré-implementação

Após auditoria do código existente no início da sessão:

- **RF-30 (editar % categoria)** — `KindEditCategoryPercentage` já estava wired: tool, binding, wiring em `agent_workflows.go`, `intent_router.go` e `module.go` todos presentes.
- **RF-26, RF-27, RF-28 (consultas de income/category/goal)** — `KindQueryIncomeSummary`, `KindQueryCategory`, `KindQueryGoal` já implementados e cobertos pelos tools existentes.
- **RF-33 (casos especiais)** — tratados pelos mecanismos existentes: clarificação de categoria (ambiguous/confirmation), cartão não encontrado, operações HITL.
- **RF-34 (undo)** — system prompt já instrui o LLM a rotear "desfaz o que acabei de registrar" para `delete_last_transaction`; marcado como "fora do MVP" no PRD.
- **RF-23, RF-35 (WhatsApp único)** — canal único já implementado desde task 2.0.

**Único item genuinamente novo em task 9.0:** `KindBudgetRecurrence` (RF-29 — replicar planejamento de orçamento para meses futuros).

---

## Arquivos alterados

### Domínio — `internal/agent/domain/intent/`

**`intent.go`**
- Adicionado `KindBudgetRecurrence` ao iota após `KindQueryIncomeSummary`
- `String()`: entry `"budget_recurrence"`
- `IsWrite()`: inclui `KindBudgetRecurrence`
- `ParseKind()`: case `"budget_recurrence" → KindBudgetRecurrence`
- Erro vars: `ErrBudgetRecurrenceMonths`, `ErrBudgetRecurrenceCompetence`
- Campos em `Intent`: `months int`, `sourceCompetence string`
- Accessors: `Months() int`, `SourceCompetence() string`
- Struct `BudgetRecurrenceFields` + constructor `NewBudgetRecurrence()` com validação de competência (regex `^\d{4}-\d{2}$`) e meses (1..12)

### Parse — `internal/agent/application/usecases/parse_inbound.go`

- `rawIntentDTO`: adicionados `Months int` e `SourceCompetence string`
- `build()`: case `KindBudgetRecurrence` com default `months=1` quando zero

### Prompts — `internal/agent/application/prompting/`

**`prompts.go`**
- `ParseIntentJSONSchema()`: adicionado `"budget_recurrence"` no enum de kinds
- Propriedades `"months"` (integer, 1..12) e `"source_competence"` (string)
- Ambas em `required`

**`parse_intent.system.tmpl`**
- 21 → 22 intents; "WhatsApp e Telegram" → "WhatsApp"
- Intent 21 `budget_recurrence` com gatilhos: "repete o orçamento", "replica o planejamento"
- `unknown` renumerado para 22
- Exemplo JSON inclui `months` e `source_competence`

### Contratos de tools — `internal/agent/application/tools/contracts.go`

- `BudgetRecurrenceCreatorInput`, `BudgetRecurrenceCreatorResult`, `BudgetRecurrenceCreator` interface

### Formatação — `internal/agent/application/tools/formatting.go`

- `formatBudgetRecurrenceCreated()`: mensagem singular ("próximo mês") vs plural ("N meses")

### Tool — `internal/agent/application/tools/budget_tools.go`

- `BudgetRecurrenceCreatorTool` struct com `NewBudgetRecurrenceCreatorTool()`
- `Execute()`: nil creator → `OutcomeMissingResolver`; usecase error → `OutcomeUsecaseError`; sucesso → `OutcomeRouted` com reply formatada

### Binding — `internal/agent/infrastructure/binding/budget_recurrence.go` (NOVO)

- `createRecurrenceUseCase` interface local (fronteira de dados isolada)
- `BudgetRecurrenceCreatorAdapter` com `Execute()` mapeando `tools.BudgetRecurrenceCreatorInput → budgetsinput.CreateRecurrenceInput`
- `countCreated()`: conta entradas com status `Created`, `Updated` ou `CompletedFromDraft`

### Wiring — `internal/agent/application/services/agent_workflows.go`

- `budgetWorkflow`: adicionado `NewBudgetRecurrenceCreatorTool(...)` via seam existente
- `routableKinds()`: adicionado `intent.KindBudgetRecurrence`

### Deps — `internal/agent/application/services/intent_router.go`

- `IntentRouterDeps.BudgetRecurrenceCreator tools.BudgetRecurrenceCreator`
- `buildToolBindingEntries()`: entry `{name: "budget_recurrence", kind: KindBudgetRecurrence, present: deps.BudgetRecurrenceCreator != nil}`

### Module — `internal/agent/module.go`

- Wiring: `if w.budgetsModule != nil && w.budgetsModule.CreateRecurrenceUC != nil { deps.BudgetRecurrenceCreator = agentbinding.NewBudgetRecurrenceCreatorAdapter(...) }`

---

## Testes

| Arquivo | Casos | Resultado |
|---------|-------|-----------|
| `internal/agent/domain/intent/intent_new_kinds_test.go` | `TestNewBudgetRecurrence` (7 sub-casos: válido 3 meses, válido 1 mês, competência vazia, competência inválida, meses zero, meses > 12, round-trip string/parse) | PASS |
| `internal/agent/application/usecases/parse_inbound_test.go` | `budget_recurrence_with_source_and_months`, `budget_recurrence_months_zero_defaults_to_1` | PASS |
| `internal/agent/application/tools/budget_recurrence_tool_test.go` (NOVO) | 4 cenários: sucesso plural, sucesso singular, usecase error, nil creator | PASS |
| `internal/agent/infrastructure/binding/budget_recurrence_test.go` (NOVO) | `TestExecuteSuccess`, `TestExecutePropagatesError`, `TestCountCreatedSkipsConflictAndFailure` | PASS |
| `internal/agent/application/services/agent_workflows_test.go` | `TestRoutableKindsHasNoDuplicates` (20 kinds), `TestWarnBindingsCoversAllNonConversationalKinds` | PASS |
| `go test ./internal/agent/... -count=1 -short` | 24 pacotes | todos PASS |
| `go build ./...` | projeto completo | sem erros |

---

## Gates de governança

| Gate | Comando | Resultado |
|------|---------|-----------|
| R-ADAPTER-001.1: zero comentários | `grep ... "^[[:space:]]*//" internal/agent/application/tools/ internal/agent/application/workflow/` | OK |
| R-ADAPTER-001.2: SQL em adapters | `grep ... QueryContext\|ExecContext tools/ workflow/` | OK |
| R-AGENT-WF-001.1: switch não cresceu | `grep -cE "case intent\.Kind" daily_ledger_agent.go` → 0 | OK |
| `scripts/ci/agent-data-boundary.sh` | todos 7/7 checks | GATE VERDE |

---

## Riscos residuais

- Nenhum. Todos os gates estão verdes e os testes passam sem falso positivo.
- RF-34 (undo) permanece "fora do MVP" conforme declarado no PRD; o system prompt já instrui o LLM a rotear para `delete_last_transaction` existente.

---

## Conclusão

Task 9.0 concluída. `KindBudgetRecurrence` implementado via padrão canônico `WorkflowRegistry → Workflow → Tool → binding → usecase`, sem novo `case` no switch de `daily_ledger_agent.go`, zero comentários, estados como tipos fechados e fronteira de dados íntegra. Todas as 9 tasks do PRD de refatoração canônica do `internal/agent` estão `done`.
