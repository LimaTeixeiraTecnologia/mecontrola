# Generated: 2026-06-25T10:15:00Z

# Relatório de Execução — Tarefa 3.0: Migration 000020 drop Telegram

## Resumo

Migration reversível `000020_drop_telegram_channel` criada com sucesso. Remove a coluna
`telegram_external_id` e o índice parcial de `onboarding_tokens`, além de restringir os
CHECK constraints das 3 tabelas (`channel_processed_messages`, `user_identities`,
`onboarding_sessions`) para aceitar apenas `'whatsapp'`. Script de verificação pré-deploy
fail-fast criado. Testes de integração verdes (11/11).

## Subtarefas Executadas

- [x] 3.1 Criado `migrations/000020_drop_telegram_channel.up.sql`
- [x] 3.2 Criado `migrations/000020_drop_telegram_channel.down.sql`
- [x] 3.3 Criado `scripts/migrations/pre-deploy-000020.sh` (fail-fast pré-deploy)
- [x] 3.4 Ajustado `migrations/migrations_integration_test.go` (removido assert telegram_external_id present; insert telegram substituído por whatsapp; assert telegram rejeitado por constraint; assertColumnMissing helper adicionado)
- [x] 3.5 Teste `TestMigration000020DropTelegramUpDown` adicionado e verde

## Arquivos Criados/Modificados

- `migrations/000020_drop_telegram_channel.up.sql` (novo)
- `migrations/000020_drop_telegram_channel.down.sql` (novo)
- `scripts/migrations/pre-deploy-000020.sh` (novo, executável)
- `migrations/migrations_integration_test.go` (modificado)
- `docs/runbooks/deploy-producao.md` (adicionada seção §13 verificação pré-deploy)

## Critérios de Aceite

- Migration up/down idempotente e reversível; aplica constraints whatsapp-only.
  -> comprovado: `TestMigration000020DropTelegramUpDown` executa up (assertions whatsapp-only), down (restaura telegram), up novamente (reaplica whatsapp-only) — PASS
- Verificação pré-deploy documentada no runbook; fail-fast se houver dados Telegram.
  -> comprovado: `scripts/migrations/pre-deploy-000020.sh` criado; seção §13 adicionada ao runbook `docs/runbooks/deploy-producao.md`; script sai com exit 1 se count > 0
- Teste de integração de migrations verde.
  -> comprovado: `go test -v -tags=integration -run TestMigrationSuite ./migrations/...` — 11/11 PASS

## DoD

- [x] Migration up remove telegram_external_id + índice + recheia constraints com whatsapp-only
- [x] Migration down restaura estado do baseline (coluna, índice, constraints whatsapp+telegram)
- [x] DELETE dedup residual na up antes do ADD CONSTRAINT
- [x] Script pré-deploy fail-fast documentado e executável
- [x] assertTableMissing(telegram_processed_updates) mantido no TestBaselineUpDownUp
- [x] Zero comentários em Go de produção (gate OK)
- [x] Nenhuma tabela de transactions/billing/identity tocada

## Comandos Executados

```bash
go build ./migrations/...            # OK
go vet ./migrations/...              # OK
go test -v -tags=integration -run TestMigrationSuite/TestMigration000020DropTelegramUpDown ./migrations/...  # PASS
go test -v -tags=integration -run TestMigrationSuite ./migrations/...  # 11/11 PASS
grep -rn ... "^[[:space:]]*//" migrations/ | grep -Ev "(//go:...)" # OK: zero comentários
```

## Validações Gate

- R-ADAPTER-001.1 (zero comentários): OK
- Nenhuma tabela de domínio (transactions/billing/identity) tocada: verificado
- assertTableMissing("mecontrola.telegram_processed_updates"): mantido em TestBaselineUpDownUp

## Riscos Residuais

- Nenhum crítico. O script pré-deploy requer `psql` disponível no ambiente de execução; alternativa
  documentada via variável `DATABASE_URL` completa.

## Veredito

APPROVED — todos os critérios de aceite comprovados, 11/11 testes verdes, gates OK.
