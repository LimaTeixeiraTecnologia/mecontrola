# Relatório de Execução de Tarefa

## Tarefa
- ID: 7.0
- Título: Editar/apagar por referência + desambiguação (search + HITL)
- Arquivo: .specs/prd-refatoracao-agent-canonico/task-7.0-edit-delete-by-reference.md
- Estado: implementado (validação unitária/workflow verde; integração/e2e compiláveis, execução com banco pendente)

## Causa Raiz
A funcionalidade da Task 7.0 estava **totalmente ausente** do código apesar de um relatório
anterior falso marcá-la como `done`. Não existia VO `SearchQuery`, método `SearchByDescription`,
usecase `SearchTransactions`, contrato `TransactionSearcher`, binding `TransactionSearcherAdapter`,
tipos fechados `AwaitingSelect`/`OperationDeleteByRef`/`OperationEditByRef`/`TargetCandidate`, steps
`resolve_candidates`/`select_target`, kinds `KindDeleteTransactionByRef`/`KindEditTransactionByRef`,
nem executores by-ref. O relatório anterior também citava artefatos inexistentes
(`agent_target_select_total`, `TransactionSearchCandidate`, `AwaitingSelectChoice`) e um veredito
"APPROVED" não realizado. Esta execução implementa a feature de fato.

## Comandos Executados (saída real)
- `go build ./internal/transactions/... ./internal/agent/...` -> OK (exit 0, zero erros)
- `go test ./internal/transactions/... ./internal/agent/... -count=1` -> PASS (todos os pacotes)
- `go test ./internal/transactions/... ./internal/agent/... -count=1 -race` -> PASS
- `go test ./internal/agent/application/prompting/ -run StrictComplete` -> PASS (schema strict invariant)
- `go test ./internal/agent/application/workflow/ -run DestructiveConfirm` -> PASS (suspend→resume→confirm→execute durável)
- `go test -tags integration -run xxx_none ./internal/transactions/infrastructure/repositories/postgres/` -> OK (compila)
- `bash scripts/ci/agent-data-boundary.sh` -> GATE VERDE (7/7 OK; nenhum SQL no agent)
- Gate switch-growth (`grep -cE "^[[:space:]]*case intent\.Kind" daily_ledger_agent.go`) -> 0 (<=1 OK)
- `gofmt -l` em todos os arquivos tocados -> limpo (após `gofmt -w` em draft.go e daily_ledger_agent.go)
- `go vet ./internal/transactions/... ./internal/agent/...` -> limpo (no escopo)
- Gate zero-comments em produção (R-ADAPTER-001.1) -> OK nos arquivos novos/alterados

### Não executado
- `go test -tags e2e ./internal/agent/e2e/` -> NÃO compila por edições concorrentes de OUTRO agente
  em `onboarding_*_test.go` e `suite_test.go` (`NewSaveOnboardingIncome`, `EventPublisher`,
  `NewIntentEventPublisher`) — fora do escopo da Task 7.0. As adições by-ref ao e2e (feature f07,
  steps, wiring do searcher) estão íntegras e referenciam apenas símbolos válidos.
- `go test -tags integration` com Postgres real (testcontainers) -> não há banco neste ambiente;
  testes de integração foram apenas compilados.

## Arquivos Alterados

### Criados (produção)
- `internal/transactions/domain/valueobjects/search_query.go` — VO `SearchQuery` (trim, len>=2; R-TXN-002)
- `internal/transactions/application/dtos/input/search_transactions.go` — input DTO com `Validate()` (R-DTO-001/002)
- `internal/transactions/application/usecases/search_transactions.go` — usecase `SearchTransactions` (span + Validate + uow.Do)
- `internal/agent/infrastructure/binding/transaction_search.go` — `TransactionSearcherAdapter` (adapter fino binding→usecase)
- `internal/agent/application/workflow/steps/resolve_candidates.go` — chama `TransactionSearcher`, popula `Candidates` no snapshot
- `internal/agent/application/workflow/steps/select_target.go` — função pura: 0→shortcut; 1→auto; N→suspend `AwaitingSelect`; resume parseia índice 1-based; reprompt único

### Criados (testes)
- `internal/transactions/domain/valueobjects/search_query_test.go`
- `internal/transactions/application/usecases/search_transactions_test.go` (suite whitebox + fake.NewProvider; R-TESTING-001)
- `internal/transactions/infrastructure/repositories/postgres/transaction_search_integration_test.go` (`//go:build integration`; ILIKE/limit/isolamento/excludes-deleted)
- `internal/agent/application/workflow/steps/select_target_test.go` (0/1/N, índice válido/inválido, reprompt-once, resume, índice fora de faixa)
- `internal/agent/domain/intent/intent_by_ref_test.go` (constructors + IsWrite + parse round-trip)
- `internal/agent/e2e/features/f07_whatsapp_agent_edit_delete_by_ref.feature` (3 cenários WhatsApp)
- `internal/agent/e2e/by_ref_steps_test.go` (step de seed de lançamento por descrição)

### Modificados (produção)
- `internal/transactions/application/interfaces/transaction_repository.go` — método `SearchByDescription` (com `option.Option[RefMonth]`)
- `internal/transactions/infrastructure/repositories/postgres/transaction_repository.go` — implementação `SearchByDescription` (`ILIKE`, `user_id`, `deleted_at IS NULL`, `ORDER BY created_at DESC`, `LIMIT` clamp 10; `defer rows.Close()`)
- `internal/transactions/application/interfaces/mocks/transaction_repository.go` — método de mock `SearchByDescription` adicionado à mão (estilo do template gerado; mockery instalado é versão incompatível com o template do arquivo)
- `internal/transactions/module.go` — `SearchTransactionsUC` construído e exportado
- `internal/agent/application/tools/contracts.go` — `TransactionSearcher` + `TransactionSearchInput` + `TransactionSearchResult`
- `internal/agent/application/tools/formatting.go` — `FormatBRL` exportado (reuso para listas/replies verbatim)
- `internal/agent/domain/confirmation/draft.go` — `AwaitingSelect`, `OperationDeleteByRef`/`OperationEditByRef` (String/IsValid/Parse atualizados), `TargetCandidate`, campos `TargetDescription`/`TargetAmountCents`/`SearchQuery`/`Candidates`/`SelectRepromptCount`
- `internal/agent/domain/intent/intent.go` — `KindDeleteTransactionByRef`/`KindEditTransactionByRef` (String/ParseKind/IsWrite), campo+accessor `SearchQuery`, constructors `NewDeleteTransactionByRef`/`NewEditTransactionByRef`, erro `ErrSearchQueryTooShort`
- `internal/agent/application/workflow/destructive_confirm.go` — dep `Searcher`; `resolve_candidates`+`select_target` antes de `prepare_target`; `formatDestructiveReply` por-ref verbatim
- `internal/agent/infrastructure/binding/hitl_adapters.go` — resolvers/executores by-ref (Target*+version, optimistic lock); `versionConflictReply` mapeia `interfaces.ErrTransactionVersionConflict` para mensagem amigável (não `auditWriteFailed`)
- `internal/agent/application/services/daily_ledger_agent.go` — `intentToOperationKind` estendido (sem novo `case`); `initialConfirmState` popula `SearchQuery`/`NewAmountCents`; `resolveIntentKindFromOperation`/`promptTextForOperation` atualizados
- `internal/agent/application/services/intent_router.go` — campo `TransactionSearcher` em `IntentRouterDeps`
- `internal/agent/application/prompting/prompts.go` — schema estrito estendido (2 kinds no enum, `search_query` em properties+required — invariante mantida)
- `internal/agent/application/prompting/parse_intent.system.tmpl` — descrições + exemplos dos 2 kinds by-ref
- `internal/agent/application/usecases/parse_inbound.go` — DTO ganha `search_query`; `build()` cobre os 2 kinds
- `internal/agent/module.go` — wiring `TransactionSearcherAdapter` + by-ref targets/executors + `Searcher` na confirm def

### Modificados (testes/e2e)
- `internal/agent/application/workflow/destructive_confirm_test.go` — cenários by-ref durável (none/single/multi/reprompt) + format de novos kinds
- `internal/agent/e2e/suite_test.go` — searcher + intents by-ref no stub parser (somente 3 linhas+intents; demais alterações do arquivo são de outro agente)
- `internal/agent/e2e/support_test.go` — `buildConfirmKernelDeps` recebe searcher e registra targets/executors by-ref
- `internal/agent/e2e/steps_test.go` — registra `registerByRefSteps`

## Resultados de Validação
- Testes unitários + workflow (scoped): PASS, inclusive `-race`
- Gate de fronteira de dados: GATE VERDE (7/7) — zero SQL/import de repo de outro BC no agent
- Gate switch-growth: 0 cases (sem crescimento de switch de domínio — R-AGENT-WF-001.1)
- Schema strict invariant: PASS (toda property em required)
- gofmt: limpo; go vet: limpo; zero comentários: OK
- Integração/e2e da Task 7.0: compiláveis; execução com Postgres/ambiente completo pendente

## Critérios de Aceite
- `Apaga o Uber` (1→confirma→apaga): comprovado por `select_target_test.go::TestSingleCandidate_AutoSelect`
  e `destructive_confirm_test.go::TestByRef_SingleFound_SuspendsConfirm` + execução durável em
  `TestByRef_MultipleFound_SelectThenConfirmThenExecute` (resume "sim" → executor roda → succeeded).
  Cenário e2e f07 escrito.
- `Apaga o mercado` (N→escolhe→confirma): comprovado por `TestByRef_MultipleFound_SelectThenConfirmThenExecute`
  (suspende lista enumerada → resume "2" mapeia `tx-2`/version 3 persistido no snapshot → confirma → exclui).
  Índice estável suspend↔resume garantido por candidatos no snapshot (R-WF-KERNEL-001.7), validado via merge-patch resume.
- `O Uber foi 42 e não 35` (edita): `KindEditTransactionByRef`→`OperationEditByRef`; `initialConfirmState`
  popula `SearchQuery`+`NewAmountCents`; executor by-ref usa `NewAmountCents`. Cenário e2e f07 escrito.
- Nenhuma efetivação sem item único selecionado e confirmado: `select_target` só sai de `AwaitingSelect`
  com índice válido; reprompt único e cancelamento na 2ª resposta inválida (`TestResume_InvalidIndex_*`).
- Busca por ILIKE no dono; zero SQL/loop no agent: `SearchByDescription` (ILIKE no repo postgres);
  gate de fronteira de dados verde.

## Definition of Done (DoD)
- [x] VO/usecase/repo/binding/steps/kinds/executors implementados de fato.
- [x] Testes unitários + de workflow criados e verdes (`-race`).
- [x] Gate de fronteira de dados, switch-growth, schema strict, gofmt, vet, zero-comments OK.
- [ ] Testes de integração e e2e executados com banco — pendente (ambiente sem Postgres; e2e bloqueado por refactor concorrente de onboarding, fora de escopo).

## Suposições
- `NewAmount` da techspec mapeia ao campo existente `NewAmountCents` de `ConfirmState` (sem campo duplicado), reusado também pelo edit by-ref.
- Target selecionado reusa `TargetTransactionID`/`TargetTransactionVersion` existentes; adicionados `TargetDescription`/`TargetAmountCents` para o texto de confirmação verbatim.
- `select_target` não mantém side-store: `Candidates`/`SelectRepromptCount`/`AwaitingApproval` vivem no `ConfirmState` (snapshot do kernel = fonte única; R-WF-KERNEL-001.7).
- Conflito de versão mapeado contra `interfaces.ErrTransactionVersionConflict` (sentinela realmente propagada pelo repo via `%w`), não a cópia homônima em `usecases`.
- Mock de `SearchByDescription` escrito à mão por incompatibilidade de versão do mockery local com o template do arquivo gerado.

## Riscos Residuais
- Integração `SearchByDescription` (ILIKE real) e o fluxo durável suspend(AwaitingSelect)→resume→confirm com Postgres não foram EXECUTADOS (apenas compilados) — sem banco no ambiente.
- E2E (feature f07) não pôde rodar: `internal/agent/e2e` não compila devido a edições concorrentes de outro agente em onboarding (`NewSaveOnboardingIncome`, `EventPublisher`, `NewIntentEventPublisher`). As adições by-ref ao e2e estão corretas e isoladas.
- `ILIKE` sem índice GIN/pg_trgm; mitigado por `LIMIT 10` + `user_id`; evolução para `pg_trgm` encapsulada no repo (ADR-008).
- Métrica `agent_target_select_total{outcome}` citada no ADR/critérios NÃO foi implementada nesta entrega (sem instrumentação adicionada ao step puro); a observabilidade do fluxo é coberta pelos logs/contadores HITL já existentes no `confirm_gate`/Run auditável. Registrar como pendência se o dashboard exigir o contador dedicado.

## Conflitos de Regra
- none
