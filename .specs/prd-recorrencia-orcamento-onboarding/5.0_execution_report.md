# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Reescrita do `BuildRecurrenceStep` e confirmação encadeada
- Arquivo: .specs/prd-recorrencia-orcamento-onboarding/task-5.0-reescrita-step-confirmacao.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-recorrencia-orcamento-onboarding/prd.md (RF-01..RF-20)
- TechSpec: .specs/prd-recorrencia-orcamento-onboarding/techspec.md (seções "Fluxo de Dados" e "Design de Implementação")
- ADRs: adr-001-decisao-recorrencia-pura-tipos-fechados.md, adr-002-extracao-unica-intent-meses.md, adr-003-confirmacao-encadeada-retrocompat-estado.md, adr-004-gate-real-llm-zero-falso-sucesso.md
- Governança: `AGENTS.md` (Trio Obrigatório Go), `.claude/rules/agent-workflows-tools.md` (R-AGENT-WF-001), `.claude/rules/go-adapters.md` (R-ADAPTER-001), `.claude/rules/go-testing.md` (R-TESTING-001), skill processual `mastra` (declarada na task)

## Comandos Executados
- `gofmt -l internal/agents/application/workflows/onboarding_workflow.go internal/agents/application/workflows/onboarding_workflow_test.go` -> limpo (sem arquivos não formatados) após `gofmt -w` no arquivo de teste
- `go build ./...` -> sem erros
- `go build ./internal/agents/...` -> sem erros
- `go vet ./...` -> sem erros
- `go vet ./internal/agents/...` -> sem erros
- `go test ./internal/agents/application/workflows/... -race -v` -> pass (422 testes no pacote workflows, incluindo os 9 cenários novos/atualizados de `TestBuildRecurrenceStep` e o novo cenário de `TestBuildCardsStep`)
- `go test ./internal/agents/... -race` -> pass (todos os pacotes do módulo `internal/agents`, sem regressão)
- `task lint:run` -> PASS (golangci-lint 0 issues; `lint:auth-bypass`, `lint:outbox-user-id` (+ 7 fixtures), `lint:deadcode` — após ajuste de allowlist — todos PASS)
- `grep` gates manuais: zero comentários em `.go` de produção tocados; sem `switch case intent.Kind` de domínio (R-AGENT-WF-001.1); sem SQL direto em tools/consumers (R-ADAPTER-001.2)
- Revisão adversarial inline (skill `review`, Etapa 1-5): 1 achado `medium` (switch não-exaustivo no dispatch de outcome) corrigido antes do veredito final; revalidado com build+vet+test race — veredito final `APPROVED`

## Arquivos Alterados
- `internal/agents/application/workflows/onboarding_workflow.go` — reescrita de `BuildRecurrenceStep` (extração única com `recurrenceDecisionSchema`/`recurrenceExtract` → `ParseRecurrenceIntentKind` → `DecideRecurrence` → dispatch exaustivo por `recurrenceOutcomeKind`); `BuildCardsStep` agora prefixa e zera `state.RecurrenceConfirmation` no prompt inicial (espelha `GoalConfirmation`→`BuildMonthlyBudgetStep`); remoção do const morto `recurrenceSystemPrompt` (usado só pelo código antigo do step, agora substituído por `recurrenceDecisionSystemPrompt`)
- `internal/agents/application/workflows/onboarding_workflow_test.go` — `TestBuildRecurrenceStep` reescrito com 9 cenários (primeira entrada; positiva sem quantidade→12; específica 3; específica 1 singular; negativa sem `CreateRecurrence`; inválida 13 sem `CreateRecurrence`; inválida 0 sem `CreateRecurrence`; ambígua sem `CreateRecurrence`; erro `CreateRecurrence`→`failStep`); novo helper `recurrenceDecisionJSON` substitui `recurrenceExtractJSON` (schema antigo); novo cenário em `TestBuildCardsStep` cobrindo o prefixo/zeragem de `RecurrenceConfirmation`; remoção do teste obsoleto `TestRecurrenceSystemPrompt_RemainsUnchangedForCurrentStepWiring`; atualização do mapa de superfícies `TestM02_NoRendaTermInAnyOnboardingSurface` (troca `recurrenceSystemPrompt` por `recurrenceDecisionSystemPrompt`, `recurrenceInvalidReprompt`, `recurrenceConfirmationNone`, `recurrenceConfirmationDefault`)
- `deployment/scripts/deadcode-agent-allowlist.txt` — adição de `recurrenceIntentKind.String`/`.IsValid` e `recurrenceOutcomeKind.IsValid` (métodos DMMF de simetria de API mantidos sem call-site direto em produção, mesmo padrão já aplicado a `distributionIntentKind`/`distributionBalanceKind`)

## Resultados de Validação
- Testes: pass (`go test ./internal/agents/... -race`)
- Lint: pass (`task lint:run`, incluindo `lint:deadcode` após atualização da allowlist)
- Veredito do Revisor: APPROVED (revisão inline; 1 achado medium identificado e corrigido antes do veredito)

## Critérios de Aceite
- 0 falso-sucesso (`CreateRecurrence` só em `Default`/`Specific`) -> comprovado: dispatch exaustivo `switch decision.Outcome { case recurrenceOutcomeDefault, recurrenceOutcomeSpecific: ...CreateRecurrence... default: failStep }`; testes `TestBuildRecurrenceStep` para negativa/inválida-13/inválida-0/ambígua usam `interfacemocks.NewBudgetPlanner(s.T())` sem nenhum `EXPECT()`, de forma que qualquer chamada inesperada a `CreateRecurrence` faria o mock (mockery) falhar o teste
- Confirmação imediata setada e prefixada no cards step -> comprovado: `BuildRecurrenceStep` seta `state.RecurrenceConfirmation` em None/Default/Specific; `BuildCardsStep` prefixa `state.RecurrenceConfirmation + "\n\n" + prompt` e zera o campo; teste `TestBuildCardsStep/primeira entrada com confirmacao de recorrencia pendente...` comprova prefixo e limpeza
- RF-09: repergunta sem cap em `Invalid`/`Ambiguous` -> comprovado: nenhum contador de tentativas é lido/incrementado nesses ramos; ambos retornam `suspendStep` preservando a fase, permitindo repergunta indefinida (testes `TestBuildRecurrenceStep` "quantidade fora do intervalo (13/0 meses)" e "resposta ambigua")
- Sem feature flag (RF-19) -> comprovado: `grep -rn "feature.*flag\|FeatureFlag" internal/agents/application/workflows/onboarding_workflow.go` retorna vazio; nenhum caminho condicional novo introduzido
- Testes unitários do step verdes -> comprovado: `go test ./internal/agents/application/workflows/... -race -v` reporta PASS para todos os subtestes de `TestBuildRecurrenceStep` e `TestBuildCardsStep`
- Testes existentes atualizados verdes -> comprovado: suíte completa do pacote `workflows` e do módulo `internal/agents` verde (`go test ./internal/agents/... -race` -> ok em todos os pacotes)
- `go build`, `go vet` e `go test ./internal/agents/application/workflows/...` verdes -> comprovado: comandos executados nesta sessão sem erro (ver seção "Comandos Executados")

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=uncommitted (working tree; PRD ainda não commitado — tarefas 1.0-5.0 em sequência no mesmo arquivo)
verdict=APPROVED
tool=claude

## Coverage

package=github.com/LimaTeixeiraTecnologia/mecontrola/internal/agents/application/workflows
delta=não medido isoladamente (sem baseline de cobertura por tarefa configurado no projeto); suíte completa do pacote permanece verde com os novos cenários adicionados

## Suposições
- O escopo de arquivos da tarefa 5.0 (`Arquivos Relevantes`) não inclui `onboarding_workflow_integration_test.go` (gate real-LLM) nem a fixture full-flow do WhatsApp — ambos permanecem sob responsabilidade da tarefa 6.0, que depende desta. A chamada `workflows.BuildRecurrenceStep(a, budgets, nil)` no gate real-LLM (linha ~716) já estava correta (assinatura com `rec` adicionada na tarefa 4.0) e não foi tocada; seu comportamento (cenário "ambiguo-talvez" esperando `StepStatusCompleted`) ficará desatualizado até a tarefa 6.0 reescrever esse teste, mas isso não compila nem roda fora de `-tags integration` + `RUN_REAL_LLM=1`.
- O const `recurrenceSystemPrompt` (e o teste que o fixava) foi removido por ser código morto após a reescrita — não estava listado explicitamente nas subtarefas 5.1-5.9, mas está diretamente dentro do escopo de "reescrita do step" e do arquivo de produção único desta tarefa.

## Riscos Residuais
- Nenhum risco crítico identificado. A tarefa 6.0 (dependente desta) precisa reescrever `TestRecurrenceExtractionGate` em `onboarding_workflow_integration_test.go` para o novo contrato de outcome (`recurrenceOutcomeKind`) e a fixture full-flow do WhatsApp (`whatsapp_inbound_consumer_integration_test.go:332/342`), conforme já mapeado em `tasks.md` e no ADR-004 — nenhuma ação necessária nesta tarefa além de manter a assinatura estável.
- O counter `agents_onboarding_recurrence_total` reconta outcomes em replay (comportamento aceito, documentado no ADR-004/techspec — consistente com os counters de distribution/monthly-budget já existentes).

## Conflitos de Regra
- none
