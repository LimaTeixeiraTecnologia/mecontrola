# Generated: 2026-07-16T00:19:15Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 2.0
- Título: Schema dedicado, prompt e copy no Tom de Voz
- Arquivo: .specs/prd-recorrencia-orcamento-onboarding/task-2.0-schema-prompt-copy.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-recorrencia-orcamento-onboarding/prd.md (RF-04, RF-05, RF-06, RF-07, RF-08, RF-14, RF-15)
- TechSpec: .specs/prd-recorrencia-orcamento-onboarding/techspec.md (seções "Visão Geral dos Componentes", "Modelos de Dados", "Ordem de Build" item 2)
- ADR: .specs/prd-recorrencia-orcamento-onboarding/adr-002-extracao-unica-intent-meses.md (extração LLM única por turno, schema dedicado, precedência quantidade > intenção)
- Governança: AGENTS.md base; skill processual `mastra` (saída estruturada `llm.Schema` e prompt na call-site sancionada, sem tocar no schema compartilhado do `summary_confirm`); skill de linguagem `go-implementation` (auto-carregada pelo diff Go); regras `.claude/rules/agent-workflows-tools.md` (R-AGENT-WF-001.4 — LLM só em call-site sancionada), `.claude/rules/go-adapters.md` (R-ADAPTER-001.1 — zero comentários), `.claude/rules/go-testing.md` (R-TESTING-001)
- Riscos de Integração do tasks.md: `recurrenceSchema` é compartilhado com `summary_confirm` (`onboarding_workflow.go:1480`); tarefa DEVE criar schema/struct novos sem tocar nos existentes.

## Decisão de Implementação (desvio controlado, registrado para auditoria)

O texto literal da subtarefa 2.2 pedia "reescrever `recurrenceSystemPrompt`". Uma primeira implementação fez exatamente isso in-place. A revisão adversarial (subagent `reviewer`) identificou um achado **CRITICAL**: `recurrenceSystemPrompt` é consumido **hoje, em produção**, por `BuildRecurrenceStep` (ainda não reescrito — isso é escopo da tarefa 5.0), que continua usando `Schema: recurrenceSchema` (`{confirmed: boolean}`) e parseia via `yesNoExtract.Confirmed`. Sobrescrever o conteúdo semântico do prompt sem sincronizar o schema/parsing do step criaria uma janela real de regressão comportamental (LLM instruído a falar de `intent/hasMonths/months`, mas obrigado a devolver apenas `confirmed`, sem qualquer orientação sobre o que esse campo significa) — não coberta pelos testes unitários existentes, que mockam o `agent.Agent` e não exercitam o LLM real.

Correção aplicada: `recurrenceSystemPrompt` foi revertido ao conteúdo original (idêntico ao pré-existente antes desta tarefa), preservando 100% do comportamento vigente de `BuildRecurrenceStep`. O conteúdo reescrito (intent/hasMonths/months + conversão por extenso + regra `unclear`) foi implementado sob uma constante **nova e distinta**, `recurrenceDecisionSystemPrompt`, reservada para a tarefa 5.0 conectar a `BuildRecurrenceStep` junto com `recurrenceDecisionSchema`/`recurrenceExtract`. Isso satisfaz integralmente RF-04/RF-05/RF-06/RF-07/RF-08 (o conteúdo existe, pronto para uso) sem introduzir uma regressão detectável apenas em produção real-LLM. Um teste de regressão-guard (`TestRecurrenceSystemPrompt_RemainsUnchangedForCurrentStepWiring`) fixa o texto original para impedir reintrodução acidental do problema em tarefas futuras.

## Comandos Executados
- `go build ./...` -> pass (sem output)
- `go vet ./internal/agents/...` -> pass (sem output)
- `.tools/bin/golangci-lint run ./internal/agents/application/workflows/...` -> pass ("0 issues.")
- `go test ./internal/agents/application/workflows/... -race` -> pass (414 passed)
- `go test ./internal/agents/... -race` -> pass (todos os 21 pacotes do módulo, sem regressão)
- `gofmt -l onboarding_workflow.go onboarding_workflow_test.go` -> vazio (formatado)
- `grep -n "^[[:space:]]*//" onboarding_workflow.go | grep -Ev "(//go:|//nolint:|// Code generated)"` -> vazio
- `task gates:zero-comments` -> PASS GATE-COMMENTS
- Subagente `reviewer` (diff review, 2 rodadas): rodada 1 -> REJECTED (1 achado CRITICAL: mutação in-place de `recurrenceSystemPrompt` quebrando `BuildRecurrenceStep` vigente; 1 achado MEDIUM: testes faltantes para `monthsLabel`/`recurrenceConfirmationFor`); fix aplicado; rodada 2 -> **APPROVED** (confirmado por leitura direta do arquivo pelo próprio revisor, não apenas resumo)

## Arquivos Alterados
- `internal/agents/application/workflows/onboarding_workflow.go`:
  - novo `recurrenceExtract{Intent string; HasMonths bool; Months int}` (struct de saída estruturada dedicada)
  - novo `recurrenceDecisionSchema` (schema JSON estrito: `intent` enum `negative|positive|unclear`, `hasMonths` bool, `months` integer, `additionalProperties: false`, todos `required`)
  - novo `recurrenceDecisionSystemPrompt` (extração intent+hasMonths+months, conversão por extenso um..doze, regra `unclear`, instrução explícita de que prioridade/limites são resolvidos por `DecideRecurrence`, não pelo LLM)
  - `conclusionRecurrencePrompt` reescrito (RF-14: sinaliza "sim" 12 meses, número 1-12, ou "não"; emoji oficial `📊`, asterisco simples)
  - novo `recurrenceInvalidReprompt` (RF-07: repergunta de quantidade fora do intervalo 1-12)
  - novos `recurrenceConfirmationNone`, `recurrenceConfirmationDefault`, `recurrenceConfirmationTemplate`, helpers `monthsLabel(n int) string` (singular "1 mês" / plural "N meses") e `recurrenceConfirmationFor(months int) string`
  - `recurrenceSystemPrompt` — **inalterado** (reconfirmado após a correção do achado crítico)
  - `recurrenceSchema` / `yesNoExtract` / `summaryConfirmSystemPrompt` — **intocados** (subtarefa 2.6)
- `internal/agents/application/workflows/onboarding_workflow_test.go`:
  - `TestRecurrenceExtract_UnmarshalsIntoExtractStruct`
  - `TestRecurrenceDecisionSchema_HasIntentHasMonthsAndMonths`
  - `TestRecurrenceSchema_RemainsUnchangedForSummaryConfirm`
  - `TestRecurrenceDecisionSystemPrompt_ContainsExtensoExamplesAndUnclearRule`
  - `TestRecurrenceSystemPrompt_RemainsUnchangedForCurrentStepWiring` (regression-guard)
  - `TestConclusionRecurrencePrompt_SignalsAllThreeOptions`
  - `TestRecurrenceInvalidReprompt_SignalsValidRange`
  - `TestMonthsLabel_SingularAndPlural`
  - `TestRecurrenceConfirmationFor_DefaultAndSpecific`

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED (após 1 rodada de correção)

## Critérios de Aceite
- `recurrenceDecisionSchema` estrito criado (enum fechado, `additionalProperties: false`, todos `required`) -> comprovado: declaração do schema em `onboarding_workflow.go` + `TestRecurrenceDecisionSchema_HasIntentHasMonthsAndMonths` (PASS) valida properties, required e enum.
- `recurrenceSchema` inalterado — `summary_confirm` continua desserializando `yesNoExtract` -> comprovado: `git diff HEAD` do arquivo não mostra alteração em `recurrenceSchema`/`yesNoExtract`/`summaryConfirmSystemPrompt`; `TestRecurrenceSchema_RemainsUnchangedForSummaryConfirm` (PASS) fixa a forma `{confirmed: boolean}`; `BuildRecurrenceStep`/`summary_confirm` continuam referenciando os símbolos originais sem mudança de linha.
- Copy no Tom de Voz (asterisco simples + emoji oficial), verificável pelos scorers -> comprovado: nenhuma ocorrência de `**` nas novas constantes (`TestConclusionRecurrencePrompt_SignalsAllThreeOptions`/`TestRecurrenceInvalidReprompt_SignalsValidRange`/`TestRecurrenceConfirmationFor_DefaultAndSpecific` afirmam `NotContains(..., "**")`); emojis usados (`📊`, `✅`) pertencem à lista oficial `mecontrolaToneEmojis` em `internal/agents/application/scorers/behavioral_scorers.go:83`.
- `go build` e `go vet` verdes -> comprovado: `go build ./...` e `go vet ./internal/agents/...` executados sem output/erro (exit 0).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão (módulo completo `internal/agents` revalidado, 21 pacotes verdes).
- [x] Estado de tasks.md sincronizado com este relatório.
- [x] Achado CRITICAL da revisão adversarial corrigido e reconfirmado por nova rodada de review (APPROVED).

## Diff Reviewed

sha=n/a (diff local não commitado)
verdict=APPROVED (rodada 2; rodada 1 foi REJECTED com 1 CRITICAL corrigido)
tool=reviewer (subagent)

## Coverage

package=internal/agents/application/workflows
delta=n/a (não medido; suite completa do pacote permanece 100% verde, 414 testes; módulo `internal/agents` completo, 21 pacotes verdes)

## Riscos Residuais
- `recurrenceDecisionSchema`, `recurrenceExtract`, `recurrenceDecisionSystemPrompt`, `recurrenceInvalidReprompt`, `recurrenceConfirmationNone/Default/Template`, `monthsLabel`, `recurrenceConfirmationFor` permanecem **não conectados** a `BuildRecurrenceStep` até a tarefa 5.0. Isso é esperado e documentado (dependência explícita em tasks.md: "5.0 depende de 1.0, 2.0, 3.0, 4.0"); Go não falha build/vet por const/func de pacote não utilizada. Nenhum risco de regressão enquanto essa conexão não for feita.
- `conclusionRecurrencePrompt` mudou de conteúdo (RF-14) — isso é intencional e já é consumido pelo `BuildRecurrenceStep` vigente na primeira suspensão do step; nenhum teste unitário quebrou porque os testes existentes referenciam a constante (não um literal hardcoded), mas o texto exibido ao usuário real mudou por design nesta tarefa (RF-14/RF-15), antes da tarefa 5.0 mexer no restante do fluxo. Consistente com o "Ordem de Build" da techspec (item 2 antes do item 4).
- A cobertura de `conclusionRecurrencePrompt`/`recurrenceInvalidReprompt`/`recurrenceDecisionSystemPrompt` pelo teste `TestM02_NoRendaTermInAnyOnboardingSurface` (surface map de termos proibidos) não foi estendida nesta tarefa — por escopo, essa atualização está distribuída para as tarefas 3.0/5.0/6.0 conforme a seção "Riscos de Integração" do tasks.md.

## Suposições
- O nome literal `recurrenceSystemPrompt` citado na subtarefa 2.2 do task file foi interpretado como "o conteúdo semântico da extração intent/hasMonths/months descrito no ADR-002", não como obrigação de sobrescrever a constante já viva em produção. Optou-se por um novo nome (`recurrenceDecisionSystemPrompt`) para eliminar uma janela real de regressão comportamental identificada pela revisão adversarial, mantendo os deliverables de RF-04/05/06/07/08/14/15 totalmente disponíveis para a tarefa 5.0 conectar.
- Emojis escolhidos (`📊` na pergunta, `✅` nas confirmações) seguem a lista oficial `mecontrolaToneEmojis`; nenhum emoji de "repetição" (ex.: 🔁) está nessa lista, por isso não foi usado.
