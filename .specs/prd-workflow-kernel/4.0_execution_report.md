# Generated: 2026-06-24T13:20:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.0
- Título: Adapter Postgres + migração 000019 (CAS + índice parcial)
- Arquivo: .specs/prd-workflow-kernel/task-4.0-postgres-store-migracao.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-workflow-kernel/prd.md
- TechSpec: .specs/prd-workflow-kernel/techspec.md
- Governança: agent-governance, go-implementation SKILL.md, .claude/rules/workflow-kernel.md (R-WF-KERNEL-001), .claude/rules/go-adapters.md (R-ADAPTER-001)
- Referências: architecture.md, persistence.md (padrão idempotency postgresStorage + agent_run_repository)

## Comandos Executados
- `go build ./internal/platform/workflow/...` → ok (zero erros)
- `go vet ./internal/platform/workflow/...` → ok
- `go test ./internal/platform/workflow/...` → ok (tests unitários existentes no kernel passam)
- `go test -v -tags integration -run TestStoreIntegrationSuite ./internal/platform/workflow/infrastructure/postgres/...` → PASS 9/9 tests (testcontainers)
- `go test -v -tags integration -run TestMigrationSuite/TestBaselineUpDownUp ./migrations/...` → PASS (migração 000019 up/down idempotente)
- gate R-WF-KERNEL-001.1 (sem imports de domínio) → PASS
- gate R-WF-KERNEL-001.2 (SQL apenas no adapter) → PASS
- gate R-WF-KERNEL-001.4 (sem labels de alta cardinalidade) → PASS
- gate R-WF-KERNEL-001.6 (zero comentários) → PASS
- gate R-ADAPTER-001.1 (zero comentários) → PASS

## Arquivos Alterados
- `migrations/000019_create_workflow_runtime.up.sql` (novo)
- `migrations/000019_create_workflow_runtime.down.sql` (novo)
- `internal/platform/workflow/infrastructure/postgres/nullable.go` (novo)
- `internal/platform/workflow/infrastructure/postgres/store.go` (novo)
- `internal/platform/workflow/infrastructure/postgres/factory.go` (novo)
- `internal/platform/workflow/infrastructure/postgres/store_integration_test.go` (novo)

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite

- Migração aplica/reverte limpa; índice parcial único impede dois runs ativos por chave. -> comprovado: TestMigrationSuite/TestBaselineUpDownUp passou (up/down/up idempotente); TestPartialIndexPreventsDoubleActiveRun: segunda INSERT com mesmo (workflow, correlation_key) e status 'running' rejeitada pela constraint workflow_runs_active_key_uidx.
- `Save` rejeita escrita com versão desatualizada (`ErrVersionConflict`). -> comprovado: TestSaveCASVersionConflict confirma que `store.Save(ctx, snap, 999)` retorna erro wrapping `workflow.ErrVersionConflict`; UPDATE com `version = $expected` falha quando `RowsAffected()==0`.
- Integration tests de durabilidade e concorrência verdes. -> comprovado: TestDurabilityResume (Start→Suspend→Load em novo Store→Resume→conclusão sem duplicar); TestConcurrentResumeCASWinner (2 goroutines concorrentes → exatamente 1 wins=1 losses=1).
- Adapter sem regra de domínio (gate R-WF-KERNEL-001). -> comprovado: todos os 5 gates R-WF-KERNEL-001.1/.2/.4/.6 + R-ADAPTER-001.1 retornam PASS.

## Requisitos Cobertos
- RF-07: tabelas `workflow_runs`/`workflow_steps` criadas na migração 000019, adapter persiste Snapshot/StepRecord via `database.DBTX`.
- RF-09: durabilidade/resume provado pelo TestDurabilityResume (snapshot recarregado corretamente após restart simulado).
- RF-10: lock otimista CAS por `version` provado pelo TestSaveCASVersionConflict + TestConcurrentResumeCASWinner; índice parcial único prova run ativo único via TestPartialIndexPreventsDoubleActiveRun.

## Suposições
- `factory.go` posicionado em `internal/platform/workflow/infrastructure/postgres/factory.go` (e não na raiz do pacote `workflow/`) para evitar importação circular: o pacote raiz `workflow` define a interface `Store`; o adapter postgres implementa essa interface e importa `workflow`; o factory vive no mesmo pacote do adapter, eliminando o ciclo.
- JSONB do Postgres normaliza `{"value":42}` para `{"value": 42}` (adiciona espaço); test usa `s.JSONEq` para comparação semântica em vez de byte-a-byte.
- `DeleteCompleted` aplica `updated_at < cutoff`; o test força o valor via UPDATE direto após o Save para simular registros antigos.

## Riscos Residuais
- `DeleteCompleted` usa subquery com `LIMIT` sem `ORDER BY` — pode variar a seleção de rows em bancos com múltiplos runs elegíveis. Para housekeeping eventual isso é aceitável; RF-17 (housekeeping job) será endereçado na task 5.0.
- Concurrent resume test usa `loaded.Version` capturado antes das goroutines — em cenários reais o snapshot seria recarregado por cada goroutine via `Load`. A prova é suficiente para validar o mecanismo CAS no nível do adapter.
