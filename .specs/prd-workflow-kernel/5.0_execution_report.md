# Generated: 2026-06-24T15:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Housekeeping job + configuração de ambiente
- Arquivo: .specs/prd-workflow-kernel/task-5.0-housekeeping-config.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-workflow-kernel/prd.md
- TechSpec: .specs/prd-workflow-kernel/techspec.md
- Governança: agent-governance, go-implementation SKILL.md, R-ADAPTER-001, R-TESTING-001
- Referências: outbox/housekeeping.go (padrão de referência), worker.Job interface, configs/config.go (padrão de config)

## Subtarefas

### 5.1 — HousekeepingJob
- Arquivo: `internal/platform/workflow/housekeeping.go` (novo)
- Implementa `worker.Job` (Name/Schedule/Timeout/Run)
- Chama `StoreFactory.Store(tx).DeleteCompleted(retention, limit)` dentro de `uow.Do`
- Loop até `n == 0` (purga em batches como outbox)
- Log quando total > 0

### 5.2 — WorkflowKernelConfig
- Arquivo: `configs/config.go`
- Novo struct `WorkflowKernelConfig` com campos:
  - `WORKFLOW_KERNEL_TRANSACTIONS_WRITE_ENABLED` (bool, default false)
  - `WORKFLOW_KERNEL_MAX_ATTEMPTS` (int, default 3)
  - `WORKFLOW_KERNEL_RETRY_BASE_BACKOFF` (duration, default 200ms)
  - `WORKFLOW_KERNEL_RETRY_MAX_BACKOFF` (duration, default 5s)
  - `WORKFLOW_KERNEL_HOUSEKEEPING_RETENTION_DAYS` (int, default 30)
  - `WORKFLOW_KERNEL_HOUSEKEEPING_SCHEDULE` (string, default "@daily")
  - `WORKFLOW_KERNEL_HOUSEKEEPING_BATCH_SIZE` (int, default 500)
- Validation: skip quando zero-values (igual outbox); rejeita MaxAttempts < 1, Backoffs ≤ 0, base > max, RetentionDays < 1, BatchSize < 1, Schedule cron inválido

### 5.3 — config_test.go
- 10 cenários de validação via `TestValidateWorkflowKernel`
- Cobre: defaults válidos, zero-values (skip), MaxAttempts=0, BaseBackoff=0, MaxBackoff=0, base>max, RetentionDays=0, BatchSize=0, Schedule inválido, Schedule vazio

## Comandos Executados
- `go build ./configs/...` → ok
- `go build ./internal/platform/workflow/...` → ok
- `go build ./...` → ok (zero erros no codebase completo)
- `go test ./configs/... -run TestConfigSuite/TestValidateWorkflowKernel` → PASS (10/10 cenários)
- `go test ./internal/platform/workflow/... -run TestHousekeepingJobSuite` → PASS (4/4 cenários: 3 Run + 1 Metadata)
- `go test ./configs/...` → ok
- `go test ./internal/platform/workflow/...` → ok

## Critérios de Aceite

- [x] `HousekeepingJob` purga runs concluídos além da retenção e preserva ativos/suspensos
  -> comprovado: `DeleteCompleted` só deleta `status IN ('succeeded','failed')` (SQL na migração + FakeStore); loop em batches; `unitOfWorkVoid.Do` passa `nil` DBTX que vai para `fakeStoreFactory`; 3 cenários de Run passam (lotes múltiplos, lote vazio, erro)
- [x] Config carrega defaults e rejeita valores inválidos com mensagem clara
  -> comprovado: `TestValidateWorkflowKernel` com 10 cenários — defaults (MaxAttempts=3, Retention=30, Batch=500, Schedule=@daily, BaseBackoff=200ms, MaxBackoff=5s) todos carregados via `setWorkflowKernelDefaults`; mensagens de erro explícitas ("WORKFLOW_KERNEL_X inválido Y: ...") verificadas em cada cenário de erro
- [x] Zero comentários em `.go`
  -> comprovado: gate R-ADAPTER-001.1 executado — `housekeeping.go` sem nenhuma linha de comentário

## Riscos Residuais
- Nenhum crítico. `StoreFactory` em `workflow.housekeeping.go` é interface local satisfeita estruturalmente por `postgres.storeFactory` (mesmo contrato `Store(database.DBTX) workflow.Store`); wiring efetivo é tarefa 8.0.
- Testes de integração de housekeeping (purge real em banco) cobertos por `store_integration_test.go` (task 4.0) que valida `DeleteCompleted` end-to-end.

## DoD
- [x] Código implementado e compilando
- [x] Testes unitários criados e passando
- [x] Zero comentários em Go de produção
- [x] Sem `init()`, sem `panic`
- [x] `context.Context` em fronteiras de IO
- [x] `errors.Join`/`fmt.Errorf %w` para wrapping
- [x] Build codebase completo limpo
