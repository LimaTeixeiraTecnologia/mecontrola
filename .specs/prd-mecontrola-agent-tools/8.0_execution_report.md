# Relatório de Execução — Tarefa 8.0

## Metadados

- **Tarefa:** 8.0 — Mapa capacidade→tool, relatório de gaps e gate anti-falso-positivo
- **Status:** done
- **Data:** 2026-07-02
- **Dependência:** 6.0 (done)

## Critérios de Sucesso

| Critério | Resultado |
|----------|-----------|
| Gaps abertos = 0 | ✓ 0 gaps |
| Mapa versionado em `.specs/` | ✓ `capacity-tool-map.md` criado |
| `go mod verify` verde | ✓ `all modules verified` |
| `go build ./...` verde | ✓ sem erros |
| `go vet ./...` verde | ✓ sem erros |
| Nenhuma capacidade em bucket 3 exposta | ✓ bucket 3 vazio |

## 8.1 — Script/Relatório Reproduzível

### Fonte de verdade: `internal/agents/module.go#buildFinancialTools`

```bash
$ grep 'agenttools\.Build' internal/agents/module.go | wc -l
25
```

### 25 tools registradas (extraídas via grep sobre tool.NewTool):

```
adjust_allocation
best_purchase_day
classify_category
count_cards
create_recurrence
delete_entry
delete_recurrence
edit_entry
get_card
get_card_purchase
get_transaction
list_card_purchases
list_cards
list_categories
list_recurrences
query_card_invoice
query_month
query_plan
register_card_purchase
register_expense
register_income
search_transactions
suggest_allocation
update_card
update_recurrence
```

Comando de reprodução:
```bash
grep -rn 'tool.NewTool\[' internal/agents/application/tools/*.go \
  | grep -oP '"[a-z_]+"' | sort
```

### Bindings wired em `module.go`:

| Binding | Use Cases injetados |
|---------|---------------------|
| `TransactionsLedgerAdapter` | CreateTransaction, CreateCardPurchase, UpdateTransaction, DeleteTransaction, UpdateCardPurchase, DeleteCardPurchase, ListMonthlyEntries, GetMonthlySummary, GetTransaction, GetCardPurchase, ListCardPurchases, GetCardInvoice, SearchTransactions |
| `CardManagerAdapter` | CreateCard, ListCards, GetCard, CountCards, BestPurchaseDay, UpdateCard, SoftDeleteCard, HasOpenInstallments |
| `BudgetPlannerAdapter` | CreateBudget, DeleteDraftBudget, ActivateBudget, CreateRecurrence, EditCategoryPercentage, GetMonthlySummary, ListAlerts, SuggestAllocation |
| `CategoriesReaderAdapter` | SearchDictionary, ResolveRootsBySlug, ListCategories |
| `RecurrenceManagerAdapter` | CreateRecurrence, UpdateRecurrence, DeleteRecurrence, ListRecurrences |

## 8.2 — Mapa Capacidade→Tool (3 Buckets)

Artefato versionado: `.specs/prd-mecontrola-agent-tools/capacity-tool-map.md`

| Bucket | Count | Gap |
|--------|-------|-----|
| 1 — Totalmente implementadas | 25 | 0 |
| 2 — Parcialmente implementadas | 0 | 0 |
| 3 — Não expostas | 0 | 0 |

Todas as 25 tools estão no Bucket 1: tool existe, binding existe, wired em `buildFinancialTools`.

## 8.3 — Gate go.mod

### `go mod verify`

```
all modules verified
```

### `go build ./...`

```
(sem saída — zero erros)
```

### `go vet ./...`

```
(sem saída — zero erros)
```

## Arquivos Produzidos

- `.specs/prd-mecontrola-agent-tools/capacity-tool-map.md` — mapa versionado
- `.specs/prd-mecontrola-agent-tools/8.0_execution_report.md` — este relatório
- `.specs/prd-mecontrola-agent-tools/.checkpoints/8.0.yaml` — checkpoint da tarefa

## Conclusão

Tarefa 8.0 concluída com sucesso. 25 tools no Bucket 1, 0 gaps abertos, todos os gates de compilação verdes.
