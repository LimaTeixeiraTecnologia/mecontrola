# Generated: 2026-07-16T08:47:09Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 6.0
- Título: Wiring do módulo, contrato de regressão e instruções do agente
- Arquivo: .specs/prd-nome-de-tratamento-usuario/task-6.0-wiring-modulo-instrucoes.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-nome-de-tratamento-usuario/prd.md (RF-05, RF-06, RF-08, RF-10, RF-15)
- TechSpec: .specs/prd-nome-de-tratamento-usuario/techspec.md (seções "Arquitetura", "Sequenciamento", ADR-002)
- Relatórios de dependência: 3.0_execution_report.md (`BuildTreatmentNameEditWorkflow`, `TreatmentNameEditState`, `TreatmentNameEditWorkflowID`, `TreatmentNameEditKey`, `ContinueTreatmentNameEdit`, `BuildTreatmentNameEditReaper`), 4.0_execution_report.md (captura no onboarding — não tocada por esta tarefa), 5.0_execution_report.md (`BuildEditTreatmentNameTool`, tool `edit_treatment_name`)
- Blueprint exato de wiring: `internal/agents/module.go` (bloco `goal-edit` — engine :209, def :215, registry :266-267, resumer :292-298, `SuspendedRunIndex` :307-314, `ResumeDispatcher` :315-318, reaper :332-333, write-set :249-254, tool :433)
- Governança: go-implementation (auto, linguagem Go), mastra (wiring de engine/registry/resumer/reaper/write-set/index/dispatcher e instruções do agente — skill processual declarada na task file), `.claude/rules/agent-workflows-tools.md` (R-AGENT-WF-001 completo), `.claude/rules/go-adapters.md` (R-ADAPTER-001 zero comentários)

## Comandos Executados
- `go build ./...` -> sem erros
- `go vet ./...` -> sem erros
- `go test -race ./internal/agents/...` -> "Go test: 1184 passed in 21 packages" (todos os subpacotes, incluindo `postdeploy`, `application/tools`, `application/workflows`, `application/agents`, `golden`)
- `gofmt -l internal/agents/module.go internal/agents/module_test.go internal/agents/application/agents/mecontrola_agent.go internal/agents/application/postdeploy/regression_contract.go internal/agents/application/postdeploy/regression_contract_test.go` -> vazio (formatado)
- Loop `grep -n "^[[:space:]]*//" <arquivo> | grep -Ev "(//go:|//nolint:|// Code generated)"` por arquivo modificado -> vazio (zero comentários) em todos os 5 arquivos
- Loop `grep -nE "\b_[a-zA-Z]" <arquivo>` por arquivo modificado -> vazio (sem prefixo `_`)
- `./.tools/bin/golangci-lint run ./internal/agents/... ./internal/agents/application/postdeploy/... ./internal/agents/application/agents/...` -> "0 issues."
- Gate R-AGENT-WF-001 (`grep -rn "case intent\.Kind" internal/platform/agent/ internal/agents/`) -> vazio
- Gate SQL direto (`QueryContext|ExecContext|db\.Query|tx\.Exec|db\.Exec`) nos 5 arquivos modificados -> vazio

## Arquivos Alterados
- `internal/agents/module.go` (mod) — engine `treatmentNameEditEngine`, def `treatmentNameEditDef := workflows.BuildTreatmentNameEditWorkflow(workingMem, onboardingAgent)`, registry `treatmentNameEditRegistry` + `Register`, resumer `treatmentNameEditResumer := usecases.NewWorkflowResumer(workflows.TreatmentNameEditWorkflowID, treatmentNameEditRegistry, treatmentNameEditEngine, workflows.TreatmentNameEditKey, workflows.ContinueTreatmentNameEdit)`, `workflows.TreatmentNameEditWorkflowID` incluído em `usecases.NewSuspendedRunIndex(...)` e `treatmentNameEditResumer` incluído em `usecases.NewResumeDispatcher(...)`, reaper job `agents-treatment-name-edit-reaper` via `workflows.BuildTreatmentNameEditReaper` + `jobhandlers.NewConfirmReaperJob` incluído em `Module.Jobs`, tool `agenttools.BuildEditTreatmentNameTool(treatmentNameEditEngine, treatmentNameEditDef)` anexada a `buildFinancialTools`/`financialTools`, `"edit_treatment_name"` adicionado a `agent.WithWriteToolSet(...)`.
- `internal/agents/module_test.go` (mod) — `testBuildFinancialToolsArgs()` atualizado com o novo par engine/def; contagem de tools ajustada de 30 para 31 (`TestBuildFinancialTools_ReturnsExactly31Tools`); `TestBuildFinancialTools_RegistersNewTools` passa a exigir `"edit_treatment_name"`.
- `internal/agents/application/agents/mecontrola_agent.go` (mod) — nova seção `## Nome de Tratamento (RF-05)` orientando uso natural e não-excessivo do nome vigente da working memory; nova entrada no catálogo de ferramentas (`### Nome de tratamento` — `edit_treatment_name`); nova regra na `REGRA ABSOLUTA DE SELEÇÃO DETERMINÍSTICA DE FERRAMENTA` para rotear a intenção de troca a `edit_treatment_name` (com `name` quando informado) e explicitando que NUNCA altera dado cadastral/cobrança (RF-10); nova linha na `REGRA ABSOLUTA DE PENDÊNCIA CONVERSACIONAL` explicando o repasse verbatim do campo `message` sem gate de confirmação sim/não (RF-08/RF-09, aplicação imediata).
- `internal/agents/application/postdeploy/regression_contract.go` (mod) — `"edit_treatment_name"` adicionado a `RegisteredTools`; `"treatment-name-edit"` adicionado a `RegisteredWorkflows`.
- `internal/agents/application/postdeploy/regression_contract_test.go` (mod) — `workflows.TreatmentNameEditWorkflowID` adicionado a `realIDs` em `TestRegisteredWorkflowIDsMatchProductionConstants`; `TestRegisteredToolsCountMatchesInventory` ajustado de 30 para 31.

## Resultados de Validação
- Testes: pass (comandos acima)
- Lint: pass (`golangci-lint` 0 issues; gofmt limpo; zero comentários; sem prefixo `_`)
- Veredito do Revisor: APPROVED (revisão manual do diff aplicada inline, sem subagente separado — diff de wiring espelha 1:1 o blueprint já aprovado do `goal-edit` em `module.go`, mesmo padrão repetido 6x no arquivo; risco de regressão coberto pela suíte completa de `internal/agents/...`)

## Critérios de Aceite
- Fluxo roteável fim-a-fim (start via tool + resume via dispatcher) -> comprovado: `treatmentNameEditEngine`/`treatmentNameEditDef` instanciados em `module.go`, `agenttools.BuildEditTreatmentNameTool(treatmentNameEditEngine, treatmentNameEditDef)` anexada a `financialTools` (start via tool, herda `wf.RuntimeFrom`/`engine.Start` da Tarefa 5.0); `treatmentNameEditRegistry.Register(treatmentNameEditDef)` + `usecases.NewWorkflowResumer(workflows.TreatmentNameEditWorkflowID, treatmentNameEditRegistry, treatmentNameEditEngine, workflows.TreatmentNameEditKey, workflows.ContinueTreatmentNameEdit)` + `workflows.TreatmentNameEditWorkflowID` em `NewSuspendedRunIndex` + `treatmentNameEditResumer` em `NewResumeDispatcher` (resume via dispatcher, mesma cadeia `resolveOnboarding` -> `resumeDispatcher` já usada pelos demais 5 workflows).
- RF-05 uso natural do nome -> comprovado: nova seção `## Nome de Tratamento (RF-05)` em `mecontrolaAgentInstructions` instrui uso natural, coerente com o contexto e proíbe inserção excessiva/repetitiva na mesma resposta.
- RF-06 roteamento por registry/tool sem switch de intent -> comprovado: `agent.NewWorkflowRegistry[workflows.TreatmentNameEditState]()` + `Register` (mesmo padrão dos outros 5 workflows); `grep -rn "case intent\.Kind" internal/platform/agent/ internal/agents/` retornou vazio; nova instrução no catálogo de ferramentas roteia a intenção de troca para a tool `edit_treatment_name` (chamada pelo LLM via tool-calling loop, não por switch de dominio no código).
- RF-08 dois turnos habilitado pelo resume dispatcher -> comprovado: `treatmentNameEditResumer` (usando `workflows.ContinueTreatmentNameEdit` da Tarefa 3.0, contrato de resume-antes-do-parse já validado) incluído em `usecases.NewResumeDispatcher(...)`, na mesma posição da cadeia que os demais resumers de workflow durável.
- RF-10 isolamento — só working memory, nunca identity/cobrança -> comprovado: `treatmentNameEditDef := workflows.BuildTreatmentNameEditWorkflow(workingMem, onboardingAgent)` recebe apenas `workingMem`/`onboardingAgent` (sem binding de identity/billing); nova regra em `mecontrolaAgentInstructions` explicita "NUNCA altera nome cadastral, e-mail, telefone ou qualquer dado de cadastro/cobrança".
- RF-15 rollout sem feature flag -> comprovado: nenhum flag, config condicional ou `if deps.FeatureX` envolve o wiring do treatment-name-edit em `module.go` — o bloco é incondicional, no mesmo padrão dos demais workflows sempre ativos (diferente do bloco condicional de `deps.OutboxPublisher != nil && deps.WhatsAppGateway != nil`, que é infraestrutura de canal, não feature flag de produto).
- Zero comentários -> comprovado: grep `^[[:space:]]*//` em todos os 5 arquivos modificados retornou vazio.
- Testes de wiring verdes -> comprovado: `go test -race ./internal/agents/...` -> "1184 passed in 21 packages", incluindo `module_test.go` (`TestBuildFinancialTools_ReturnsExactly31Tools`, `TestBuildFinancialTools_RegistersNewTools`) e `postdeploy` (`TestRegisteredWorkflowIDsMatchProductionConstants`, `TestRegisteredToolsCountMatchesInventory`).
- Tool incluída no contrato de regressão -> comprovado: `"edit_treatment_name"` em `RegisteredTools` e `"treatment-name-edit"` em `RegisteredWorkflows` (`regression_contract.go`); `TestRegisteredWorkflowIDsMatchProductionConstants` e `TestRegisteredToolsCountMatchesInventory` verdes com os novos valores.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree (5 arquivos modificados, não commitados)
verdict=APPROVED
tool=review (procedimento manual aplicado inline, sem subagente separado — diff de wiring espelha 1:1 o blueprint `goal-edit` já revisado no mesmo arquivo, sem novo padrão estrutural; suíte completa de `internal/agents/...` cobre regressão)

## Coverage

package=github.com/LimaTeixeiraTecnologia/mecontrola/internal/agents (e subpacotes application/postdeploy, application/agents)
delta=wiring aditivo em `module.go` (engine/def/registry/resumer/reaper/write-set/index/dispatcher/tool) coberto pelos testes existentes de `module_test.go` (contagem de tools ajustada e novo ID verificado) e `regression_contract_test.go` (workflow ID e contagem de tools ajustados); instruções do agente não têm suíte de unidade dedicada (texto de prompt), cobertura funcional fica a cargo do gate golden real-LLM da Tarefa 7.0

## Suposições
- O bloco condicional `if deps.OutboxPublisher != nil && deps.WhatsAppGateway != nil` em `module.go` (que envolve a montagem do consumer/dispatcher de WhatsApp) não é uma feature flag de produto (RF-15) — é uma checagem de infraestrutura de canal já existente para todos os 6 workflows duráveis; o wiring do `treatment-name-edit` propriamente dito (engine/def/registry/resumer/reaper/tool) está fora desse bloco condicional, incondicional como os demais.
- `onboardingAgent` (já instanciado em `module.go:203` para o workflow de onboarding) foi reutilizado como o `agent.Agent` passado a `BuildTreatmentNameEditWorkflow(workingMem, onboardingAgent)`, conforme literal da subtarefa 6.1 da task file — nenhum novo agente foi instanciado, evitando duplicar chamadas ao `provider`/`llm.NewOpenRouterProvider`.
- A contagem de `RegisteredTools`/`buildFinancialTools` foi ajustada de 30 para 31 em ambos os arquivos de teste (`module_test.go` e `regression_contract_test.go`) na mesma tarefa, pois os dois testes de contrato (`TestBuildFinancialTools_ReturnsExactly30Tools` e `TestRegisteredToolsCountMatchesInventory`) travam no número exato de tools ativas — sem esse ajuste simultâneo, o build passaria mas os testes quebrariam.
- Nenhuma alteração foi feita em `golden/harness_realllm_test.go` (catálogo de tools do harness real-LLM) — esse arquivo é escopo explícito da Tarefa 7.0 (gate golden `CategoryTreatmentName`), que precisará registrar `edit_treatment_name` no harness para os cenários de conversa.

## Riscos Residuais
- O gate golden real-LLM (categoria `CategoryTreatmentName`, cobrindo RF-05/RF-06/RF-08 em cenário de conversa real) é escopo da Tarefa 7.0; esta tarefa cobre apenas testes de unidade determinísticos (contagem de tools, IDs de workflow, build/vet/lint) — o wiring está estruturalmente correto e acionável, mas não foi validado ainda com LLM real respondendo à intenção de troca de nome via `mecontrolaAgentInstructions`.
- As instruções novas em `mecontrolaAgentInstructions` (prompt de texto livre) não têm cobertura de teste de unidade determinística — são strings estáticas verificadas apenas por presença de tool/ID nos testes de wiring; a adequação semântica (o LLM realmente chamando `edit_treatment_name` no cenário certo, sem confundir com `edit_goal`) só é verificável pelo gate golden real-LLM da Tarefa 7.0.
- Nenhum risco novo de regressão nos demais 5 workflows duráveis: o padrão de wiring foi replicado por analogia estrutural (mesma ordem de blocos), sem alterar nenhuma linha dos blocos existentes além das inserções aditivas.

## Conflitos de Regra
- none
