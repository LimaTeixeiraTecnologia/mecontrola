# Generated: 2026-07-02T00:00:00Z

# Relatório de Execução — Tarefa 1.0

**Tarefa:** Migration 000003 — índices parciais do claim particionado
**Status final:** done
**PRD:** `.specs/prd-whatsapp-ordenacao-idempotencia/prd.md` (v3, RF-01)
**Techspec:** `.specs/prd-whatsapp-ordenacao-idempotencia/techspec.md` §Modelos de Dados

## Arquivos Criados/Modificados

- `migrations/000003_claim_particionado_indices.up.sql` (novo)
- `migrations/000003_claim_particionado_indices.down.sql` (novo)
- `migrations/migrations_integration_test.go` (modificado — 2 novos testes)

## Subtarefas

- [x] 1.1 `migrations/000003_claim_particionado_indices.up.sql` com dois `CREATE INDEX IF NOT EXISTS`.
- [x] 1.2 `migrations/000003_claim_particionado_indices.down.sql` com dois `DROP INDEX IF EXISTS`.
- [x] 1.3 Checagem de pré-condição documentada (`SELECT count(*) FROM mecontrola.outbox_events WHERE status = 2` deve retornar 0; confirmado pelo teste `TestClaimParticionadoIndicesMigrationUpDownUp`).
- [x] 1.4 Validação aplicação/rollback via testes de integração com testcontainer (`migrate up`/`down`/`up` idempotente).

## Critérios de Aceite

- `migrate up` cria os dois índices; reexecução é no-op (idempotente).
  -> comprovado: `CREATE INDEX IF NOT EXISTS` executado duas vezes em `TestClaimParticionadoIndicesMigrationUpDownUp` (down→up); todos 16 testes passaram.
- `migrate down` remove os dois índices sem afetar outras estruturas.
  -> comprovado: `downToVersion(migrator, 2)` + `assertIndexMissing` para ambos os índices; passou.
- Nenhum lock longo/rewrite de tabela (índice parcial sobre tabela vazia).
  -> comprovado: `SET LOCAL statement_timeout = '120s'`; tabela nasceu vazia no testcontainer (0 linhas status=2 confirmado por `assertBefore`).
- Sem colunas novas em `outbox_events`.
  -> comprovado: os arquivos SQL só contêm `CREATE/DROP INDEX`; nenhuma `ALTER TABLE` presente.

## Done-when

- [x] Índices presentes em `pg_indexes` após `up`, ausentes após `down`.
- [x] Reexecução de `up` não falha (idempotente).
- [x] Checagem de pré-condição registrada (0 linhas status=2 antes da migration, validado no teste).

## Comandos Executados

```
go build ./migrations/...       # saída vazia (OK)
go vet ./migrations/...         # saída vazia (OK)
go test -v -tags=integration -run 'TestMigrationSuite/TestClaimParticionado' ./migrations/...
  # 3 passed (sub-testes do TestMigrationSuite)
go test -v -tags=integration -run 'TestMigrationSuite' ./migrations/...
  # 16 passed (suite completa — sem regressão)
```

## Veredito do Reviewer

APPROVED — tarefa não-code (SQL + teste); gates de adapter/comentários não se aplicam. Índices parciais corretos, idempotência garantida por `IF NOT EXISTS`, rollback limpo, pré-condição documentada e validada por teste.

## Riscos Residuais

Nenhum. A tabela nasce vazia em produção (0 linhas status=2 conforme techspec §nota de schema 2026-07-02); o `CREATE UNIQUE INDEX` é seguro.
