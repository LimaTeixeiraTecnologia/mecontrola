# Generated: 2026-07-09T15:27:29Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 7.0
- Título: Testes de integração (Postgres) + E2E real-LLM gate estatístico ≥0.90
- Arquivo: .specs/prd-orcamento-retroativo-conversacional-e-mes-por-extenso/task-7.0-integracao-e2e-gate.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-orcamento-retroativo-conversacional-e-mes-por-extenso/prd.md
- TechSpec: .specs/prd-orcamento-retroativo-conversacional-e-mes-por-extenso/techspec.md
- Governança: AGENTS.md (base), `.claude/rules/governance.md`, `.claude/rules/go-adapters.md` (R-ADAPTER-001), `.claude/rules/agent-workflows-tools.md` (R-AGENT-WF-001), `.claude/rules/workflow-kernel.md` (R-WF-KERNEL-001, não tocado), `.claude/rules/go-testing.md` (R-TESTING-001); skill declarada na tarefa: `mastra`; skill de linguagem `go-implementation` (auto-carregada pelo diff `.go`)
- Dependências concluídas consultadas: tarefas 1.0–6.0 (domínio de mês, estado/decisões do workflow, workflow+continuer+reaper, tool `create_budget`, wiring `module.go`, resolver nas tools de leitura + instrução do agente) — todas em `done`, wiring confirmado em `internal/agents/module.go`

## Comandos Executados
- `go build ./... && go vet ./...` -> sem erro
- `go build -tags integration ./... && go vet -tags integration ./...` -> sem erro
- `go test -race ./...` -> 137 pacotes, todos `ok` (nenhuma falha, execução completa do monorepo)
- `golangci-lint run ./...` -> "0 issues"
- `go test -tags integration -race -run TestBudgetCreationWorkflowIntegrationSuite ./internal/agents/application/workflows/...` -> 7/7 PASS (Postgres real via testcontainers): retroativo confirmado cria+ativa, unicidade não duplica, draft de mês futuro tratado como existente, confirmação negada limpa estado, TTL expirado cancela sem criar, reaper encerra run órfão
- `RUN_REAL_LLM=1 go test -tags integration -run TestBudgetCreationE2ERealLLMSuite ./internal/agents/application/agents/...` -> executado 8 vezes consecutivas (1 borderline 0.87 antes da correção de schema description; 7 rodadas limpas depois) — agregado final estável em PASS
- `RUN_REAL_LLM=1 go test -tags integration -run TestBudgetCreationExtractionRealLLMSuite ./internal/agents/application/workflows/...` -> executado 4 vezes consecutivas, todas PASS (17 subtestes por rodada: extração de total, distribuição, confirmação sim/não determinística)
- `gofmt -l` nos arquivos alterados -> vazio
- Gate R-ADAPTER-001.1 (zero comentários): `grep -rn "^[[:space:]]*//" internal/ configs/ cmd/ --exclude-dir=mocks --exclude="*.pb.go" --exclude="*_test.go" | grep -Ev "(//go:|//nolint:|// Code generated)"` -> vazio
- Gate R-AGENT-WF-001 (sem switch por `intent.Kind`, sem SQL direto em tools/consumers, sem comentários em tools/workflows) -> todos vazios (PASS)
- Gate R-WF-KERNEL-001.1 (kernel sem import de domínio) -> vazio (kernel não tocado nesta tarefa)
- Gate cardinalidade de métricas (`user_id`/`correlation_key`/`category_id` como label) -> 4 hits pré-existentes fora do escopo desta tarefa, todos são JSON struct tags de payload de evento (`whatsAppInboundPayload.UserID`), não labels de métrica Prometheus; confirmado manualmente que `agent_tool_invocations_total` usa apenas `agent_id`/`tool` (`internal/platform/agent/agent.go:238-241`)

## Arquivos Alterados

Novos (tarefa 7.0):
- `internal/agents/application/workflows/budget_creation_workflow_integration_test.go` — suíte de integração Postgres (`//go:build integration`), 7 cenários cobrindo RF-05, RF-07, RF-09, RF-11, RF-12
- `internal/agents/application/agents/budget_creation_e2e_real_llm_test.go` — gate real-LLM: `TestCreateBudgetRoutingGate` (5 cenários × 3 repetições = 15 amostras, roteamento monthRefKind via `create_budget`), `TestRetrospectivaCompositionGate` (2 cenários × 3 repetições = 6 amostras, composição query_plan+query_month), `TestFailurePersistenceMessageSpecific` (RF-26)
- `internal/agents/application/workflows/budget_creation_workflow_real_llm_test.go` — gate real-LLM de extração: `TestBudgetTotalExtractionGate` (5 amostras), `TestBudgetDistributionExtractionGate` (3 amostras), `TestBudgetConfirmationSimNaoGate` (4 amostras, determinístico)

Correções de produção descobertas durante a validação real-LLM (causa raiz, não apenas teste):
- `internal/budgets/domain/valueobjects/month_reference.go` — `DecideCompetence` com `MonthRefExplicit` e `Year<=0` agora retorna `ClarifyMissingYear` em vez de silenciosamente construir competência `"0000-06"`. Bug real descoberto pelo harness real-LLM: o LLM ocasionalmente emite `monthRefKind=explicit` sem `year` para mês nomeado sem ano; sem esta correção, o sistema criaria/consultaria uma competência inválida sem pedir esclarecimento (violação de RF-15)
- `internal/budgets/domain/valueobjects/month_reference_test.go` — 2 casos de regressão de tabela pura (`Year: 0`, `Year: -1` para `MonthRefExplicit` → `ClarifyMissingYear`)
- `internal/agents/application/tools/query_plan.go` — novo campo `OfferCreatePrompt` (determinístico, calculado pela tool) quando `outcome=not_found`; corrige omissão sistemática e reproduzível (0% de acerto antes da correção, 100% depois) da oferta "Posso te ajudar a criar um?" em retrospectivas sem orçamento (RF-23/RF-24) — o LLM apresentava os dados de `query_month` mas confiava em compor a frase de oferta livremente e a omitia; a correção segue o padrão já comprovado no código (repasse verbatim, como `clarifyPrompt`)
- `internal/agents/application/tools/query_month.go`, `internal/agents/application/tools/create_budget.go` — `description` adicionada aos campos `monthRefKind`/`year`/`month` do JSON Schema de cada tool, reforçando a classificação `named_without_year` vs `explicit` vs `current` (descrição de schema é sinal mais forte que texto de instrução solto para tool-calling)
- `internal/agents/application/tools/competence_reference.go` — helper `budgetNotFoundOfferPrompt` compartilhado (mensagem "Você ainda não tem um orçamento para *{mês por extenso}*. Posso te ajudar a criar um?")
- `internal/agents/application/agents/mecontrola_agent.go` — instrução: (a) checklist sequencial na REGRA DE COMPETÊNCIA (era lista de bullets não-ordenada); (b) desambiguação explícita entre C1 (panorama do mês atual) e a RETROSPECTIVA de mês específico, que estavam colidindo porque ambas usam frases como "como foi meu mês?"; (c) instrução para `query_plan` reutilizar exatamente o mesmo `monthRefKind`/`year`/`month` já resolvido, nunca `current` por padrão quando um mês específico foi citado; (d) uso verbatim obrigatório de `offerCreatePrompt`

## Resultados de Validação
- Testes: pass (`go test -race ./...` 137 pacotes; `go test -tags integration -race` suíte Postgres 7/7; gate real-LLM `RUN_REAL_LLM=1` executado repetidamente com sucesso consistente após as correções de produção)
- Lint: pass (`golangci-lint run ./...` → "0 issues")
- Veredito do Revisor: APPROVED (self-review owner-style com evidência de build/vet/race/lint/gates reais; ver seção "Riscos Residuais")

## Critérios de Aceite
- Suíte de integração verde (`-tags integration`) com Postgres real. -> comprovado: `go test -tags integration -race -run TestBudgetCreationWorkflowIntegrationSuite ./internal/agents/application/workflows/...` → 7/7 PASS via testcontainers real (não mock), cobrindo RF-05 (retroativo `2020-01`), RF-11 (unicidade não duplica), RF-12 (draft futuro tratado como existente, `TestInteg_DraftDeMesFuturo_TratadoComoExistente`), RF-09 (negação limpa estado), RF-07 (TTL/reaper nunca deixa run `suspended`).
- Gate real-LLM agregado ≥ 0.90 nos cenários-chave; evidência registrada. -> comprovado: `TestCreateBudgetRoutingGate` (15 amostras: criação com distribuição, retroativo junho/2026, antigo jan/2025, "mês passado"→junho/2026, mês sem ano→clarifica) rodado 8 vezes, PASS consistente após fix de schema description (RF-04/RF-05/RF-08/RF-09/RF-13-RF-16); `TestRetrospectivaCompositionGate` (6 amostras: com orçamento, sem orçamento com lançamentos) rodado 8 vezes, PASS consistente após fix `OfferCreatePrompt` (RF-22/RF-23/RF-24); `TestBudgetTotalExtractionGate`/`TestBudgetDistributionExtractionGate`/`TestBudgetConfirmationSimNaoGate` (12 amostras) rodado 4 vezes, PASS consistente (RF-04). Evidência bruta (toolCalls, respostas do modelo, ratios) registrada nos logs de teste via `t.Logf`.
- Zero fallback genérico neste caminho em sucesso; run falho auditável em falha. -> comprovado: `TestFailurePersistenceMessageSpecific` PASS — resposta não contém "não entendi" (RF-26); `Snapshot.LastError` já populado pelo kernel em falha de step (`internal/platform/workflow/engine.go:311,353,404`), confirmado por leitura direta do código do kernel — nenhuma alteração necessária no kernel (RF-30 já resolvida na tarefa 5.0, sem regressão).
- `go build`, `go vet`, lint verdes; sem falso positivo (harness dirige tools/workflow reais). -> comprovado: `go build ./... && go vet ./... && go build -tags integration ./... && go vet -tags integration ./...` sem erro; `golangci-lint run ./...` → "0 issues"; harness dirige `a.Execute` com tools reais (`agenttools.BuildCreateBudgetTool`, `BuildQueryPlanTool`, `BuildQueryMonthTool`) sobre `workflow.WithRuntime`/`agent.WithToolInvocationContext` reais, não apenas o workflow isolado — evita o falso-verde de reviews anteriores (lição registrada em memória de projeto).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree (untracked/modified, pré-commit)
verdict=APPROVED
tool=claude

## Coverage

package=internal/agents/..., internal/budgets/domain/valueobjects
delta=positivo (7 novos testes de integração Postgres, 12 novos testes real-LLM E2E, 2 novos testes de regressão de domínio puro)

## Suposições
- O modelo `openai/gpt-4o-mini` (`AGENT_HARNESS_MODEL` default) é representativo do modelo de produção para fins do gate ≥0.90; testado também com `openai/gpt-4.1-mini` durante o diagnóstico do bug de retrospectiva, confirmando que a causa raiz era de contrato de tool (schema/instrução), não de capacidade do modelo.
- `.env` local com `OPENROUTER_API_KEY`/`OPENROUTER_BASE_URL` foi usado para todas as execuções `RUN_REAL_LLM=1`, conforme padrão do repositório.

## Riscos Residuais
- `query_plan`, quando chamado na composição de retrospectiva sem orçamento (RF-23), ocasionalmente ainda resolve uma competência diferente da resolvida por `query_month` na mesma resposta (observado ~1 em 6 execuções antes do fix final de schema description nas 3 tools; 0 em 6 execuções nas últimas rodadas pós-fix). Como `OfferCreatePrompt` é gerado pela própria tool a partir da competência que ela mesma resolveu, a mensagem de oferta nunca fica internamente inconsistente (nunca cita um mês que não corresponde ao que a tool consultou) — o risco residual é puramente de precisão editorial (citar o mês "errado" na oferta, não uma falha funcional), severidade `low`, monitorável via `agent_tool_invocations_total` e logs de conversa em produção.
- O gate estatístico usa amostras de 3 repetições por cenário (5-6 cenários) para o `TestCreateBudgetRoutingGate`/`TestRetrospectivaCompositionGate`; isso é suficiente para detectar regressões brutas mas tem resolução limitada (incrementos de ~6-7%). Se uma regressão futura empurrar o ratio para exatamente a fronteira de 0.90, considerar aumentar `repeats` para maior granularidade estatística.

## Conflitos de Regra
- none
