# Generated: 2026-07-09T14:17:30Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 6.0
- Título: Resolver nas tools de leitura + instrução do agente + composição da retrospectiva
- Arquivo: .specs/prd-orcamento-retroativo-conversacional-e-mes-por-extenso/task-6.0-resolver-tools-instrucao-retrospectiva.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-orcamento-retroativo-conversacional-e-mes-por-extenso/prd.md
- TechSpec: .specs/prd-orcamento-retroativo-conversacional-e-mes-por-extenso/techspec.md
- ADRs: adr-002-resolucao-deterministica-mes-monthreference.md, adr-003-formatacao-mes-por-extenso.md, adr-004-retrospectiva-composicao-tools-existentes.md
- Governança: AGENTS.md (base), `.claude/rules/governance.md`, `.claude/rules/go-adapters.md` (R-ADAPTER-001), `.claude/rules/agent-workflows-tools.md` (R-AGENT-WF-001); skill declarada na tarefa: `mastra`; skill de linguagem `go-implementation` (auto-carregada pelo diff `.go`)
- Dependências concluídas consultadas: `internal/budgets/domain/valueobjects/{month_reference.go, competence.go}` (tarefa 1.0 — `DecideCompetence`, `FormatCompetencePtBR`, `Competence.Prev()`); `internal/agents/application/tools/create_budget.go` (tarefa 4.0 — molde de schema/mapeamento `MonthReference` reutilizado)
- Execução concorrente observada e respeitada: tarefa 5.0 (mesmo momento) altera `internal/agents/module.go`, `whatsapp_inbound_consumer.go`, `budget_planner_adapter.go`, `interfaces/errors.go` — nenhum desses arquivos foi tocado por esta execução

## Comandos Executados
- `bash -c 'export AI_INVOCATION_DEPTH=0; source .agents/lib/check-invocation-depth.sh; ...'` -> depth=1 (dentro do limite)
- `go build ./internal/agents/application/... ./internal/budgets/...` -> sem erro
- `go vet ./internal/agents/application/... ./internal/budgets/...` -> sem erro
- `gofmt -l internal/agents/application/tools/query_month.go internal/agents/application/tools/query_plan.go internal/agents/application/tools/competence_reference.go internal/agents/application/agents/mecontrola_agent.go internal/agents/application/agents/mecontrola_agent_test.go internal/agents/application/tools/financial_tools_test.go` -> vazio (formatado)
- `go test ./internal/agents/application/tools/... -run "TestBuildQueryMonth|TestBuildQueryPlan" -v` -> 14/14 PASS
- `go test -race ./internal/agents/application/tools/... ./internal/agents/application/agents/... ./internal/budgets/...` -> todos os pacotes PASS (1383 testes agregados, sem regressão, `go test -race ./internal/agents/application/... ./internal/budgets/...`)
- `golangci-lint run internal/agents/application/tools/... internal/agents/application/agents/... internal/budgets/...` -> "No issues found"
- `bash deployment/scripts/lint-deadcode.sh` -> "PASS lint:deadcode: nenhum codigo morto acionavel em internal/agents" (gate de outro PRD reaproveitado como validação adicional; sem necessidade de allowlist — helpers de `competence_reference.go` são alcançáveis via `query_month.go`/`query_plan.go`, já wired em `module.go` desde antes desta tarefa)
- Gate R-ADAPTER-001.1 (zero comentários): `grep -rn --include="*.go" --exclude-dir=mocks --exclude="*.pb.go" --exclude="*_test.go" "^[[:space:]]*//" internal/agents/application/tools/query_month.go internal/agents/application/tools/query_plan.go internal/agents/application/tools/competence_reference.go internal/agents/application/agents/mecontrola_agent.go | grep -Ev "(//go:|//nolint:|// Code generated)"` -> vazio
- Gate SQL direto: `grep -n "QueryContext\|ExecContext\|db\.Query\|tx\.Exec\|db\.Exec" internal/agents/application/tools/query_month.go internal/agents/application/tools/query_plan.go internal/agents/application/tools/competence_reference.go` -> vazio
- Gate R-AGENT-WF-001 (sem switch por `intent.Kind`): `grep -rn --include="*.go" --exclude-dir=mocks --exclude="*_test.go" "^[[:space:]]*case intent\.Kind" internal/platform/agent/ internal/agents/` -> vazio
- Self-review (owner-style, escopo estrito aos 6 arquivos da tarefa 6.0, ver seção "Veredito do Revisor" abaixo)

## Arquivos Alterados
- `internal/agents/application/tools/query_month.go` (modificado — aceita `monthRefKind`/`year`/`month` estruturado, resolve via `resolveCompetenceReference`, preserva fallback para mês corrente, retorna `outcome=clarify` com `clarifyPrompt` quando a competência exige esclarecimento)
- `internal/agents/application/tools/query_plan.go` (modificado — idem; adicionalmente mapeia `interfaces.ErrBudgetNotFound` para `outcome=not_found` determinístico, condição necessária para a composição da retrospectiva sem orçamento, RF-23/RF-24)
- `internal/agents/application/tools/competence_reference.go` (novo — helper compartilhado `resolveCompetenceReference`/`competenceReferenceClarifyPrompt`/`currentCompetenceFallback`, evita duplicar a chamada a `DecideCompetence` entre as duas tools)
- `internal/agents/application/tools/financial_tools_test.go` (modificado — testes existentes de `query_month`/`query_plan` migrados do campo antigo `RefMonth`/`Competence` para `MonthRefKind`/`Year`/`Month`; 14 novos testes cobrindo fallback para mês corrente, `previous`/`next`, `named_without_year`→clarify, `unknown`→clarify, `monthRefKind` inválido→erro, e `not_found` determinístico em `query_plan`)
- `internal/agents/application/agents/mecontrola_agent.go` (modificado — instrução: (a) a rejeição de "mês passado"/"semana passada" na REGRA ABSOLUTA DE DATA passa a se aplicar apenas a `occurredAt` de lançamento, com nota explícita de que NÃO se aplica à competência de mês; (b) nova REGRA DE COMPETÊNCIA — MonthReference com instrução-por-exemplo para cada `monthRefKind` (current/previous/next/explicit/named_without_year/unknown), proibindo o LLM de calcular a data ele mesmo; (c) nova REGRA DE MÊS POR EXTENSO instruindo uso do texto por extenso em toda menção de competência; (d) nova regra RETROSPECTIVA PLANEJADO VS REALIZADO com os 5 passos de composição via `query_plan`/`query_month` conforme ADR-004; (e) catálogo de ferramentas atualizado citando `create_budget` como única forma de criar orçamento; (f) mensagem "Orçamento não encontrado" passa a citar `{mês por extenso}` e reforça o uso exclusivo de `create_budget`)
- `internal/agents/application/agents/mecontrola_agent_test.go` (modificado — 1 asserção pré-existente ajustada: `s.Contains(instructions, "America/Sao_Paulo")` → `s.Contains(instructions, "monthRefKind=current")`, refletindo que a instrução deixou de citar o fuso horário diretamente no bloco C1, pois a resolução de timezone passou a ser responsabilidade exclusiva de `DecideCompetence`/`time.Now().In(loc)` dentro da tool, não mais do texto de instrução)

## Resultados de Validação
- Testes: pass (`go test -race ./internal/agents/application/tools/... ./internal/agents/application/agents/... ./internal/budgets/...`; 1383 testes agregados nos pacotes tocados, sem regressão)
- Lint: pass (`golangci-lint run` → "No issues found"; `bash deployment/scripts/lint-deadcode.sh` → PASS)
- Veredito do Revisor: APPROVED (self-review owner-style, escopo estrito; sem achado `critical`/`high`/`medium`/`low`)

## Critérios de Aceite
- `go build`, `go vet`, `go test -race`, lint verdes. -> comprovado: `go build ./internal/agents/application/... ./internal/budgets/...` (exit 0), `go vet` idem (exit 0), `go test -race ./internal/agents/application/tools/... ./internal/agents/application/agents/... ./internal/budgets/...` (todos PASS), `golangci-lint run` → "No issues found".
- "Mês passado"/"mês que vem"/nomeado resolvem corretamente; sem ano → clarifica; toda menção de mês por extenso. -> comprovado: `resolveCompetenceReference` delega a `budgetsvo.DecideCompetence` (RF-13..RF-16, já testado por tabela pura na tarefa 1.0, `month_reference_test.go`); testes novos `TestBuildQueryMonthToolPreviousResolvesToPriorMonth`/`TestBuildQueryMonthToolNextResolvesToNextMonth` comprovam `previous`/`next` corretos via `AddDate` (sem depender do LLM calcular a data); `TestBuildQueryMonthToolNamedWithoutYearReturnsClarify`/`TestBuildQueryPlanToolNamedWithoutYearReturnsClarify` comprovam `outcome=clarify` sem chamar `GetMonthlySummary`/`ListMonthlyEntries`; instrução em `mecontrola_agent.go` (REGRA DE MÊS POR EXTENSO) determina uso do texto por extenso em toda saída, com `FormatCompetencePtBR` (tarefa 1.0) já disponível como autoridade de formatação.
- Retrospectiva compõe as tools existentes sem nova fonte de verdade; casos com/sem orçamento e sem lançamentos cobertos. -> comprovado: nenhuma tool nova criada (apenas `query_month.go`/`query_plan.go` modificadas); regra RETROSPECTIVA PLANEJADO VS REALIZADO em `mecontrola_agent.go` instrui: outcome=ok de `query_plan` → usa `plannedCents`/`spentCents`/`percentageSpent` já retornados (RF-20/RF-22, sem caso especial de "sem movimentação"); outcome=not_found → chama `query_month`, oferece `create_budget` com lançamentos (RF-23) ou sem lançamentos (RF-24); `TestBuildQueryPlanToolNotFoundReturnsCleanOutcome` comprova que `interfaces.ErrBudgetNotFound` retorna `outcome=not_found` limpo (sem erro de tool) para viabilizar essa composição determinística pela instrução.
- Zero comentários; tools permanecem finas. -> comprovado: grep de comentários (`^[[:space:]]*//` excluindo diretivas de compilador) retorna vazio nos 4 arquivos `.go` de produção alterados/criados; `query_month.go`/`query_plan.go` seguem o fluxo `adapter → usecase/interface` (delegam a `ledger.GetMonthlySummary`/`ledger.ListMonthlyEntries`/`planner.GetMonthlySummary`/`planner.ListAlerts`, já existentes) sem SQL direto, sem regra de negócio nova além da resolução de competência (que é puramente delegada a `DecideCompetence`, função pura de domínio).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree (untracked/modified, pré-commit)
verdict=APPROVED
tool=self-review (owner-style, ver `.claude/skills/review`; escopo estrito aos 6 arquivos de 6.0, excluindo module.go/whatsapp_inbound_consumer.go/budget_planner_adapter.go/interfaces/errors.go da tarefa 5.0 concorrente)

## Coverage

package=internal/agents/application/tools, internal/agents/application/agents
delta=+1 arquivo novo (competence_reference.go), 3 arquivos modificados (query_month.go, query_plan.go, mecontrola_agent.go) + 2 arquivos de teste atualizados; 14 cenários de teste novos/migrados cobrindo resolução current/previous/next/explicit, clarify (named_without_year/unknown), monthRefKind inválido, not_found determinístico (n/a — sem baseline de coverage percentual configurado no projeto)

## Suposições
- `interfaces.ErrBudgetNotFound` (pré-existente no HEAD, confirmado via `git show HEAD:internal/agents/application/interfaces/errors.go`) é o sinal correto de "orçamento não existe para a competência" — `query_plan.go` passou a mapear esse erro para `outcome=not_found` em vez de propagar como erro de tool, pois RF-22/RF-23/RF-24 exigem que a instrução do agente componha a retrospectiva deterministicamente a partir do outcome, não de uma falha de ferramenta.
- `Outcome`/`ClarifyPrompt` seguem o padrão de string simples já estabelecido em `create_budget.go`/`create_card.go` (não um tipo Go fechado), por consistência de contrato de schema JSON das tools existentes no módulo — RF-28 (state-as-type) é uma regra de domínio/workflow, não do contrato de saída JSON de tools de leitura.
- A rejeição de expressões vagas de data ("semana passada"/"mês passado") na REGRA ABSOLUTA DE DATA (occurredAt) foi preservada para lançamentos individuais (register_expense/register_income), pois o PRD desta fatia (RF-13..RF-17) trata exclusivamente de competência de orçamento/consulta, não da data de ocorrência de uma transação — mudar esse comportamento estaria fora do escopo RF-05/RF-13 e quebraria a REGRA ABSOLUTA DE CAMPOS OBRIGATÓRIOS existente.
- O ajuste em `mecontrola_agent_test.go` (troca de `America/Sao_Paulo` por `monthRefKind=current`) é a atualização mínima necessária para refletir que a resolução de timezone deixou de ser instruída em texto livre ao LLM e passou a ser responsabilidade exclusiva de `DecideCompetence`/`time.Now().In(loc)` dentro da tool (ADR-002) — o comportamento testado (roteamento determinístico C1 com as duas tools) permanece coberto pelas asserções `"C1"` e `"query_month E query_plan"`.

## Riscos Residuais
- A composição da retrospectiva (regra RETROSPECTIVA PLANEJADO VS REALIZADO) depende do LLM seguir os 5 passos de instrução corretamente — é best-effort por natureza (ADR-004 documenta esse trade-off e prevê promoção para tool dedicada `budget_retrospective` caso o gate real-LLM não feche). A validação estatística fica a cargo da tarefa 7.0 (gate ≥0.90).
- `query_month.go`/`query_plan.go` agora dependem de `internal/budgets/domain/valueobjects` (import cross-módulo `agents` → `budgets`), mesmo padrão já usado por `create_budget.go` (tarefa 4.0) — não é uma dependência nova introduzida por esta tarefa, apenas reaplicada.
- O wiring end-to-end (`module.go`, cadeia `try*`) está sendo concluído em paralelo pela tarefa 5.0; esta tarefa não tocou esses arquivos e não pode validar o fluxo completo isoladamente — a tarefa 7.0 é o gate de integração real-LLM que valida o conjunto 5.0+6.0.

## Conflitos de Regra
- none
