# Generated: 2026-06-30T12:33:00Z

# Execution Report — Task 2.0: Migration aditiva (índice telefone, timestamps, throttle)

## Status Final

`done`

## Arquivos Criados/Modificados

- `migrations/000005_activation_journey.up.sql` (novo)
- `migrations/000005_activation_journey.down.sql` (novo)
- `migrations/migrations_integration_test.go` (adicionados 2 testes + 2 helpers)

## Critérios de Aceite

- [x] Migration aplica e reverte sem erro em banco limpo e em banco com dados.
  -> comprovado: `TestActivationJourneyMigrationUpDown` PASS — up aplica índice, colunas e tabela; down (to version 4) remove todos os objetos; re-up recria corretamente.

- [x] Índice parcial e tabela de throttle criados conforme DDL da techspec.
  -> comprovado: `TestActivationJourneyMigrationUpDown` verifica `assertIndexPresent("mecontrola", "onboarding_tokens_mobile_activable_idx")` e `assertTablePresent("mecontrola.onboarding_activation_nomatch_throttle")` após up; verifica ausência após down.

- [x] Sem alteração destrutiva em `onboarding_tokens`.
  -> comprovado: DDL usa exclusivamente `ADD COLUMN IF NOT EXISTS` e `CREATE INDEX IF NOT EXISTS`. Nenhuma coluna existente é removida ou alterada. O down remove apenas os novos objetos adicionados pela migration 5.

## Testes Executados

```
go test -v -tags=integration -run "TestMigrationSuite/TestActivationJourney" ./migrations/... -timeout 120s

--- PASS: TestMigrationSuite/TestActivationJourneyMigrationUpDown (0.17s)
--- PASS: TestMigrationSuite/TestActivationJourneyThrottleTableConstraints (0.04s)
PASS
ok  	github.com/LimaTeixeiraTecnologia/mecontrola/migrations	3.135s
```

## Gate de Zero Comentários (R-ADAPTER-001.1)

```bash
grep -rn --include="*.go" --exclude-dir=mocks --exclude="*_test.go" \
  "^[[:space:]]*//" migrations/ | grep -Ev "(//go:|//nolint:|// Code generated)"
# saída vazia — PASS
```

## Subtarefas

- [x] 2.1 `migrations/000005_activation_journey.up.sql` criado com índice parcial, 4 ADD COLUMN e CREATE TABLE do throttle.
- [x] 2.2 `migrations/000005_activation_journey.down.sql` criado com drop da tabela, colunas e índice.
- [x] 2.3 Numeração sequencial correta: 000001..000004 já existiam; 000005 é o próximo número.

## Falhas Pré-existentes Confirmadas

`TestBaselineUpDownUp` falha com e sem os arquivos desta tarefa (verificado via `git stash`). Causa: migração 000004 dropa `onboarding_tokens` e o teste ainda espera sua existência. Não é regressão desta tarefa.

## Riscos Residuais

Nenhum.
