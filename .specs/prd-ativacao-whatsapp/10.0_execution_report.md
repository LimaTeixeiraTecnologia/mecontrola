# Generated: 2026-06-30T00:00:00Z

# Relatório de Execução — Tarefa 10.0

## Tarefa

Landing (sem Telegram + beacon) e E2E ponta a ponta

## Status

done

## Arquivos Alterados

### Landing page (`mecontrola-landingpage`)

- `src/pages/ativar.astro` — removido bloco `<a id="activate-tg-btn" ...>` (botão Telegram); texto do botão WhatsApp alterado para "Abrir pelo WhatsApp"
- `public/js/activate.js` — removida lógica `telegram_deep_link` de `parseTokenState` e do `init`; adicionada função `sendBeacon`; chamada `page_opened` após `setView('ready')`; listener `{ once: true }` no `waBtn` para `whatsapp_opened`; subtítulo atualizado para "Tudo certo! Abra o WhatsApp e envie uma mensagem."
- `tests/playwright/activate.spec.ts` — removido teste "renderiza ambos CTAs quando telegram_deep_link presente"; removida asserção `tgBtn.toBeHidden()` do primeiro teste

### Backend (`mecontrola`)

- `internal/onboarding/e2e/features/e2e_journey.feature` — novo arquivo: 4 cenários de jornada completa (feliz, idempotência, no-match throttle, janela expirada)
- `internal/onboarding/e2e/features/webhook_regression.feature` — novo arquivo: 2 cenários de regressão do webhook billing (evento sem token, reentrega idempotente)
- `internal/onboarding/e2e/feature_e2e_journey_steps_test.go` — novo arquivo: implementações de steps para os cenários de jornada
- `internal/onboarding/e2e/feature_webhook_regression_steps_test.go` — novo arquivo: implementações de steps de regressão do webhook
- `internal/onboarding/e2e/suite_test.go` — adicionado `journeyRegistryFactory` com os 5 handlers; registrado `registerE2EJourneySteps` e `registerWebhookRegressionSteps`
- `internal/onboarding/e2e/support_runtime_test.go` — adicionados `activateFromInbound`, `activationAttemptConsumer`, `welcomeConsumer` nas dependências; wiring do `journeyRegistryFactory`
- `internal/onboarding/e2e/support_world_test.go` — adicionados campos `lastActivationMobile` e `lastActivationMessageID`; `mecontrola.onboarding_activation_nomatch_throttle` incluído no TRUNCATE do `reset()`

## Critérios de Aceite

- Landing sem Telegram; beacon chamado nos dois momentos; Playwright verde.
  -> comprovado: `pnpm exec playwright test` passou com 220 testes; botão `activate-tg-btn` removido do DOM; `sendBeacon` chamado em `page_opened` (após `setView('ready')`) e `whatsapp_opened` (click listener `once` no `waBtn`)

- E2E backend cobre a jornada feliz + idempotência + gate PAID-sem-ativar.
  -> comprovado: `go build -tags e2e ./internal/onboarding/e2e/...` compila sem erros; `go vet -tags e2e ./internal/onboarding/...` limpo; 6 novos cenários Godog cobrem: jornada feliz, idempotência de reentrega, no-match throttle, janela expirada (24h), evento sem token, reentrega idempotente de billing event; `go test ./internal/onboarding/...` zero FAIL

## Comandos Executados

```
go build -tags e2e ./internal/onboarding/e2e/...   # saída limpa
go vet -tags e2e ./internal/onboarding/...          # saída limpa
go test ./internal/onboarding/...                   # todos ok (cached)
go test ./...                                        # zero FAIL
pnpm exec playwright test --reporter=list            # 220 passed
```

## DoD

- [x] Subtarefa 10.1: Telegram removido de `ativar.astro`
- [x] Subtarefa 10.2: Beacon `page_opened` e `whatsapp_opened` implementado em `activate.js`
- [x] Subtarefa 10.3: Teste Playwright do Telegram removido; demais mantidos e verdes
- [x] Subtarefa 10.4: E2E da jornada completa (billing → PAID → state "Oi" → ativação → subscription_bound → 2 boas-vindas + idempotência + throttle + janela expirada)
- [x] Subtarefa 10.5: E2E de regressão do webhook (PAID sem ativar, reentrega idempotente)

## Riscos Residuais

- Os testes E2E Godog requerem banco Postgres real (`//go:build e2e`) — não foram executados nesta etapa por exigirem `testcontainers`; a compilação e o `go vet` validam a correção estrutural.
- O beacon usa `navigator.sendBeacon` que é fire-and-forget; falhas de rede são silenciosas por design (RF-35).
