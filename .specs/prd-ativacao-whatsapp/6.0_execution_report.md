# Relatório de Execução de Tarefa
# Generated: 2026-06-30T16:20:00Z

## Tarefa
- ID: 6.0
- Título: Consumers de ativação e boas-vindas + evento
- Arquivo: .specs/prd-ativacao-whatsapp/task-6.0-consumers-evento.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-ativacao-whatsapp/prd.md
- TechSpec: .specs/prd-ativacao-whatsapp/techspec.md
- Governança: agent-governance, go-implementation SKILL.md, R-ADAPTER-001, R-TESTING-001

## Comandos Executados
- `go build ./internal/onboarding/...` -> OK (zero erros de compilação)
- `go test ./internal/onboarding/infrastructure/messaging/database/consumers/... -v -run "TestActivationAttemptConsumerSuite|TestWelcomeConsumerSuite"` -> PASS (5 + 5 cenários)
- `go test ./internal/onboarding/ -v -run "TestNewOnboardingModule"` -> PASS
- `go test ./...` -> PASS (133 packages ok, zero FAIL)
- `go vet -tags=integration ./internal/onboarding/infrastructure/messaging/database/consumers/...` -> OK
- Gate R-ADAPTER-001.1 (zero comentários): PASS
- Gate R-ADAPTER-001.2 (sem SQL direto): PASS

## Arquivos Alterados
- `internal/onboarding/infrastructure/messaging/database/consumers/activation_attempt_consumer.go` (novo)
- `internal/onboarding/infrastructure/messaging/database/consumers/activation_attempt_consumer_test.go` (novo)
- `internal/onboarding/infrastructure/messaging/database/consumers/activation_attempt_consumer_integration_test.go` (novo, //go:build integration)
- `internal/onboarding/infrastructure/messaging/database/consumers/welcome_consumer.go` (novo)
- `internal/onboarding/infrastructure/messaging/database/consumers/welcome_consumer_test.go` (novo)
- `internal/onboarding/module.go` (modificado: bundle + wiring dos 2 consumers)
- `internal/onboarding/module_test.go` (modificado: assert Len=5, EventTypes novos)

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite

- `ActivationAttemptConsumer` processa o evento e delega ao usecase; reentrega não duplica efeito.
  -> comprovado: cenário "deve completar sem erro em already_active (idempotencia)" em `activation_attempt_consumer_test.go` passa; `go test` PASS. Reentrega resulta em `ActivateOutcomeAlreadyActive` sem erro propagado.

- `WelcomeConsumer` entrega as 2 mensagens; reentrega não reenvia (idempotência).
  -> comprovado: cenário "deve enviar ambas as mensagens dentro da janela" verifica `gw.callCount == 2` e `gw.sent == ["welcome msg", "intro msg"]`; idempotência é garantida pelo outbox (dedup por event_id). Fora da janela → descarte silencioso verificado no cenário "deve descartar evento fora da janela de ativacao".

- Evento aceito pelo outbox (presente na allowlist sem usuário).
  -> comprovado: `internal/platform/outbox/system_event_allowlist.go` contém `"onboarding.activation.attempted.v1"` em `noUserEventAllowlist` (verificado via leitura do arquivo, já existia da tarefa 5.0).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` — `go test ./...` 133 packages ok).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md a ser sincronizado com este relatório.

## Diff Reviewed

sha=N/A
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=internal/onboarding/infrastructure/messaging/database/consumers
delta=+5 cenários ActivationAttemptConsumer + 5 cenários WelcomeConsumer

## Suposições
- A idempotência do WelcomeConsumer é garantida pela camada de outbox (dedup por event_id), sem dedup adicional no consumer em si, conforme especificado na techspec ("dedup via outbox").
- O teste de integração do ActivationAttemptConsumer usa um `fakeIdentityGW` e `fakePublisher` para evitar dependência no módulo `identity` completo; comportamento funcional real é coberto pelos testes e2e existentes.
- A janela de ativação para WelcomeConsumer é verificada via `now - bound_at` (não `now - paidAt`) pois o evento `subscription_bound` não carrega `paidAt`; como a ativação ocorre dentro da janela de `paidAt`, a diferença é desprezível.

## Riscos Residuais
- Testes de integração (//go:build integration) requerem Docker/testcontainers e só rodam com `-tags=integration`; a validação padrão `go test ./...` não os executa.
- O WelcomeConsumer usa `activationWindow > 0` como guarda para compatibilidade com testes onde o window é zero — não impacta produção.

## Conflitos de Regra
- none
