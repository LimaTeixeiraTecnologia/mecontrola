# Generated: 2026-06-30T13:10:00Z

# Relatório de Execução — Tarefa 5.0

## Tarefa
Usecase `ActivateFromInbound` + DTO `Validate()`

## Status
done

## Arquivos Criados/Modificados

### Criados
- `internal/onboarding/application/dtos/input/activate_from_inbound_input.go` — DTO com `Validate()` (R-DTO-VALIDATE-001)
- `internal/onboarding/application/usecases/activate_from_inbound.go` — Usecase com pipeline DMMF
- `internal/onboarding/application/usecases/activate_from_inbound_test.go` — Testes unitários (11 cenários)

### Modificados
- `internal/onboarding/application/dtos/input/errors.go` — erros `ErrPeerE164Required`, `ErrMessageIDRequired`
- `internal/onboarding/application/interfaces/magic_token_repository.go` — método `UpdateMarkActivationStartedAt`
- `internal/onboarding/application/interfaces/mocks/magic_token_repository.go` — mock do novo método
- `internal/onboarding/infrastructure/repositories/postgres/magic_token_repository.go` — implementação `UpdateMarkActivationStartedAt`
- `internal/platform/outbox/system_event_allowlist.go` — `onboarding.activation.attempted.v1` em `noUserEventAllowlist`
- `internal/onboarding/application/usecases/send_activation_email_test.go` — `stubMagicTokenRepository` implementa novo método

## Critérios de Aceite

- [x] DTO `Validate()` rejeita `PeerE164`/`MessageID` vazios.
  -> comprovado: `TestActivateFromInbound/TestValidate` — 4 cenários passam incluindo validação de ambos os campos

- [x] Cenário phone-matched: `FindActivableByMobile` → `BindAndConsume` → `ActivateOutcomePhoneMatched`
  -> comprovado: `deve_retornar_PhoneMatched_quando_token_encontrado_por_telefone` PASS

- [x] Cenário already-active (race): `UpdateMarkConsumed` retorna `ErrTokenAlreadyConsumedRace` → `ActivateOutcomeAlreadyActive`
  -> comprovado: `deve_retornar_AlreadyActive_quando_corrida_na_ativacao_por_telefone` PASS

- [x] Cenário token-matched (borda): texto contém token válido → `ConsumeMagicToken.Execute` → `ActivateOutcomeTokenMatched`
  -> comprovado: `deve_retornar_TokenMatched_quando_texto_contem_token_valido` PASS

- [x] Cenário already-active via token: `ConsumeMagicToken` retorna `AlreadyActive` → `ActivateOutcomeAlreadyActive`
  -> comprovado: `deve_retornar_AlreadyActive_quando_token_ja_consumido` PASS

- [x] Cenário no-match throttle permite: envia mensagem, retorna `ActivateOutcomeNoMatch`
  -> comprovado: `deve_retornar_NoMatch_e_enviar_mensagem_quando_throttle_permite` PASS

- [x] Cenário no-match throttle bloqueia: não envia mensagem, retorna `ActivateOutcomeNoMatch`
  -> comprovado: `deve_retornar_NoMatch_sem_enviar_mensagem_quando_throttle_bloqueia` PASS

- [x] `activation_started_at` marcado (set-once-if-null) no início da ativação por telefone
  -> comprovado: `UpdateMarkActivationStartedAt` chamado via mock no cenário phone-matched

- [x] `ActivateOutcome` é tipo fechado com `iota+1`
  -> comprovado: arquivo `activate_from_inbound.go` usa `type ActivateOutcome uint8` com `iota+1`

- [x] `onboarding.activation.attempted.v1` em `noUserEventAllowlist`
  -> comprovado: `internal/platform/outbox/system_event_allowlist.go` atualizado

- [x] Testes: `go test ./internal/onboarding/application/usecases/...` — PASS
  -> comando: `go test ./internal/onboarding/application/usecases/... -run TestActivateFromInbound -v`
  -> resultado: `PASS` (11/11 cenários)

- [x] Suite completa sem regressão: `go test ./... 2>&1 | grep FAIL` → vazio
  -> resultado: zero falhas em todo o projeto

## Métricas Implementadas
- `onboarding_activation_attempt_total{outcome}` — `phone_matched|token_matched|already_active|no_match`
- `onboarding_activation_window_expired_total` — quando token encontrado mas janela expirou

## Regras Verificadas
- R-ADAPTER-001.1: zero comentários em produção — OK
- R-DTO-VALIDATE-001: `Validate()` logo após `defer span.End()` — OK
- R-TESTING-001: testify/suite whitebox, `fake.NewProvider()`, IIFE por mock — OK
- R-TXN-004: métricas sem `user_id`/telefone como label — OK
- DMMF: `ActivateOutcome` tipo fechado, pipeline privado — OK

## Riscos Residuais
- Nenhum crítico. A interface `tokenActivator` é local ao pacote `usecases` (não exportada via `interfaces/`), adequado para consumer-side interface Go.
