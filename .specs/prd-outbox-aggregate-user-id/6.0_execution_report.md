# Generated: 2026-06-13T11:30:00Z

# Relatório de Execução — Tarefa 6.0: Atualizar 3 sites de onboarding + dispatcher whatsapp

## Status Final
`done`

## Arquivos Alterados

| Arquivo | Tipo de Alteração |
|---|---|
| `internal/onboarding/application/events/subscription_bound.go` | Populado `AggregateUserID: evt.UserID` em `outbox.EventInput` |
| `internal/onboarding/application/events/subscription_bound_test.go` | UserID atualizado para UUID válido; asserção `envelope.AggregateUserID` adicionada |
| `internal/platform/whatsapp/dispatcher/dispatcher.go` | Populado `AggregateUserID: userID` em `outbox.Event` no `publishAuthFailed` |
| `internal/platform/whatsapp/dispatcher/dispatcher_test.go` | Import `outbox` adicionado; asserção `AggregateUserID == userID.String()` no cenário rate_limited |
| `internal/platform/outbox/system_event_allowlist.go` | `auth.failed` adicionado à allowlist (ADR-004); comentário proibido removido |
| `internal/onboarding/application/usecases/consume_magic_token_test.go` | `UserID: "user-123"` → UUID válido |
| `internal/onboarding/application/usecases/try_fallback_activation_test.go` | `UserID: "uid-fallback"` → UUID válido |

## Subtarefas Executadas

- [x] 6.1 `subscription_binding.go` — apenas cabeamento para `events.NewSubscriptionBoundEvent`; sem EventInput direto. Fix aplicado em 6.2.
- [x] 6.2 `subscription_bound.go` — `AggregateUserID: evt.UserID` adicionado ao `outbox.EventInput`.
- [x] 6.3 `module.go` — inspecionado. Puro cabeamento de DI; não constrói `EventInput`. Nenhuma alteração necessária.
- [x] 6.4 `dispatcher.go` — `publishAuthFailed` constrói `outbox.Event` diretamente (bypassando `NewEvent`). Populado `AggregateUserID: userID` (string vazia para eventos de sistema sem usuário). `auth.failed` adicionado à allowlist (ADR-004) pois pode ser emitido sem userID (invalid_payload, stale_webhook).
- [x] 6.5 Testes atualizados: `subscription_bound_test.go` asserta `AggregateUserID`; `dispatcher_test.go` asserta `AggregateUserID == userID.String()` no cenário rate_limited. Testes de usecases corrigidos para usar UUID válidos.

## Critérios de Aceite

- [x] **RF-14 (onboarding + whatsapp populam AggregateUserID)**: comprovado — `subscription_bound.go` passa `evt.UserID`, dispatcher passa `userID` string.
  -> comprovado: `go test -count=1 ./internal/onboarding/application/events/...` PASS
- [x] **Asserções de teste cobrindo AggregateUserID (ou allowlist)**: comprovado — teste `TestPayloadGolden` asserta `envelope.AggregateUserID == testUserID`; dispatcher test asserta `evt.AggregateUserID == userID.String()` via `MatchedBy`.
  -> comprovado: `go test -count=1 ./internal/platform/whatsapp/dispatcher/...` PASS
- [x] **Inspeção module.go documentada**: comprovado — arquivo inspecionado, apenas cabeamento DI, sem construção de EventInput.
  -> comprovado: leitura manual confirmada
- [x] **Caso dispatcher whatsapp justificado**: comprovado — `auth.failed` é evento misto (per-user quando rate_limited, sistema quando invalid_payload/stale_webhook); allowlist ADR-004 cobre ausência de userID sem gerar warning falso-positivo.
  -> comprovado: `systemEventAllowlist["auth.failed"]` adicionado em `system_event_allowlist.go`
- [x] **Zero comentários em .go de produção (R-ADAPTER-001.1)**: comprovado — comentário proibido em `system_event_allowlist.go` removido; gate retorna vazio.
  -> comprovado: `grep` gate retornou "OK: zero comments"

## Comandos Executados

```bash
go test -count=1 ./internal/onboarding/application/events/...
# ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/onboarding/application/events 0.475s

go test -count=1 ./internal/platform/whatsapp/dispatcher/...
# ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/platform/whatsapp/dispatcher 0.436s

go test -count=1 ./internal/onboarding/... ./internal/platform/whatsapp/...
# Todos PASS — 20+ pacotes verificados

go test -count=1 ./internal/platform/outbox/...
# ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/platform/outbox 0.497s

go vet ./internal/onboarding/... ./internal/platform/whatsapp/... ./internal/platform/outbox/...
# (sem saída — nenhum problema)

goimports -l <arquivos alterados>
# (sem saída — todos formatados corretamente)
```

## Notas de Implementação

- `subscription_binding.go` (6.1) não constrói `EventInput` diretamente; delega para `events.NewSubscriptionBoundEvent`. A correção foi aplicada em `subscription_bound.go` (6.2), que é o ponto canônico de construção do evento.
- O dispatcher `publishAuthFailed` constrói `outbox.Event` diretamente (não via `NewEvent`) para evitar re-validação UUID quando userID pode ser vazio. O campo `AggregateUserID` é string vazia para eventos de sistema, preenchido para rate_limited.
- O lint pré-existente em `internal/identity/domain/valueobjects/gateway_timestamp_test.go` (goimports) não foi introduzido por esta tarefa — arquivo não modificado.

## Riscos Residuais

Nenhum crítico. O dispatcher continua publicando `auth.failed` sem userID para eventos de sistema — comportamento correto e coberto pela allowlist ADR-004.
