# Generated: 2026-07-01T01:13:21Z

# Relatório de Execução — Tarefa 6.0

## Tarefa
Onboarding workflow durável de 8 etapas (fases fechadas, suspend/resume)

## Status
done

## Arquivos Criados

- `internal/agents/application/workflows/onboarding_workflow.go` (650 linhas)
- `internal/agents/application/workflows/onboarding_workflow_test.go`
- `internal/agents/application/usecases/resolve_onboarding_or_agent.go` (109 linhas)
- `internal/agents/application/usecases/resolve_onboarding_or_agent_test.go`

## Critérios de Aceite

- `OnboardingPhase` fechado (DMMF state-as-type); `Decide*` puro e determinístico (sem IO/context/time).
  -> comprovado: `OnboardingPhase` é `int` com constantes iota; 4 funções `Decide*` sem `context.Context` e sem IO. Gate `grep "func Decide"` confirma.

- Retomada na fase exata após dúvida/dias (suspend/resume durável); sem reinício (RF-18/RF-19/RF-19.1).
  -> comprovado: `BuildOnboardingWorkflow` usa `Durable:true`; resume aplica merge-patch RFC 7386 via `engine.Resume(ctx, def, key, []byte(`{"resumeText":...}`))`; `ResolveOnboardingOrAgent.Execute` detecta run suspenso via `store.Load` e chama `Resume` antes de qualquer novo `Start`.

- Distribuição em mensagem única fecha 100%; cartão com defaults aplicados.
  -> comprovado: `DecideDistribution` valida todos os 5 slugs presentes e soma==100; step-methodology re-suspende (não falha) quando inválido; `NewCard` no step-cards usa `ClosingDay=DueDay` via adapter (RF-15.2).

- LLM só nas call-sites sancionadas (Stream/parse); sem LLM no kernel (R-WF-KERNEL-001/R-AGENT-WF-001.4).
  -> comprovado: `agent.Stream` usado nos steps para gerar mensagens; `llm.Provider.Complete` com JSON schema usado nos steps para parse de respostas; zero referência a LLM em `resolve_onboarding_or_agent.go`.

- Conclusão: objetivo na WM + orçamento ativo (RF-17); reuso de estado pré-existente (RF-15.1).
  -> comprovado: step-conclusion chama `budgets.ActivateBudget`, opcionalmente `budgets.CreateRecurrence(12)`, e `workingMem.Upsert` com `"## Objetivo Financeiro\n\n<goal>"`. `ResolveOnboardingOrAgent` verifica `strings.Contains(wm, "## Objetivo Financeiro")` para detectar onboarding concluído.

- Zero comentários; build/gofmt/governança verdes.
  -> comprovado: gate `grep -rn "^[[:space:]]*//"` retornou vazio nos arquivos de produção. `go build ./...` retornou vazio (sucesso).

## Comandos Executados

```
go build ./internal/agents/... → sucesso (sem output)
go test ./internal/agents/application/workflows/... → ok (1.292s)
go test ./internal/agents/application/usecases/... → ok (0.655s)
go build ./... → sucesso (sem output)
```

## Testes

### Workflows
- `TestOnboardingWorkflowSuite/TestDecideGoal` — 3 cenários (vazio, só espaços, válido)
- `TestOnboardingWorkflowSuite/TestDecideIncomeCents` — 4 cenários (zero, negativo, 3000 BRL, fração)
- `TestOnboardingWorkflowSuite/TestDecideDistribution` — 3 cenários (faltando chave, soma≠100, válido)
- `TestOnboardingWorkflowSuite/TestDecideCardEntry` — 5 cenários (nickname vazio, dueDay 0, dueDay 32, válido, limite 31)
- `TestOnboardingWorkflowSuite/TestParseOnboardingPhase` — 3 cenários
- `TestOnboardingWorkflowSuite/TestBuildWelcomeStep` — 3 cenários (suspend, resume/complete, stream error)
- `TestOnboardingWorkflowSuite/TestBuildGoalStep` — 3 cenários (suspend, resume válido, resume vazio)
- `TestOnboardingWorkflowSuite/TestBuildMethodologyStep` — 3 cenários (suspend, distribuição válida, distribuição inválida → re-suspend)
- `TestOnboardingWorkflowSuite/TestBuildOnboardingWorkflow_IDAndStructure` — estrutura e ID

### Usecases
- `TestResolveOnboardingOrAgentSuite/TestExecute` — 8 cenários: run suspenso→resume, run concluído→done+finalMessage, WM com objetivo→não handled, sem objetivo→start, erros de load/resume/start/WM

## Subtarefas

- [x] 6.1 `OnboardingPhase` (8 constantes fechadas) e `OnboardingState`
- [x] 6.2 `Sequence` dos 8 steps; `Durable:true`; suspend após cada pergunta; resume por merge-patch antes do parse
- [x] 6.3 Step `goal` grava objetivo na working memory; step `income` coleta renda
- [x] 6.4 Step `methodology`+`distribution`: mensagem única; `Decide*` puro valida 100%; re-pergunta se falhar
- [x] 6.5 Step `cards`: coleta só apelido+vencimento; defaults; reuso via `ListCards`
- [x] 6.6 Step `summary`+`conclusion`: resumo, confirmação, ativa orçamento, recorrência 12m, exemplos
- [x] 6.7 Mensagens via `agent.Stream`; parse via `llm.Provider.Complete` com JSON schema (`Strict:true`)
- [x] 6.8 `ResolveOnboardingOrAgent`: detecta onboarded (WM+objetivo), roteia onboarding×operação (RF-30.1)

## Riscos Residuais

- `ResolveOnboardingOrAgent` ainda não está conectado ao consumer `WhatsAppInboundConsumer` — isso é responsabilidade da tarefa 8.0 (module.go + wiring). O use case está pronto para ser injetado.
- `FinalMessage` (campo extra adicionado a `OnboardingState`) não está na especificação original da techspec. Necessário para retornar a mensagem final de exemplos de uso ao caller após `RunStatusSucceeded`.
- O step-distribution (step 6) deriva a competência via `time.Now()` em `America/Sao_Paulo`. Risco de timezone conforme ADR-008 (pré-existente, fora do escopo desta tarefa).
