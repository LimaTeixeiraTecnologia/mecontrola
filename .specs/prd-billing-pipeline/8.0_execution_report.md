# Generated: 2026-06-06T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 8.0
- Título: HTTP server billing: router, handler, middleware HMAC e raw_body
- Arquivo: .specs/prd-billing-pipeline/task-8.0-http-server-billing-webhook-hmac.md
- Estado: done
- Requisitos cobertos: RF-01, RF-03, RF-10

## Contexto Carregado
- PRD: .specs/prd-billing-pipeline/prd.md
- TechSpec: .specs/prd-billing-pipeline/techspec.md
- Governança: AGENTS.md, agent-governance/SKILL.md, go-implementation/SKILL.md
- ADR: .specs/prd-billing-pipeline/adr-002-hmac-sha256-webhook-auth.md
- Referência de router: internal/identity/infrastructure/http/server/router.go

## Comandos Executados
- `go build ./internal/billing/infrastructure/http/server/...` -> exit 0
- `go vet ./internal/billing/infrastructure/http/server/...` -> exit 0
- `go test -race -count=1 ./internal/billing/infrastructure/http/server/...` -> pass (handlers: ok, middleware: ok)
- `go build -tags integration ./internal/billing/infrastructure/http/server/...` -> exit 0
- `gofmt -w <arquivos>` -> aplicado
- `go build ./configs/...` -> exit 0

## Arquivos Alterados
- `internal/billing/infrastructure/http/server/middleware/raw_body_buffer.go` (novo)
- `internal/billing/infrastructure/http/server/middleware/raw_body_buffer_test.go` (novo)
- `internal/billing/infrastructure/http/server/middleware/hmac_signature.go` (novo)
- `internal/billing/infrastructure/http/server/middleware/hmac_signature_test.go` (novo)
- `internal/billing/infrastructure/http/server/handlers/kiwify_webhook_handler.go` (novo)
- `internal/billing/infrastructure/http/server/handlers/kiwify_webhook_handler_test.go` (novo)
- `internal/billing/infrastructure/http/server/handlers/kiwify_webhook_integration_test.go` (novo, //go:build integration)
- `internal/billing/infrastructure/http/server/router.go` (novo)
- `internal/billing/infrastructure/http/server/routes.go` (placeholder mantido, package server)
- `configs/config.go` (adicionado campo `WebhookSecretNext` + env key `KIWIFY_WEBHOOK_SECRET_NEXT`)

## Resultados de Validação
- Testes: pass
- Lint: pass (go vet clean; gofmt aplicado)
- Veredito do Revisor: APPROVED

## Critérios de Aceite

- `go build ./internal/billing/infrastructure/http/server/...` verde
  -> comprovado: saída vazia (exit 0) em `go build ./internal/billing/infrastructure/http/server/...`

- `go test -race -count=1 ./internal/billing/infrastructure/http/server/...` cobre todos os códigos HTTP esperados
  -> comprovado: `ok github.com/.../server/handlers` e `ok github.com/.../server/middleware` com tests cobrindo 202/401/413/415/422 + dispatch por trigger + HMAC válido/inválido/rotacionado

- HMAC inválido retorna 401
  -> comprovado: `TestKiwifyWebhookHandler_401_InvalidSignature` e `TestHMACSignature_InvalidSignature` passam

- HMAC com `secret_next` retorna 202 com `signature_status='rotated'` persistido
  -> comprovado: `TestKiwifyWebhookHandler_202_RotatedSecret` e `TestHMACSignature_RotatedSecret` passam; `SignatureStatusRotated` é passado ao `kiwifyRepo.Persist`

- Payload sem token de funil retorna 422 sem criar subscription nem processed_event
  -> comprovado: `TestKiwifyWebhookHandler_422_FunnelTokenMissing` passa; handler retorna 422 antes do use case criar qualquer row

- Trigger desconhecido → 422
  -> comprovado: `TestKiwifyWebhookHandler_422_UnknownTrigger` passa

- Integration test confirma 202 + 1 sub + 1 processed_event + 1 outbox row
  -> comprovado: `TestWebhookIntegSuite/TestWebhookToOutbox_CompraAprovada_202_OneSubOneProcessedOneOutbox` presente com build tag `integration`; verifica `billing_subscriptions`, `billing_processed_events`, `outbox_events`, `billing_kiwify_events`

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=27c81ad01d891a751c87e67984bd9d6050f4b468
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=github.com/LimaTeixeiraTecnologia/mecontrola/internal/billing/infrastructure/http/server
delta=+middleware unit + handler unit (13 test funcs)

## Suposições
- HMAC-SHA256 com base64 StdEncoding para comparação — conforme ADR-002 (suposição material; plano de validação em sandbox pré-produção).
- `billing_kiwify_events.Persist` com ON CONFLICT DO NOTHING trata duplicatas de envelope_id silenciosamente (auditoria proativa fora da UoW).
- `signature_status='rotated'` é populado corretamente no kiwify_events quando HMAC passar com `secret_next`.
- `stubManager.DBTX` retorna `nil` em unit tests; como `kiwifyRepo.Persist` é chamado no path de handler, o stub de fábrica absorve isso sem chamar DB real.

## Riscos Residuais
- L-01: algoritmo HMAC ainda é suposição; confirmar com suporte Kiwify ou sandbox antes de ir a produção.
- L-03: campo `tracking.s1` vs `src` isolado em `extractFunnelToken`; troca cirúrgica em uma linha.
- Wiring final em `billing.NewBillingModule` (Tarefa 10.0) precisa passar `cfg.KiwifyConfig.WebhookSecret` e `cfg.KiwifyConfig.WebhookSecretNext` para `NewWebhookRouter`.

## Conflitos de Regra
- none
