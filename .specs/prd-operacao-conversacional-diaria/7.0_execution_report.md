# Generated: 2026-07-15T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 7.0
- Título: Roteamento de resume por registry (SuspendedRunIndex)
- Arquivo: .specs/prd-operacao-conversacional-diaria/task-7.0-resume-routing.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-operacao-conversacional-diaria/prd.md (RF-08, RF-09)
- TechSpec: .specs/prd-operacao-conversacional-diaria/techspec.md (seção "Remoção Total do Legado", item consumer/module.go)
- ADR: .specs/prd-operacao-conversacional-diaria/adr-002-resume-routing-registry.md
- Governança: agent-governance (base), mastra (roteamento por WorkflowRegistry, resume por merge-patch), design-patterns-mandatory (gate Registry+Adapter/Strategy para o dispatcher), R-AGENT-WF-001, R-ADAPTER-001, R-WF-KERNEL-001.7 (merge-patch), governance.md (precedência)

## Comandos Executados
- `go build ./...` -> OK
- `go vet ./...` -> OK
- `go build -tags integration ./...` -> OK
- `go vet -tags integration ./internal/agents/infrastructure/messaging/database/consumers/...` -> OK (após corrigir stub `consumerIntegrationLedger.SearchEditCandidates`)
- `go test -race ./internal/agents/application/usecases/...` -> 117 passed
- `go test -race ./internal/agents/application/workflows/...` -> 738 passed
- `go test -race ./internal/agents/infrastructure/messaging/database/consumers/...` -> 29 passed
- `go test -race ./internal/agents/...` -> 1484 passed em 21 pacotes
- `./.tools/bin/golangci-lint run ./internal/agents/... ./deployment/...` -> 0 issues
- `bash deployment/scripts/lint-deadcode.sh` -> PASS lint:deadcode (RF-40) após atualizar `deadcode-agent-allowlist.txt`
- `gofmt -l <arquivos tocados>` -> vazio (formatados)
- Gates manuais R-AGENT-WF-001/R-ADAPTER-001 (grep zero comentários, sem `case intent.Kind`, sem SQL no consumer) -> PASS

## Arquivos Alterados
- `internal/agents/application/usecases/suspended_run_index.go` (novo)
- `internal/agents/application/usecases/suspended_run_index_test.go` (novo)
- `internal/agents/application/usecases/workflow_resumer.go` (novo)
- `internal/agents/application/usecases/workflow_resumer_test.go` (novo)
- `internal/agents/application/usecases/resume_dispatcher.go` (novo)
- `internal/agents/application/usecases/resume_dispatcher_test.go` (novo)
- `internal/agents/application/workflows/correlation_key.go` (novo — `CorrelationKey(resourceID, threadID, workflowID)`)
- `internal/agents/application/workflows/budget_manage_workflow.go` (Key uniformizada com threadID)
- `internal/agents/application/workflows/card_manage_workflow.go` (Key uniformizada com threadID)
- `internal/agents/application/workflows/goal_edit_workflow.go` (Key uniformizada com threadID)
- `internal/agents/application/workflows/destructive_manage_workflow.go` (Key uniformizada com threadID)
- `internal/agents/application/workflows/transaction_write_workflow.go` (adicionado `ContinueTransactionWrite`)
- `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer.go` (removido `tryResumeChain` e os 4 continuers específicos + interfaces; adicionado `resumeDispatcherResolver`/`WithResumeDispatcher`; onboarding preservado e chamado após o dispatcher)
- `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer_test.go` (reescrito para o novo dispatcher único; teste de precedência dispatcher > onboarding)
- `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer_integration_test.go` (substituído cenário de pending-entry por transaction-write via `usecases.NewWorkflowResumer`/`NewSuspendedRunIndex`/`NewResumeDispatcher`; corrigido stub `consumerIntegrationLedger.SearchEditCandidates`)
- `internal/agents/module.go` (5 engines/defs novos, 5 `agent.WorkflowRegistry[S]`, 5 `usecases.WorkflowResumer`, `SuspendedRunIndex`, `ResumeDispatcher`; removida instanciação dos 4 continuers legados; `WithResumeDispatcher` substitui as 4 opções antigas)
- `deployment/scripts/deadcode-agent-allowlist.txt` (removidas entradas dos símbolos das tarefas 5.0/6.0 que se tornaram alcançáveis pelo wiring desta tarefa; adicionado bloco para os continuers legados agora órfãos, com remoção prevista no cutover da tarefa 9.0; adicionado bloco residual para reapers/Parse*/mensagens ainda não wired, previsto para a tarefa 8.0)

## Resultados de Validação
- Testes: pass (`go test -race ./internal/agents/...` — 1484 passed)
- Lint: pass (golangci-lint 0 issues; deadcode gate RF-40 pass; gofmt limpo)
- Veredito do Revisor: APPROVED_WITH_REMARKS (achado `medium`, não bloqueante — ver Riscos Residuais)

## Critérios de Aceite
- Run correto retomado por `(resource, thread)` sem `tryResumeChain` -> comprovado: `tryResumeChain` e os métodos `tryContinuePendingEntry/tryContinueDestructive/tryContinueCardCreate/tryContinueBudgetCreation` foram removidos de `whatsapp_inbound_consumer.go`; `Handle` agora chama `tryResume` -> `tryDispatchResume` (um único `resumeDispatcher.Continue`) -> `tryResolveOnboarding`. `SuspendedRunIndex.Resolve` faz uma única consulta por workflow candidato via `workflow.Store.Load(workflow, key)` e retorna o workflow dono.
- Ponto único de despacho de resume via `WorkflowRegistry` -> comprovado: `usecases.NewWorkflowResumer[S]` resolve `Definition[S]` em `agent.WorkflowRegistry[S]` e cada um dos 5 workflows (`transaction-write`, `budget-manage`, `card-manage`, `goal-edit`, `destructive-confirm`/destructive-manage) é registrado em `internal/agents/module.go`; `ResumeDispatcher.Continue` é o único ponto de despacho (`internal/agents/application/usecases/resume_dispatcher.go`).
- Invariante de no máximo 1 run suspenso por thread (RF-09) coberto por teste -> comprovado: `SuspendedRunIndexSuite.TestResolve/"deve retornar ErrMultipleSuspendedRuns..."` (`suspended_run_index_test.go`) força dois workflows suspensos na mesma chave e verifica `errors.Is(err, ErrMultipleSuspendedRuns)`.
- Adicionar um workflow não exige tocar o consumer (registro no registry basta) -> comprovado: o consumer depende apenas da interface `resumeDispatcherResolver` (`Continue(ctx, resourceID, threadID, message, messageID)`); novos workflows entram via `usecases.NewWorkflowResumer` + `SuspendedRunIndex` em `module.go`, sem alterar `whatsapp_inbound_consumer.go`.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`go test -race ./internal/agents/...` — 1484 passed).
- [x] Lint/vet/build sem regressão (build/vet/lint/deadcode gates todos verdes para o módulo `internal/agents`).
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree (multi-task branch não commitado; diff isolado às tarefas 5.0/6.0 já concluídas)
verdict=APPROVED_WITH_REMARKS
tool=review (skill inline, subagente executor)

## Coverage

package=internal/agents (usecases, workflows, consumers)
delta=+7 arquivos novos de produção/teste em usecases; 5 arquivos de workflows tocados; consumer reescrito; module.go +78/-8

## Suposições
- Reapers para os 5 workflows novos (`Build*Reaper`) NÃO foram wired em `module.go` nesta tarefa — a task file (subtarefas 7.1–7.6) não lista reapers como escopo; o próprio techspec associa "reapers" ao wiring completo da tarefa 8.0 ("Ferramentas... + bindings + wiring em module.go (engines, defs, registry, reapers, WithWriteToolSet)"). Documentado no allowlist de deadcode.
- `ContinueTransactionWrite` foi implementado como wrapper simples de `engine.Resume` (mesmo padrão de `ContinueBudgetManage/ContinueCardManage/ContinueGoalEdit/ContinueDestructiveManage`), sem replicar a lógica extra de retry-após-falha (`tryResumeFailedWrite`) do antigo `PendingEntryContinuer` — esse comportamento usa o campo `FailedWriteResumeCount` que já existe em `TransactionWriteState` mas não foi conectado por nenhuma tarefa até aqui (5.0 também não o wireou); fora do escopo explícito de 7.0.
- O teste de integração do consumer foi migrado de `pending-entry` (antigo, removido do wiring do consumer) para `transaction-write` (workflow novo, sucessor direto por techspec), preservando a mesma asserção de precedência (workflow suspenso > onboarding) e de estado durável no Postgres.

## Riscos Residuais
- Janela entre a tarefa 7.0 e a 8.0: as tools legadas ainda ativas (`delete_entry`, `update_recurrence`, `delete_recurrence`, `update_card`) continuam suspendendo o workflow antigo `ConfirmState` (`confirmEngine`/`confirmDef`, ID `"destructive-confirm"`) via `buildFinancialTools`, mas o consumer não resolve mais esse resume (a opção `WithDestructiveConfirmResolver` foi removida). Uma confirmação pendente desse fluxo antigo fica sem retomada até a tarefa 8.0 reapontar essas tools para `destructive-manage`. Consistente com o plano de cutover somente na tarefa 9.0 (ADR-005); não deployar 7.0 isoladamente em produção antes de 8.0.
- 5 falhas de `go vet -tags integration` pré-existentes e fora do escopo de 7.0 (heranças das tarefas 1.0/2.0/5.0, por assinaturas desatualizadas de `NewTransactionsLedgerAdapter`/stubs sem `SearchEditCandidates`) permanecem em: `pending_entry_continuer_integration_test.go`, `ca09_reconciled_integration_test.go`, `mecontrola_agent_e2e_test.go`, `register_expense_integration_test.go`, `pending_entry_workflow_integration_test.go`. Corrigido apenas o stub do arquivo tocado nesta tarefa (`whatsapp_inbound_consumer_integration_test.go`).

## Conflitos de Regra
- none
