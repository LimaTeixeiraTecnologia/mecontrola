# Generated: 2026-07-09T09:52:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 1.0
- Título: Dia de fechamento explícito opcional e reconhecimento de banco no módulo card
- Arquivo: .specs/prd-cadastro-conversacional-cartao/task-1.0-card-closing-day-bank-recognition.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-cadastro-conversacional-cartao/prd.md (RF-07, RF-08, RF-09, RF-10, RF-11, RF-20)
- TechSpec: .specs/prd-cadastro-conversacional-cartao/techspec.md (seções "Reconhecimento de Banco e Dia de Fechamento", "Modelos de Dados", "Sequenciamento de Desenvolvimento" item 1, "Pontos de Integração")
- ADR: .specs/prd-cadastro-conversacional-cartao/adr-002-closing-day-optional-modeling.md
- Governança: R-DTO-VALIDATE-001, R-ADAPTER-001.1, governance.md (precedência DMMF sentinela+bool), go-testing.md R-TESTING-001; skills processuais da task: domain-modeling-production, postgresql-production-standards, design-patterns-mandatory; skill de linguagem: go-implementation (SKILL.md)

## Comandos Executados
- `task mocks:mocks` -> regenerou `internal/card/application/interfaces/mocks/bank_days_reader.go` com o novo método `IsBankRecognized`; falha pré-existente e não relacionada em `internal/budgets` (`CardThresholdReader`) não afeta o escopo desta tarefa; mocks de `internal/agents` tocados por drift de template do mockery foram revertidos (`git checkout --`) por estarem fora de escopo
- `gofmt -l internal/card/application/dtos/input/create_card.go internal/card/application/dtos/input/errors.go internal/card/application/interfaces/bank_days_reader.go internal/card/application/usecases/create_card.go internal/card/infrastructure/repositories/postgres/bank_repository.go internal/card/application/usecases/create_card_test.go internal/card/infrastructure/repositories/postgres/bank_repository_test.go internal/card/infrastructure/repositories/postgres/bank_repository_integration_test.go` -> vazio (limpo)
- `go build ./internal/card/...` -> sucesso, sem erros
- `go vet ./internal/card/...` -> sucesso, sem erros
- `go test -race ./internal/card/...` -> 376 passed in 23 packages (0 falhas)
- `golangci-lint run ./internal/card/...` -> 0 issues (após remover campo `bankReader` não utilizado detectado por `unused`)

## Arquivos Alterados
- `internal/card/application/dtos/input/create_card.go` — `+ClosingDay int`, `+ClosingDayProvided bool`; `Validate()` valida `ClosingDay` 1..31 só quando provided (R-DTO-001)
- `internal/card/application/dtos/input/errors.go` — `+ErrCardClosingDayInvalid`
- `internal/card/application/interfaces/bank_days_reader.go` — `+IsBankRecognized(ctx, bank) (bool, error)` na interface `BankDaysReader`
- `internal/card/application/interfaces/mocks/bank_days_reader.go` — mock regenerado (mockery) com `IsBankRecognized`
- `internal/card/application/usecases/create_card.go` — branch por `in.ClosingDayProvided` (~87-101): provided → `NewBillingCycle` direto; senão → derivação atual inalterada (bankReader construído dentro do `else`, evitando chamada desnecessária ao factory quando provided=true)
- `internal/card/application/usecases/create_card_test.go` — +6 cenários/testes: provided-path ignora bank reader, derive-path inalterado (regressão), banco reconhecido ignora closing informado, validações nomeadas (0, 32, provided=false)
- `internal/card/infrastructure/repositories/postgres/bank_repository.go` — `+IsBankRecognized` (`SELECT EXISTS(...) FROM mecontrola.banks WHERE code = $1`, reusa `bank.LookupKey()` = normalização de `NewBankCode`)
- `internal/card/infrastructure/repositories/postgres/bank_repository_test.go` (novo) — suite testify com `sqlmock`, paridade de normalização (Nubank, banco XP, Itaú acentuado)
- `internal/card/infrastructure/repositories/postgres/bank_repository_integration_test.go` — `+TestIsBankRecognized` (build tag `integration`, opcional conforme task)

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `Validate()` retorna erro com campo nomeado apenas quando `ClosingDayProvided` e `ClosingDay` fora de [1,31]; nenhum erro para `ClosingDayProvided=false` -> comprovado: `internal/card/application/dtos/input/create_card.go` (`if i.ClosingDayProvided && (i.ClosingDay < 1 || i.ClosingDay > 31) { errs = append(errs, ErrCardClosingDayInvalid) }`) + teste `TestExecute_ClosingDayInvalid_ReturnsNamedValidationError` (3 cenários: 0/provided=true, 32/provided=true, 32/provided=false) em `create_card_test.go`
- Provided-path constrói `BillingCycle` direto; derive-path e onboarding produzem exatamente o mesmo resultado de antes (regressão verde) -> comprovado: branch em `create_card.go` (~87-116) preserva o caminho de derivação original byte-a-byte no `else`; testes `TestExecute_ClosingDayProvided_UsesDirectCycle` (assert `AssertNotCalled` em `DaysBeforeDue`) e `TestExecute_ClosingDayNotProvided_DerivesAsBefore` + suíte completa 376 passed
- Banco reconhecido com `closingDay` informado não altera o cálculo (defesa via branch por intenção) -> comprovado: o usecase decide exclusivamente por `ClosingDayProvided` (nunca consulta reconhecimento), conforme ADR-002 decisão 2/3 — a supressão do `closingDay` para banco reconhecido é responsabilidade da tool (fora do escopo desta tarefa, task 6.0); teste `TestExecute_RecognizedBank_IgnoresProvidedClosingDay` comprova que `ClosingDayProvided=false` (equivalente ao que a tool enviaria para banco reconhecido) ignora `ClosingDay=20` informado e deriva normalmente
- `IsBankRecognized` normaliza "Nubank", "banco XP" e entrada acentuada com o mesmo `code` da derivação -> comprovado: `IsBankRecognized` usa `bank.LookupKey()` (idêntico a `DaysBeforeDue`); teste `TestIsBankRecognized_NormalizationParity` em `bank_repository_test.go` cobre os 3 casos exatos citados (Nubank→nubank, banco XP→banco-xp, Itaú→itau) via sqlmock com `WithArgs` no lookup key esperado
- `gofmt -l` limpo; zero comentários em arquivos `.go` de produção (R-ADAPTER-001.1); build, vet, test race e lint do módulo verdes -> comprovado: comandos acima (gofmt -l vazio, go build/vet sem erro, go test -race 376 passed, golangci-lint 0 issues); grep de comentários nos 5 arquivos de produção alterados retornou vazio

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=working-tree (task 1.0 scope: internal/card/*)
verdict=APPROVED
tool=review (skill, inline)

## Coverage

package=internal/card/application/usecases, internal/card/infrastructure/repositories/postgres
delta=+6 test functions (usecase), +1 test file (bank_repository_test.go), +1 test function (integration, opcional)

## Suposições
- `IsBankRecognized` foi adicionado à interface `BankDaysReader` existente (em vez de uma interface nova), pois ambos os métodos leem a mesma tabela `mecontrola.banks` e são resolvidos pelo mesmo `RepositoryFactory.BankDaysReader(db)`; a techspec cita "read repo/interface" sem prescrever uma interface separada.
- O path físico do adapter postgres do módulo card é `internal/card/infrastructure/repositories/postgres/` (não `internal/card/infrastructure/postgres/` como mencionado na task file/techspec) — path real do repositório confirmado por `find`; a leitura foi implementada no arquivo existente `bank_repository.go`, mantendo o padrão do módulo.
- A construção de `bankReader := u.factory.BankDaysReader(tx)` foi movida para dentro do branch `else` (não estava explícita no pseudocódigo da task) para evitar uma chamada desnecessária ao factory quando `ClosingDayProvided=true`; comportamento observável do onboarding/derive-path é idêntico ao anterior.

## Riscos Residuais
- A defesa "banco reconhecido ignora closingDay informado" (RF-07) depende da tool `create_card` (task 6.0, fora de escopo) sempre enviar `ClosingDayProvided=false` para banco reconhecido; o usecase por si só não consulta `IsBankRecognized`, por design do ADR-002 (branch por intenção do chamador, não por reconhecimento) — este é o comportamento pretendido, não uma lacuna, mas fica registrado como dependência de integração para a task 6.0.
- Teste de integração de `IsBankRecognized` (`bank_repository_integration_test.go`) requer Docker/testcontainers para rodar; não foi executado nesta sessão (build tag `integration`), conforme a task marca testes de integração como opcional para esta tarefa.

## Conflitos de Regra
- none
