# Relatório de Execução de Tarefa
# Generated: 2026-06-23

## Tarefa
- ID: 5.0
- Título: [onboarding] SuggestBudgetSplit (resolve perfil → delega cents a budgets) + binding
- Arquivo: .specs/prd-onboarding-v2/task-5.0-suggest-budget-split.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-onboarding-v2/prd.md
- TechSpec: .specs/prd-onboarding-v2/techspec.md
- Governança: AGENTS.md, go-implementation (auto), governance.md

## Arquivos Criados/Alterados
- `internal/onboarding/application/usecases/suggest_budget_split.go` — SuggestBudgetSplit usecase; resolve ObjectiveProfile via classifyByKeyword/hint; delega cálculo cents ao SuggestAllocation de internal/budgets
- `internal/onboarding/application/usecases/suggest_budget_split_test.go` — 8 cenários cobrindo: hint válido, fallback keyword, ambíguo→default, income zero, userID nil, erro do allocator, sem cents math no usecase

## Comandos Executados

```
go build ./internal/onboarding/... -> OK
go test ./internal/onboarding/application/usecases/... -run SuggestBudget -count=1 -> 8/8 PASS
go test ./internal/onboarding/... -count=1 -> todos OK
grep zero-comments gate -> OK
```

## Definition of Done

- [x] `SuggestBudgetSplit` resolve perfil via `classifyByKeyword` + hint, sem cálculo cents
- [x] Delega cálculo cents ao `SuggestAllocation` de `internal/budgets` (ADR-006)
- [x] Zero `×` incomeCents ou distribuição monetária no usecase
- [x] Rejeita income_cents=0 e user_id nil
- [x] 8 cenários cobrindo todos os caminhos (hint, keyword, fallback, erro)
- [x] Zero comentários em `.go` de produção (R-ADAPTER-001.1)
- [x] `go build ./internal/onboarding/...` passa
- [x] `go test ./internal/onboarding/application/usecases/... -run SuggestBudget` passa (8/8)
