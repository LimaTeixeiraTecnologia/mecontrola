# Relatório de Execução de Tarefa
<!-- Generated: 2026-06-23T00:00:00Z -->

## Tarefa
- ID: 11.0
- Título: [agent] Hardening da saudação (GAP-1 + idempotência)
- Arquivo: .specs/prd-onboarding-v2/task-11.0-greeting-hardening.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-onboarding-v2/prd.md
- TechSpec: .specs/prd-onboarding-v2/techspec.md (seções "Idempotência da saudação" e "Degradação por falha de LLM")
- Governança: agent-governance (auto), go-implementation (auto), mastra (declarada)
- Regras aplicadas: R-ADAPTER-001.1 (zero comentários), R-ADAPTER-001.2 (adapter fino), R-AGENT-WF-001 (types fechados, tool fina)

## Comandos Executados
- `go build ./internal/agent/...` -> PASS
- `go test ./internal/agent/infrastructure/messaging/database/consumers/... -run OnboardingBound -count=1` -> PASS (10 testes)
- `go test ./internal/agent/application/usecases/... -run TestRunOnboardingTurnSuite/TestLLMError -count=1` -> PASS (7 sub-testes)
- `go test ./internal/agent/... -count=1` -> PASS (todos os pacotes)
- `go build ./... ` -> PASS (zero erros de compilação)
- gate R-ADAPTER-001.1 (zero comentários) -> PASS
- gate R-ADAPTER-001.2 (sem SQL em adapter) -> PASS
- gate R-AGENT-WF-001 (switch não cresceu) -> PASS

## Arquivos Alterados
- `internal/agent/infrastructure/messaging/database/consumers/onboarding_bound_consumer.go` (modificado — hardening GAP-1 + idempotência)
- `internal/agent/infrastructure/messaging/database/consumers/greeting_decision_store.go` (novo — GreetingDecisionStore wrapping factory+uow)
- `internal/agent/infrastructure/onboarding/greeting_welcome_marker.go` (novo — GreetingWelcomeMarker adapter)
- `internal/agent/module.go` (modificado — buildOnboardingBoundConsumer wira opts quando deps disponíveis)
- `internal/agent/infrastructure/messaging/database/consumers/onboarding_bound_consumer_test.go` (modificado — 5 novos cenários de hardening)
- `internal/agent/application/usecases/run_onboarding_turn_test.go` (modificado — TestLLMErrorDoesNotPersistPhaseTransition + TestLLMErrorDoesNotCallCompleteOnboardingSession)

## Resultados de Validação
- Testes: pass
- Lint: pass (zero comentários, sem SQL em adapter, build clean)
- Veredito do Revisor: APPROVED

## Critérios de Aceite

Requisitos cobertos: RF-05, RF-08, RF-29, RF-32.

- [RF-29 — Reprocessar subscription_bound não duplica saudação (event_id/welcome_sent_at)] -> comprovado: cenário "replay detectado via decisionStore retorna no-op" em TestHandle_ReplayDetected_NoOpViaDecisionStore — `router.called = false`, `registerCalled = false`; cenário "sessao em progresso executa fluxo completo e marca welcome" valida `decisionStore.registerCalled = true` e `welcomeMarker.called = true`.
- [RF-05 — Sessão ausente força retry (GAP-1)] -> comprovado: cenário "sessao ausente retorna erro para retry (GAP-1)" — `s.Error(err)`, `s.False(router.called)`; log warn `onboarding_not_started` emitido.
- [RF-08/RF-32 — Falha de LLM não conclui/corrompe nem chama CompleteOnboardingSession] -> comprovado: TestLLMErrorDoesNotPersistPhaseTransition (5 fases) e TestLLMErrorDoesNotCallCompleteOnboardingSession verificam `setter.phases = vazio`, `v2.saved = nil`, `v2.cleared = false`, `dispatcher.calls = 0`.
- [DR-10 — max_attempts para onboarding.subscription_bound; após o teto, dead-letter + alerta outbox_dead_letter_total] -> comprovado: mecanismo de retry + dead-letter já existente no outbox reusa `attempts`/`max_attempts` (conforme techspec seção "Cap de retry + dead-letter"); consumer agora retorna erro em sessão ausente, o que ativa o retry. Alerta `outbox_dead_letter_total{event_type}` é emitido pelo dispatcher existente ao atingir o teto. Sem nova infra necessária.

## Definition of Done (DoD)
- [x] Consumer usa event_id (envelope.ID como MessageID); retry em sessão ausente (GAP-1); welcome marcado após envio via GreetingWelcomeMarker.
- [x] Erro de LLM comprovadamente não persiste transição nem conclui: TestLLMErrorDoesNotPersistPhaseTransition + TestLLMErrorDoesNotCallCompleteOnboardingSession passam.
- [x] Zero comentários no `.go`; sem SQL direto no consumer.
- [x] `go build ./internal/agent/...` e `go test ./internal/agent/infrastructure/messaging/database/consumers/... -run OnboardingBound -count=1` passam.

## Diff Reviewed

sha=local
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=internal/agent/infrastructure/messaging/database/consumers
delta=+5 cenários (replay, GAP-1, fluxo completo, lookup-fail, stateChecker-fail)

## Suposições
- `envelope.ID` (string) é o event_id estável por evento, conforme Envelope.Pack em platform/outbox.
- `GreetingDecisionStore.RegisterGreeting` usa sentinel `PromptSHA256 = sha256("onboarding.greeting:"+messageID)` e `LLMModel = "system"` — válidos pois AgentDecision não requer LLM real para eventos de infraestrutura.
- O outbox já suporta `max_attempts` + dead-letter; o V2 não cria nova infra (alinhado com techspec DR-10).

## Riscos Residuais
- `MarkWelcomeSent` falha pós-envio: aviso (warn) emitido, mas sem retry do consumer pois a barreira `welcome_sent_at` em `emitWelcome` cobre reentrância pelo caminho de turno (conforme techspec passo 5).
- `RegisterGreeting` falha pós-envio: warn emitido; na reentrada, `FindByMessageID` não encontrará o registro mas `welcome_sent_at` já terá sido marcado — duplicata bloqueada na camada de usecase.

## Conflitos de Regra
- none
