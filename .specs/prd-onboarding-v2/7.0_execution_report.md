# Generated: 2026-06-23T21:00:00Z

# Relatório de Execução — Tarefa 7.0: SaveOnboardingCard por closing_day

## Resumo
Ajuste completo do `SaveOnboardingCard` para coletar `closing_day` (1..31) em vez de `due_day`, propagando ao `SynchronousCardCreator` e ao evento `onboarding.card_registered` sem enviar `due_day`.

## Status Final
**done**

## Arquivos Modificados

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `internal/onboarding/application/dtos/input/save_onboarding_card_input.go` | novo | DTO `SaveOnboardingCardInput` com `Validate()` (R-DTO-VALIDATE-001) |
| `internal/onboarding/application/usecases/save_onboarding_card.go` | modificado | Input agora é `input.SaveOnboardingCardInput`; `SynchronousCardCreator.Execute` usa `closingDay`; evento sem `DueDay` |
| `internal/onboarding/application/usecases/save_onboarding_card_test.go` | modificado | Suite completa com cenários closing_day válido/inválido, delegação ao card creator mockado |
| `internal/onboarding/domain/entities/onboarding_session.go` | modificado | `NewOnboardingCardDraft(nickname, closingDay)` — aceita `closingDay` diretamente via `CardClosingDay` VO |
| `internal/onboarding/domain/entities/onboarding_session_events.go` | modificado | `CardRegistered` sem campo `DueDay` |
| `internal/onboarding/domain/entities/onboarding_session_methods_test.go` | modificado | Testes atualizados para `closingDay` |
| `internal/onboarding/domain/services/onboarding_workflow.go` | modificado | `CardRegistered` construído sem `DueDay` |
| `internal/agent/infrastructure/onboarding/onboarding_tool_dispatcher.go` | modificado | `dispatchCard` usa `onbinput.SaveOnboardingCardInput` com `ClosingDay`; lê `closing_day` do JSON da tool call |

## Comandos Executados

```
go build ./internal/onboarding/... → OK
go test ./internal/onboarding/application/usecases/... -run SaveOnboardingCard -count=1 → PASS
grep -rn "internal/card/infrastructure" internal/onboarding --include="*.go" | grep -v _test → sem resultados (OK)
go test ./internal/onboarding/... → 16/17 pacotes OK (1 falha pre-existente: task3.0 payload_test)
go test ./internal/card/... → 14/14 pacotes OK
go build ./internal/... → OK
```

## Critérios de Aceite

- [x] `closing_day` fora de 1..31 é rejeitado na validação do DTO (re-pergunta no fluxo — EB-14)
  -> comprovado: `TestValidationErrors/closing_day_zero_rejeitado_antes_de_IO` e `closing_day_32_rejeitado_antes_de_IO` passam; erro `ErrCardClosingDayRange` (errors.Join, nome de campo)

- [x] O cartão real é criado via `SynchronousCardCreator`/evento; onboarding não toca a tabela de cartões.
  -> comprovado: `grep -rn "internal/card/infrastructure" internal/onboarding --include="*.go" | grep -v _test` retorna vazio; `TestCardCreatorDelegado` verifica delegação

- [x] Contrato `internal/card` honrado: `nickname` validado 1..32 na fronteira; onboarding envia somente `closing_day`
  -> comprovado: `SaveOnboardingCardInput.Validate()` valida `Nickname` obrigatório e `ClosingDay` 1..31; `CardRegistered` evento sem campo `DueDay`; `SynchronousCardCreator.Execute(ctx, userID, nickname, closingDay)` sem `dueDay`

## Definition of Done (DoD)

- [x] `ClosingDay` coletado e propagado ao card creator + evento
- [x] DTO com `Validate()` (errors.Join, nome de campo); zero comentários no `.go`
- [x] `go build ./internal/onboarding/...` e `go test ./internal/onboarding/application/usecases/... -run SaveOnboardingCard` passam

## Testes Executados

```
=== PASS: TestSaveOnboardingCardSuite/TestHappyPath_ClosingDay/closing_day_valido_persiste_cartao_e_publica_evento
=== PASS: TestSaveOnboardingCardSuite/TestValidationErrors/nickname_vazio_rejeitado_antes_de_IO
=== PASS: TestSaveOnboardingCardSuite/TestValidationErrors/closing_day_zero_rejeitado_antes_de_IO
=== PASS: TestSaveOnboardingCardSuite/TestValidationErrors/closing_day_32_rejeitado_antes_de_IO
=== PASS: TestSaveOnboardingCardSuite/TestValidationErrors/user_id_nulo_rejeitado_antes_de_IO
=== PASS: TestSaveOnboardingCardSuite/TestNaoUso_NicknameVazio
=== PASS: TestSaveOnboardingCardSuite/TestSessionNotFound
=== PASS: TestSaveOnboardingCardSuite/TestCardCreatorDelegado/card_creator_chamado_com_closing_day_correto
=== PASS: TestSaveOnboardingCardSuite/TestCardCreatorDelegado/card_creator_retorna_erro_propagado
```

## Gates de Conformidade

| Gate | Resultado |
|------|-----------|
| R-ADAPTER-001.1 — zero comentários | OK |
| R-ADAPTER-001.2 — sem SQL direto em adapter/usecase | OK |
| R-DTO-VALIDATE-001 — Validate() com errors.Join | OK |
| Boundary — onboarding não importa card/infrastructure | OK |
| Build completo `./internal/...` | OK |

## Riscos Residuais

- `onboarding_session_payload_test.go` (task 3.0, untracked) falha ao compilar por referenciar `WelcomeSentAt` ausente na struct JSON. Pré-existente, fora do escopo da tarefa 7.0.
- `internal/platform/outbox/storage_postgres_integration_test.go` tem conflito de merge pré-existente; não afeta compilação de produção.
- `OnboardingCardDraft.DueDay` permanece no struct para compatibilidade com o fluxo multi-step do `onboarding_workflow.go` (que recebe ambos `closing_day` e `due_day` em etapas separadas). O campo fica zerado quando criado via `NewOnboardingCardDraft(nickname, closingDay)`.
