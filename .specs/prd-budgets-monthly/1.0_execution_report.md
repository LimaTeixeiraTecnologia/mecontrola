# Execution Report — Task 1.0

**Task:** Migration baseline + schema budgets (6 tabelas + índices)
**Status:** done
**DiffSHA:** (local changes)

## Artefatos Criados

- `migrations/000012_create_budgets_baseline.up.sql` — 6 tabelas + 8 índices
- `migrations/000012_create_budgets_baseline.down.sql` — rollback em ordem reversa de FK
- `migrations/migrations_integration_test.go` — atualizado downSteps + `TestBudgetsMigrationUpAndDown`

## Validações

- `go build ./migrations/...` → PASS
- Migration up: todas 6 tabelas criadas com constraints corretas
- Migration down: drop em ordem reversa sem objetos remanescentes
- Índices: 8 índices incluindo parciais `WHERE deleted_at IS NULL` e `WHERE state = 1`

## Tabelas Criadas

1. `mecontrola.budgets` — PK uuid, UK (user_id, competence)
2. `mecontrola.budgets_allocations` — PK composta, FK cascade, CHECK basis_points/root_slug
3. `mecontrola.budgets_expenses` — PK uuid, UK identity, partial indexes para summary
4. `mecontrola.budgets_threshold_states` — PK composta (user_id, competence, root_slug, threshold)
5. `mecontrola.budgets_alerts` — PK uuid, partial index para listing ativos
6. `mecontrola.budgets_expense_events_pending` — PK uuid, UK event_id, partial indexes estado=1
