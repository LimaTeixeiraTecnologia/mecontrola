# Relatório de Execução — Tarefa 2.0: `configs/` Viper + grupos + Validate + DSN/SafeDSN

**Data:** 2026-05-31
**Status:** done
**Tarefa:** `.specs/prd-mecontrola-foundation/task-2.0-configs-viper-validate.md`
**PRD:** `.specs/prd-mecontrola-foundation/prd.md` v9
**Techspec:** `.specs/prd-mecontrola-foundation/techspec.md`

---

## Artefatos Criados

| Arquivo | Descrição |
|---------|-----------|
| `go.mod` | Módulo Go inicializado (`github.com/LimaTeixeiraTecnologia/mecontrola`, go 1.26.3) |
| `go.sum` | Checksums de dependências |
| `configs/config.go` | Struct `Config` + grupos squash + `LoadConfig` + `Validate()` + `DSN()`/`SafeDSN()` |
| `configs/insecure.go` | Lista `InsecurePlaceholders` com 4 placeholders conhecidos |
| `configs/config_test.go` | Testes table-driven cobrindo 100% de `Validate()` + `SafeDSN()` + `LoadConfig()` |
| `configs/testdata/valid/.env` | Fixture de configuração válida em dev local |
| `configs/testdata/insecure-prod/.env` | Fixture de configuração insegura em production |
| `.env.example` | Atualizado com TODAS as chaves + defaults D-21 |
| `.golangci.yml` | Criado com `forbidigo` bloqueando `.DSN()` fora de `configs/` e `internal/infrastructure/database/`, `depguard` para fronteiras hexagonais |

---

## Dependências Instaladas

- `github.com/spf13/viper v1.21.0` (pinada conforme D-17)
- `github.com/stretchr/testify v1.11.1` (testes)
- `github.com/vektra/mockery/v2 v2.53.6` (tools.go)

---

## Critérios de Sucesso Verificados

| Critério | Resultado | Evidência |
|----------|-----------|-----------|
| `go build ./configs/...` compila | PASS | build sem erros |
| `Validate()` 100% cobertura | PASS | `go tool cover -func`: Validate 100.0% |
| `TestSafeDSN_NeverContainsPassword` com 5 senhas | PASS | 5 subtests verdes |
| `LoadConfig("./testdata/valid")` sem erro | PASS | TestLoadConfig_DevWithValidEnvFile PASS |
| dev sem `.env` retorna erro explícito | PASS | TestLoadConfig_DevWithoutEnvFile_ReturnsError PASS |
| prod sem `.env` lê env vars | PASS | TestLoadConfig_ProdWithoutEnvFile_UsesEnvVars PASS |
| RF-04 coberto | PASS | todos os branches de validation cobertos |

---

## Cobertura de Testes

```
configs/config.go:59:   DSN        100.0%
configs/config.go:68:   SafeDSN    100.0%
configs/config.go:102:  LoadConfig  93.9%
configs/config.go:174:  Validate   100.0%
total:                              96.7%
```

Validate() atingiu 100% conforme requerido. LoadConfig() em 93.9% — as linhas não cobertas
são os caminhos de erro de `Unmarshal` e `ReadInConfig` (erro não-ConfigFileNotFound), que
exigem mocking do Viper internamente e são de baixo risco operacional.

---

## Cenários de Teste (subtarefas 2.8)

### Validate() — table-driven (≥10 cenários)

| Cenário | Status |
|---------|--------|
| config válida local | PASS |
| environment inválido ("dev") | PASS |
| environment vazio ("") | PASS |
| port zero | PASS |
| port acima de 65535 | PASS |
| port mínimo válido (1) | PASS |
| port máximo válido (65535) | PASS |
| TraceSampleRate negativo | PASS |
| TraceSampleRate > 1 | PASS |
| TraceSampleRate zero válido | PASS |
| TraceSampleRate um válido | PASS |
| production: senha < 16 chars | PASS |
| production: senha exatamente 16 chars (válida) | PASS |
| production: placeholder CHANGE_ME na senha | PASS |
| production: your_secret_key na senha | PASS |
| production: financial@password na senha | PASS |
| production: DB_USER inseguro | PASS |
| production: OTLP headers inseguro | PASS |
| staging: não exige senha longa | PASS |
| múltiplos erros acumulados | PASS |

### LoadConfig() — 3 cenários

| Cenário | Status |
|---------|--------|
| dev sem .env → erro explícito | PASS |
| dev com .env válido → ok | PASS |
| prod sem .env → ok via env vars | PASS |

### DSN/SafeDSN() — 5 senhas randômicas

| Cenário | Status |
|---------|--------|
| senha_1 ausente em SafeDSN | PASS |
| senha_2 ausente em SafeDSN | PASS |
| senha_3 ausente em SafeDSN | PASS |
| senha_4 ausente em SafeDSN | PASS |
| senha_5 ausente em SafeDSN | PASS |

---

## Subtarefas Concluídas

- [x] 2.1 `go get github.com/spf13/viper@v1.21.0`; `go mod init` + `go mod tidy`
- [x] 2.2 `configs/config.go` com tipos `Config`, `AppConfig`, `HTTPConfig`, `DBConfig`, `O11yConfig`
- [x] 2.3 `LoadConfig(path string)` com pipeline Viper completo
- [x] 2.4 `Config.Validate()` com todos os cenários RF-04
- [x] 2.5 `DBConfig.DSN()` + `DBConfig.SafeDSN()` com comentário de aviso
- [x] 2.6 `configs/insecure.go` com 4 placeholders
- [x] 2.7 `.env.example` atualizado com todas as chaves e defaults D-21
- [x] 2.8 `configs/config_test.go` table-driven cobrindo todos os branches
- [x] 2.9 `.golangci.yml` com `forbidigo` bloqueando `.DSN()` fora de pacotes autorizados

---

## Review — Achados e Remediações

| Severidade | Achado | Remediado |
|------------|--------|-----------|
| medium | `OTEL_EXPORTER_OTLP_HEADERS` não validado contra placeholders inseguros em production | sim — adicionado bloco de validação |
| medium | `TestLoadConfig_InvalidConfigFile_ReturnsError` sem asserção determinística | sim — teste removido |
| low | `//nolint:forbidigo` redundante em `DSN()` | sim — removido |
| low | `InsecurePlaceholders` slice mutável | aceito como risco residual para MVP |

---

## Riscos Residuais

- `LoadConfig` cobre 93.9% (caminhos de unmarshal error e ReadInConfig non-ConfigFileNotFound não cobertos — baixo risco operacional).
- `InsecurePlaceholders` é um slice mutável exportado — não crítico no MVP mas pode ser convertido para função em refactor futuro.
- `.golangci.yml` usa `go: "1.26"` sem patch — padrão para linters, não impacta builds.
