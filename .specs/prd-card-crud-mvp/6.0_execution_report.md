# Generated: 2026-06-09T00:00:00Z

# Execution Report — Task 6.0: Identity additive — `auth.SourceHeader` + `InjectPrincipalFromHeader` middleware

## Status
done

## Resumo
Adicionada constante `SourceHeader` em `principal.go`, criado middleware `InjectPrincipalFromHeader` com variante `InjectPrincipalFromHeaderWithO11y`, testes com 4 cenários (ausente, malformado, nil UUID, válido) + chain test cobrindo 401 via RequireUser. RF-27 coberto.

## Subtarefas

- [x] 6.1 — `SourceHeader PrincipalSource = "header"` adicionado em `internal/identity/application/auth/principal.go`.
- [x] 6.2 — `TestSourceHeader_Value` + `TestRoundTrip_SourceHeader` adicionados em `principal_test.go`.
- [x] 6.3 — `inject_principal_from_header.go` criado com `InjectPrincipalFromHeader` (simples) e `InjectPrincipalFromHeaderWithO11y` (span `auth.inject_principal_from_header`, atributos `principal.source=header` e `result=injected|missing|invalid`).
- [x] 6.4 — `inject_principal_from_header_test.go` criado com 4 cenários no `TestInjectPrincipalFromHeader` + `TestChain_InjectThenRequire` (header ausente → 401, header válido → 200).

## Arquivos Alterados

- `internal/identity/application/auth/principal.go` — adicionada constante `SourceHeader`
- `internal/identity/application/auth/principal_test.go` — 2 novos testes para `SourceHeader`
- `internal/identity/infrastructure/http/server/middleware/inject_principal_from_header.go` — novo
- `internal/identity/infrastructure/http/server/middleware/inject_principal_from_header_test.go` — novo

## Critérios de Aceite

- `go test -race -count=1 ./internal/identity/application/auth/... ./internal/identity/infrastructure/http/server/middleware/...` verde.
  -> comprovado: `ok github.com/LimaTeixeiraTecnologia/mecontrola/internal/identity/application/auth 1.277s` + `ok github.com/LimaTeixeiraTecnologia/mecontrola/internal/identity/infrastructure/http/server/middleware 1.620s`

- Test cobre 4 cenários: header ausente, malformado, nil UUID, válido.
  -> comprovado: `TestInjectPrincipalSuite/TestInjectPrincipalFromHeader/header_ausente`, `header_malformado`, `nil_UUID`, `UUID_válido` passando.

- `go vet` limpo.
  -> comprovado: `go vet ./internal/identity/...` sem output (exit 0).

- Gate de zero comentários verde.
  -> comprovado: `grep` retornou vazio em todos os arquivos de produção novos/modificados.

- Comportamento: header ausente → ctx sem principal → `RequireUser` em sequência produz 401.
  -> comprovado: `TestChain_InjectThenRequire/header_ausente_resulta_em_401_via_RequireUser` passa com status 401.

- RF-27 coberto: middleware `InjectPrincipalFromHeader` implementado conforme ADR-003.
  -> comprovado: implementação em `inject_principal_from_header.go` respeitando decisão do ADR-003.

## Definition of Done

- [x] Constante `SourceHeader` exportada e referenciada em `inject_principal_from_header.go`.
- [x] Middleware com 2 variantes (simples + com o11y).
- [x] Tests verdes (4 cenários + chain test).
- [x] Gate de zero comentários verde.
- [x] RF-27 explicitamente coberto neste relatório.

## Comandos Executados

```
go test -race -count=1 ./internal/identity/application/auth/... ./internal/identity/infrastructure/http/server/middleware/...
# ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/identity/application/auth        1.277s
# ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/identity/infrastructure/http/server/middleware  1.620s

go vet ./internal/identity/application/auth/... ./internal/identity/infrastructure/http/server/middleware/...
# (sem output — limpo)

grep -rn --include="*.go" --exclude-dir=mocks --exclude="*.pb.go" --exclude="*_test.go" "^[[:space:]]*//" ...
# PASS: zero comments gate
```

## Riscos Residuais

- `SourceHeader` deve ser removida junto com `InjectPrincipalFromHeader` quando JWT/OIDC entrar (ADR-003 marca status Transitória).
- Spoofing de `X-User-ID` em ambiente sem gateway — mitigação operacional documentada no ADR-003.
