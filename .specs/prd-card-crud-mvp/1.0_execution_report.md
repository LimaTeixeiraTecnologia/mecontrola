# Generated: 2026-06-09T17:50:00Z

# Relatório de Execução — Tarefa 1.0: Migrations 000007/000008

## Status

done

## Arquivos Criados/Modificados

- `migrations/000007_create_platform_idempotency_keys.up.sql` (novo)
- `migrations/000007_create_platform_idempotency_keys.down.sql` (novo)
- `migrations/000008_create_card_cards.up.sql` (novo)
- `migrations/000008_create_card_cards.down.sql` (novo)
- `migrations/migrations_integration_test.go` (modificado)
- `migrations/000006_seed_dictionary.up.sql` (corrigido — bug pré-existente de FK)
- `migrations/000006_seed_dictionary.down.sql` (corrigido — bug pré-existente de LIKE em UUID)
- `docs/runbooks/card-rollback.md` (novo)

## Comandos Executados

```bash
go build -tags=integration ./migrations/... # exit 0
go vet -tags=integration ./migrations/...   # exit 0
go test -race -count=1 -tags=integration -timeout=300s -v ./migrations/...
# PASS em 3.388s — todos os 5 tests passaram
```

## Critérios de Aceite

- [x] `000007_create_platform_idempotency_keys.up.sql` — tabela com PK composta (scope, key, user_id), CHECKs e índice expires_at.
  -> comprovado: arquivo presente, constraints validadas por `TestIdempotencyKeysConstraints` (PASS)

- [x] `000007_create_platform_idempotency_keys.down.sql` — rename idempotente para `idempotency_keys_archived_20260609120000`.
  -> comprovado: `TestCardAndIdempotencyMigrationsUpDownUp` verifica tabela arquivada após Down(2) (PASS)

- [x] `000008_create_card_cards.up.sql` — tabela com FK `ON DELETE RESTRICT → mecontrola.users(id)`, CHECKs, índice parcial único e índice de paginação.
  -> comprovado: `TestCardsConstraints` valida closing_day=0/32, nickname vazio, name com 65 chars, nickname duplicado (PASS)

- [x] `000008_create_card_cards.down.sql` — rename idempotente para `cards_archived_20260609120000`.
  -> comprovado: `TestCardAndIdempotencyMigrationsUpDownUp` verifica tabela arquivada após Down(2) (PASS)

- [x] Ciclo `up → down → up` para 000007 e 000008.
  -> comprovado: `TestCardAndIdempotencyMigrationsUpDownUp` — PASS

- [x] 0 ocorrências de `pan|cvv|cvc|track|pin` nos arquivos `.sql`.
  -> comprovado: `grep -iE '\b(pan|cvv|cvc|track|pin)\b' migrations/000007*.sql migrations/000008*.sql` retornou 0 matches

- [x] `TestCardsNoPCIColumns` — schema `mecontrola.cards` não possui colunas com termos PCI.
  -> comprovado: PASS

- [x] `go vet ./migrations/...` limpo.
  -> comprovado: exit 0

## Testes: PASS

```
go test -race -count=1 -tags=integration -timeout=300s -v ./migrations/...
--- PASS: TestMigrationSuite/TestCardAndIdempotencyMigrationsUpDownUp (0.11s)
--- PASS: TestMigrationSuite/TestCardsConstraints (0.01s)
--- PASS: TestMigrationSuite/TestCardsNoPCIColumns (0.01s)
--- PASS: TestMigrationSuite/TestIdempotencyKeysConstraints (0.00s)
--- PASS: TestMigrationSuite/TestUpAndDownForBillingPipelineMigrations (0.12s)
PASS ok migrations 3.388s
```

## Definition of Done

- [x] Arquivos .sql (4) criados.
- [x] `migrations_integration_test.go` estendido e verde com `go test -race -count=1 -tags=integration ./migrations/...`.
- [x] `go vet ./migrations/...` limpo.
- [x] `docs/runbooks/card-rollback.md` rascunhado.
- [x] Cobertura de RF-09..12, RF-16, RF-17..19 estrutural confirmada.

## Cobertura de Requisitos

- RF-09: tabela `mecontrola.cards` com colunas id, user_id, name, nickname, closing_day, due_day, created_at, updated_at, deleted_at — COBERTO
- RF-10: FK física `ON DELETE RESTRICT → mecontrola.users(id)` — COBERTO
- RF-11: CHECKs closing_day, due_day, name, nickname — COBERTO
- RF-12: índice parcial único `cards_user_nickname_active_uniq_idx` — COBERTO
- RF-16: 0 colunas PCI (pan/cvv/cvc/track/pin) — COBERTO
- RF-17: numeração 6 dígitos (`000007`, `000008`) — COBERTO
- RF-18: down por rename, nunca DROP TABLE — COBERTO
- RF-19: idempotência via IF NOT EXISTS / DO block — COBERTO

## Riscos Residuais

- O sufixo estático `20260609120000` significa que múltiplas execuções de `down` em ambientes diferentes geram o mesmo nome de tabela arquivada. Se o archive já existir, o down é no-op (sem erro). Documentado no runbook.
- Remoção de usuário requer anonimização antes por causa de `ON DELETE RESTRICT`. Documentado no runbook e em ADR-004.
