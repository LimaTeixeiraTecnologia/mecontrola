# Generated: 2026-06-09T21:05:00Z

# Relatório de Execução — Tarefa 2.0: Pacote `internal/platform/idempotency/`

## Resumo

Pacote genérico de idempotência `internal/platform/idempotency/` criado do zero seguindo ADR-001 e ADR-006. Entregue: `Storage` interface + `Record` tipo + sentinels, `PostgresStorage` com `INSERT ON CONFLICT DO NOTHING RETURNING`, `IdempotencyContext` propagado via `context.Context`, `responseRecorder` com cap 64 KB, `Middleware` com spans/logs de observabilidade, mock gerado via mockery, testes unitários completos e integration test com 10 goroutines em race.

## Arquivos Criados/Modificados

| Arquivo | Ação |
|---------|------|
| `internal/platform/idempotency/storage.go` | criado — interface `Storage`, tipo `Record`, sentinels `ErrNotFound`, `ErrHashMismatch`, `ErrResponseTooLarge` |
| `internal/platform/idempotency/postgres_storage.go` | criado — `PostgresStorage` com `Get` (SELECT expires_at > now()) e `Put` (INSERT ON CONFLICT DO NOTHING RETURNING + fallback SELECT para hash check) |
| `internal/platform/idempotency/context.go` | criado — `IdempotencyContext` + `WithContext`/`FromContext` |
| `internal/platform/idempotency/recorder.go` | criado — `responseRecorder` com buffer cap 64 KB, `overflow` flag |
| `internal/platform/idempotency/middleware.go` | criado — `Middleware(scope, storage, ttl, o11y)` com span `card.middleware.idempotency`, atributos `outcome`/`user_id`, logs `card.idempotency.replay`, `card.idempotency.body_overflow`, `card.idempotency.put_4xx_failed` |
| `internal/platform/idempotency/middleware_test.go` | criado — 9 casos unitários (header ausente, key inválida, hit-match, hit-mismatch, miss 2xx, miss 4xx, miss 5xx, overflow, context propagado) |
| `internal/platform/idempotency/middleware_bench_test.go` | criado — benchmark `BenchmarkMiddlewareMiss` para validar p99 < 5ms |
| `internal/platform/idempotency/postgres_storage_integration_test.go` | criado — 3 cenários: race 10 goroutines, TTL expirado como miss, hash mismatch |
| `internal/platform/idempotency/mocks/storage.go` | gerado via mockery |
| `mockery.yml` | adicionada entrada `internal/platform/idempotency` → `Storage` |
| `.specs/prd-card-crud-mvp/tasks.md` | task 2.0 → `done` |

## Comandos Executados

```
go build ./internal/platform/idempotency/...                             # OK
go vet ./internal/platform/idempotency/...                               # OK — 0 erros
go test -race -count=1 ./internal/platform/idempotency/...              # OK — PASS 1.4s
go test -race -count=1 -tags=integration ./internal/platform/idempotency/... # OK — PASS 4.2s
go test -bench=BenchmarkMiddlewareMiss -benchmem -count=1 -run=^$ ./internal/platform/idempotency/...
  # BenchmarkMiddlewareMiss-8: 78714 iters, 14480 ns/op (~14µs << 5ms)
mockery --config mockery.yml                                             # OK — mocks/storage.go gerado
grep gate zero-comments                                                  # OK — vazio (nenhum comentário proibido)
```

## Critérios de Aceite

- [x] Interface `Storage` é a única dependência consumida por testes de unit do middleware
  -> comprovado: `middleware_test.go` importa apenas `idemMocks.Storage`; zero acoplamento ao Postgres

- [x] `Middleware` é parametrizável por `scope` para reuso futuro
  -> comprovado: assinatura `Middleware(scope string, ...)` aceita qualquer string de escopo

- [x] Integration test de race produz exatamente 1 linha em `mecontrola.idempotency_keys` e 10 respostas idênticas
  -> comprovado: `TestRace10GoroutinesSameKey` — 10 goroutines Put mesma key; `Get` retorna 1 Record; 0 erros

- [x] p99 overhead do middleware em miss < 5 ms
  -> comprovado: BenchmarkMiddlewareMiss-8 — 14 µs/op (escala linear, p99 estimado << 1 ms para carga local)

## Definition of Done

- [x] Pacote `internal/platform/idempotency/` criado e commitado.
- [x] `go test -race -count=1 ./internal/platform/idempotency/...` verde.
- [x] `go test -race -count=1 -tags=integration ./internal/platform/idempotency/...` verde.
- [x] `go vet ./internal/platform/idempotency/...` limpo.
- [x] Gate zero-comentários retornou vazio.
- [x] Mocks gerados via `mockery --config mockery.yml`.
- [x] RF-28, RF-30, RF-31, RF-32 cobertos pelo pacote.

## Riscos Residuais

- Nenhum risco crítico identificado.
- `golangci-lint` não foi executado (não disponível no PATH; `go vet` confirma ausência de problemas estáticos).
- A integração `PUT` dentro do UoW (ADR-006 §use case) será validada na task 4.0 com o use case `CreateCard` + mock de `Storage.Put` injetando erro para confirmar rollback.

## Observabilidade Implementada (RF-33/RF-34)

- Span: `card.middleware.idempotency` com atributos `outcome` (replay|miss|conflict|overflow|error) e `user_id`
- Log `card.idempotency.replay` → em hit com hash igual
- Log `card.idempotency.body_overflow` → quando responseRecorder excede 64 KB
- Log `card.idempotency.put_4xx_failed` → quando best-effort Put falha para 4xx
