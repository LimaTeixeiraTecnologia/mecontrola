# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Métrica de outcome da distribuição e wiring de observabilidade
- Arquivo: .specs/prd-distribuicao-personalizada-onboarding/task-5.0-metrica-outcome-wiring.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-distribuicao-personalizada-onboarding/prd.md
- TechSpec: .specs/prd-distribuicao-personalizada-onboarding/techspec.md
- ADR: .specs/prd-distribuicao-personalizada-onboarding/adr-004-distribution-outcome-metric.md
- Governança: AGENTS.md, .claude/rules/governance.md, .claude/rules/go-adapters.md, .claude/rules/agent-workflows-tools.md, .claude/rules/transactions-workflows.md; skills `mastra`, `go-implementation`

## Observação sobre continuidade
Esta execução retomou trabalho de uma tentativa anterior que travou em `task lint:run` (watchdog de 600s)
após build/vet/testes já estarem verdes. O código já estava implementado corretamente no working tree;
não houve reimplementação — apenas verificação, validação completa (incluindo lint até o fim) e
persistência de evidência (Etapa 5).

## Comandos Executados
- `go build ./...` -> pass (sem output/erros)
- `go vet ./...` -> pass (sem output/erros)
- `go test -race ./internal/agents/...` -> pass (1283 testes, 20 pacotes)
- `task lint:run` (executado em background com monitor, sem timeout truncado) -> pass:
  - `golangci-lint run ./...` -> 0 issues
  - `lint:auth-bypass` -> PASS
  - `lint:outbox-user-id` + `outbox-user-id:test` -> 7 pass, 0 fail
  - `lint:deadcode` -> nenhum código morto acionável em internal/agents (gate de governança do repositório, independente do escopo de RFs deste PRD)
- Inspeção manual: `grep` para comentários proibidos em `onboarding_workflow.go`, `budget_creation_workflow.go`, `module.go` -> vazio (zero comentários confirmados)
- Revisão de diff (self-review baseado em inspeção direta de código + testes): sem achados bloqueantes

## Arquivos Alterados
- `deployment/scripts/deadcode-agent-allowlist.txt` (allowlist para métodos DMMF `String()`/`IsValid()`/`Parse*` de simetria de API)
- `internal/agents/application/workflows/onboarding_workflow.go` (contador `agents_onboarding_distribution_total`, threading de `observability.Counter` em `BuildBudgetReviewStep`/handlers, `observability.Observability` em `BuildOnboardingWorkflow`)
- `internal/agents/application/workflows/onboarding_workflow_test.go` (testes dos 7 outcomes via `fake.FakeMetrics`)
- `internal/agents/application/workflows/budget_creation_workflow.go` (mensagem de reprompt com razão de balanço — reaproveita `DecideDistributionBalance` do núcleo compartilhado ADR-005/tarefa 6.0)
- `internal/agents/application/workflows/budget_creation_workflow_test.go` (testes de reprompt com razão)
- `internal/agents/application/workflows/onboarding_workflow_integration_test.go`, `onboarding_workflow_postgres_resume_integration_test.go`, `internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer_integration_test.go` (atualização de call sites para as assinaturas ampliadas, passando `nil` para o contador — valida guarda nil-safe)
- `internal/agents/module.go` (wiring de `deps.O11y` em `BuildOnboardingWorkflow`)

## Resultados de Validação
- Testes: pass (`go test -race ./internal/agents/...`, 1283 testes)
- Lint: pass (`task lint:run`, 0 issues + todos os gates de governança)
- Veredito do Revisor: APPROVED (self-review contra prd.md/techspec.md/ADR-004; sem achados)

## Critérios de Aceite
- Cada um dos 7 valores de `outcome` é emitido no caminho correto; precedência `tolerance_absorbed` respeitada (RF-16) -> comprovado: `onboarding_workflow.go:1341-1345` (`if balance.DeltaAbs > 0 { tolerance_absorbed } else { accepted_values }`); testes em `onboarding_workflow_test.go:2233-2439` cobrem os 7 outcomes via `fake.FakeMetrics`.
- Nenhum rótulo carrega `user_id`/`category_id`; cardinalidade fechada -> comprovado: único rótulo é `outcome` (`onboarding_workflow.go:1230`, `observability.String("outcome", outcome)`); constantes fechadas em `onboarding_workflow.go:1208-1216`; `grep` por `user_id\|category_id` em arquivos alterados retorna vazio.
- Métricas existentes (`agents_onboarding_*` completed/resumed, reaper) preservadas -> comprovado: nenhuma alteração nos arquivos de `resolve_onboarding_or_agent.go`/reaper; `go build`/`go test -race` verdes sem regressão nesses pacotes.
- `go build ./...`/`go vet ./...` verdes com as assinaturas ampliadas; contador nil-safe não quebra testes -> comprovado: `go build ./...` e `go vet ./...` sem erros; `recordDistributionOutcome` com guarda `if dist == nil { return }` (`onboarding_workflow.go:1227-1230`); `BuildOnboardingWorkflow` com guarda `if o11y != nil` (`onboarding_workflow.go:1580-1583`); testes de integração chamam com `nil` (`onboarding_workflow_integration_test.go:321,455`, `onboarding_workflow_postgres_resume_integration_test.go:65`, `whatsapp_inbound_consumer_integration_test.go:147,348`) e passam.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=73fde0f061d38b51cfbde7ced12dce7688940744fbf53ab16b177290bc9ca916
verdict=APPROVED
tool=claude

## Coverage

package=internal/agents/application/workflows
delta=n/a (sem baseline de cobertura numérica configurado neste módulo; validação por suíte completa via `go test -race`)

## Suposições
- A alteração em `budget_creation_workflow.go` (mensagem de reprompt com razão de balanço via `budgetBalanceReason`) foi tratada como parte do núcleo compartilhado ADR-005 (tarefa 6.0, já `done`) e não como escopo da tarefa 5.0; mantida no diff pois já estava presente e é consistente/correta, sem violar nenhuma regra.
- A execução anterior que travou em `task lint:run` foi considerada apenas uma falha de watchdog/timeout do ambiente, não um sinal de lint quebrado — confirmado nesta execução com lint completo até o fim (0 issues).

## Riscos Residuais
- Nenhum identificado. Os 7 outcomes têm cobertura de teste unitário direta; precedência `tolerance_absorbed` é testada explicitamente.

## Conflitos de Regra
- none
