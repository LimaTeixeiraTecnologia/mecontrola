# Generated: 2026-07-14T01:04:54Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 2.0
- Título: Decisão pura de saldo e refactor da conversão em basis points
- Arquivo: .specs/prd-distribuicao-personalizada-onboarding/task-2.0-decide-balance-refactor-bp.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-distribuicao-personalizada-onboarding/prd.md
- TechSpec: .specs/prd-distribuicao-personalizada-onboarding/techspec.md
- ADRs: adr-001-decide-distribution-balance.md (saldo puro + tipo fechado, gate design-patterns `reject`), adr-003-rounding-tolerance.md (bandas de tolerância ±0,5pp / ±R$0,05, maior-resto), adr-005-shared-core-change-policy.md (núcleo compartilhado, testes de baseline enumerados). adr-002 e adr-004 apenas referenciados (fora do escopo estrito de 2.0 — classificação de intenção e métrica de outcome pertencem às tarefas 3.0/5.0).
- Governança: AGENTS.md (base contract), `.claude/rules/governance.md`, `.claude/rules/go-adapters.md` (R-ADAPTER-001, zero comentários), `.claude/rules/agent-workflows-tools.md` (R-AGENT-WF-001, estados fechados), `.claude/rules/workflow-kernel.md` (R-WF-KERNEL-001, kernel intocado — verificado), `.claude/rules/go-testing.md` (R-TESTING-001). Skills processuais: `domain-modeling-production`, `design-patterns-mandatory` (declaradas na task file), `mastra` (declarada), `go-implementation` (linguagem, inferida do diff Go).
- 1.0_execution_report.md lido para confirmar estado de partida (tipos fechados `reviewAwaitPersonalize`/`distributionIntentKind`/`distributionBalanceKind` e prompt por extenso já existentes, aprovados APPROVED).

## Comandos Executados
- `go build ./...` -> pass (exit 0)
- `go vet ./...` -> pass (exit 0)
- `go test ./internal/agents/application/workflows/... -run 'TestOnboardingWorkflowSuite/TestDecideDistributionBalance|TestOnboardingWorkflowSuite/TestDecideAllocationsBP|TestOnboardingWorkflowSuite/TestDecideAllocationsBP_ToleranceAbsorbedAlwaysClosesInvariant' -v -count=1` -> pass (21+ subtests, incluindo os novos cenários de saldo e tolerância)
- `go test ./internal/agents/... ./internal/platform/... -count=1 -race` -> 1965 passed, 1 falha conhecida e documentada (ver Riscos Residuais) — sem regressão em `budget_creation` (556 testes verdes no pacote `workflows`)
- `task lint:run` (golangci-lint pinado + gates auth-bypass/outbox-user-id/deadcode) -> pass (0 issues; cyclomatic complexity de `DecideAllocationsBP` corrigida via extração de `decideAllocationsBPConfirm/Percent/Reais`/`finalizeAllocationsBP`/`sumUnits`)
- Reprodução manual de bug (script ad-hoc `zzz_repro_test.go`, removido após uso): confirmou que a primeira versão da implementação violava RF-11 (soma ≠ 10000) quando a soma bruta ficava dentro da tolerância mas não exatamente no alvo nominal (ex.: percent somando 99,7% ou 100,3%) — corrigido usando a soma real como divisor de `centsToBasisPoints` em vez do alvo nominal fixo (100000 / `monthlyBudgetCents`)
- `review` (skill, análise manual como dono do código sobre o diff isolado de 710 linhas): achado crítico de arredondamento identificado e corrigido nesta própria execução; após a correção, zero achados pendentes -> **APPROVED**

## Arquivos Alterados
- `internal/agents/application/workflows/onboarding_workflow.go`
- `internal/agents/application/workflows/onboarding_workflow_test.go`

## Resultados de Validação
- Testes: pass (`go test ./internal/agents/... ./internal/platform/... -count=1 -race`; 1965 passed / 1 falha conhecida fora do escopo desta tarefa — ver Riscos Residuais)
- Lint: pass (`task lint:run`, 0 issues, gates auth-bypass/outbox-user-id/deadcode PASS)
- Veredito do Revisor: APPROVED (após correção do bug crítico de arredondamento encontrado durante a própria revisão desta execução)

## Critérios de Aceite
- `DecideDistributionBalance` retorna `over`/`under`/`balanced` corretos, com `DeltaAbs` e `Unit` na unidade do usuário (RF-04/05/06) -> comprovado: `onboarding_workflow.go:194-232` (`DecideDistributionBalance`); teste `TestDecideDistributionBalance` (8 cenários: percent exato balanced, percent 33,3+33,3+33,4 balanced, percent over com delta 10.0, percent under com delta 10.0, reais exato balanced, reais R$0,04 balanced, reais R$0,06 over, reais under com delta 1350.0) — todos `PASS`.
- NR-01: para somas exatas (100% ou orçamento), os basis points produzidos são idênticos aos de hoje (`40/10/10/10/30 → {4000,1000,1000,1000,3000}`; reais exatos idênticos ao caminho `centsToBasisPoints`) — teste comparando BP antes/depois -> comprovado: `TestDecideAllocationsBP_NR01_PreservesExactBP` (2 cenários: percent exato e reais exato) comparando `bp` produzido contra os valores literais `{4000,1000,1000,1000,3000}` mais `DecideDistribution(bp)` sem erro — `PASS`.
- RF-09: `33,3+33,3+33,4` e reais a até R$0,05 do orçamento fecham 10000 por absorção; fora da banda cai em over/under -> comprovado: `TestDecideAllocationsBP_ToleranceAbsorbedAlwaysClosesInvariant` (4 cenários incluindo o caso originalmente quebrado — percent 99,7% e 100,3% — todos fecham soma=10000 via `s.Equal(10000, total)`); `TestDecideDistributionBalance` confirma banda `[99.5;100.5]` (percent) e `±R$0,05` (reais) via casos limítrofes R$0,04/R$0,06.
- RF-11: `DecideDistribution` (sum=10000) nunca é violado após a conversão -> comprovado: `finalizeAllocationsBP` (onboarding_workflow.go:459-470) chama `DecideDistribution(bp)` e propaga erro; bug de violação do invariante encontrado e corrigido nesta execução (divisor de `centsToBasisPoints` trocado do alvo nominal fixo para a soma real via `sumUnits`); todos os testes de tolerância confirmam soma=10000.
- NR-06: nenhuma mensagem produzida vaza a palavra "renda" -> comprovado: `TestM02_NoRendaTermInAnyOnboardingSurface` (pré-existente, atualizado para exercitar os novos caminhos de `DecideAllocationsBP`) permanece `PASS`; `errAllocationOutOfTolerance` e as demais mensagens novas não contêm "renda" (regex `(?i)\brenda\b` do teste cobre os erros de `DecideAllocationsBP`).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão (exceto a falha conhecida documentada, fora do escopo da tarefa 2.0).
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=a8b1908842348001f87a6dba30b23abf79c1381b0048c2835602cfa0729d1cc2 (git diff dos 2 arquivos alterados por esta tarefa contra HEAD, ainda não commitado; inclui também o diff da tarefa 1.0 já aprovado por não haver commit intermediário)
verdict=APPROVED
tool=claude

## Coverage

package=internal/agents/application/workflows
delta=+4 testes novos (`TestDecideDistributionBalance` com 8 subtestes, `TestDecideAllocationsBP_NR01_PreservesExactBP` com 2, `TestDecideAllocationsBP_ToleranceAbsorbedAlwaysClosesInvariant` com 4, 2 cenários de `TestDecideAllocationsBP` atualizados); sem ferramenta de coverage delta configurada — suíte completa do pacote permanece verde exceto a falha conhecida.

## Suposições
- O contrato de `DecideAllocationsBP` pós-refactor retorna um erro genérico e tipado (`errAllocationOutOfTolerance`) quando a soma cai fora da tolerância, em vez de reproduzir a mensagem amigável antiga ("a soma dos percentuais precisa ser 100%..."). Isso é intencional e decorre diretamente do ADR-001 ("`DecideAllocationsBP` deixa de produzir as mensagens de over/under") — a mensagem amigável ao usuário passa a ser responsabilidade de `renderBalanceMessage`, que consome `DecideDistributionBalance` na camada de workflow (task 3.0/4.0, fora do escopo desta tarefa).
- Os dois testes de baseline `onboarding_workflow_test.go` (linhas originais ~482 e ~508 do arquivo antes desta tarefa) foram atualizados para refletir o novo contrato de erro (`errAllocationOutOfTolerance` via `ErrorIs`) em vez de checar substring da mensagem antiga — mudança de asserção prevista e enumerada em ADR-005.
- O teste de handler `TestBuildBudgetReviewStep/resume_em_reviewAwaitDistribution_com_soma_que_nao_fecha...` (que ainda embute `decErr.Error()` diretamente no reprompt em `handleReviewAwaitDistribution`, linha ~1158) foi deixado falhando intencionalmente: a rewiring desse handler para consumir `renderBalanceMessage`/`DecideDistributionBalance` é escopo explícito da tarefa 4.0 (`task-4.0-handlers-persistencia.md`, subtarefa 4.1, critério "Cenários de baseline de resume ... permanecem verdes (atualizados apenas onde o texto de saldo mudou)"). Não modifiquei `handleReviewAwaitDistribution` nesta tarefa para não extrapolar o escopo de 2.0 (que cobre exclusivamente `DecideDistributionBalance` + `DecideAllocationsBP` + constantes + `DecideBudgetDistribution` como rede de segurança).
- `distributionPercentToleranceAbs`/`distributionReaisToleranceCents` foram nomeadas sem prefixo `_` (R5.26 revogado pelo projeto) e sem comentário (R-ADAPTER-001.1), conforme subtarefa 2.3.
- `DecideBudgetDistribution` (`budget_creation_decisions.go:50-59`) não foi tocada (subtarefa 2.4) — permanece a rede de segurança pós-conversão validando `sum=10000`; confirmado que nenhuma chamada a `DecideAllocationsBP` em `budget_creation_workflow.go` foi alterada nesta tarefa (wiring do saldo ao `budget_creation` é escopo da tarefa 6.0, conforme ADR-005).

## Riscos Residuais
- **Falha de teste conhecida, fora do escopo de 2.0, a ser resolvida pela tarefa 4.0**: `TestOnboardingWorkflowSuite/TestBuildBudgetReviewStep/resume_em_reviewAwaitDistribution_com_soma_que_nao_fecha_deve_reprompt_sem_ativar,_mantendo_mesmo_sub-estado` falha porque `handleReviewAwaitDistribution` ainda embute `decErr.Error()` (agora `"distribution: soma fora da tolerancia de fechamento"`, sem o texto "90%") diretamente no reprompt via `methodologyReprompt(decErr.Error(), preview)`. O texto de UI de over/under passou a ser responsabilidade de `renderBalanceMessage`/`DecideDistributionBalance`, que ainda não está ligado ao handler — isso é explicitamente o escopo da tarefa 4.0 (subtarefa 4.1: "values→extração compartilhada→DecideAllocationKind→DecideDistributionBalance"). O comportamento efetivo (re-suspender sem ativar, no mesmo sub-estado) permanece correto — apenas o texto do reprompt mudou, exatamente como previsto no NR-04 da techspec ("muda apenas o texto da mensagem... não o efeito").
- **Bug crítico encontrado e corrigido durante esta execução (não chega a produção)**: a primeira versão da minha implementação de `decideAllocationsBPPercent`/`decideAllocationsBPReais` passava o alvo nominal fixo (`100000` para percent, `monthlyBudgetCents` para reais) como divisor de `centsToBasisPoints`, em vez da soma real informada pelo usuário. Isso violava RF-11 sempre que a soma bruta caía dentro da tolerância (`balanced`) mas não era exatamente igual ao alvo nominal — ex.: percent somando 99,7% resultava em basis points somando 8975 em vez de 10000. Reproduzido com teste ad-hoc, corrigido substituindo o divisor pela soma real via nova função `sumUnits`, e coberto permanentemente por `TestDecideAllocationsBP_ToleranceAbsorbedAlwaysClosesInvariant` (4 cenários, incluindo o caso exato que expôs o bug). Nenhum código com esse defeito foi commitado ou ficou em estado "aprovado" antes da correção.
- `finalizeAllocationsBP` tem uma guarda defensiva `if total <= 0 { return nil, errAllocationOutOfTolerance }` que hoje é inalcançável na prática (a checagem de negativo por categoria já ocorre antes, e `DecideDistributionBalance` com status `balanced` implica soma bruta positiva pela banda de tolerância). Mantida como defesa em profundidade (R5.12, sem panic) sem custo de legibilidade relevante.
- As mensagens específicas antigas de over/under ("a soma dos percentuais precisa ser 100%, mas você informou X%", "a soma dos valores... precisa ser igual ao seu orçamento mensal") foram removidas de `DecideAllocationsBP` conforme ADR-001; não há mais teste que dependa delas nesta tarefa (os dois testes que dependiam foram atualizados). O texto amigável ao usuário só retorna quando a tarefa 3.0/4.0 implementar `renderBalanceMessage` consumindo `DecideDistributionBalance`.

## Conflitos de Regra
- none
