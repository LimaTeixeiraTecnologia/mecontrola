# Generated: 2026-08-08T13:01:53Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Wiring do módulo agents e do worker com ajuste do stub de boot
- Arquivo: .specs/prd-indicador-digitando-whatsapp/task-5.0-wiring-modulo-worker.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-indicador-digitando-whatsapp/prd.md
- TechSpec: .specs/prd-indicador-digitando-whatsapp/techspec.md
- Governança: AGENTS.md; agent-governance (complexidade standard); go-implementation (task_type `cross-cutting`/`consumer`, plumbing de DI em composition root); mastra (wiring do consumidor agentivo `internal/agents` no fluxo WhatsApp inbound, DI explícita, nenhum primitivo de `internal/platform/{agent,memory,workflow}` reimplementado)

## Comandos Executados
- `go build ./...` -> sucesso
- `go build -tags integration ./...` -> sucesso
- `go build -tags e2e ./...` -> sucesso
- `go vet ./...` -> sucesso
- `go test -race -count=1 ./internal/agents/...` -> ok em todos os pacotes com testes
- `go test -tags integration -race -count=1 -run 'TestModuleBootSuite' ./internal/agents/...` -> ok (boot do módulo verde com deps completos)
- `go test -tags integration -race -count=1 -run 'TestWhatsAppInboundConsumerIntegrationSuite' ./internal/agents/infrastructure/messaging/database/consumers/... -v` -> PASS nos 4 cenários (3 preexistentes inalterados + 1 novo `TestInteg_TypingIndicatorLigado_EmiteUmaUnicaVezComWamidCorreto`)
- `go test -tags integration -race -count=1 ./internal/onboarding/...` -> ok em todos os pacotes (zero regressão da mudança de tipo em `internal/onboarding/module.go`)
- `go test -tags e2e -count=1 ./internal/onboarding/e2e/...` -> ok (contagens de mensagens do e2e inalteradas, flag off)
- `./.tools/bin/golangci-lint run ./internal/agents/... ./internal/onboarding/... ./cmd/worker/... ./cmd/server/...` -> 0 issues
- `bash deployment/scripts/lint-deadcode.sh` -> PASS (entrada `WithTypingIndicator` removida da allowlist agora que está wired em produção)
- `grep` R-ADAPTER-001.1 (zero comentários) nos arquivos alterados de produção -> vazio (OK)
- `git diff -- <arquivos da tarefa> | shasum -a 256` -> 2a42c9464996a0eebfffe7185e3320e988280c2441efcfdb0bee5d44c5de2b4a
- Skill `review` (contexto prd.md + techspec.md) -> APPROVED, 0 achados

## Arquivos Alterados
- internal/agents/module.go
- internal/agents/module_boot_integration_test.go
- cmd/worker/worker.go
- internal/onboarding/module.go
- internal/agents/infrastructure/messaging/database/consumers/whatsapp_inbound_consumer_integration_test.go
- deployment/scripts/deadcode-agent-allowlist.txt

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `go test -race -count=1 ./internal/agents/...` verde, incluindo boot e integração -> comprovado: `go test -race -count=1 ./internal/agents/...` retornou `ok` em todos os pacotes; `go test -tags integration -race -count=1 -run 'TestModuleBootSuite'` e `-run 'TestWhatsAppInboundConsumerIntegrationSuite'` retornaram PASS
- `go build ./...`, `go vet ./...` e `golangci-lint run` no escopo verdes -> comprovado: os três comandos executados com sucesso; lint com `0 issues` no binário pinado `./.tools/bin/golangci-lint` sobre `internal/agents/...`, `internal/onboarding/...`, `cmd/worker/...`, `cmd/server/...`
- Diff sem alteração de asserts preexistentes; única mudança em testes antigos é o método novo no stub de boot -> comprovado: `internal/agents/module_boot_integration_test.go` recebeu apenas o método `SendTypingIndicator` no stub (nenhum assert alterado); `whatsapp_inbound_consumer_integration_test.go` recebeu apenas um caso de teste novo (aditivo, nenhum cenário preexistente tocado); os 3 cenários preexistentes da suíte de integração do consumer passaram inalterados

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=2a42c9464996a0eebfffe7185e3320e988280c2441efcfdb0bee5d44c5de2b4a
verdict=APPROVED
tool=skill review (diff do working tree; AI_VALIDATE_GIT_HISTORY=0 — sha é hash de conteúdo do diff, não commit)

## Coverage

package=internal/agents (module.go) + internal/agents/infrastructure/messaging/database/consumers + internal/onboarding
delta=n/a (task não exige threshold de cobertura; caso de integração novo cobre RF-01/RF-05/RF-06/RF-09 no ponto de wiring)

## Suposições
- Nenhuma suposição: todas as dependências (1.0, 3.0, 4.0) estavam `done` e os contratos (`AgentConfig.WhatsAppTypingIndicatorEnabled`, `gateway.WhatsAppGateway.SendTypingIndicator`, `consumers.WithTypingIndicator`) já existiam conforme especificado na techspec.

## Riscos Residuais
- Mudança adicional não prevista literalmente no task file, mas exigida pela extensão da interface local `whatsAppGateway`: `internal/onboarding/module.go` teve `OnboardingModule.WhatsAppGateway`, `onboardingDependencies.whatsAppGateway` e o retorno de `newWhatsAppGateway` alterados de `appinterfaces.WhatsAppGateway` (interface) para `*gateway.WhatsAppGateway` (tipo concreto). Motivo: a interface `interfaces.WhatsAppGateway` foi deliberadamente mantida sem `SendTypingIndicator` (decisão da techspec, evita regenerar mockery), então a interface local mais ampla do agents não era satisfeita pelo valor de tipo interface estreito — só o tipo concreto satisfaz ambas as interfaces (a estreita de onboarding/notification e a ampla de agents) simultaneamente. Validado sem side effects: `bootstrap.BuildChannelGateway` (cmd/worker/worker.go:409 e cmd/server/server.go:205) e `WhatsAppMessageProcessor`/`buildNotificationChannelGateway` continuam recebendo o valor via satisfação implícita de interface; suíte completa de onboarding (unit + integration + e2e) permanece verde.
- `cmd/server/server.go` também constrói `agents.NewModule` mas não popula `TypingIndicatorEnabled` (campo fica `false`, default seguro). Não é regressão: o `EventHandlers` desse módulo nunca é registrado em nenhum dispatcher no server (só o worker consome outbox), portanto o consumer ali construído nunca executa `Handle`. Fora do escopo desta tarefa (arquivo não listado em "Arquivos Relevantes"), mas registrado aqui para consciência caso o server passe a consumir eventos no futuro.
- Reemissão de typing quando o processamento falha após a emissão e o dedup é compensado: comportamento aceito e documentado na techspec (Riscos Conhecidos), herdado da tarefa 4.0.
- Gate externo RF-07 (versão da Graph API aceitar `typing_indicator`) permanece pendente na tarefa 6.0 e bloqueia apenas a ativação, não o merge.

## Conflitos de Regra
- none
