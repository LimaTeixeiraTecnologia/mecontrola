# Execution Report — Task 5.0

- **Task:** Workflow destructive_confirm + wiring no module.go
- **PRD:** `.specs/prd-agent-platform-evolution/prd.md`
- **Status:** done
- **Date:** 2026-06-24

## Subtasks executadas

### 5.1 — DestructiveConfirmDefinition

Arquivo criado: `internal/agent/application/workflow/destructive_confirm.go`

- `const DestructiveConfirmWorkflowID = "destructive_confirm"` — distinto de `TransactionsWriteWorkflowID = "transactions_write"` (resume sem ambiguidade)
- `DestructiveConfirmDeps` com `Authorize`, `Replay`, `Policy`, `AuditBegin`, `OnSettle`, `Targets map[OperationKind]TargetResolver`, `Executors map[OperationKind]DestructiveExecutor`, `TTL`, `DenyReply`, `ReplayReply`, `AuditFailReply`
- `NewDestructiveConfirmDefinition` monta sequência de 8 passos: `authorize → replay → policy → audit_begin → prepare_target → confirm_gate → execute_destructive → format`
- `formatDestructiveReply` retorna strings Portuguese por `OperationKind` (sem `fmt.Sprintf` sem args, sem dependências desnecessárias)
- `platform.Definition[confirmation.ConfirmState]{ID: DestructiveConfirmWorkflowID, Root: root, Durable: true}`

### 5.2 — Typed guard steps para ConfirmState

Arquivo criado: `internal/agent/application/workflow/steps/confirm_guard.go`

- Os steps existentes (`authorize`, `replay`, `policy`, `audit_begin`) são tipados para `ExpenseState`; criados equivalentes tipados para `confirmation.ConfirmState`
- `NewConfirmAuthorize`, `NewConfirmReplay`, `NewConfirmPolicy`, `NewConfirmAuditBegin`, `NewConfirmFormat`
- `ConfirmAuditBeginResult{Conflicted, Failed, Settle, DecisionID}`
- `ConfirmOnSettleRegistered func(runID uuid.UUID, settle ConfirmAuditSettleFunc)`
- Outcome usa cast `int(tools.OutcomeAuthzDenied)` (ConfirmState.Outcome é int)

### 5.3 — HITL Adapters

Arquivo criado: `internal/agent/infrastructure/binding/hitl_adapters.go`

- `NewLastTransactionDeleterResolver(lister)` — lista transações, monta PromptText
- `NewLastTransactionDeleterExecutor(deleter, lister)` — deleta a última
- `NewLastTransactionEditorResolver(lister)` — lista, monta PromptText
- `NewLastTransactionEditorExecutor(editor, lister)` — chama editor com `EditTransactionInput`
- `NewCardDeleterResolver()` — stub com PromptText fixo
- `NewCardDeleterExecutorFn(deleter)` — chama `deleter.Execute(ctx, uid, "")`
- `NewBudgetCommitResolver()` — stub
- `NewBudgetCommitExecutor(committer)` — chama `committer.Commit(ctx, uid, budgetdraft.Draft{})`
- `parseHITLUserID(raw string) (uuid.UUID, error)` — helper interno

### 5.4 — Wiring em module.go + KernelDeps + DailyLedgerAgent

- `internal/agent/application/services/intent_router.go`: `KernelDeps` estendido com `ConfirmEngine platform.Engine[confirmation.ConfirmState]` e `ConfirmDef platform.Definition[confirmation.ConfirmState]`
- `internal/agent/application/services/daily_ledger_agent.go`: campos `confirmEngine` e `confirmDef` adicionados à struct; wiring em `newDailyLedgerAgent` via guarda nil
- `internal/agent/module.go`: `attachKernel` cria `platform.NewEngine[confirmation.ConfirmState]` + `buildConfirmDefinition` com 4 resolvers e 4 executors; atribui a `deps.Kernel.ConfirmEngine/ConfirmDef`

### 5.5 — Testes

Arquivo criado: `internal/agent/application/workflow/destructive_confirm_test.go`

- `TestDefinition_ID_And_Durable` — ID correto e Durable=true
- `TestDefinition_AuthzDenied_ShortCircuit` — curto-circuito em authz negado
- `TestDefinition_Replay_ShortCircuit` — curto-circuito em replay
- `TestDefinition_PrepareTarget_MissingResolver_ShortCircuits` — OutcomeMissingResolver quando resolver ausente
- `TestDefinition_OperationKindMapping` — os 4 OperationKinds mapeiam para Suspended
- `TestDefinition_FullFlow_SuspendsOnFirstPass` — primeira passagem suspende com PromptText correto
- `TestFormatDestructiveReply_AllKinds` — reply não vazia para os 4 kinds

### 5.6 — Correção de regressão (ADR-001 MergePatch)

O test `TestResumeStatePreservesAmbiguousData` detectou que o resume via `json.Marshal(ExpenseState{...})` incluía campos zero que sobrescreviam o estado suspenso após MergePatch.

Correção dupla:
- `daily_ledger_agent.go` `continuePendingExpenseConfirmationKernel`: substituído `json.Marshal(steps.ExpenseState{...})` por `json.Marshal(struct{ ResumeText string }{ResumeText: text})` — delta puro per ADR-001
- `kernel_state_recovery_test.go`: substituído encoding de struct completa por `[]byte(`{"ResumeText":"1"}`)` — alinha o teste ao contrato de delta

## Gates verificados

| Gate | Resultado |
|------|-----------|
| Zero comentários em `internal/agent/application/workflow/` | OK |
| Zero SQL em `internal/agent/application/workflow/` | OK |
| Switch de daily_ledger_agent.go não cresceu | OK (cases=0) |
| Kernel sem import de domínio | OK |

## Evidência de build e testes

```
go build ./internal/agent/... → BUILD OK
go test ./internal/agent/... → todos PASS
go test ./internal/platform/workflow/... → PASS
```

## Arquivos criados/modificados

- `internal/agent/application/workflow/destructive_confirm.go` (novo)
- `internal/agent/application/workflow/destructive_confirm_test.go` (novo)
- `internal/agent/application/workflow/steps/confirm_guard.go` (novo)
- `internal/agent/infrastructure/binding/hitl_adapters.go` (novo)
- `internal/agent/application/services/intent_router.go` (modificado — KernelDeps)
- `internal/agent/application/services/daily_ledger_agent.go` (modificado — campos + resume delta)
- `internal/agent/application/services/kernel_state_recovery_test.go` (modificado — delta resume)
- `internal/agent/module.go` (modificado — buildConfirmDefinition + wiring)
- `.specs/prd-agent-platform-evolution/tasks.md` (5.0 → done)

## Riscos residuais

- `ConfirmEngine`/`ConfirmDef` estão disponíveis em `DailyLedgerAgent` mas ainda não utilizados para roteamento HITL — isso é escopo da task 6.0 (roteamento + resume antes do parse).
- HITL adapters para card e budget são stubs funcionais; a task 7.0 validará E2E real.
