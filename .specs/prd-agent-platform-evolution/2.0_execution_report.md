# Generated: 2026-06-24T00:00:00Z

# Relatório de Execução — Tarefa 2.0: Kernel merge-patch no resume (ADR-001)

## Status Final
`done`

## Resumo
Implementado `Codec[S].MergePatch` em `codec.go` com merge recursivo JSON (RFC 7386): chave `null` remove, objeto aninhado faz deep-merge, payload vazio é no-op. Substituído bloco de replace direto em `Engine.Resume` (`engine.go`) por `MergePatch(snap.State, resume)` + `Decode`. Adicionados testes cobrindo todos os cenários do contrato, incluindo regressão do defeito (estado rico suspenso → resume com delta parcial → campos originais sobrevivem).

## Arquivos Modificados

- `internal/platform/workflow/codec.go` — adicionado `MergePatch(base, patch []byte) ([]byte, error)` e helper privado `mergeMaps`
- `internal/platform/workflow/engine.go` — `Resume` usa `MergePatch` em vez de substituição direta
- `internal/platform/workflow/codec_test.go` — 6 novos testes de `MergePatch`
- `internal/platform/workflow/engine_test.go` — 2 novos testes de regressão (`TestResume_MergePatch_OriginalFieldsSurvive`, `TestResume_EmptyPayload_IsNoOp`) + tipo auxiliar `richEngineState`

## Subtarefas

- [x] 2.1 `Codec[S].MergePatch` em `codec.go`
- [x] 2.2 `Engine.Resume` usa `MergePatch`
- [x] 2.3 Teste de regressão do defeito (`richEngineState` com `UserID`, `Amount`, `ResumeText`)
- [x] 2.4 `parity_test.go` e fluxo de clarificação de categoria verdes

## Critérios de Aceite

- `Engine.Resume` aplica merge-patch; estado suspenso sobrevive ao resume com delta mínimo.
  -> comprovado: `TestResume_MergePatch_OriginalFieldsSurvive` — `UserID="user-123"`, `Amount=9900` sobrevivem após resume com `{"resume_text":"sim"}`; `ResumeText` atualizado para `"sim"`.

- Resume vazio (`len(resume)==0`) é no-op idêntico ao comportamento anterior.
  -> comprovado: `TestResume_EmptyPayload_IsNoOp` — `nil` payload, resultado `Value=47` (base 42 + delta 5 do step).

- Teste de regressão do defeito passa; `parity_test.go` e testes existentes do kernel verdes.
  -> comprovado: `go test ./internal/platform/workflow/... ok` e `go test ./internal/agent/application/workflow/... ok`.

- Gate `R-WF-KERNEL-001` retorna vazio.
  -> comprovado: todos os três gates retornaram vazio (sem import de domínio, sem SQL fora do adapter, sem comentários proibidos).

## Comandos Executados

```
go test ./internal/platform/workflow/...
# ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/platform/workflow  0.423s

go test ./internal/agent/application/workflow/...
# ok  github.com/LimaTeixeiraTecnologia/mecontrola/internal/agent/application/workflow  0.394s

# Gate 1 (domínio): PASS — vazio
grep -rn --include="*.go" --exclude-dir=mocks --exclude="*_test.go" "internal/agent\|internal/transactions\|internal/billing\|internal/identity" internal/platform/workflow/

# Gate 2 (SQL): PASS — vazio
grep -rn --include="*.go" --exclude-dir=mocks --exclude="*_test.go" "QueryContext\|ExecContext\|db\.Query\|tx\.Exec\|db\.Exec" internal/platform/workflow/ | grep -v "infrastructure/postgres"

# Gate 3 (comentários): PASS — vazio
grep -rn --include="*.go" --exclude-dir=mocks --exclude="*_test.go" "^[[:space:]]*//" internal/platform/workflow/ | grep -Ev "(//go:|//nolint:|// Code generated)"
```

## Riscos Residuais

Nenhum. A alteração é semanticamente compatível com resume vazio (no-op) e validada por regressão.

## Conformidade R-WF-KERNEL-001

- R-WF-KERNEL-001.1 (sem import de domínio): PASS
- R-WF-KERNEL-001.2 (sem SQL fora do adapter): PASS
- R-WF-KERNEL-001.3 (estados como tipos fechados): não alterado; estados existentes preservados
- R-WF-KERNEL-001.4 (cardinalidade controlada em métricas): não alterado
- R-WF-KERNEL-001.5 (sem LLM): PASS
- R-WF-KERNEL-001.6 (zero comentários): PASS
