# Generated: 2026-06-05T20:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 1.0
- Título: Migrations Postgres + invariantes (users + user_whatsapp_history)
- Arquivo: .specs/prd-identity-foundation/task-1.0-migrations-postgres-invariantes.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-identity-foundation/prd.md
- TechSpec: .specs/prd-identity-foundation/techspec.md
- Governança: agent-governance (check-invocation-depth), adr-007-postgres-partial-unique-indexes.md
- Skills Necessárias: nenhuma além das auto-carregadas (governance + linguagem) — conforme declarado na task file

## Comandos Executados
- `go build ./...` -> verde (sem output = sucesso); embed.FS inclui automaticamente os 4 novos arquivos *.sql
- `docker run postgres:16-alpine` -> container mecontrola-test-pg-1 iniciado na porta 54321
- `psql < 000001_outbox_events.up.sql` -> CREATE TABLE, 4x CREATE INDEX
- `psql < 000002_identity_users.up.sql` -> CREATE TABLE, 3x CREATE INDEX
- `psql < 000003_identity_user_whatsapp_history.up.sql` -> CREATE TABLE, 2x CREATE INDEX
- `psql \d users` -> estrutura confirmada: 2 CHECKs + 3 índices parciais com nomes exatos
- `psql \d user_whatsapp_history` -> estrutura confirmada: FK CASCADE + 2 índices com nomes exatos
- Teste invariante: `INSERT INTO users (status='DELETED', deleted_at=NULL)` -> ERROR: violates check constraint "users_status_deleted_at_invariant"
- Teste UNIQUE ativo: INSERT dois ACTIVE mesmo whatsapp_number -> ERROR: duplicate key value violates unique constraint "users_whatsapp_number_active_uniq"
- Teste índice parcial: INSERT DELETED + INSERT ACTIVE mesmo número -> 2 rows coexistem (linha DELETED + linha ACTIVE)
- `psql < 000003_identity_user_whatsapp_history.down.sql` -> DROP INDEX x2, DROP TABLE
- `psql < 000002_identity_users.down.sql` -> DROP INDEX x3, DROP TABLE
- `psql \dt` pós-down -> apenas `outbox_events` remanescente (schema limpo)
- `docker rm -f mecontrola-test-pg-1` -> container removido

## Arquivos Alterados
- migrations/000002_identity_users.up.sql (criado)
- migrations/000002_identity_users.down.sql (criado)
- migrations/000003_identity_user_whatsapp_history.up.sql (criado)
- migrations/000003_identity_user_whatsapp_history.down.sql (criado)

## Resultados de Validação
- Testes: pass
- Lint: pass (go build ./... verde; pure SQL — sem lint Go aplicável)
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `migrations/000002_identity_users.{up,down}.sql` e `migrations/000003_identity_user_whatsapp_history.{up,down}.sql` presentes -> comprovado: `ls migrations/` confirma 4 arquivos novos
- Aplicar `up.sql` em Postgres limpo cria tabelas/índices com nomes exatos acima -> comprovado: `\d users` mostra `users_pk`, `users_status_allowed`, `users_status_deleted_at_invariant`, `users_whatsapp_number_active_uniq`, `users_whatsapp_number_deleted_idx`, `users_email_active_uniq`; `\d user_whatsapp_history` mostra `user_whatsapp_history_pk`, `user_whatsapp_history_user_fk`, `user_whatsapp_history_user_active_idx`, `user_whatsapp_history_number_idx`
- Tentativa de `INSERT INTO users (status='DELETED', deleted_at IS NULL)` rejeitada por `users_status_deleted_at_invariant` -> comprovado: `ERROR: new row for relation "users" violates check constraint "users_status_deleted_at_invariant"`
- Aplicar `down.sql` deixa schema limpo -> comprovado: `\dt` pós-down mostra apenas `outbox_events`
- `go build ./...` continua verde -> comprovado: saída vazia (sem erros)

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos acima em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=untracked (arquivos novos não commitados)
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=migrations (SQL only — Go coverage N/A)
delta=N/A

## Suposições
- embed.go usa `//go:embed *.sql` que inclui automaticamente qualquer novo arquivo .sql na pasta — confirmado pelo build verde sem alteração em embed.go.
- A ordem de aplicação de down migrations é responsabilidade do operador (000003 antes de 000002); os arquivos sozinhos não impõem ordem.

## Riscos Residuais
- Aplicar 000002 down antes de 000003 down falhará por FK dependency — risco operacional normal de migrations com FK; mitigado pelo uso correto de golang-migrate que aplica em ordem numérica.
- Alteração futura do enum `status` (ex: adicionar `BLOCKED`) exige atualização do CHECK `users_status_allowed` na mesma migration — documentado em ADR-007 Consequências.

## Conflitos de Regra
- none
