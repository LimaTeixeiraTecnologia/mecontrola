# Generated: 2026-06-03T00:00:00Z

# Relatório de Execução — Tarefa 3.0

## Tarefa
Agregado `User` + `UserID` + `RehydrateUser` + sentinelas de domain

## Status
`done`

## Arquivos Alterados

| Arquivo | Operação |
|---------|----------|
| `internal/identity/domain/errors.go` | criado |
| `internal/identity/domain/entities/user.go` | criado |
| `internal/identity/domain/entities/user_test.go` | criado |
| `internal/identity/domain/entities/user_id_test.go` | criado |
| `go.mod` | alterado — `google/uuid` promovido para dependência direta |

## Comandos Executados

```
go mod tidy
# resultado: github.com/google/uuid v1.6.0 (sem // indirect)

go test -race -count=1 ./internal/identity/domain/entities/...
# resultado: ok  (1.459s)

go test -cover ./internal/identity/domain/entities/...
# resultado: coverage: 100.0% of statements

golangci-lint run ./internal/identity/domain/...
# resultado: 0 issues

go doc ./internal/identity/domain/entities User
# resultado: struct com "Has unexported fields." apenas

grep -rn '^func ' internal/identity/domain/entities/*.go | grep -vE '_test.go|func New|func Rehydrate'
# resultado: apenas métodos com receiver (R1 confirmado)

grep "google/uuid" go.mod
# resultado: github.com/google/uuid v1.6.0  (sem // indirect)
```

## Critérios de Aceite

- `NewUserID` valida que o UUID é versão 4 — UUID v3/v5 rejeitado.
  -> comprovado: `TestNewUserID_RejectsV3` e `TestNewUserID_RejectsV5` passam com `ErrorIs(err, ErrInvalidUserID)`

- `User` não expõe nenhum setter; campos não exportados.
  -> comprovado: `go doc ./internal/identity/domain/entities User` retorna `Has unexported fields.`; sem campo público

- `SoftDelete` em user já soft-deletado retorna `ErrUserAlreadyDeleted` (sem mutação).
  -> comprovado: `TestSoftDelete_AlreadyDeleted` — segunda chamada retorna `ErrUserAlreadyDeleted`

- `RehydrateUser` aceita timestamps zero (delegação para banco) e status arbitrário do enum.
  -> comprovado: `TestRehydrateUser_AcceptsZeroTimestamps` e `TestRehydrateUser_AcceptsArbitraryState` passam sem erro

- `domain/entities` não importa `application`, `infrastructure`, `platform` ou `configs` (depguard).
  -> comprovado: `golangci-lint run ./internal/identity/domain/...` retorna `0 issues`

- Cobertura `go test -cover ./internal/identity/domain/entities/...` ≥ 95%.
  -> comprovado: `coverage: 100.0% of statements`

## Definition of Done (DoD)

- [x] `go test -race -count=1 ./internal/identity/domain/entities/...` passa.
- [x] `go test -cover ./internal/identity/domain/entities/...` reporta ≥ 95% (obtido: 100%).
- [x] `grep -rn '^func ' ...` retorna apenas métodos com receiver (R1).
- [x] `golangci-lint run ./internal/identity/domain/...` passa.
- [x] `go.mod` lista `github.com/google/uuid` como direta (sem `// indirect`).
- [x] Nenhum campo público em `User`.

## Riscos Residuais

Nenhum.

## Suposições

- Os sentinelas foram declarados diretamente em `entities/user.go` (além de `domain/errors.go`) para que o pacote `entities` seja auto-contido e não precise importar o pacote `domain` pai, evitando dependência circular. O arquivo `domain/errors.go` foi criado conforme subtarefa 3.1, mas o pacote `entities` usa suas próprias cópias dos sentinelas.
