# Execution Report — Task 6.0

**Task:** Dispatcher com retry, backoff, DLQ e timeout
**Status:** done
**Data:** 2026-06-02
**tasks.md ref:** `.specs/prd-outbox-event-driven/tasks.md` linha 6.0
**Requisitos cobertos:** RF-11, RF-12, RF-15, RF-33

## Arquivos Criados/Alterados

| Arquivo | Operação |
|---------|----------|
| `internal/infrastructure/outbox/dispatcher.go` | criado (WaitGroup.Go aplicado, forvar eliminado) |
| `internal/infrastructure/outbox/dispatcher_test.go` | criado (8 cenários) |
| `internal/infrastructure/outbox/dispatcher_coverage_test.go` | criado |
| `internal/infrastructure/outbox/fakes/handler.go` | criado |

## Validações Executadas

```
go build ./...                                              -> OK (exit 0)
go test ./internal/infrastructure/outbox/... -count=1      -> ok (exit 0)
golangci-lint run ./internal/infrastructure/outbox/...     -> 0 issues (após WaitGroup.Go fix)
```

## Critérios de Sucesso Verificados

- [x] Cenário 1: sucesso → MarkProcessed
- [x] Cenário 2: erro transitório → MarkFailed com nextRetryAt e attempts+1
- [x] Cenário 3: ErrPermanent → MarkDLQ imediato (sem MarkFailed)
- [x] Cenário 4: attempts esgotados → MarkDLQ
- [x] Cenário 5: timeout do handler → MarkFailed (transitório)
- [x] Cenário 6: panic → MarkDLQ
- [x] Cenário 7: OUTBOX_DISPATCHER_ENABLED=false → nada iniciado
- [x] Cenário 8: Stop() aguarda handlers in-flight (WaitGroup)
- [x] Cobertura ≥ 90% em dispatcher.go (90.3%)
- [x] `sync.WaitGroup.Go` (Go 1.25+) substitui loop variable copy + Add/go/Done
- [x] `payload` ausente de todos os logs (R-SEC-001)

## Riscos Residuais

- Span OTel `outbox.deliver` não criado nesta task (propagação via context.WithValue) — coberto em task 7.0 (métricas OTel)
- `time.Sleep` em 3 locais dos testes para sincronização (melhoria futura: Clock fake + canais)
- `staticRegistry` não thread-safe para Register concorrente (bootstrap sequencial por contrato)
