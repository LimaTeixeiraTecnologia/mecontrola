# Execution Report — Task 5.0

**Task:** Publisher transacional opt-in
**Status:** done
**Data:** 2026-06-02
**tasks.md ref:** `.specs/prd-outbox-event-driven/tasks.md` linha 5.0
**Requisitos cobertos:** RF-01, RF-02, RF-05

## Arquivos Criados/Alterados

| Arquivo | Operação |
|---------|----------|
| `internal/infrastructure/outbox/publisher.go` | criado |
| `internal/infrastructure/outbox/publisher_test.go` | criado (8 subcasos, 100% cobertura) |
| `go.mod` | modificado (otel/trace direto após go mod tidy) |
| `go.sum` | modificado |

## Validações Executadas

```
go build ./...                                         -> OK (exit 0)
go test ./internal/infrastructure/outbox/... -count=1 -> ok (exit 0, 100% em publisher.go)
golangci-lint run ./internal/infrastructure/outbox/... -> 0 issues
go mod tidy                                            -> otel/trace corrigido para direct
```

## Critérios de Sucesso Verificados

- [x] Assinatura `Publish(ctx, tx database.DBTX, evt Event) error` sem import pgx direto
- [x] 1 handler → 1 InsertEvent + 1 InsertDeliveries([s1])
- [x] 3 handlers → 1 InsertEvent + 1 InsertDeliveries([s1,s2,s3]) (lote único)
- [x] SubscriptionsFor vazio → `ErrHandlerNotRegistered`
- [x] InsertEvent falha → erro wrappado com `outbox.publish: %w`
- [x] tx é a mesma instância passada (sem cópia)
- [x] Span `outbox.publish` criado + `traceparent` injetado nos Headers antes do InsertEvent
- [x] `payload` ausente de todos os logs (R-SEC-001)
- [x] Cobertura 100% em publisher.go
- [x] `go build ./...` + `go test` verdes

## Riscos Residuais

- Noop tracer não injeta traceparent (comportamento esperado, protegido por guard `tp != ""`)
- Coexistência com `events.Bus` (ADR-003) — Publisher é caminho opt-in; nenhum conflito em runtime
