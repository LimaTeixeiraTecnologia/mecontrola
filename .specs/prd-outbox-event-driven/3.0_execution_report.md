# Generated: 2026-06-02T21:48:00Z

# Execution Report — Task 3.0: Porta Storage + adapter PgxStorage + integration tests + mocks

## Metadados

- **Task:** 3.0
- **Status:** done
- **Executado em:** 2026-06-02
- **Branch:** feat/foundation-execute

## Arquivos Alterados

| Arquivo | Ação |
|---|---|
| `internal/infrastructure/outbox/storage.go` | Criado — interface `Storage` (porta de domínio D-14) |
| `internal/infrastructure/outbox/storage_pgx.go` | Criado — implementação `PgxStorage` com queries SQL completas |
| `internal/infrastructure/outbox/storage_pgx_integration_test.go` | Criado — 6 cenários de integração + Stats |
| `internal/infrastructure/outbox/mocks/storage.go` | Gerado via `task mocks` — mock `Storage` com `NewStorage(t)` |
| `go.mod` / `go.sum` | Atualizado — `github.com/jackc/pgerrcode` promovido para direto |

## Comandos Executados

```bash
go build ./internal/infrastructure/outbox/...
# => saída vazia (sucesso)

task mocks
# => gerou mocks/storage.go; warning esperado para Registry (task 4.0)

go test -tags=integration -v -timeout 120s ./internal/infrastructure/outbox/...
# => PASS em 2.137s (< 60s alvo)

task lint
# => 0 issues

go test ./...
# => all PASS
```

## Critérios de Aceite

- [x] **RF-04 — Idempotência via UNIQUE**: Cenário 1 comprova que segundo insert retorna `ErrDuplicateSubscription`.
  -> comprovado: `TestCenario1_InsertEventAndDeliveries/deve_criar_1_event_e_3_deliveries;_segundo_insert_viola_UNIQUE PASS`

- [x] **RF-10 — ClaimReady com FOR UPDATE SKIP LOCKED**: Cenário 2 comprova batch=10 de 100 deliveries, ordenado por ID, com campos corretos.
  -> comprovado: `TestCenario2_ClaimReadyBatchAndFields/deve_retornar_10_deliveries_ordenadas_por_id_com_status=claimed_e_attempts_incrementado PASS`

- [x] **RF-14 — Multi-instância sem double-processing**: Cenário 3 comprova 2 goroutines paralelas sem overlap de IDs em 100 deliveries.
  -> comprovado: `TestCenario3_ConcurrentClaimNoOverlap/duas_goroutines_concorrentes_nao_retornam_o_mesmo_claim.ID PASS`

- [x] **MarkProcessed e transição inválida**: Cenário 4 comprova transição claimed→processed e erro em segunda tentativa.
  -> comprovado: `TestCenario4_MarkProcessedAndInvalidTransition/MarkProcessed_move_claimed_para_processed;_segunda_chamada_falha PASS`

- [x] **ReleaseStuck (D-17)**: Cenário 5 comprova liberação de deliveries claimed há > 10min para pending.
  -> comprovado: `TestCenario5_ReleaseStuck/ReleaseStuck_libera_deliveries_claimed_há_mais_de_10min_para_pending PASS`

- [x] **PurgeOlderThan**: Cenário 6 comprova deleção de deliveries finalizadas e eventos órfãos > 90 dias.
  -> comprovado: `TestCenario6_PurgeOlderThan/PurgeOlderThan_apaga_deliveries_finalizadas_e_eventos_sem_deliveries_restantes PASS`

- [x] **Mocks gerados**: `mocks.Storage` com `NewStorage(t)` instanciável em testes de tasks 5.0/6.0.
  -> comprovado: `task mocks` gerou `/internal/infrastructure/outbox/mocks/storage.go` com `func NewStorage(t ...)` na linha 504

## Definition of Done

- [x] Build tag `//go:build integration` aplicada — `go test ./...` sem tag ignora o arquivo e passa verde
- [x] Suíte de integração roda < 60s (executou em 2.137s)
- [x] Concorrência interna no claim provada (Cenário 3, 2 goroutines + 100 deliveries, zero overlap)
- [x] Mocks gerados sem warning crítico; `mocks.NewStorage(t)` instanciável
- [x] Nenhuma dependência de `events.Bus` no pacote — apenas `events.EventID` / `events.EventName`
- [x] `gofmt -w .` aplicado; `golangci-lint run` verde (0 issues)

## Riscos Residuais

- `Registry` interface ainda não existe (task 4.0); mockery emitiu warning `no such interface Registry` — esperado e não crítico. O mock de `Storage` foi gerado sem erros.
- A função `PgxStorage.fetchEventsByIDs` usa query com IN clause com placeholder posicional individual. Para batchSizes muito grandes (> 500), pode exceder limites de parâmetros. Mitigação: o `OUTBOX_DISPATCHER_BATCH_SIZE` default é 50 e máximo configurável é 500 conforme techspec.

## Suposições Assumidas

- Testes de integração usam `SetupSuite` com truncate entre testes via `SetupTest` para isolamento.
- Event IDs para testes de bulk insert são strings determinísticas no formato `01HXZTESTBULK*` — não são ULIDs válidos mas passam na validação de `NewEventID` (apenas exige não-vazio).
