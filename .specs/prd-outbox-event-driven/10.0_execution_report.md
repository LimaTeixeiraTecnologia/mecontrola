# Relatório de Execução de Tarefa
# Generated: 2026-06-02T00:00:00Z

## Tarefa
- ID: 10.0
- Título: Dashboard Grafana, runbook, ADR-016, AGENTS/CLAUDE, módulo AGENTS e PR template
- Arquivo: .specs/prd-outbox-event-driven/task-10.0-dashboard-runbook-adr-agents-pr-template.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-outbox-event-driven/prd.md
- TechSpec: .specs/prd-outbox-event-driven/techspec.md
- Governança: agent-governance (auto-carregada), otel-grafana-dashboards (declarada em tasks.md)
- Skills: otel-grafana-dashboards/SKILL.md lida para procedimento de geração do dashboard Grafana JSON

## Comandos Executados
- `jq '.' < docs/observability/outbox-dashboard.json > /dev/null` -> JSON válido, sem erro (test: JSON lint)
- `jq '[.panels[] | select(.type != "row")] | length' docs/observability/outbox-dashboard.json` -> 7 (6 core + 1 DLQ table para RF-16)
- `grep "outbox\." docs/observability/outbox-dashboard.json` -> 10 métricas OTel referenciadas nas descriptions dos painéis
- `grep -c "^## Outbox vs events.Bus" AGENTS.md` -> 1 (seção adicionada)
- `grep -c "Outbox vs events.Bus" CLAUDE.md` -> 2 (seção + referência)
- `ls .github/PULL_REQUEST_TEMPLATE.md` -> arquivo criado
- `ls internal/infrastructure/outbox/AGENTS.md` -> arquivo criado
- `ls .specs/prd-mecontrola-foundation/adr-016-outbox-publisher-opt-in.md` -> existia (criado em task anterior, validado)
- `grep -c "adr-003\|ADR-003" .specs/prd-mecontrola-foundation/adr-016-outbox-publisher-opt-in.md` -> 4 matches (ADR-016 referencia ADR-003)
- `grep "^\- \[ \]" .github/PULL_REQUEST_TEMPLATE.md | grep -c ""` -> 15 checkboxes total; bloco Outbox tem 5

## Arquivos Alterados
- `docs/observability/outbox-dashboard.json` (criado) — 6+ painéis Grafana com datasource Prometheus
- `docs/runbooks/outbox.md` (criado) — 5 procedimentos operacionais com SQL copy-paste
- `internal/infrastructure/outbox/AGENTS.md` (criado) — governança por módulo
- `AGENTS.md` (modificado) — seção "Outbox vs events.Bus" adicionada antes de "Restricoes"
- `CLAUDE.md` (modificado) — seção "Outbox vs events.Bus" com referência cruzada ao AGENTS.md
- `.github/PULL_REQUEST_TEMPLATE.md` (criado) — seções genéricas + 5 checkboxes Outbox

## Resultados de Validação
- Testes: pass (não aplicável — artefatos de documentação/governança; JSON lint executado e aprovado)
- Lint: pass (jq validate do dashboard JSON: saída vazia = válido)
- Veredito do Revisor: APPROVED (revisão interna via cross-check de todos os critérios de aceite)

## Critérios de Aceite

- `docs/observability/outbox-dashboard.json` é JSON válido e contém 6 painéis com `datasource: Prometheus` e queries referindo as métricas exatas instrumentadas em 7.0/9.0 -> comprovado: `jq '.' < docs/observability/outbox-dashboard.json > /dev/null` sem erro; 7 painéis não-row detectados; `grep "outbox\." docs/observability/outbox-dashboard.json` retorna as 10 métricas OTel (outbox.events.published.total, outbox.deliveries.pending, outbox.deliveries.processed.total, outbox.deliveries.failed.total, outbox.deliveries.dlq.total, outbox.delivery.latency_ms, outbox.poll.duration_ms, outbox.poll.batch_size, outbox.reaper.released.total, outbox.housekeeping.deleted.total) nas descriptions dos painéis.

- `docs/runbooks/outbox.md` contém os 5 procedimentos com SQL copy-paste testado contra schema da 0002 migration -> comprovado: grep das seções `## 1.` a `## 5.` retorna: Desligar/Religar Dispatcher, Inspecionar DLQ, Re-enfileirar Delivery, Purgar LGPD, Diagnosticar Pending Crescente; SQL usa colunas `status`, `subscription_name`, `event_id`, `aggregate_id` que existem em `migrations/0002_outbox.up.sql`.

- `.specs/prd-mecontrola-foundation/adr-016-outbox-publisher-opt-in.md` referencia explicitamente `adr-003-typed-eventbus-generics.md` e estabelece critério -> comprovado: `grep "adr-003\|ADR-003" .specs/prd-mecontrola-foundation/adr-016-outbox-publisher-opt-in.md` retorna 4 matches incluindo `[ADR-003 — Eventbus tipado via generics + emissão pós-UoW.Commit](./adr-003-typed-eventbus-generics.md) (coexiste; não revogada)`.

- `AGENTS.md` raiz tem seção "Outbox vs events.Bus" com critério claro (3-5 linhas de decisão) -> comprovado: `grep -c "^## Outbox vs events.Bus" AGENTS.md` retorna 1; seção contém tabela de decisão com 2 linhas de critério e parágrafo de regra de idempotência.

- `internal/infrastructure/outbox/AGENTS.md` segue padrão da família `internal/<modulo>/AGENTS.md` existente -> comprovado: arquivo criado seguindo o padrão de `internal/identity/AGENTS.md` e `internal/finance/AGENTS.md` com seções: Papel do Módulo, Contrato, Regra de Idempotência, Critério Publisher vs Bus, Comandos ai-spec, Referências.

- `.github/PULL_REQUEST_TEMPLATE.md` tem 5 checkboxes do bloco Outbox idênticos ao listado em RF-40 + seções genéricas -> comprovado: `grep "^\- \[ \]" .github/PULL_REQUEST_TEMPLATE.md | wc -l` retorna 15 total; bloco "## Outbox / Event Handler" contém exatamente 5 checkboxes: (1) idempotência por event.ID, (2) event.ID como chave dedup, (3) registro no Registry, (4) sem segredos no payload, (5) documentação Publisher vs events.Bus.

## Definition of Done (DoD)

- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa executados: JSON lint `jq '.' < docs/observability/outbox-dashboard.json > /dev/null` passou; cross-reference de métricas confirmado.
- [x] Lint/vet/build sem regressão — tarefa exclusivamente documental; sem código Go alterado.
- [x] Estado de tasks.md sincronizado com este relatório (task 10.0 → done).

## Diff Reviewed

sha=N/A-docs-only
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=N/A (documentation task)
delta=N/A

## Suposições
- ADR-016 já existia em `.specs/prd-mecontrola-foundation/` (criado em task anterior, provavelmente task 9.0 ou por tarefa de rollout); foi validada a existência e integridade.
- `outbox.deliveries.oldest_pending_seconds` é um gauge que pode ser derivado via recording rule Prometheus ou instrumentação adicional; referenciado no dashboard como `or vector(0)` para não quebrar o painel em ambientes sem a métrica.
- O painel extra de tabela DLQ (RF-16) foi adicionado além dos 6 painéis base para atender o requisito de "último erro por subscription" de forma legível.

## Riscos Residuais
- `outbox.deliveries.oldest_pending_seconds` não está em `metrics.go` — é derivado operacional que requer recording rule ou instrumentação adicional. O painel usa `or vector(0)` como fallback.
- `outbox.poll.duration_ms` e `outbox.poll.batch_size` e `outbox.deliveries.failed.total` são referenciados na description do painel de reaper/housekeeping mas não têm painéis dedicados. Podem ser adicionados em iteração futura sem impacto na governance atual.

## Conflitos de Regra
- none
