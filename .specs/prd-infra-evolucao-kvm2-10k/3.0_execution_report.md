# Relatório de Execução — Task 3.0

**PRD:** `.specs/prd-infra-evolucao-kvm2-10k/prd.md`
**Data:** 2026-07-03
**Status:** done

## Nota de Honestidade — Artefato vs Execução Operacional

O status `done` desta tarefa refere-se à **entrega de código/artefato**, não à execução operacional
na VPS. As operações abaixo **NÃO foram executadas** neste ambiente (sem acesso ao host de produção):
remoção real do runner self-hosted no host + `df` pós-limpeza, e deploy real verde via o novo pipeline
GitHub-hosted SSH. Enquanto não houver execução real:

- **RF-07 (execução da remoção do runner no host): execução operacional pendente — NÃO COMPROVADO neste ambiente.** Script entregue e validado por sintaxe; remoção real + `df` não realizados.
- **RF-09 (deploy real verde pelo pipeline reescrito): execução operacional pendente — NÃO COMPROVADO neste ambiente.** Job reescrito e YAML válido; nenhum deploy real observado (ver também RF-19 no relatório 7.0, igualmente não comprovado).

## Requisitos Cobertos

- RF-07: script `deployment/scripts/remove-runner.sh` criado (artefato) — desregistro no GitHub, systemctl stop/disable, remoção de `/home/github-runner`, `userdel`, `docker builder prune -af`, `docker image prune -af`. Execução real no host: NÃO COMPROVADO (pendente).
- RF-08: build/scan(Trivy)/sign(cosign) definidos em `ubuntu-24.04` (GitHub-hosted); gates bloqueantes intactos no YAML (verificação estática)
- RF-09: job `deploy` em `.github/workflows/ci-cd.yml` reescrito — `runs-on: ubuntu-24.04`, SSH via `DEPLOY_SSH_KEY`/`VPS_HOST_KEY` secrets, `StrictHostKeyChecking=yes`; SOPS, `create-secrets.sh`, migrate com advisory lock, `docker stack deploy`, waiters de health e rollback preservados. Deploy real verde: NÃO COMPROVADO (pendente).
- RF-10: `deployment/scripts/docker-prune.sh` + `docker-prune.service` + `docker-prune.timer` (systemd, domingo 03:00 UTC); alerta `mc-disk-low-bytes` em `rules.yaml` (critical < 10 GiB); instruções de instalação do timer em `deploy.md` (artefatos; instalação real do timer não executada)

## Arquivos Modificados/Criados

- `.github/workflows/ci-cd.yml` — job deploy reescrito para GitHub-hosted SSH
- `deployment/scripts/remove-runner.sh` (novo) — remoção idempotente do runner self-hosted
- `deployment/scripts/docker-prune.sh` (novo) — prune controlado por uso de disco
- `deployment/scripts/docker-prune.service` (novo) — unidade systemd oneshot
- `deployment/scripts/docker-prune.timer` (novo) — timer semanal domingo 03:00 UTC
- `deployment/runbooks/deploy.md` — seção de higiene de disco com instalação do timer
- `deployment/telemetry/grafana/provisioning/alerting/rules.yaml` — alerta `mc-disk-low-bytes`

## Correções Aplicadas

- Variável `RUNNER_SERVICE` não utilizada removida de `remove-runner.sh`
- `--keep-storage` em `docker-prune.sh` protegido com detecção de versão Docker
- Unidades systemd `.service`/`.timer` criadas para agendamento versionado do prune (lacuna da subtarefa 3.4)

## Validações

- `bash -n remove-runner.sh` → OK
- `bash -n docker-prune.sh` → OK
- `yaml.safe_load(ci-cd.yml)` → OK
- `yaml.safe_load(rules.yaml)` → OK
- Gates Trivy/cosign verificados intactos no ci-cd.yml

## Riscos Residuais Documentados

- `remove-runner.sh` precisa ser executado manualmente na VPS (script entregue, não a execução)
- `VPS_HOST_KEY` secret deve ser preenchido antes do primeiro deploy via SSH
